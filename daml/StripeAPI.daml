daml 1.2
-- Generated by Metagen
module StripeAPI
  ( module StripeAPI ) where


data PaymentMethodDetailsAchCreditTransfer = PaymentMethodDetailsAchCreditTransfer with
    swiftCode : Optional Text
        -- ^ SWIFT code of the bank associated with the routing
        --   number.
    accountNumber : Optional Text
        -- ^ Account number to transfer funds to.
    routingNumber : Optional Text
        -- ^ Routing transit number for the bank account to
        --   transfer funds to.
    bankName : Optional Text
        -- ^ Name of the bank associated with the routing number.
      deriving (Eq, Ord, Show)

data SubscriptionScheduleConfigurationItemTaxRates = SubscriptionScheduleConfigurationItemTaxRates with
      deriving (Eq, Ord, Show)

data SubscriptionScheduleConfigurationItem = SubscriptionScheduleConfigurationItem with
    billingThresholds : Optional SubscriptionItemBillingThresholds
    quantity : Optional Int
        -- ^ Quantity of the plan to which the customer should be
        --   subscribed.
    plan : Text
        -- ^ ID of the plan to which the customer should be
        --   subscribed.
    taxRates : [TaxRate]
        -- ^ The tax rates which apply to this `phase_item`. When
        --   set, the `default_tax_rates` on the phase do not
        --   apply to this `phase_item`.
      deriving (Eq, Ord, Show)

data ChargeFraudDetails = ChargeFraudDetails with
    userReport : Optional Text
        -- ^ Assessments reported by you. If set, possible values
        --   of are `safe` and `fraudulent`.
    stripeReport : Optional Text
        -- ^ Assessments from Stripe. If set, the value is
        --   `fraudulent`.
      deriving (Eq, Ord, Show)

data PlatformTaxFee = PlatformTaxFee with
    account : Text
        -- ^ The Connected account that incurred this charge.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    type_ : Optional Text
        -- ^ The type of tax (VAT).
    sourceTransaction : Optional Text
        -- ^ The payment object that caused this tax to be
        --   inflicted.
      deriving (Eq, Ord, Show)

data UsageRecordSummary = UsageRecordSummary with
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    period : Period
    totalUsage : Optional Int
        -- ^ The total usage within this usage period.
    invoice : Optional Text
        -- ^ The invoice in which this usage period has been
        --   billed for.
    subscriptionItem : Optional Text
        -- ^ The ID of the subscription item this summary is
        --   describing.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
      deriving (Eq, Ord, Show)

data CardMetadata = CardMetadata with
      deriving (Eq, Ord, Show)

data Card = Card with
    dynamicLast4 : Optional Text
        -- ^ (For tokenized numbers only.) The last four digits of
        --   the device account number.
    defaultForCurrency : Optional Bool
        -- ^ Whether this card is the default external account for
        --   its currency.
    tokenizationMethod : Optional Text
        -- ^ If the card number is tokenized, this is the method
        --   that was used. Can be `amex_express_checkout`,
        --   `android_pay` (includes Google Pay), `apple_pay`,
        --   `masterpass`, `visa_checkout`, or null.
    country : Optional Text
        -- ^ Two-letter ISO code representing the country of the
        --   card. You could use this attribute to get a sense of
        --   the international breakdown of cards you've
        --   collected.
    fingerprint : Optional Text
        -- ^ Uniquely identifies this particular card number. You
        --   can use this attribute to check whether two customers
        --   who've signed up with you are using the same card
        --   number, for example.
    availablePayoutMethods : [Text]
        -- ^ A set of available payout methods for this card. Will
        --   be either `["standard"]` or `["standard",
        --   "instant"]`. Only values from this set should be
        --   passed as the `method` when creating a transfer.
    addressZipCheck : Optional Text
        -- ^ If `address_zip` was provided, results of the check:
        --   `pass`, `fail`, `unavailable`, or `unchecked`.
    brand : Text
        -- ^ Card brand. Can be `American Express`, `Diners Club`,
        --   `Discover`, `JCB`, `MasterCard`, `UnionPay`, `Visa`,
        --   or `Unknown`.
    addressCity : Optional Text
        -- ^ City/District/Suburb/Town/Village.
    addressState : Optional Text
        -- ^ State/County/Province/Region.
    account : Optional Text
        -- ^ The account this card belongs to. This attribute will
        --   not be in the card object if the card belongs to a
        --   customer or recipient instead.
    funding : Text
        -- ^ Card funding type. Can be `credit`, `debit`,
        --   `prepaid`, or `unknown`.
    customer : Optional Text
        -- ^ The customer that this card belongs to. This
        --   attribute will not be in the card object if the card
        --   belongs to an account or recipient instead.
    name : Optional Text
        -- ^ Cardholder name.
    addressLine1Check : Optional Text
        -- ^ If `address_line1` was provided, results of the
        --   check: `pass`, `fail`, `unavailable`, or `unchecked`.
    cvcCheck : Optional Text
        -- ^ If a CVC was provided, results of the check: `pass`,
        --   `fail`, `unavailable`, or `unchecked`.
    currency : Optional Text
    expMonth : Optional Int
        -- ^ Two-digit number representing the card's expiration
        --   month.
    addressZip : Optional Text
        -- ^ ZIP or postal code.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : CardMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    expYear : Optional Int
        -- ^ Four-digit number representing the card's expiration
        --   year.
    addressLine2 : Optional Text
        -- ^ Address line 2 (Apartment/Suite/Unit/Building).
    recipient : Optional Text
        -- ^ The recipient that this card belongs to. This
        --   attribute will not be in the card object if the card
        --   belongs to a customer or account instead.
    addressLine1 : Optional Text
        -- ^ Address line 1 (Street address/PO Box/Company name).
    last4 : Text
        -- ^ The last four digits of the card.
    addressCountry : Optional Text
        -- ^ Billing address country, if provided when creating
        --   card.
      deriving (Eq, Ord, Show)

data AccountBusinessProfile = AccountBusinessProfile with
    supportPhone : Optional Text
        -- ^ A publicly available phone number to call with
        --   support issues.
    supportEmail : Optional Text
        -- ^ A publicly available email address for sending
        --   support issues to.
    url : Optional Text
        -- ^ The business's publicly available website.
    supportUrl : Optional Text
        -- ^ A publicly available website for handling support
        --   issues.
    supportAddress : Optional Address
    name : Optional Text
        -- ^ The customer-facing business name.
    productDescription : Optional Text
        -- ^ Internal-only description of the product sold or
        --   service provided by the business. It's used by Stripe
        --   for risk and underwriting purposes.
    mcc : Optional Text
        -- ^ [The merchant category code for the
        --   account](https://stripe.com/docs/connect/setting-mcc).
        --   MCCs are used to classify businesses based on the
        --   goods or services they provide.
      deriving (Eq, Ord, Show)

data PaymentMethodDetailsCardWalletSamsungPay = PaymentMethodDetailsCardWalletSamsungPay with
      deriving (Eq, Ord, Show)

data OrderItem = OrderItem with
    parent : Optional Text
        -- ^ The ID of the associated object for this line item.
        --   Expandable if not null (e.g., expandable to a SKU).
    amount : Int
        -- ^ A positive integer in the smallest currency unit
        --   (that is, 100 cents for $1.00, or 1 for ¥1, Japanese
        --   Yen being a zero-decimal currency) representing the
        --   total amount for the line item.
    quantity : Optional Int
        -- ^ A positive integer representing the number of
        --   instances of `parent` that are included in this order
        --   item. Applicable/present only if `type` is `sku`.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    type_ : Optional Text
        -- ^ The type of line item. One of `sku`, `tax`,
        --   `shipping`, or `discount`.
    description : Text
        -- ^ Description of the line item, meant to be displayable
        --   to the user (e.g., `"Express shipping"`).
      deriving (Eq, Ord, Show)

data Event = Event with
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    apiVersion : Optional Text
        -- ^ The Stripe API version used to render `data`. *Note:
        --   This property is populated only for events on or
        --   after October 31, 2014*.
    data_ : Optional NotificationEventData
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    account : Optional Text
        -- ^ The connected account that originated the event.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    pendingWebhooks : Optional Int
        -- ^ Number of webhooks that have yet to be successfully
        --   delivered (i.e., to return a 20x response) to the
        --   URLs you've specified.
    type_ : Optional Text
        -- ^ Description of the event (e.g., `invoice.created` or
        --   `charge.refunded`).
    request : Optional NotificationEventRequest
      deriving (Eq, Ord, Show)

data TaxDeductedAtSource = TaxDeductedAtSource with
    taxDeductionAccountNumber : Optional Text
        -- ^ The TAN that was supplied to Stripe when TDS was
        --   assessed
    periodStart : Optional Int
        -- ^ The start of the invoicing period. This TDS applies
        --   to Stripe fees collected during this invoicing
        --   period.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    periodEnd : Optional Int
        -- ^ The end of the invoicing period. This TDS applies to
        --   Stripe fees collected during this invoicing period.
    id : Text
        -- ^ Unique identifier for the object.
      deriving (Eq, Ord, Show)

data PaymentMethodDetailsWechat = PaymentMethodDetailsWechat with
      deriving (Eq, Ord, Show)

data BalanceAmountBySourceType = BalanceAmountBySourceType with
    card : Optional Int
        -- ^ Amount for card.
    bankAccount : Optional Int
        -- ^ Amount for bank account.
    fpx : Optional Int
        -- ^ Amount for FPX.
      deriving (Eq, Ord, Show)

data PaymentMethodDetailsCardWallet = PaymentMethodDetailsCardWallet with
    dynamicLast4 : Optional Text
        -- ^ (For tokenized numbers only.) The last four digits of
        --   the device account number.
    samsungPay : Optional PaymentMethodDetailsCardWalletSamsungPay
    visaCheckout : Optional PaymentMethodDetailsCardWalletVisaCheckout
    masterpass : Optional PaymentMethodDetailsCardWalletMasterpass
    googlePay : Optional PaymentMethodDetailsCardWalletGooglePay
    applePay : Optional PaymentMethodDetailsCardWalletApplePay
    type_ : Optional Text
        -- ^ The type of the card wallet, one of
        --   `amex_express_checkout`, `apple_pay`, `google_pay`,
        --   `masterpass`, `samsung_pay`, or `visa_checkout`. An
        --   additional hash is included on the Wallet subhash
        --   with a name matching this value. It contains
        --   additional information specific to the card wallet
        --   type.
    amexExpressCheckout : Optional PaymentMethodDetailsCardWalletAmexExpressCheckout
      deriving (Eq, Ord, Show)

data RadarValueListMetadata = RadarValueListMetadata with
      deriving (Eq, Ord, Show)

data RadarValueListListItemsData = RadarValueListListItemsData with
      deriving (Eq, Ord, Show)

data RadarValueListListItems = RadarValueListListItems with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [RadarValueListItem]
        -- ^ Details about each object.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data RadarValueList = RadarValueList with
    itemType : Optional Text
        -- ^ The type of items in the value list. One of
        --   `card_fingerprint`, `card_bin`, `email`,
        --   `ip_address`, `country`, `string`, or
        --   `case_sensitive_string`.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    alias : Text
        -- ^ The name of the value list for use in rules.
    createdBy : Optional Text
        -- ^ The name or email address of the user who created
        --   this value list.
    name : Text
        -- ^ The name of the value list.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : RadarValueListMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    listItems : Optional RadarValueListListItems
        -- ^ List of items contained within this value list.
      deriving (Eq, Ord, Show)

data PaymentMethodDetailsAlipay = PaymentMethodDetailsAlipay with
      deriving (Eq, Ord, Show)

data ChargeOutcome = ChargeOutcome with
    riskScore : Optional Int
        -- ^ Stripe's evaluation of the riskiness of the payment.
        --   Possible values for evaluated payments are between 0
        --   and 100. For non-card payments, card-based payments
        --   predating the public assignment of risk scores, or in
        --   the event of an error during evaluation, this field
        --   will not be present. This field is only available
        --   with Radar for Fraud Teams.
    riskLevel : Optional Text
        -- ^ Stripe's evaluation of the riskiness of the payment.
        --   Possible values for evaluated payments are `normal`,
        --   `elevated`, `highest`. For non-card payments, and
        --   card-based payments predating the public assignment
        --   of risk levels, this field will have the value
        --   `not_assessed`. In the event of an error in the
        --   evaluation, this field will have the value `unknown`.
    networkStatus : Optional Text
        -- ^ Possible values are `approved_by_network`,
        --   `declined_by_network`, `not_sent_to_network`, and
        --   `reversed_after_approval`. The value
        --   `reversed_after_approval` indicates the payment was
        --   [blocked by
        --   Stripe](https://stripe.com/docs/declines#blocked-payments)
        --   after bank authorization, and may temporarily appear
        --   as "pending" on a cardholder's statement.
    rule : Optional Text
        -- ^ The ID of the Radar rule that matched the payment, if
        --   applicable.
    reason : Optional Text
        -- ^ An enumerated value providing a more detailed
        --   explanation of the outcome's `type`. Charges blocked
        --   by Radar's default block rule have the value
        --   `highest_risk_level`. Charges placed in review by
        --   Radar's default review rule have the value
        --   `elevated_risk_level`. Charges authorized, blocked,
        --   or placed in review by custom rules have the value
        --   `rule`. See [understanding
        --   declines](https://stripe.com/docs/declines) for more
        --   details.
    sellerMessage : Optional Text
        -- ^ A human-readable description of the outcome type and
        --   reason, designed for you (the recipient of the
        --   payment), not your customer.
    type_ : Optional Text
        -- ^ Possible values are `authorized`, `manual_review`,
        --   `issuer_declined`, `blocked`, and `invalid`. See
        --   [understanding
        --   declines](https://stripe.com/docs/declines) and
        --   [Radar
        --   reviews](https://stripe.com/docs/radar/reviews) for
        --   details.
      deriving (Eq, Ord, Show)

data UsageRecord = UsageRecord with
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    quantity : Int
        -- ^ The usage quantity for the specified date.
    subscriptionItem : Optional Text
        -- ^ The ID of the subscription item this usage record
        --   contains data for.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    timestamp : Int
        -- ^ The timestamp when this usage occurred.
      deriving (Eq, Ord, Show)

data IssuingCardholderCompany = IssuingCardholderCompany with
    taxIdProvided : Optional Bool
        -- ^ Whether the company's business ID number was
        --   provided.
      deriving (Eq, Ord, Show)

data WebhookEndpoint = WebhookEndpoint with
    status : Text
        -- ^ The status of the webhook. It can be `enabled` or
        --   `disabled`.
    application : Optional Text
        -- ^ The ID of the associated Connect application.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    apiVersion : Optional Text
        -- ^ The API version events are rendered as for this
        --   webhook endpoint.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    url : Text
        -- ^ The URL of the webhook endpoint.
    secret : Optional Text
        -- ^ The endpoint's secret, used to generate [webhook
        --   signatures](https://stripe.com/docs/webhooks/signatures).
        --   Only returned at creation.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    enabledEvents : [Text]
        -- ^ The list of events to enable for this endpoint.
        --   `['*']` indicates that all events are enabled, except
        --   those that require explicit selection.
      deriving (Eq, Ord, Show)

data PaymentMethodDetailsCardInstallmentsPlan = PaymentMethodDetailsCardInstallmentsPlan with
    count : Optional Int
        -- ^ For `fixed_count` installment plans, this is the
        --   number of installment payments your customer will
        --   make to their credit card.
    interval : Optional Text
        -- ^ For `fixed_count` installment plans, this is the
        --   interval between installment payments your customer
        --   will make to their credit card. One of `month`.
    type_ : Optional Text
        -- ^ Type of installment plan, one of `fixed_count`.
      deriving (Eq, Ord, Show)

data DeletedSku = DeletedSku with
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    deleted : Bool
        -- ^ Always true for a deleted object
      deriving (Eq, Ord, Show)

data ThreeDSecureUsage = ThreeDSecureUsage with
    supported : Bool
        -- ^ Whether 3D Secure is supported on this card.
      deriving (Eq, Ord, Show)

data IssuingCardholderIndividualDob = IssuingCardholderIndividualDob with
    day : Optional Int
        -- ^ The day of birth, between 1 and 31.
    year : Optional Int
        -- ^ The four-digit year of birth.
    month : Optional Int
        -- ^ The month of birth, between 1 and 12.
      deriving (Eq, Ord, Show)

data TerminalLocationMetadata = TerminalLocationMetadata with
      deriving (Eq, Ord, Show)

data TerminalLocation = TerminalLocation with
    displayName : Optional Text
        -- ^ The display name of the location.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    address : Address
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : TerminalLocationMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
      deriving (Eq, Ord, Show)

data TaxId = TaxId with
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    country : Optional Text
        -- ^ Two-letter ISO code representing the country of the
        --   tax ID.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    value : Text
        -- ^ Value of the tax ID.
    customer : Text
        -- ^ ID of the customer.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    type_ : Optional Text
        -- ^ Type of the tax ID, one of `eu_vat`, `nz_gst`,
        --   `au_abn`, `in_gst`, `no_vat`, `za_vat`, `ch_vat`,
        --   `mx_rfc`, `sg_uen`, `ru_inn`, `ca_bn`, `hk_br`,
        --   `es_cif`, `tw_vat`, `th_vat`, `jp_cn`, `li_uid`,
        --   `my_itn`, `us_ein`, `kr_brn`, or `ca_qst`. Note that
        --   some legacy tax IDs have type `unknown`
    verification : TaxIdVerification
      deriving (Eq, Ord, Show)

data IssuingCardPin = IssuingCardPin with
    card : IssuingCard
    pin : Optional Text
        -- ^ The PIN (4 digits number)
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
      deriving (Eq, Ord, Show)

data MandateSepaDebit = MandateSepaDebit with
    url : Text
        -- ^ The URL of the mandate. This URL generally contains
        --   sensitive information about the customer and should
        --   be shared with them exclusively.
    reference : Text
        -- ^ The unique reference of the mandate.
      deriving (Eq, Ord, Show)

data StatusTransitions = StatusTransitions with
    fulfiled : Optional Int
        -- ^ The time that the order was fulfilled.
    paid : Optional Int
        -- ^ The time that the order was paid.
    canceled : Optional Int
        -- ^ The time that the order was canceled.
    returned : Optional Int
        -- ^ The time that the order was returned.
      deriving (Eq, Ord, Show)

data CountrySpecSupportedBankAccountCurrencies = CountrySpecSupportedBankAccountCurrencies with
      deriving (Eq, Ord, Show)

data CountrySpec = CountrySpec with
    verificationFields : Optional CountrySpecVerificationFields
    supportedBankAccountCurrencies : Optional CountrySpecSupportedBankAccountCurrencies
        -- ^ Currencies that can be accepted in the specific
        --   country (for transfers).
    supportedPaymentMethods : [Text]
        -- ^ Payment methods available in the specified country.
        --   You may need to enable some payment methods (e.g.,
        --   [ACH](https://stripe.com/docs/ach)) on your account
        --   before they appear in this list. The `stripe` payment
        --   method refers to [charging through your
        --   platform](https://stripe.com/docs/connect/destination-charges).
    supportedTransferCountries : [Text]
        -- ^ Countries that can accept transfers from the
        --   specified country.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    supportedPaymentCurrencies : [Text]
        -- ^ Currencies that can be accepted in the specified
        --   country (for payments).
    id : Text
        -- ^ Unique identifier for the object. Represented as the
        --   ISO country code for this country.
    defaultCurrency : Optional Text
        -- ^ The default currency for this country. This applies
        --   to both payment methods and bank accounts.
      deriving (Eq, Ord, Show)

data PaymentMethodCardGeneratedCard = PaymentMethodCardGeneratedCard with
    paymentMethodDetails : Optional PaymentMethodDetails
    charge : Optional Text
        -- ^ The charge that created this object.
      deriving (Eq, Ord, Show)

data CountrySpecVerificationFields = CountrySpecVerificationFields with
    company : CountrySpecVerificationFieldDetails
    individual : CountrySpecVerificationFieldDetails
      deriving (Eq, Ord, Show)

data PaymentMethodMetadata = PaymentMethodMetadata with
      deriving (Eq, Ord, Show)

data PaymentMethod = PaymentMethod with
    card : Optional PaymentMethodCard
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    ideal : Optional PaymentMethodIdeal
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    fpx : Optional PaymentMethodFpx
    customer : Optional Text
        -- ^ The ID of the Customer to which this PaymentMethod is
        --   saved. This will not be set when the PaymentMethod
        --   has not been saved to a Customer.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : PaymentMethodMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    cardPresent : Optional PaymentMethodCardPresent
    type_ : Optional Text
        -- ^ The type of the PaymentMethod. An additional hash is
        --   included on the PaymentMethod with a name matching
        --   this value. It contains additional information
        --   specific to the PaymentMethod type.
    billingDetails : Optional BillingDetails
    sepaDebit : Optional PaymentMethodSepaDebit
      deriving (Eq, Ord, Show)

data AccountRequirements = AccountRequirements with
    disabledReason : Optional Text
        -- ^ If the account is disabled, this string describes why
        --   the account can’t create charges or receive payouts.
        --   Can be `requirements.past_due`,
        --   `requirements.pending_verification`,
        --   `rejected.fraud`, `rejected.terms_of_service`,
        --   `rejected.listed`, `rejected.other`, `listed`,
        --   `under_review`, or `other`.
    currentDeadline : Optional Int
        -- ^ The date the fields in `currently_due` must be
        --   collected by to keep payouts enabled for the account.
        --   These fields might block payouts sooner if the next
        --   threshold is reached before these fields are
        --   collected.
    currentlyDue : [Text]
        -- ^ The fields that need to be collected to keep the
        --   account enabled. If not collected by the
        --   `current_deadline`, these fields appear in `past_due`
        --   as well, and the account is disabled.
    eventuallyDue : [Text]
        -- ^ The fields that need to be collected assuming all
        --   volume thresholds are reached. As they become
        --   required, these fields appear in `currently_due` as
        --   well, and the `current_deadline` is set.
    pastDue : [Text]
        -- ^ The fields that weren't collected by the
        --   `current_deadline`. These fields need to be collected
        --   to re-enable the account.
    pendingVerification : [Text]
        -- ^ Fields that may become required depending on the
        --   results of verification or review. An empty array
        --   unless an asynchronous verification is pending. If
        --   verification fails, the fields in this array become
        --   required and move to `currently_due` or `past_due`.
      deriving (Eq, Ord, Show)

data IssuingAuthorizationMerchantData = IssuingAuthorizationMerchantData with
    postalCode : Optional Text
        -- ^ Postal code where the seller is located
    state : Optional Text
        -- ^ State where the seller is located
    country : Optional Text
        -- ^ Country where the seller is located
    category : Text
        -- ^ A categorization of the seller's type of business.
        --   See our [merchant categories
        --   guide](https://stripe.com/docs/issuing/merchant-categories)
        --   for a list of possible values.
    url : Optional Text
        -- ^ The url an online purchase was made from
    networkId : Optional Text
        -- ^ Identifier assigned to the seller by the card brand
    name : Optional Text
        -- ^ Name of the seller
    city : Optional Text
        -- ^ City where the seller is located
      deriving (Eq, Ord, Show)

data IssuingCardShipping = IssuingCardShipping with
    carrier : Optional Text
        -- ^ The delivery company that shipped a card.
    status : Optional Text
        -- ^ The delivery status of the card.
    eta : Optional Int
        -- ^ A unix timestamp representing a best estimate of when
        --   the card will be delivered.
    trackingNumber : Optional Text
        -- ^ A tracking number for a card shipment.
    address : Address
    name : Text
        -- ^ Recipient name.
    speed : Text
        -- ^ Shipment speed.
    type_ : Optional Text
        -- ^ Packaging options.
    trackingUrl : Optional Text
        -- ^ A link to the shipping carrier's site where you can
        --   view detailed information about a card shipment.
      deriving (Eq, Ord, Show)

data Application = Application with
    name : Optional Text
        -- ^ The name of the application.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
      deriving (Eq, Ord, Show)

data InvoiceSettingCustomerSettingCustomFields = InvoiceSettingCustomerSettingCustomFields with
      deriving (Eq, Ord, Show)

data InvoiceSettingCustomerSetting = InvoiceSettingCustomerSetting with
    footer : Optional Text
        -- ^ Default footer to be displayed on invoices for this
        --   customer.
    customFields : [InvoiceSettingCustomField]
        -- ^ Default custom fields to be displayed on invoices for
        --   this customer.
    defaultPaymentMethod : Optional Text
        -- ^ ID of a payment method that's attached to the
        --   customer, to be used as the customer's default
        --   payment method for subscriptions and invoices.
      deriving (Eq, Ord, Show)

data InvoiceThresholdReasonItemReasons = InvoiceThresholdReasonItemReasons with
      deriving (Eq, Ord, Show)

data InvoiceThresholdReason = InvoiceThresholdReason with
    amountGte : Optional Int
        -- ^ The total invoice amount threshold boundary if it
        --   triggered the threshold invoice.
    itemReasons : [InvoiceItemThresholdReason]
        -- ^ Indicates which line items triggered a threshold
        --   invoice.
      deriving (Eq, Ord, Show)

data DeletedApplePayDomain = DeletedApplePayDomain with
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    deleted : Bool
        -- ^ Always true for a deleted object
      deriving (Eq, Ord, Show)

data BalanceTransactionFeeDetails = BalanceTransactionFeeDetails with
      deriving (Eq, Ord, Show)

data BalanceTransaction = BalanceTransaction with
    status : Text
        -- ^ If the transaction's net funds are available in the
        --   Stripe balance yet. Either `available` or `pending`.
    amount : Int
        -- ^ Gross amount of the transaction, in %s.
    net : Int
        -- ^ Net amount of the transaction, in %s.
    availableOn : Optional Int
        -- ^ The date the transaction's net funds will become
        --   available in the Stripe balance.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    feeDetails : [Fee]
        -- ^ Detailed breakdown of fees (in %s) paid for this
        --   transaction.
    fee : Int
        -- ^ Fees (in %s) paid for this transaction.
    exchangeRate : Optional Decimal
        -- ^ The exchange rate used, if applicable, for this
        --   transaction. Specifically, if money was converted
        --   from currency A to currency B, then the `amount` in
        --   currency A, times `exchange_rate`, would be the
        --   `amount` in currency B. For example, suppose you
        --   charged a customer 10.00 EUR. Then the
        --   PaymentIntent's `amount` would be `1000` and
        --   `currency` would be `eur`. Suppose this was converted
        --   into 12.34 USD in your Stripe account. Then the
        --   BalanceTransaction's `amount` would be `1234`,
        --   `currency` would be `usd`, and `exchange_rate` would
        --   be `1.234`.
    reportingCategory : Optional Text
        -- ^ [Learn
        --   more](https://stripe.com/docs/reports/reporting-categories)
        --   about how reporting categories can help you
        --   understand balance transactions from an accounting
        --   perspective.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    source : Optional Text
        -- ^ The Stripe object to which this transaction is
        --   related.
    id : Text
        -- ^ Unique identifier for the object.
    type_ : Optional Text
        -- ^ Transaction type: `adjustment`, `advance`,
        --   `advance_funding`, `application_fee`,
        --   `application_fee_refund`, `charge`,
        --   `connect_collection_transfer`,
        --   `issuing_authorization_hold`,
        --   `issuing_authorization_release`,
        --   `issuing_transaction`, `payment`,
        --   `payment_failure_refund`, `payment_refund`, `payout`,
        --   `payout_cancel`, `payout_failure`, `refund`,
        --   `refund_failure`, `reserve_transaction`,
        --   `reserved_funds`, `stripe_fee`, `stripe_fx_fee`,
        --   `tax_fee`, `topup`, `topup_reversal`, `transfer`,
        --   `transfer_cancel`, `transfer_failure`, or
        --   `transfer_refund`. [Learn
        --   more](https://stripe.com/docs/reports/balance-transaction-types)
        --   about balance transaction types and what they
        --   represent. If you are looking to classify
        --   transactions for accounting purposes, you might want
        --   to consider `reporting_category` instead.
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users.
      deriving (Eq, Ord, Show)

data Discount = Discount with
    coupon : Coupon
    start : Int
        -- ^ Date that the coupon was applied.
    customer : Optional Text
        -- ^ The ID of the customer associated with this discount.
    end : Optional Int
        -- ^ If the coupon has a duration of `repeating`, the date
        --   that this discount will end. If the coupon has a
        --   duration of `once` or `forever`, this attribute will
        --   be null.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    subscription : Optional Text
        -- ^ The subscription that this coupon is applied to, if
        --   it is applied to a particular subscription.
      deriving (Eq, Ord, Show)

data SourceTypeMultibanco = SourceTypeMultibanco with
    reference : Optional Text
    refundAccountHolderAddressPostalCode : Optional Text
    refundAccountHolderAddressState : Optional Text
    refundAccountHolderAddressCity : Optional Text
    refundAccountHolderName : Optional Text
    refundIban : Optional Text
    refundAccountHolderAddressLine2 : Optional Text
    entity : Optional Text
    refundAccountHolderAddressLine1 : Optional Text
    refundAccountHolderAddressCountry : Optional Text
      deriving (Eq, Ord, Show)

data SetupIntentPaymentMethodOptionsCard = SetupIntentPaymentMethodOptionsCard with
    requestThreeDSecure : Optional Text
        -- ^ We strongly recommend that you rely on our SCA Engine
        --   to automatically prompt your customers for
        --   authentication based on risk level and [other
        --   requirements](https://stripe.com/docs/strong-customer-authentication).
        --   However, if you wish to request 3D Secure based on
        --   logic from your own fraud engine, provide this
        --   option. Permitted values include: `automatic` or
        --   `any`. If not provided, defaults to `automatic`. Read
        --   our guide on [manually requesting 3D
        --   Secure](https://stripe.com/docs/payments/3d-secure#manual-three-ds)
        --   for more information on how this configuration
        --   interacts with Radar and our SCA Engine.
      deriving (Eq, Ord, Show)

data SetupIntentNextActionUseStripeSdk = SetupIntentNextActionUseStripeSdk with
      deriving (Eq, Ord, Show)

data SetupIntentNextAction = SetupIntentNextAction with
    redirectToUrl : Optional SetupIntentNextActionRedirectToUrl
    type_ : Optional Text
        -- ^ Type of the next action to perform, one of
        --   `redirect_to_url` or `use_stripe_sdk`.
    useStripeSdk : Optional SetupIntentNextActionUseStripeSdk
        -- ^ When confirming a SetupIntent with Stripe.js,
        --   Stripe.js depends on the contents of this dictionary
        --   to invoke authentication flows. The shape of the
        --   contents is subject to change and is only intended to
        --   be used by Stripe.js.
      deriving (Eq, Ord, Show)

data PaymentMethodCardWalletAmexExpressCheckout = PaymentMethodCardWalletAmexExpressCheckout with
      deriving (Eq, Ord, Show)

data RadarReviewResourceSession = RadarReviewResourceSession with
    browser : Optional Text
        -- ^ The browser used in this browser session (e.g.,
        --   `Chrome`).
    platform : Optional Text
        -- ^ The platform for the browser session (e.g.,
        --   `Macintosh`).
    device : Optional Text
        -- ^ Information about the device used for the browser
        --   session (e.g., `Samsung SM-G930T`).
    version : Optional Text
        -- ^ The version for the browser session (e.g.,
        --   `61.0.3163.100`).
      deriving (Eq, Ord, Show)

data PaymentMethodDetailsCardWalletGooglePay = PaymentMethodDetailsCardWalletGooglePay with
      deriving (Eq, Ord, Show)

data PersonMetadata = PersonMetadata with
      deriving (Eq, Ord, Show)

data Person = Person with
    relationship : Optional PersonRelationship
    requirements : Optional PersonRequirements
    email : Optional Text
    addressKana : Optional LegalEntityJapanAddress
    phone : Optional Text
    lastNameKanji : Optional Text
    firstName : Optional Text
    lastNameKana : Optional Text
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    address : Optional Address
    account : Text
    dob : Optional LegalEntityDob
    gender : Optional Text
    firstNameKanji : Optional Text
    lastName : Optional Text
    maidenName : Optional Text
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : Optional PersonMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    firstNameKana : Optional Text
    ssnLast4Provided : Optional Bool
    addressKanji : Optional LegalEntityJapanAddress
    idNumberProvided : Optional Bool
    verification : Optional LegalEntityPersonVerification
      deriving (Eq, Ord, Show)

data CardMandatePaymentMethodDetails = CardMandatePaymentMethodDetails with
      deriving (Eq, Ord, Show)

data AccountCapabilityRequirements = AccountCapabilityRequirements with
    disabledReason : Optional Text
        -- ^ If the capability is disabled, this string describes
        --   why. Possible values are `requirement.fields_needed`,
        --   `pending.onboarding`, `pending.review`,
        --   `rejected_fraud`, or `rejected.other`.
    currentDeadline : Optional Int
        -- ^ The date the fields in `currently_due` must be
        --   collected by to keep the capability enabled for the
        --   account.
    currentlyDue : [Text]
        -- ^ The fields that need to be collected to keep the
        --   capability enabled. If not collected by the
        --   `current_deadline`, these fields appear in `past_due`
        --   as well, and the capability is disabled.
    eventuallyDue : [Text]
        -- ^ The fields that need to be collected assuming all
        --   volume thresholds are reached. As they become
        --   required, these fields appear in `currently_due` as
        --   well, and the `current_deadline` is set.
    pastDue : [Text]
        -- ^ The fields that weren't collected by the
        --   `current_deadline`. These fields need to be collected
        --   to enable the capability for the account.
    pendingVerification : [Text]
        -- ^ Fields that may become required depending on the
        --   results of verification or review. An empty array
        --   unless an asynchronous verification is pending. If
        --   verification fails, the fields in this array become
        --   required and move to `currently_due` or `past_due`.
      deriving (Eq, Ord, Show)

data SourceTypeCardPresent = SourceTypeCardPresent with
    dataType : Optional Text
    readMethod : Optional Text
    dedicatedFileName : Optional Text
    country : Optional Text
    applicationPreferredName : Optional Text
    fingerprint : Optional Text
    evidenceCustomerSignature : Optional Text
    posEntryMode : Optional Text
    brand : Optional Text
    emvAuthData : Optional Text
    funding : Optional Text
    transactionStatusInformation : Optional Text
    terminalVerificationResults : Optional Text
    applicationCryptogram : Optional Text
    cvmType : Optional Text
    evidenceTransactionCertificate : Optional Text
    expMonth : Optional Int
    authorizationResponseCode : Optional Text
    reader : Optional Text
    expYear : Optional Int
    last4 : Optional Text
    posDeviceId : Optional Text
    authorizationCode : Optional Text
      deriving (Eq, Ord, Show)

data PaymentMethodCardPresent = PaymentMethodCardPresent with
      deriving (Eq, Ord, Show)

data SourceTransactionGbpCreditTransferData = SourceTransactionGbpCreditTransferData with
    fingerprint : Optional Text
        -- ^ Bank account fingerprint associated with the Stripe
        --   owned bank account receiving the transfer.
    reference : Optional Text
        -- ^ Sender entered arbitrary information about the
        --   transfer.
    senderSortCode : Optional Text
        -- ^ Sender sort code associated with the transfer.
    senderName : Optional Text
        -- ^ Sender name associated with the transfer.
    last4 : Optional Text
        -- ^ Last 4 digits of sender account number associated
        --   with the transfer.
    senderAccountNumber : Optional Text
        -- ^ Sender account number associated with the transfer.
    fundingMethod : Optional Text
        -- ^ The credit transfer rails the sender used to push
        --   this transfer. The possible rails are: Faster
        --   Payments, BACS, CHAPS, and wire transfers. Currently
        --   only Faster Payments is supported.
      deriving (Eq, Ord, Show)

data LegalEntityJapanAddress = LegalEntityJapanAddress with
    line2 : Optional Text
        -- ^ Building details.
    postalCode : Optional Text
        -- ^ ZIP or postal code.
    state : Optional Text
        -- ^ Prefecture.
    town : Optional Text
        -- ^ Town/cho-me.
    country : Optional Text
        -- ^ Two-letter country code ([ISO 3166-1
        --   alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)).
    city : Optional Text
        -- ^ City/Ward.
    line1 : Optional Text
        -- ^ Block/Building number.
      deriving (Eq, Ord, Show)

data IssuingDisputeDuplicateEvidence = IssuingDisputeDuplicateEvidence with
    originalTransaction : Optional Text
        -- ^ Transaction (e.g., ipi_...) that the disputed
        --   transaction is a duplicate of. Of the two or more
        --   transactions that are copies of each other, this is
        --   original undisputed one.
    disputeExplanation : Optional Text
        -- ^ Brief freeform text explaining why you are disputing
        --   this transaction.
    uncategorizedFile : Optional Text
        -- ^ (ID of a [file
        --   upload](https://stripe.com/docs/guides/file-upload))
        --   Additional file evidence supporting your dispute.
      deriving (Eq, Ord, Show)

data DeletedTerminalReader = DeletedTerminalReader with
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    deleted : Bool
        -- ^ Always true for a deleted object
      deriving (Eq, Ord, Show)

data IssuingDisputeEvidence = IssuingDisputeEvidence with
    other : Optional IssuingDisputeOtherEvidence
    duplicate : Optional IssuingDisputeDuplicateEvidence
    fraudulent : Optional IssuingDisputeFraudulentEvidence
    productNotReceived : Optional IssuingDisputeProductNotReceivedEvidence
      deriving (Eq, Ord, Show)

data Period = Period with
    start : Optional Int
        -- ^ The start date of this usage period. All usage after
        --   this point in time is included.
    end : Optional Int
        -- ^ The end date of this usage period. All usage up to
        --   and including this point in time is included.
      deriving (Eq, Ord, Show)

data BalanceAmount = BalanceAmount with
    amount : Int
        -- ^ Balance amount.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    sourceTypes : Optional BalanceAmountBySourceType
      deriving (Eq, Ord, Show)

data SourceMandateNotificationSepaDebitData = SourceMandateNotificationSepaDebitData with
    creditorIdentifier : Optional Text
        -- ^ SEPA creditor ID.
    mandateReference : Optional Text
        -- ^ Mandate reference associated with the debit.
    last4 : Optional Text
        -- ^ Last 4 digits of the account number associated with
        --   the debit.
      deriving (Eq, Ord, Show)

data InvoiceLineItemPeriod = InvoiceLineItemPeriod with
    start : Int
        -- ^ Start of the line item's billing period
    end : Int
        -- ^ End of the line item's billing period
      deriving (Eq, Ord, Show)

data PaymentMethodDetailsCardPresent = PaymentMethodDetailsCardPresent with
    receipt : Optional PaymentMethodDetailsCardPresentReceipt
    readMethod : Optional Text
        -- ^ How were card details read in this transaction. Can
        --   be contact_emv, contactless_emv,
        --   magnetic_stripe_fallback, magnetic_stripe_track2, or
        --   contactless_magstripe_mode
    country : Optional Text
        -- ^ Two-letter ISO code representing the country of the
        --   card. You could use this attribute to get a sense of
        --   the international breakdown of cards you've
        --   collected.
    fingerprint : Optional Text
        -- ^ Uniquely identifies this particular card number. You
        --   can use this attribute to check whether two customers
        --   who've signed up with you are using the same card
        --   number, for example.
    brand : Optional Text
        -- ^ Card brand. Can be `amex`, `diners`, `discover`,
        --   `jcb`, `mastercard`, `unionpay`, `visa`, or
        --   `unknown`.
    network : Optional Text
        -- ^ Identifies which network this charge was processed
        --   on. Can be `amex`, `diners`, `discover`, `interac`,
        --   `jcb`, `mastercard`, `unionpay`, `visa`, or
        --   `unknown`.
    emvAuthData : Optional Text
        -- ^ Authorization response cryptogram.
    funding : Optional Text
        -- ^ Card funding type. Can be `credit`, `debit`,
        --   `prepaid`, or `unknown`.
    generatedCard : Optional Text
        -- ^ ID of a card PaymentMethod generated from the
        --   card_present PaymentMethod that may be attached to a
        --   Customer for future transactions. Only present if it
        --   was possible to generate a card PaymentMethod.
    expMonth : Optional Int
        -- ^ Two-digit number representing the card's expiration
        --   month.
    expYear : Optional Int
        -- ^ Four-digit number representing the card's expiration
        --   year.
    last4 : Optional Text
        -- ^ The last four digits of the card.
      deriving (Eq, Ord, Show)

data BankAccountMetadata = BankAccountMetadata with
      deriving (Eq, Ord, Show)

data BankAccount = BankAccount with
    status : Text
        -- ^ For bank accounts, possible values are `new`,
        --   `validated`, `verified`, `verification_failed`, or
        --   `errored`. A bank account that hasn't had any
        --   activity or validation performed is `new`. If Stripe
        --   can determine that the bank account exists, its
        --   status will be `validated`. Note that there often
        --   isn’t enough information to know (e.g., for smaller
        --   credit unions), and the validation is not always run.
        --   If customer bank account verification has succeeded,
        --   the bank account status will be `verified`. If the
        --   verification failed for any reason, such as
        --   microdeposit failure, the status will be
        --   `verification_failed`. If a transfer sent to this
        --   bank account fails, we'll set the status to `errored`
        --   and will not continue to send transfers until the
        --   bank details are updated. For external accounts,
        --   possible values are `new` and `errored`. Validations
        --   aren't run against external accounts because they're
        --   only used for payouts. This means the other statuses
        --   don't apply. If a transfer fails, the status is set
        --   to `errored` and transfers are stopped until account
        --   details are updated.
    defaultForCurrency : Optional Bool
        -- ^ Whether this bank account is the default external
        --   account for its currency.
    country : Text
        -- ^ Two-letter ISO code representing the country the bank
        --   account is located in.
    fingerprint : Optional Text
        -- ^ Uniquely identifies this particular bank account. You
        --   can use this attribute to check whether two bank
        --   accounts are the same.
    account : Optional Text
        -- ^ The ID of the account that the bank account is
        --   associated with.
    accountHolderType : Optional Text
        -- ^ The type of entity that holds the account. This can
        --   be either `individual` or `company`.
    customer : Optional Text
        -- ^ The ID of the customer that the bank account is
        --   associated with.
    currency : Text
        -- ^ Three-letter [ISO code for the
        --   currency](https://stripe.com/docs/payouts) paid out
        --   to the bank account.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : Optional BankAccountMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    routingNumber : Optional Text
        -- ^ The routing transit number for the bank account.
    bankName : Optional Text
        -- ^ Name of the bank associated with the routing number
        --   (e.g., `WELLS FARGO`).
    accountHolderName : Optional Text
        -- ^ The name of the person or business that owns the bank
        --   account.
    last4 : Text
        -- ^ The last four digits of the bank account number.
      deriving (Eq, Ord, Show)

data CheckoutSessionMetadata = CheckoutSessionMetadata with
      deriving (Eq, Ord, Show)

data CheckoutSessionDisplayItems = CheckoutSessionDisplayItems with
      deriving (Eq, Ord, Show)

data CheckoutSession = CheckoutSession with
    clientReferenceId : Optional Text
        -- ^ A unique string to reference the Checkout Session.
        --   This can be a customer ID, a cart ID, or similar, and
        --   can be used to reconcile the session with your
        --   internal systems.
    successUrl : Optional Text
        -- ^ The URL the customer will be directed to after the
        --   payment or subscription creation is successful.
    cancelUrl : Optional Text
        -- ^ The URL the customer will be directed to if they
        --   decide to cancel payment and return to your website.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    locale : Optional Text
        -- ^ The IETF language tag of the locale Checkout is
        --   displayed in. If blank or `auto`, the browser's
        --   locale is used.
    mode : Optional Text
        -- ^ The mode of the Checkout Session, one of `payment`,
        --   `setup`, or `subscription`.
    billingAddressCollection : Optional Text
        -- ^ The value (`auto` or `required`) for whether Checkout
        --   collected the customer's billing address.
    paymentIntent : Optional Text
        -- ^ The ID of the PaymentIntent for Checkout Sessions in
        --   `payment` mode.
    customer : Optional Text
        -- ^ The ID of the customer for this session. For Checkout
        --   Sessions in `payment` or `subscription` mode,
        --   Checkout will create a new customer object based on
        --   information provided during the session unless an
        --   existing customer was provided when the session was
        --   created.
    setupIntent : Optional Text
        -- ^ The ID of the SetupIntent for Checkout Sessions in
        --   `setup` mode.
    customerEmail : Optional Text
        -- ^ If provided, this value will be used when the
        --   Customer object is created. If not provided,
        --   customers will be asked to enter their email address.
        --   Use this parameter to prefill customer data if you
        --   already have an email on file. To access information
        --   about the customer once a session is complete, use
        --   the `customer` field.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : Optional CheckoutSessionMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object. Used to pass to
        --   `redirectToCheckout` in Stripe.js.
    submitType : Optional Text
        -- ^ Describes the type of transaction being performed by
        --   Checkout in order to customize relevant text on the
        --   page, such as the submit button. `submit_type` can
        --   only be specified on Checkout Sessions in `payment`
        --   mode, but not Checkout Sessions in `subscription` or
        --   `setup` mode.
    subscription : Optional Text
        -- ^ The ID of the subscription for Checkout Sessions in
        --   `subscription` mode.
    displayItems : [CheckoutSessionDisplayItem]
        -- ^ The line items, plans, or SKUs purchased by the
        --   customer.
    paymentMethodTypes : [Text]
        -- ^ A list of the types of payment methods (e.g. card)
        --   this Checkout Session is allowed to accept.
      deriving (Eq, Ord, Show)

data PaymentSourceMetadata = PaymentSourceMetadata with
      deriving (Eq, Ord, Show)

data PaymentSource = PaymentSource with
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : Optional PaymentSourceMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
      deriving (Eq, Ord, Show)

data AccountCapabilities = AccountCapabilities with
    cardIssuing : Optional Text
        -- ^ The status of the card issuing capability of the
        --   account, or whether you can use Issuing to distribute
        --   funds on cards
    cardPayments : Optional Text
        -- ^ The status of the card payments capability of the
        --   account, or whether the account can directly process
        --   credit and debit card charges.
    legacyPayments : Optional Text
        -- ^ The status of the legacy payments capability of the
        --   account.
    transfers : Optional Text
        -- ^ The status of the transfers capability of the
        --   account, or whether your platform can transfer funds
        --   to the account.
      deriving (Eq, Ord, Show)

data DeletedAlipayAccount = DeletedAlipayAccount with
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    deleted : Bool
        -- ^ Always true for a deleted object
      deriving (Eq, Ord, Show)

data PaymentMethodCardWalletGooglePay = PaymentMethodCardWalletGooglePay with
      deriving (Eq, Ord, Show)

data DeletedBitcoinReceiver = DeletedBitcoinReceiver with
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    deleted : Bool
        -- ^ Always true for a deleted object
      deriving (Eq, Ord, Show)

data CouponMetadata = CouponMetadata with
      deriving (Eq, Ord, Show)

data Coupon = Coupon with
    maxRedemptions : Optional Int
        -- ^ Maximum number of times this coupon can be redeemed,
        --   in total, across all customers, before it is no
        --   longer valid.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    amountOff : Optional Int
        -- ^ Amount (in the `currency` specified) that will be
        --   taken off the subtotal of any invoices for this
        --   customer.
    name : Optional Text
        -- ^ Name of the coupon displayed to customers on for
        --   instance invoices or receipts.
    currency : Optional Text
        -- ^ If `amount_off` has been set, the three-letter [ISO
        --   code for the
        --   currency](https://stripe.com/docs/currencies) of the
        --   amount to take off.
    durationInMonths : Optional Int
        -- ^ If `duration` is `repeating`, the number of months
        --   the coupon applies. Null if coupon `duration` is
        --   `forever` or `once`.
    redeemBy : Optional Int
        -- ^ Date after which the coupon can no longer be
        --   redeemed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : CouponMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    percentOff : Optional Decimal
        -- ^ Percent that will be taken off the subtotal of any
        --   invoices for this customer for the duration of the
        --   coupon. For example, a coupon with percent_off of 50
        --   will make a %s100 invoice %s50 instead.
    duration : Text
        -- ^ One of `forever`, `once`, and `repeating`. Describes
        --   how long a customer who applies this coupon will get
        --   the discount.
    timesRedeemed : Optional Int
        -- ^ Number of times this coupon has been applied to a
        --   customer.
    valid : Bool
        -- ^ Taking account of the above properties, whether this
        --   coupon can still be applied to a customer.
      deriving (Eq, Ord, Show)

data TaxRateMetadata = TaxRateMetadata with
      deriving (Eq, Ord, Show)

data TaxRate = TaxRate with
    inclusive : Bool
        -- ^ This specifies if the tax rate is inclusive or
        --   exclusive.
    displayName : Optional Text
        -- ^ The display name of the tax rates as it will appear
        --   to your customer on their receipt email, PDF, and the
        --   hosted invoice page.
    jurisdiction : Optional Text
        -- ^ The jurisdiction for the tax rate.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    active : Bool
        -- ^ Defaults to `true`. When set to `false`, this tax
        --   rate cannot be applied to objects in the API, but
        --   will still be applied to subscriptions and invoices
        --   that already have it set.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : TaxRateMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    percentage : Decimal
        -- ^ This represents the tax rate percent out of 100.
    description : Optional Text
        -- ^ An arbitrary string attached to the tax rate for your
        --   internal use only. It will not be visible to your
        --   customers.
      deriving (Eq, Ord, Show)

data Error = Error with
    error : ApiErrors
      deriving (Eq, Ord, Show)

data AccountDashboardSettings = AccountDashboardSettings with
    displayName : Optional Text
        -- ^ The display name for this account. This is used on
        --   the Stripe Dashboard to differentiate between
        --   accounts.
    timezone : Optional Text
        -- ^ The timezone used in the Stripe Dashboard for this
        --   account. A list of possible time zone values is
        --   maintained at the [IANA Time Zone
        --   Database](http://www.iana.org/time-zones).
      deriving (Eq, Ord, Show)

data PaymentMethodDetailsMultibanco = PaymentMethodDetailsMultibanco with
    reference : Optional Text
        -- ^ Reference number associated with this Multibanco
        --   payment.
    entity : Optional Text
        -- ^ Entity number associated with this Multibanco
        --   payment.
      deriving (Eq, Ord, Show)

data SetupIntentNextActionRedirectToUrl = SetupIntentNextActionRedirectToUrl with
    url : Optional Text
        -- ^ The URL you must redirect your customer to in order
        --   to authenticate.
    returnUrl : Optional Text
        -- ^ If the customer does not exit their browser while
        --   authenticating, they will be redirected to this
        --   specified URL after completion.
      deriving (Eq, Ord, Show)

data AccountTosAcceptance = AccountTosAcceptance with
    userAgent : Optional Text
        -- ^ The user agent of the browser from which the Stripe
        --   Services Agreement was accepted by the account
        --   representative
    ip : Optional Text
        -- ^ The IP address from which the Stripe Services
        --   Agreement was accepted by the account representative
    date : Optional Int
        -- ^ The Unix timestamp marking when the Stripe Services
        --   Agreement was accepted by the account representative
      deriving (Eq, Ord, Show)

data SourceTypeThreeDSecure = SourceTypeThreeDSecure with
    card : Optional Text
    dynamicLast4 : Optional Text
    tokenizationMethod : Optional Text
    country : Optional Text
    fingerprint : Optional Text
    addressZipCheck : Optional Text
    brand : Optional Text
    funding : Optional Text
    customer : Optional Text
    name : Optional Text
    addressLine1Check : Optional Text
    cvcCheck : Optional Text
    expMonth : Optional Int
    expYear : Optional Int
    threeDSecure : Optional Text
    last4 : Optional Text
    authenticated : Optional Bool
      deriving (Eq, Ord, Show)

data IssuingDisputeMetadata = IssuingDisputeMetadata with
      deriving (Eq, Ord, Show)

data IssuingDispute = IssuingDispute with
    status : Text
        -- ^ Current status of dispute. One of `unsubmitted`,
        --   `under_review`, `won`, or `lost`.
    amount : Int
        -- ^ Disputed amount. Usually the amount of the
        --   `disputed_transaction`, but can differ (usually
        --   because of currency fluctuation or because only part
        --   of the order is disputed).
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    reason : Text
        -- ^ Reason for this dispute. One of `duplicate`,
        --   `product_not_received`, `fraudulent`, or `other`.
    currency : Text
        -- ^ The currency the `disputed_transaction` was made in.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : IssuingDisputeMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    disputedTransaction : Optional Text
        -- ^ The transaction being disputed.
    evidence : IssuingDisputeEvidence
      deriving (Eq, Ord, Show)

data SourceTypeIdeal = SourceTypeIdeal with
    bic : Optional Text
    ibanLast4 : Optional Text
    bank : Optional Text
    statementDescriptor : Optional Text
      deriving (Eq, Ord, Show)

data PaymentMethodIdeal = PaymentMethodIdeal with
    bic : Optional Text
        -- ^ The Bank Identifier Code of the customer's bank, if
        --   the bank was provided.
    bank : Optional Text
        -- ^ The customer's bank, if provided. Can be one of
        --   `abn_amro`, `asn_bank`, `bunq`, `handelsbanken`,
        --   `ing`, `knab`, `moneyou`, `rabobank`, `regiobank`,
        --   `sns_bank`, `triodos_bank`, or `van_lanschot`.
      deriving (Eq, Ord, Show)

data ThreeDSecureDetails = ThreeDSecureDetails with
    version : Text
        -- ^ The version of 3D Secure that was used for this
        --   payment.
    succeeded : Bool
        -- ^ Whether or not 3D Secure succeeded.
    authenticated : Bool
        -- ^ Whether or not authentication was performed. 3D
        --   Secure will succeed without authentication when the
        --   card is not enrolled.
      deriving (Eq, Ord, Show)

data PaymentMethodDetailsCardPresentReceipt = PaymentMethodDetailsCardPresentReceipt with
    cardholderVerificationMethod : Optional Text
        -- ^ How the cardholder verified ownership of the card.
    dedicatedFileName : Optional Text
        -- ^ EMV tag 84. Similar to the application identifier
        --   stored on the integrated circuit chip.
    applicationPreferredName : Optional Text
        -- ^ Mnenomic of the Application Identifier.
    transactionStatusInformation : Optional Text
        -- ^ An indication of various EMV functions performed
        --   during the transaction.
    terminalVerificationResults : Optional Text
        -- ^ The outcome of a series of EMV functions performed by
        --   the card reader.
    applicationCryptogram : Optional Text
        -- ^ EMV tag 9F26, cryptogram generated by the integrated
        --   circuit chip.
    authorizationResponseCode : Optional Text
        -- ^ EMV tag 8A. A code returned by the card issuer.
    authorizationCode : Optional Text
        -- ^ Identifier for this transaction.
      deriving (Eq, Ord, Show)

data ExternalAccountMetadata = ExternalAccountMetadata with
      deriving (Eq, Ord, Show)

data ExternalAccount = ExternalAccount with
    defaultForCurrency : Optional Bool
        -- ^ Whether this bank account is the default external
        --   account for its currency.
    country : Text
        -- ^ Two-letter ISO code representing the country the bank
        --   account is located in.
    fingerprint : Optional Text
        -- ^ Uniquely identifies this particular bank account. You
        --   can use this attribute to check whether two bank
        --   accounts are the same.
    account : Optional Text
        -- ^ The ID of the account that the bank account is
        --   associated with.
    customer : Optional Text
        -- ^ The ID of the customer that the bank account is
        --   associated with.
    currency : Text
        -- ^ Three-letter [ISO code for the
        --   currency](https://stripe.com/docs/payouts) paid out
        --   to the bank account.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : Optional ExternalAccountMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    last4 : Text
        -- ^ The last four digits of the bank account number.
      deriving (Eq, Ord, Show)

data IssuingCardDetails = IssuingCardDetails with
    card : IssuingCard
    expMonth : Optional Int
        -- ^ The expiration month of the card.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    expYear : Optional Int
        -- ^ The expiration year of the card.
    number : Text
        -- ^ The card number.
    cvc : Text
        -- ^ The CVC number for the card.
      deriving (Eq, Ord, Show)

data PaymentMethodDetailsCardInstallments = PaymentMethodDetailsCardInstallments with
    plan : Optional PaymentMethodDetailsCardInstallmentsPlan
      deriving (Eq, Ord, Show)

data PlanTiers = PlanTiers with
      deriving (Eq, Ord, Show)

data PlanMetadata = PlanMetadata with
      deriving (Eq, Ord, Show)

data Plan = Plan with
    amount : Optional Int
        -- ^ The amount in %s to be charged on the interval
        --   specified.
    usageType : Optional Text
        -- ^ Configures how the quantity per period should be
        --   determined. Can be either `metered` or `licensed`.
        --   `licensed` automatically bills the `quantity` set
        --   when adding it to a subscription. `metered`
        --   aggregates the total usage based on usage records.
        --   Defaults to `licensed`.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    tiersMode : Optional Text
        -- ^ Defines if the tiering price should be `graduated` or
        --   `volume` based. In `volume`-based tiering, the
        --   maximum quantity within a period determines the per
        --   unit price. In `graduated` tiering, pricing can
        --   change as the quantity grows.
    trialPeriodDays : Optional Int
        -- ^ Default number of trial days when subscribing a
        --   customer to this plan using
        --   [`trial_from_plan=true`](https://stripe.com/docs/api#create_subscription-trial_from_plan).
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    transformUsage : Optional TransformUsage
    tiers : [PlanTier]
        -- ^ Each element represents a pricing tier. This
        --   parameter requires `billing_scheme` to be set to
        --   `tiered`. See also the documentation for
        --   `billing_scheme`.
    billingScheme : Optional Text
        -- ^ Describes how to compute the price per period. Either
        --   `per_unit` or `tiered`. `per_unit` indicates that the
        --   fixed amount (specified in `amount`) will be charged
        --   per unit in `quantity` (for plans with
        --   `usage_type=licensed`), or per unit of total usage
        --   (for plans with `usage_type=metered`). `tiered`
        --   indicates that the unit pricing will be computed
        --   using a tiering strategy as defined using the `tiers`
        --   and `tiers_mode` attributes.
    active : Bool
        -- ^ Whether the plan is currently available for new
        --   subscriptions.
    interval : Text
        -- ^ The frequency at which a subscription is billed. One
        --   of `day`, `week`, `month` or `year`.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : PlanMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    amountDecimal : Optional Text
        -- ^ Same as `amount`, but contains a decimal value with
        --   at most 12 decimal places.
    nickname : Optional Text
        -- ^ A brief description of the plan, hidden from
        --   customers.
    intervalCount : Optional Int
        -- ^ The number of intervals (specified in the `interval`
        --   attribute) between subscription billings. For
        --   example, `interval=month` and `interval_count=3`
        --   bills every 3 months.
    product : Optional Text
        -- ^ The product whose pricing this plan determines.
    aggregateUsage : Optional Text
        -- ^ Specifies a usage aggregation strategy for plans of
        --   `usage_type=metered`. Allowed values are `sum` for
        --   summing up all usage during a period,
        --   `last_during_period` for using the last usage record
        --   reported within a period, `last_ever` for using the
        --   last usage record ever (across period bounds) or
        --   `max` which uses the usage record with the maximum
        --   reported usage during a period. Defaults to `sum`.
      deriving (Eq, Ord, Show)

data LegalEntityPersonVerificationDocument = LegalEntityPersonVerificationDocument with
    front : Optional Text
        -- ^ The front of an ID returned by a [file
        --   upload](https://stripe.com/docs/api#create_file) with
        --   a `purpose` value of `identity_document`.
    details : Optional Text
        -- ^ A user-displayable string describing the verification
        --   state of this document. For example, if a document is
        --   uploaded and the picture is too fuzzy, this may say
        --   "Identity document is too unclear to read".
    detailsCode : Optional Text
        -- ^ One of `document_corrupt`,
        --   `document_country_not_supported`, `document_expired`,
        --   `document_failed_copy`, `document_failed_other`,
        --   `document_failed_test_mode`, `document_fraudulent`,
        --   `document_failed_greyscale`, `document_incomplete`,
        --   `document_invalid`, `document_manipulated`,
        --   `document_missing_back`, `document_missing_front`,
        --   `document_not_readable`, `document_not_uploaded`,
        --   `document_photo_mismatch`, `document_too_large`, or
        --   `document_type_not_supported`. A machine-readable
        --   code specifying the verification state for this
        --   document.
    back : Optional Text
        -- ^ The back of an ID returned by a [file
        --   upload](https://stripe.com/docs/api#create_file) with
        --   a `purpose` value of `identity_document`.
      deriving (Eq, Ord, Show)

data ConnectCollectionTransfer = ConnectCollectionTransfer with
    destination : Text
        -- ^ ID of the account that funds are being collected for.
    amount : Int
        -- ^ Amount transferred, in %s.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
      deriving (Eq, Ord, Show)

data Token = Token with
    card : Optional Card
    used : Bool
        -- ^ Whether this token has already been used (tokens can
        --   be used only once).
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    bankAccount : Optional BankAccount
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    clientIp : Optional Text
        -- ^ IP address of the client that generated the token.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    type_ : Optional Text
        -- ^ Type of the token: `account`, `bank_account`, `card`,
        --   or `pii`.
      deriving (Eq, Ord, Show)

data IssuingVerification = IssuingVerification with
    card : Text
        -- ^ The id of the `Card` on which the verification was
        --   requested
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    verificationMethod : Optional Text
        -- ^ The method by which the cardholder will be sent a
        --   one-time code (one of `email` or `sms`)
    scope : Text
        -- ^ The scope of the verification (one of
        --   `card_pin_retrieve` or `card_pin_update`)
    expiresAt : Optional Int
        -- ^ Timestamp of the expiry for that verification
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
      deriving (Eq, Ord, Show)

data SourceTypeP24 = SourceTypeP24 with
    reference : Optional Text
      deriving (Eq, Ord, Show)

data DisputeEvidenceDetails = DisputeEvidenceDetails with
    hasEvidence : Optional Bool
        -- ^ Whether evidence has been staged for this dispute.
    submissionCount : Optional Int
        -- ^ The number of times evidence has been submitted.
        --   Typically, you may only submit evidence once.
    pastDue : Optional Bool
        -- ^ Whether the last evidence submission was submitted
        --   past the due date. Defaults to `false` if no evidence
        --   submissions have occurred. If `true`, then delivery
        --   of the latest evidence is *not* guaranteed.
    dueBy : Optional Int
        -- ^ Date by which evidence must be submitted in order to
        --   successfully challenge dispute. Will be null if the
        --   customer's bank or credit card company doesn't allow
        --   a response for this particular dispute.
      deriving (Eq, Ord, Show)

data SubscriptionsResourcePendingUpdateSubscriptionItems = SubscriptionsResourcePendingUpdateSubscriptionItems with
      deriving (Eq, Ord, Show)

data SubscriptionsResourcePendingUpdate = SubscriptionsResourcePendingUpdate with
    trialFromPlan : Optional Bool
        -- ^ Indicates if a plan's `trial_period_days` should be
        --   applied to the subscription. Setting `trial_end` per
        --   subscription is preferred, and this defaults to
        --   `false`. Setting this flag to `true` together with
        --   `trial_end` is not allowed.
    subscriptionItems : [SubscriptionItem]
        -- ^ List of subscription items, each with an attached
        --   plan, that will be set if the update is applied.
    expiresAt : Optional Int
        -- ^ The point after which the changes reflected by this
        --   update will be discarded and no longer applied.
    trialEnd : Optional Int
        -- ^ Unix timestamp representing the end of the trial
        --   period the customer will get before being charged for
        --   the first time, if the update is applied.
    billingCycleAnchor : Optional Int
        -- ^ If the update is applied, determines the date of the
        --   first full invoice, and, for plans with `month` or
        --   `year` intervals, the day of the month for subsequent
        --   invoices.
      deriving (Eq, Ord, Show)

data SourceTypeEps = SourceTypeEps with
    reference : Optional Text
    statementDescriptor : Optional Text
      deriving (Eq, Ord, Show)

data PaymentMethodDetailsGiropay = PaymentMethodDetailsGiropay with
    bankCode : Optional Text
        -- ^ Bank code of bank associated with the bank account.
    bic : Optional Text
        -- ^ Bank Identifier Code of the bank associated with the
        --   bank account.
    verifiedName : Optional Text
        -- ^ Owner's verified full name. Values are verified or
        --   provided by Giropay directly (if supported) at the
        --   time of authorization or settlement. They cannot be
        --   set or mutated.
    bankName : Optional Text
        -- ^ Name of the bank associated with the bank account.
      deriving (Eq, Ord, Show)

data AccountSettings = AccountSettings with
    cardPayments : Optional AccountCardPaymentsSettings
    dashboard : AccountDashboardSettings
    payouts : Optional AccountPayoutSettings
    branding : AccountBrandingSettings
    payments : AccountPaymentsSettings
      deriving (Eq, Ord, Show)

data AccountCardPaymentsSettings = AccountCardPaymentsSettings with
    statementDescriptorPrefix : Optional Text
        -- ^ The default text that appears on credit card
        --   statements when a charge is made. This field prefixes
        --   any dynamic `statement_descriptor` specified on the
        --   charge. `statement_descriptor_prefix` is useful for
        --   maximizing descriptor space for the dynamic portion.
    declineOn : Optional AccountDeclineChargeOn
      deriving (Eq, Ord, Show)

data MandatePaymentMethodDetails = MandatePaymentMethodDetails with
    card : Optional CardMandatePaymentMethodDetails
    type_ : Optional Text
        -- ^ The type of the payment method associated with this
        --   mandate. An additional hash is included on
        --   `payment_method_details` with a name matching this
        --   value. It contains mandate information specific to
        --   the payment method.
    sepaDebit : Optional MandateSepaDebit
      deriving (Eq, Ord, Show)

data RadarReviewResourceLocation = RadarReviewResourceLocation with
    country : Optional Text
        -- ^ Two-letter ISO code representing the country where
        --   the payment originated.
    latitude : Optional Decimal
        -- ^ The geographic latitude where the payment originated.
    city : Optional Text
        -- ^ The city where the payment originated.
    region : Optional Text
        -- ^ The state/county/province/region where the payment
        --   originated.
    longitude : Optional Decimal
        -- ^ The geographic longitude where the payment
        --   originated.
      deriving (Eq, Ord, Show)

data BalancePending = BalancePending with
      deriving (Eq, Ord, Show)

data BalanceConnectReserved = BalanceConnectReserved with
      deriving (Eq, Ord, Show)

data BalanceAvailable = BalanceAvailable with
      deriving (Eq, Ord, Show)

data Balance = Balance with
    pending : [BalanceAmount]
        -- ^ Funds that are not yet available in the balance, due
        --   to the 7-day rolling pay cycle. The pending balance
        --   for each currency, and for each payment type, can be
        --   found in the `source_types` property.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    connectReserved : [BalanceAmount]
        -- ^ Funds held due to negative balances on connected
        --   Custom accounts. The connect reserve balance for each
        --   currency and payment type can be found in the
        --   `source_types` property.
    available : [BalanceAmount]
        -- ^ Funds that are available to be transferred or paid
        --   out, whether automatically by Stripe or explicitly
        --   via the [Transfers
        --   API](https://stripe.com/docs/api#transfers) or
        --   [Payouts API](https://stripe.com/docs/api#payouts).
        --   The available balance for each currency and payment
        --   type can be found in the `source_types` property.
      deriving (Eq, Ord, Show)

data PaymentMethodDetailsSepaDebit = PaymentMethodDetailsSepaDebit with
    bankCode : Optional Text
        -- ^ Bank code of bank associated with the bank account.
    country : Optional Text
        -- ^ Two-letter ISO code representing the country the bank
        --   account is located in.
    fingerprint : Optional Text
        -- ^ Uniquely identifies this particular bank account. You
        --   can use this attribute to check whether two bank
        --   accounts are the same.
    mandate : Optional Text
        -- ^ ID of the mandate used to make this payment.
    branchCode : Optional Text
        -- ^ Branch code of bank associated with the bank account.
    last4 : Optional Text
        -- ^ Last four characters of the IBAN.
      deriving (Eq, Ord, Show)

data Rule = Rule with
    action : Text
        -- ^ The action taken on the payment.
    predicate : Text
        -- ^ The predicate to evaluate the payment against.
    id : Text
        -- ^ Unique identifier for the object.
      deriving (Eq, Ord, Show)

data IssuingCardMetadata = IssuingCardMetadata with
      deriving (Eq, Ord, Show)

data IssuingCard = IssuingCard with
    cardholder : Optional IssuingCardholder
    status : Text
        -- ^ Whether authorizations can be approved on this card.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    replacementReason : Optional Text
        -- ^ The reason why the previous card needed to be
        --   replaced.
    authorizationControls : Optional IssuingCardAuthorizationControls
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    brand : Text
        -- ^ The brand of the card.
    name : Text
        -- ^ The name of the cardholder, printed on the card.
    pin : Optional IssuingCardPin
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    shipping : Optional IssuingCardShipping
    replacementFor : Optional Text
        -- ^ The card this card replaces, if any.
    expMonth : Optional Int
        -- ^ The expiration month of the card.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : IssuingCardMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    expYear : Optional Int
        -- ^ The expiration year of the card.
    type_ : Optional Text
        -- ^ The type of the card.
    last4 : Text
        -- ^ The last 4 digits of the card number.
      deriving (Eq, Ord, Show)

data Fee = Fee with
    amount : Int
        -- ^ Amount of the fee, in cents.
    application : Optional Text
        -- ^ ID of the Connect application that earned the fee.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    type_ : Optional Text
        -- ^ Type of the fee, one of: `application_fee`,
        --   `stripe_fee` or `tax`.
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users.
      deriving (Eq, Ord, Show)

data PaymentMethodDetailsP24 = PaymentMethodDetailsP24 with
    reference : Optional Text
        -- ^ Unique reference for this Przelewy24 payment.
    verifiedName : Optional Text
        -- ^ Owner's verified full name. Values are verified or
        --   provided by Przelewy24 directly (if supported) at the
        --   time of authorization or settlement. They cannot be
        --   set or mutated.
      deriving (Eq, Ord, Show)

data SourceReceiverFlow = SourceReceiverFlow with
    amountCharged : Optional Int
        -- ^ The total amount that was charged by you. The amount
        --   charged is expressed in the source's currency.
    refundAttributesStatus : Optional Text
        -- ^ Type of refund attribute status, one of `missing`,
        --   `requested`, or `available`.
    amountReturned : Optional Int
        -- ^ The total amount that was returned to the customer.
        --   The amount returned is expressed in the source's
        --   currency.
    address : Optional Text
        -- ^ The address of the receiver source. This is the value
        --   that should be communicated to the customer to send
        --   their funds to.
    refundAttributesMethod : Optional Text
        -- ^ Type of refund attribute method, one of `email`,
        --   `manual`, or `none`.
    amountReceived : Optional Int
        -- ^ The total amount received by the receiver source.
        --   `amount_received = amount_returned + amount_charged`
        --   is true at all time. The amount received is expressed
        --   in the source's currency.
      deriving (Eq, Ord, Show)

data Mandate = Mandate with
    status : Text
        -- ^ The status of the Mandate, one of `pending`,
        --   `inactive`, or `active`. The Mandate can be used to
        --   initiate a payment only if status=active.
    paymentMethod : Optional Text
        -- ^ ID of the payment method associated with this
        --   mandate.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    multiUse : Optional MandateMultiUse
    singleUse : Optional MandateSingleUse
    paymentMethodDetails : Optional MandatePaymentMethodDetails
    customerAcceptance : Optional CustomerAcceptance
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    type_ : Optional Text
        -- ^ The type of the mandate, one of `single_use` or
        --   `multi_use`
      deriving (Eq, Ord, Show)

data CustomerBalanceTransactionMetadata = CustomerBalanceTransactionMetadata with
      deriving (Eq, Ord, Show)

data CustomerBalanceTransaction = CustomerBalanceTransaction with
    amount : Int
        -- ^ The amount of the transaction. A negative value is a
        --   credit for the customer's balance, and a positive
        --   value is a debit to the customer's `balance`.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    endingBalance : Optional Int
        -- ^ The customer's `balance` after the transaction was
        --   applied. A negative value decreases the amount due on
        --   the customer's next invoice. A positive value
        --   increases the amount due on the customer's next
        --   invoice.
    invoice : Optional Text
        -- ^ The ID of the invoice (if any) related to the
        --   transaction.
    customer : Text
        -- ^ The ID of the customer the transaction belongs to.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    creditNote : Optional Text
        -- ^ The ID of the credit note (if any) related to the
        --   transaction.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : Optional CustomerBalanceTransactionMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    type_ : Optional Text
        -- ^ Transaction type: `adjustment`, `applied_to_invoice`,
        --   `credit_note`, `initial`, `invoice_too_large`,
        --   `invoice_too_small`, `unspent_receiver_credit`, or
        --   `unapplied_from_invoice`. See the [Customer Balance
        --   page](https://stripe.com/docs/billing/customer/balance#types)
        --   to learn more about transaction types.
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users.
      deriving (Eq, Ord, Show)

data SourceOrderItem = SourceOrderItem with
    amount : Optional Int
        -- ^ The amount (price) for this order item.
    quantity : Optional Int
        -- ^ The quantity of this order item. When type is `sku`,
        --   this is the number of instances of the SKU to be
        --   ordered.
    currency : Optional Text
        -- ^ This currency of this order item. Required when
        --   `amount` is present.
    type_ : Optional Text
        -- ^ The type of this order item. Must be `sku`, `tax`, or
        --   `shipping`.
    description : Optional Text
        -- ^ Human-readable description for this order item.
      deriving (Eq, Ord, Show)

data SetupIntentPaymentMethodOptions = SetupIntentPaymentMethodOptions with
    card : Optional SetupIntentPaymentMethodOptionsCard
      deriving (Eq, Ord, Show)

data DeletedRadarValueList = DeletedRadarValueList with
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    deleted : Bool
        -- ^ Always true for a deleted object
      deriving (Eq, Ord, Show)

data SourceTypeSofort = SourceTypeSofort with
    bankCode : Optional Text
    country : Optional Text
    bic : Optional Text
    ibanLast4 : Optional Text
    preferredLanguage : Optional Text
    bankName : Optional Text
    statementDescriptor : Optional Text
      deriving (Eq, Ord, Show)

data CheckoutSessionDisplayItem = CheckoutSessionDisplayItem with
    amount : Optional Int
        -- ^ Amount for the display item.
    quantity : Optional Int
        -- ^ Quantity of the display item being purchased.
    plan : Optional Plan
    custom : Optional CheckoutSessionCustomDisplayItemDescription
    currency : Optional Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    sku : Optional Sku
    type_ : Optional Text
        -- ^ The type of display item. One of `custom`, `plan` or
        --   `sku`
      deriving (Eq, Ord, Show)

data PaymentMethodDetailsCardWalletMasterpass = PaymentMethodDetailsCardWalletMasterpass with
    email : Optional Text
        -- ^ Owner's verified email. Values are verified or
        --   provided by the wallet directly (if supported) at the
        --   time of authorization or settlement. They cannot be
        --   set or mutated.
    billingAddress : Optional Address
    shippingAddress : Optional Address
    name : Optional Text
        -- ^ Owner's verified full name. Values are verified or
        --   provided by the wallet directly (if supported) at the
        --   time of authorization or settlement. They cannot be
        --   set or mutated.
      deriving (Eq, Ord, Show)

data PackageDimensions = PackageDimensions with
    length : Decimal
        -- ^ Length, in inches.
    height : Decimal
        -- ^ Height, in inches.
    weight : Decimal
        -- ^ Weight, in ounces.
    width : Decimal
        -- ^ Width, in inches.
      deriving (Eq, Ord, Show)

data SourceMandateNotification = SourceMandateNotification with
    status : Text
        -- ^ The status of the mandate notification. Valid
        --   statuses are `pending` or `submitted`.
    amount : Optional Int
        -- ^ A positive integer in the smallest currency unit
        --   (that is, 100 cents for $1.00, or 1 for ¥1, Japanese
        --   Yen being a zero-decimal currency) representing the
        --   amount associated with the mandate notification. The
        --   amount is expressed in the currency of the underlying
        --   source. Required if the notification type is
        --   `debit_initiated`.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    reason : Text
        -- ^ The reason of the mandate notification. Valid reasons
        --   are `mandate_confirmed` or `debit_initiated`.
    bacsDebit : Optional SourceMandateNotificationBacsDebitData
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    source : Source
    id : Text
        -- ^ Unique identifier for the object.
    type_ : Optional Text
        -- ^ The type of source this mandate notification is
        --   attached to. Should be the source type identifier
        --   code for the payment method, such as
        --   `three_d_secure`.
    sepaDebit : Optional SourceMandateNotificationSepaDebitData
      deriving (Eq, Ord, Show)

data PlanTier = PlanTier with
    flatAmount : Optional Int
        -- ^ Price for the entire tier.
    upTo : Optional Int
        -- ^ Up to and including to this quantity will be
        --   contained in the tier.
    unitAmountDecimal : Optional Text
        -- ^ Same as `unit_amount`, but contains a decimal value
        --   with at most 12 decimal places.
    unitAmount : Optional Int
        -- ^ Per unit price for units relevant to the tier.
    flatAmountDecimal : Optional Text
        -- ^ Same as `flat_amount`, but contains a decimal value
        --   with at most 12 decimal places.
      deriving (Eq, Ord, Show)

data RefundMetadata = RefundMetadata with
      deriving (Eq, Ord, Show)

data Refund = Refund with
    status : Optional Text
        -- ^ Status of the refund. For credit card refunds, this
        --   can be `pending`, `succeeded`, or `failed`. For other
        --   types of refunds, it can be `pending`, `succeeded`,
        --   `failed`, or `canceled`. Refer to our
        --   [refunds](https://stripe.com/docs/refunds#failed-refunds)
        --   documentation for more details.
    amount : Int
        -- ^ Amount, in %s.
    balanceTransaction : Optional Text
        -- ^ Balance transaction that describes the impact on your
        --   account balance.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    receiptNumber : Optional Text
        -- ^ This is the transaction number that appears on email
        --   receipts sent for this refund.
    paymentIntent : Optional Text
        -- ^ ID of the PaymentIntent that was refunded.
    reason : Optional Text
        -- ^ Reason for the refund, either user-provided
        --   (`duplicate`, `fraudulent`, or
        --   `requested_by_customer`) or generated by Stripe
        --   internally (`expired_uncaptured_charge`).
    transferReversal : Optional Text
        -- ^ If the accompanying transfer was reversed, the
        --   transfer reversal object. Only applicable if the
        --   charge was created using the destination parameter.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    failureBalanceTransaction : Optional Text
        -- ^ If the refund failed, this balance transaction
        --   describes the adjustment made on your account balance
        --   that reverses the initial balance transaction.
    charge : Optional Text
        -- ^ ID of the charge that was refunded.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : RefundMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    failureReason : Optional Text
        -- ^ If the refund failed, the reason for refund failure
        --   if known. Possible values are `lost_or_stolen_card`,
        --   `expired_or_canceled_card`, or `unknown`.
    sourceTransferReversal : Optional Text
        -- ^ The transfer reversal that is associated with the
        --   refund. Only present if the charge came from another
        --   Stripe account. See the Connect documentation for
        --   details.
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users. (Available on
        --   non-card refunds only)
      deriving (Eq, Ord, Show)

data SourceTypeKlarna = SourceTypeKlarna with
    purchaseType : Optional Text
    shippingFirstName : Optional Text
    shippingLastName : Optional Text
    payNowAssetUrlsDescriptive : Optional Text
    firstName : Optional Text
    payLaterAssetUrlsDescriptive : Optional Text
    backgroundImageUrl : Optional Text
    payLaterAssetUrlsStandard : Optional Text
    payNowAssetUrlsStandard : Optional Text
    payNowRedirectUrl : Optional Text
    locale : Optional Text
    payLaterRedirectUrl : Optional Text
    paymentMethodCategories : Optional Text
    redirectUrl : Optional Text
    payLaterName : Optional Text
    payNowName : Optional Text
    purchaseCountry : Optional Text
    pageTitle : Optional Text
    payOverTimeAssetUrlsStandard : Optional Text
    logoUrl : Optional Text
    lastName : Optional Text
    payOverTimeRedirectUrl : Optional Text
    clientToken : Optional Text
    payOverTimeAssetUrlsDescriptive : Optional Text
    payOverTimeName : Optional Text
      deriving (Eq, Ord, Show)

data IssuingAuthorizationRequestViolatedAuthorizationControls = IssuingAuthorizationRequestViolatedAuthorizationControls with
      deriving (Eq, Ord, Show)

data IssuingAuthorizationRequest = IssuingAuthorizationRequest with
    authorizedAmount : Optional Int
        -- ^ The amount that was authorized at the time of this
        --   request
    heldCurrency : Optional Text
        -- ^ The currency of the [held
        --   amount](https://stripe.com/docs/api#issuing_authorization_object-held_amount)
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    heldAmount : Optional Int
        -- ^ The amount Stripe held from your account to fund the
        --   authorization, if the request was approved
    reason : Text
        -- ^ The reason for the approval or decline.
    violatedAuthorizationControls : [IssuingAuthorizationViolatedAuthControl]
        -- ^ When an authorization is declined due to
        --   `authorization_controls`, this array contains details
        --   about the authorization controls that were violated.
        --   Otherwise, it is empty.
    authorizedCurrency : Optional Text
        -- ^ The currency that was presented to the cardholder for
        --   the authorization. Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    approved : Bool
        -- ^ Whether this request was approved.
      deriving (Eq, Ord, Show)

data IssuingDisputeOtherEvidence = IssuingDisputeOtherEvidence with
    disputeExplanation : Optional Text
        -- ^ Brief freeform text explaining why you are disputing
        --   this transaction.
    uncategorizedFile : Optional Text
        -- ^ (ID of a [file
        --   upload](https://stripe.com/docs/guides/file-upload))
        --   Additional file evidence supporting your dispute.
      deriving (Eq, Ord, Show)

data Address = Address with
    line2 : Optional Text
        -- ^ Address line 2 (e.g., apartment, suite, unit, or
        --   building).
    postalCode : Optional Text
        -- ^ ZIP or postal code.
    state : Optional Text
        -- ^ State, county, province, or region.
    country : Optional Text
        -- ^ Two-letter country code ([ISO 3166-1
        --   alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)).
    city : Optional Text
        -- ^ City, district, suburb, town, or village.
    line1 : Optional Text
        -- ^ Address line 1 (e.g., street, PO Box, or company
        --   name).
      deriving (Eq, Ord, Show)

data IssuingCardholderMetadata = IssuingCardholderMetadata with
      deriving (Eq, Ord, Show)

data IssuingCardholder = IssuingCardholder with
    requirements : IssuingCardholderRequirements
    email : Optional Text
        -- ^ The cardholder's email address.
    status : Text
        -- ^ Specifies whether to permit authorizations on this
        --   cardholder's cards.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    phoneNumber : Optional Text
        -- ^ The cardholder's phone number.
    authorizationControls : Optional IssuingCardholderAuthorizationControls
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    isDefault : Optional Bool
        -- ^ Whether or not this cardholder is the default
        --   cardholder.
    name : Text
        -- ^ The cardholder's name. This will be printed on cards
        --   issued to them.
    company : Optional IssuingCardholderCompany
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : IssuingCardholderMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    type_ : Optional Text
        -- ^ One of `individual` or `business_entity`.
    billing : IssuingCardholderAddress
    individual : Optional IssuingCardholderIndividual
      deriving (Eq, Ord, Show)

data TopupMetadata = TopupMetadata with
      deriving (Eq, Ord, Show)

data Topup = Topup with
    failureMessage : Optional Text
        -- ^ Message to user further explaining reason for top-up
        --   failure if available.
    status : Text
        -- ^ The status of the top-up is either `canceled`,
        --   `failed`, `pending`, `reversed`, or `succeeded`.
    amount : Int
        -- ^ Amount transferred.
    balanceTransaction : Optional Text
        -- ^ ID of the balance transaction that describes the
        --   impact of this top-up on your account balance. May
        --   not be specified depending on status of top-up.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    failureCode : Optional Text
        -- ^ Error code explaining reason for top-up failure if
        --   available (see [the errors
        --   section](https://stripe.com/docs/api#errors) for a
        --   list of codes).
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : TopupMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    source : Source
    id : Text
        -- ^ Unique identifier for the object.
    expectedAvailabilityDate : Optional Int
        -- ^ Date the funds are expected to arrive in your Stripe
        --   account for payouts. This factors in delays like
        --   weekends or bank holidays. May not be specified
        --   depending on status of top-up.
    transferGroup : Optional Text
        -- ^ A string that identifies this top-up as part of a
        --   group.
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users.
    statementDescriptor : Optional Text
        -- ^ Extra information about a top-up. This will appear on
        --   your source's bank statement. It must contain at
        --   least one letter.
      deriving (Eq, Ord, Show)

data DeletedCard = DeletedCard with
    currency : Optional Text
        -- ^ Three-letter [ISO code for the
        --   currency](https://stripe.com/docs/payouts) paid out
        --   to the bank account.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    deleted : Bool
        -- ^ Always true for a deleted object
      deriving (Eq, Ord, Show)

data SourceTypeGiropay = SourceTypeGiropay with
    bankCode : Optional Text
    bic : Optional Text
    bankName : Optional Text
    statementDescriptor : Optional Text
      deriving (Eq, Ord, Show)

data DisputeEvidence = DisputeEvidence with
    receipt : Optional Text
        -- ^ (ID of a [file
        --   upload](https://stripe.com/docs/guides/file-upload))
        --   Any receipt or message sent to the customer notifying
        --   them of the charge.
    shippingDocumentation : Optional Text
        -- ^ (ID of a [file
        --   upload](https://stripe.com/docs/guides/file-upload))
        --   Documentation showing proof that a product was
        --   shipped to the customer at the same address the
        --   customer provided to you. This could include a copy
        --   of the shipment receipt, shipping label, etc. It
        --   should show the customer's full shipping address, if
        --   possible.
    duplicateChargeDocumentation : Optional Text
        -- ^ (ID of a [file
        --   upload](https://stripe.com/docs/guides/file-upload))
        --   Documentation for the prior charge that can uniquely
        --   identify the charge, such as a receipt, shipping
        --   label, work order, etc. This document should be
        --   paired with a similar document from the disputed
        --   payment that proves the two payments are separate.
    refundRefusalExplanation : Optional Text
        -- ^ A justification for why the customer is not entitled
        --   to a refund.
    billingAddress : Optional Text
        -- ^ The billing address provided by the customer.
    uncategorizedText : Optional Text
        -- ^ Any additional evidence or statements.
    accessActivityLog : Optional Text
        -- ^ Any server or activity logs showing proof that the
        --   customer accessed or downloaded the purchased digital
        --   product. This information should include IP
        --   addresses, corresponding timestamps, and any detailed
        --   recorded activity.
    cancellationRebuttal : Optional Text
        -- ^ A justification for why the customer's subscription
        --   was not canceled.
    cancellationPolicy : Optional Text
        -- ^ (ID of a [file
        --   upload](https://stripe.com/docs/guides/file-upload))
        --   Your subscription cancellation policy, as shown to
        --   the customer.
    cancellationPolicyDisclosure : Optional Text
        -- ^ An explanation of how and when the customer was shown
        --   your refund policy prior to purchase.
    shippingAddress : Optional Text
        -- ^ The address to which a physical product was shipped.
        --   You should try to include as complete address
        --   information as possible.
    shippingTrackingNumber : Optional Text
        -- ^ The tracking number for a physical product, obtained
        --   from the delivery service. If multiple tracking
        --   numbers were generated for this purchase, please
        --   separate them with commas.
    serviceDate : Optional Text
        -- ^ The date on which the customer received or began
        --   receiving the purchased service, in a clear
        --   human-readable format.
    duplicateChargeExplanation : Optional Text
        -- ^ An explanation of the difference between the disputed
        --   charge versus the prior charge that appears to be a
        --   duplicate.
    uncategorizedFile : Optional Text
        -- ^ (ID of a [file
        --   upload](https://stripe.com/docs/guides/file-upload))
        --   Any additional evidence or statements.
    refundPolicyDisclosure : Optional Text
        -- ^ Documentation demonstrating that the customer was
        --   shown your refund policy prior to purchase.
    customerName : Optional Text
        -- ^ The name of the customer.
    customerSignature : Optional Text
        -- ^ (ID of a [file
        --   upload](https://stripe.com/docs/guides/file-upload))
        --   A relevant document or contract showing the
        --   customer's signature.
    productDescription : Optional Text
        -- ^ A description of the product or service that was
        --   sold.
    serviceDocumentation : Optional Text
        -- ^ (ID of a [file
        --   upload](https://stripe.com/docs/guides/file-upload))
        --   Documentation showing proof that a service was
        --   provided to the customer. This could include a copy
        --   of a signed contract, work order, or other form of
        --   written agreement.
    duplicateChargeId : Optional Text
        -- ^ The Stripe ID for the prior charge which appears to
        --   be a duplicate of the disputed charge.
    customerCommunication : Optional Text
        -- ^ (ID of a [file
        --   upload](https://stripe.com/docs/guides/file-upload))
        --   Any communication with the customer that you feel is
        --   relevant to your case. Examples include emails
        --   proving that the customer received the product or
        --   service, or demonstrating their use of or
        --   satisfaction with the product or service.
    customerPurchaseIp : Optional Text
        -- ^ The IP address that the customer used when making the
        --   purchase.
    customerEmailAddress : Optional Text
        -- ^ The email address of the customer.
    refundPolicy : Optional Text
        -- ^ (ID of a [file
        --   upload](https://stripe.com/docs/guides/file-upload))
        --   Your refund policy, as shown to the customer.
    shippingDate : Optional Text
        -- ^ The date on which a physical product began its route
        --   to the shipping address, in a clear human-readable
        --   format.
    shippingCarrier : Optional Text
        -- ^ The delivery service that shipped a physical product,
        --   such as Fedex, UPS, USPS, etc. If multiple carriers
        --   were used for this purchase, please separate them
        --   with commas.
      deriving (Eq, Ord, Show)

data PaymentMethodDetailsEps = PaymentMethodDetailsEps with
    verifiedName : Optional Text
        -- ^ Owner's verified full name. Values are verified or
        --   provided by EPS directly (if supported) at the time
        --   of authorization or settlement. They cannot be set or
        --   mutated.
      deriving (Eq, Ord, Show)

data TransformUsage = TransformUsage with
    round : Text
        -- ^ After division, either round the result `up` or
        --   `down`.
    divideBy : Optional Int
        -- ^ Divide usage by this number.
      deriving (Eq, Ord, Show)

data ExchangeRateRates = ExchangeRateRates with
      deriving (Eq, Ord, Show)

data ExchangeRate = ExchangeRate with
    rates : ExchangeRateRates
        -- ^ Hash where the keys are supported currencies and the
        --   values are the exchange rate at which the base id
        --   currency converts to the key currency.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object. Represented as the
        --   three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html)
        --   in lowercase.
      deriving (Eq, Ord, Show)

data InvoiceLinesData = InvoiceLinesData with
      deriving (Eq, Ord, Show)

data InvoiceLines = InvoiceLines with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [LineItem]
        -- ^ Details about each object.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data InvoiceDefaultTaxRates = InvoiceDefaultTaxRates with
      deriving (Eq, Ord, Show)

data InvoiceCustomerTaxIds = InvoiceCustomerTaxIds with
      deriving (Eq, Ord, Show)

data InvoiceMetadata = InvoiceMetadata with
      deriving (Eq, Ord, Show)

data InvoiceCustomFields = InvoiceCustomFields with
      deriving (Eq, Ord, Show)

data InvoiceTotalTaxAmounts = InvoiceTotalTaxAmounts with
      deriving (Eq, Ord, Show)

data Invoice = Invoice with
    thresholdReason : Optional InvoiceThresholdReason
    attempted : Bool
        -- ^ Whether an attempt has been made to pay the invoice.
        --   An invoice is not attempted until 1 hour after the
        --   `invoice.created` webhook, for example, so you might
        --   not want to display that invoice as unpaid to your
        --   users.
    status : Optional Text
        -- ^ The status of the invoice, one of `draft`, `open`,
        --   `paid`, `uncollectible`, or `void`. [Learn
        --   more](https://stripe.com/docs/billing/invoices/workflow#workflow-overview)
    amountDue : Optional Int
        -- ^ Final amount due at this time for this invoice. If
        --   the invoice's total is smaller than the minimum
        --   charge amount, for example, or if there is account
        --   credit that can be applied to the invoice, the
        --   `amount_due` may be 0. If there is a positive
        --   `starting_balance` for the invoice (the customer owes
        --   money), the `amount_due` will also take that into
        --   account. The charge that gets generated for the
        --   invoice will be for the amount specified in
        --   `amount_due`.
    prePaymentCreditNotesAmount : Optional Int
        -- ^ Total amount of all pre-payment credit notes issued
        --   for this invoice.
    amountRemaining : Optional Int
        -- ^ The amount remaining, in %s, that is due.
    amountPaid : Optional Int
        -- ^ The amount, in %s, that was paid.
    statusTransitions : Optional InvoicesStatusTransitions
    dueDate : Optional Int
        -- ^ The date on which payment for this invoice is due.
        --   This value will be `null` for invoices where
        --   `collection_method=charge_automatically`.
    periodStart : Optional Int
        -- ^ Start of the usage period during which invoice items
        --   were added to this invoice.
    autoAdvance : Optional Bool
        -- ^ Controls whether Stripe will perform [automatic
        --   collection](https://stripe.com/docs/billing/invoices/workflow/#auto_advance)
        --   of the invoice. When `false`, the invoice's state
        --   will not automatically advance without an explicit
        --   action.
    discount : Optional Discount
    lines : InvoiceLines
        -- ^ The individual line items that make up the invoice.
        --   `lines` is sorted as follows: invoice items in
        --   reverse chronological order, followed by the
        --   subscription, if any.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    customerAddress : Optional Address
    billingReason : Optional Text
        -- ^ Indicates the reason why the invoice was created.
        --   `subscription_cycle` indicates an invoice created by
        --   a subscription advancing into a new period.
        --   `subscription_create` indicates an invoice created
        --   due to creating a subscription. `subscription_update`
        --   indicates an invoice created due to updating a
        --   subscription. `subscription` is set for all old
        --   invoices to indicate either a change to a
        --   subscription or a period advancement. `manual` is set
        --   for all invoices unrelated to a subscription (for
        --   example: created via the invoice editor). The
        --   `upcoming` value is reserved for simulated invoices
        --   per the upcoming invoice endpoint.
        --   `subscription_threshold` indicates an invoice created
        --   due to a billing threshold being reached.
    postPaymentCreditNotesAmount : Optional Int
        -- ^ Total amount of all post-payment credit notes issued
        --   for this invoice.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    receiptNumber : Optional Text
        -- ^ This is the transaction number that appears on email
        --   receipts sent for this invoice.
    hostedInvoiceUrl : Optional Text
        -- ^ The URL for the hosted invoice page, which allows
        --   customers to view and pay an invoice. If the invoice
        --   has not been finalized yet, this will be null.
    subtotal : Int
        -- ^ Total of all subscriptions, invoice items, and
        --   prorations on the invoice before any discount or tax
        --   is applied.
    endingBalance : Optional Int
        -- ^ Ending customer balance after the invoice is
        --   finalized. Invoices are finalized approximately an
        --   hour after successful webhook delivery or when
        --   payment collection is attempted for the invoice. If
        --   the invoice has not been finalized yet, this will be
        --   null.
    collectionMethod : Optional Text
        -- ^ Either `charge_automatically`, or `send_invoice`.
        --   When charging automatically, Stripe will attempt to
        --   pay this invoice using the default source attached to
        --   the customer. When sending an invoice, Stripe will
        --   email this invoice to the customer with payment
        --   instructions.
    webhooksDeliveredAt : Optional Int
        -- ^ The time at which webhooks for this invoice were
        --   successfully delivered (if the invoice had no
        --   webhooks to deliver, this will match `created`).
        --   Invoice payment is delayed until webhooks are
        --   delivered, or until all webhook delivery attempts
        --   have been exhausted.
    paymentIntent : Optional Text
        -- ^ The PaymentIntent associated with this invoice. The
        --   PaymentIntent is generated when the invoice is
        --   finalized, and can then be used to pay the invoice.
        --   Note that voiding an invoice will cancel the
        --   PaymentIntent.
    invoicePdf : Optional Text
        -- ^ The link to download the PDF for the invoice. If the
        --   invoice has not been finalized yet, this will be
        --   null.
    customer : Text
        -- ^ The ID of the customer who will be billed.
    paid : Bool
        -- ^ Whether payment was successfully collected for this
        --   invoice. An invoice can be paid (most commonly) with
        --   a charge or with credit from the customer's account
        --   balance.
    customerName : Optional Text
        -- ^ The customer's name. Until the invoice is finalized,
        --   this field will equal `customer.name`. Once the
        --   invoice is finalized, this field will no longer be
        --   updated.
    customerEmail : Optional Text
        -- ^ The customer's email. Until the invoice is finalized,
        --   this field will equal `customer.email`. Once the
        --   invoice is finalized, this field will no longer be
        --   updated.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    customerPhone : Optional Text
        -- ^ The customer's phone number. Until the invoice is
        --   finalized, this field will equal `customer.phone`.
        --   Once the invoice is finalized, this field will no
        --   longer be updated.
    defaultTaxRates : [TaxRate]
        -- ^ The tax rates applied to this invoice, if any.
    customerTaxIds : [InvoicesResourceInvoiceTaxId]
        -- ^ The customer's tax IDs. Until the invoice is
        --   finalized, this field will contain the same tax IDs
        --   as `customer.tax_ids`. Once the invoice is finalized,
        --   this field will no longer be updated.
    total : Int
        -- ^ Total after discounts and taxes.
    charge : Optional Text
        -- ^ ID of the latest charge generated for this invoice,
        --   if any.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : Optional InvoiceMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    periodEnd : Optional Int
        -- ^ End of the usage period during which invoice items
        --   were added to this invoice.
    subscriptionProrationDate : Optional Int
        -- ^ Only set for upcoming invoices that preview
        --   prorations. The time used to calculate prorations.
    id : Optional Text
        -- ^ Unique identifier for the object.
    footer : Optional Text
        -- ^ Footer displayed on the invoice.
    tax : Optional Int
        -- ^ The amount of tax on this invoice. This is the sum of
        --   all the tax amounts on this invoice.
    customFields : [InvoiceSettingCustomField]
        -- ^ Custom fields displayed on the invoice.
    customerTaxExempt : Optional Text
        -- ^ The customer's tax exempt status. Until the invoice
        --   is finalized, this field will equal
        --   `customer.tax_exempt`. Once the invoice is finalized,
        --   this field will no longer be updated.
    applicationFeeAmount : Optional Int
        -- ^ The fee in %s that will be applied to the invoice and
        --   transferred to the application owner's Stripe account
        --   when the invoice is paid.
    nextPaymentAttempt : Optional Int
        -- ^ The time at which payment will next be attempted.
        --   This value will be `null` for invoices where
        --   `collection_method=send_invoice`.
    totalTaxAmounts : [InvoiceTaxAmount]
        -- ^ The aggregate amounts calculated per tax rate for all
        --   line items.
    number : Optional Text
        -- ^ A unique, identifying string that appears on emails
        --   sent to the customer for this invoice. This starts
        --   with the customer's unique invoice_prefix if it is
        --   specified.
    subscription : Optional Text
        -- ^ The subscription that this invoice was prepared for,
        --   if any.
    defaultPaymentMethod : Optional Text
        -- ^ ID of the default payment method for the invoice. It
        --   must belong to the customer associated with the
        --   invoice. If not set, defaults to the subscription's
        --   default payment method, if any, or to the default
        --   payment method in the customer's invoice settings.
    accountCountry : Optional Text
        -- ^ The country of the business associated with this
        --   invoice, most often the business creating the
        --   invoice.
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users. Referenced as 'memo'
        --   in the Dashboard.
    attemptCount : Optional Int
        -- ^ Number of payment attempts made for this invoice,
        --   from the perspective of the payment retry schedule.
        --   Any payment attempt counts as the first attempt, and
        --   subsequently only automatic retries increment the
        --   attempt count. In other words, manual payment
        --   attempts after the first attempt do not affect the
        --   retry schedule.
    statementDescriptor : Optional Text
        -- ^ Extra information about an invoice for the customer's
        --   credit card statement.
    customerShipping : Optional Shipping
    accountName : Optional Text
        -- ^ The public name of the business associated with this
        --   invoice, most often the business creating the
        --   invoice.
    defaultSource : Optional Text
        -- ^ ID of the default payment source for the invoice. It
        --   must belong to the customer associated with the
        --   invoice and be in a chargeable state. If not set,
        --   defaults to the subscription's default source, if
        --   any, or to the customer's default source.
    startingBalance : Optional Int
        -- ^ Starting customer balance before the invoice is
        --   finalized. If the invoice has not been finalized yet,
        --   this will be the current customer balance.
    taxPercent : Optional Decimal
        -- ^ This percentage of the subtotal has been added to the
        --   total amount of the invoice, including invoice line
        --   items and discounts. This field is inherited from the
        --   subscription's `tax_percent` field, but can be
        --   changed before the invoice is paid. This field
        --   defaults to null.
      deriving (Eq, Ord, Show)

data CreditNoteLineItemTaxAmounts = CreditNoteLineItemTaxAmounts with
      deriving (Eq, Ord, Show)

data CreditNoteLineItemTaxRates = CreditNoteLineItemTaxRates with
      deriving (Eq, Ord, Show)

data CreditNoteLineItem = CreditNoteLineItem with
    amount : Int
        -- ^ The integer amount in **%s** representing the gross
        --   amount being credited for this line item, excluding
        --   (exclusive) tax and discounts.
    taxAmounts : [CreditNoteTaxAmount]
        -- ^ The amount of tax calculated per tax rate for this
        --   line item
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    quantity : Optional Int
        -- ^ The number of units of product being credited.
    unitAmountDecimal : Optional Text
        -- ^ Same as `unit_amount`, but contains a decimal value
        --   with at most 12 decimal places.
    invoiceLineItem : Optional Text
        -- ^ ID of the invoice line item being credited
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    unitAmount : Optional Int
        -- ^ The cost of each unit of product being credited.
    type_ : Optional Text
        -- ^ The type of the credit note line item, one of
        --   `invoice_line_item` or `custom_line_item`. When the
        --   type is `invoice_line_item` there is an additional
        --   `invoice_line_item` property on the resource the
        --   value of which is the id of the credited line item on
        --   the invoice.
    taxRates : [TaxRate]
        -- ^ The tax rates which apply to the line item.
    discountAmount : Optional Int
        -- ^ The integer amount in **%s** representing the
        --   discount being credited for this line item.
    description : Optional Text
        -- ^ Description of the item being credited.
      deriving (Eq, Ord, Show)

data PaymentMethodDetailsCardWalletVisaCheckout = PaymentMethodDetailsCardWalletVisaCheckout with
    email : Optional Text
        -- ^ Owner's verified email. Values are verified or
        --   provided by the wallet directly (if supported) at the
        --   time of authorization or settlement. They cannot be
        --   set or mutated.
    billingAddress : Optional Address
    shippingAddress : Optional Address
    name : Optional Text
        -- ^ Owner's verified full name. Values are verified or
        --   provided by the wallet directly (if supported) at the
        --   time of authorization or settlement. They cannot be
        --   set or mutated.
      deriving (Eq, Ord, Show)

data LegalEntityCompanyVerification = LegalEntityCompanyVerification with
    document : LegalEntityCompanyVerificationDocument
      deriving (Eq, Ord, Show)

data InvoicesResourceInvoiceTaxId = InvoicesResourceInvoiceTaxId with
    value : Optional Text
        -- ^ The value of the tax ID.
    type_ : Optional Text
        -- ^ The type of the tax ID, one of `eu_vat`, `nz_gst`,
        --   `au_abn`, `in_gst`, `no_vat`, `za_vat`, `ch_vat`,
        --   `mx_rfc`, `sg_uen`, `ru_inn`, `ca_bn`, `hk_br`,
        --   `es_cif`, `tw_vat`, `th_vat`, `jp_cn`, `li_uid`,
        --   `my_itn`, `us_ein`, `kr_brn`, `ca_qst`, or `unknown`
      deriving (Eq, Ord, Show)

data SourceTypeWechat = SourceTypeWechat with
    prepayId : Optional Text
    qrCodeUrl : Optional Text
    statementDescriptor : Optional Text
      deriving (Eq, Ord, Show)

data DeletedRecipient = DeletedRecipient with
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    deleted : Bool
        -- ^ Always true for a deleted object
      deriving (Eq, Ord, Show)

data SubscriptionItemMetadata = SubscriptionItemMetadata with
      deriving (Eq, Ord, Show)

data SubscriptionItemTaxRates = SubscriptionItemTaxRates with
      deriving (Eq, Ord, Show)

data SubscriptionItem = SubscriptionItem with
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    billingThresholds : Optional SubscriptionItemBillingThresholds
    quantity : Optional Int
        -- ^ The
        --   [quantity](https://stripe.com/docs/subscriptions/quantities)
        --   of the plan to which the customer should be
        --   subscribed.
    plan : Plan
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : SubscriptionItemMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    taxRates : [TaxRate]
        -- ^ The tax rates which apply to this
        --   `subscription_item`. When set, the
        --   `default_tax_rates` on the subscription do not apply
        --   to this `subscription_item`.
    subscription : Text
        -- ^ The `subscription` this `subscription_item` belongs
        --   to.
      deriving (Eq, Ord, Show)

data AccountExternalAccountsData = AccountExternalAccountsData with
      deriving (Eq, Ord, Show)

data AccountExternalAccounts = AccountExternalAccounts with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [BankAccount]
        -- ^ The list contains all external accounts that have
        --   been attached to the Stripe account. These may be
        --   bank accounts or cards.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data AccountMetadata = AccountMetadata with
      deriving (Eq, Ord, Show)

data Account = Account with
    detailsSubmitted : Optional Bool
        -- ^ Whether account details have been submitted. Standard
        --   accounts cannot receive payouts before this is true.
    requirements : Optional AccountRequirements
    email : Optional Text
        -- ^ The primary user's email address.
    businessProfile : Optional AccountBusinessProfile
    settings : Optional AccountSettings
    country : Optional Text
        -- ^ The account's country.
    created : Optional Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    externalAccounts : Optional AccountExternalAccounts
        -- ^ External accounts (bank accounts and debit cards)
        --   currently attached to this account
    chargesEnabled : Optional Bool
        -- ^ Whether the account can create live charges.
    businessType : Optional Text
        -- ^ The business type.
    company : Optional LegalEntityCompany
    payoutsEnabled : Optional Bool
        -- ^ Whether Stripe can send payouts to this account.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : Optional AccountMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    type_ : Optional Text
        -- ^ The Stripe account type. Can be `standard`,
        --   `express`, or `custom`.
    defaultCurrency : Optional Text
        -- ^ Three-letter ISO currency code representing the
        --   default currency for the account. This must be a
        --   currency that [Stripe supports in the account's
        --   country](https://stripe.com/docs/payouts).
    tosAcceptance : Optional AccountTosAcceptance
    capabilities : Optional AccountCapabilities
    individual : Optional Person
      deriving (Eq, Ord, Show)

data AccountDeclineChargeOn = AccountDeclineChargeOn with
    avsFailure : Optional Bool
        -- ^ Whether Stripe automatically declines charges with an
        --   incorrect ZIP or postal code. This setting only
        --   applies when a ZIP or postal code is provided and
        --   they fail bank verification.
    cvcFailure : Optional Bool
        -- ^ Whether Stripe automatically declines charges with an
        --   incorrect CVC. This setting only applies when a CVC
        --   is provided and it fails bank verification.
      deriving (Eq, Ord, Show)

data LegalEntityDob = LegalEntityDob with
    day : Optional Int
        -- ^ The day of birth, between 1 and 31.
    year : Optional Int
        -- ^ The four-digit year of birth.
    month : Optional Int
        -- ^ The month of birth, between 1 and 12.
      deriving (Eq, Ord, Show)

data IssuingCardholderVerification = IssuingCardholderVerification with
    document : Optional IssuingCardholderIdDocument
      deriving (Eq, Ord, Show)

data DeletedProduct = DeletedProduct with
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    deleted : Bool
        -- ^ Always true for a deleted object
      deriving (Eq, Ord, Show)

data IssuingCardholderIndividual = IssuingCardholderIndividual with
    firstName : Optional Text
        -- ^ The first name of this cardholder.
    dob : Optional IssuingCardholderIndividualDob
    lastName : Optional Text
        -- ^ The last name of this cardholder.
    verification : Optional IssuingCardholderVerification
      deriving (Eq, Ord, Show)

data PaymentIntentChargesData = PaymentIntentChargesData with
      deriving (Eq, Ord, Show)

data PaymentIntentCharges = PaymentIntentCharges with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Charge]
        -- ^ This list only contains the latest charge, even if
        --   there were previously multiple unsuccessful charges.
        --   To view all previous charges for a PaymentIntent, you
        --   can filter the charges list using the
        --   `payment_intent`
        --   [parameter](https://stripe.com/docs/api/charges/list#list_charges-payment_intent).
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PaymentIntentMetadata = PaymentIntentMetadata with
      deriving (Eq, Ord, Show)

data PaymentIntent = PaymentIntent with
    status : Text
        -- ^ Status of this PaymentIntent, one of
        --   `requires_payment_method`, `requires_confirmation`,
        --   `requires_action`, `processing`, `requires_capture`,
        --   `canceled`, or `succeeded`. Read more about each
        --   PaymentIntent
        --   [status](https://stripe.com/docs/payments/intents#intent-statuses).
    cancellationReason : Optional Text
        -- ^ Reason for cancellation of this PaymentIntent, either
        --   user-provided (`duplicate`, `fraudulent`,
        --   `requested_by_customer`, or `abandoned`) or generated
        --   by Stripe internally (`failed_invoice`,
        --   `void_invoice`, or `automatic`).
    captureMethod : Optional Text
        -- ^ Controls when the funds will be captured from the
        --   customer's account.
    amount : Int
        -- ^ Amount intended to be collected by this
        --   PaymentIntent. A positive integer representing how
        --   much to charge in the [smallest currency
        --   unit](https://stripe.com/docs/currencies#zero-decimal)
        --   (e.g., 100 cents to charge $1.00 or 100 to charge
        --   ¥100, a zero-decimal currency). The minimum amount is
        --   $0.50 US or [equivalent in charge
        --   currency](https://stripe.com/docs/currencies#minimum-and-maximum-charge-amounts).
        --   The amount value supports up to eight digits (e.g., a
        --   value of 99999999 for a USD charge of $999,999.99).
    confirmationMethod : Optional Text
    setupFutureUsage : Optional Text
        -- ^ Indicates that you intend to make future payments
        --   with this PaymentIntent's payment method. If present,
        --   the payment method used with this PaymentIntent can
        --   be
        --   [attached](https://stripe.com/docs/api/payment_methods/attach)
        --   to a Customer, even after the transaction completes.
        --   For more, learn to [save card details during
        --   payment](https://stripe.com/docs/payments/save-during-payment).
        --   Stripe uses `setup_future_usage` to dynamically
        --   optimize your payment flow and comply with regional
        --   legislation and network rules. For example, if your
        --   customer is impacted by
        --   [SCA](https://stripe.com/docs/strong-customer-authentication),
        --   using `off_session` will ensure that they are
        --   authenticated while processing this PaymentIntent.
        --   You will then be able to collect [off-session
        --   payments](https://stripe.com/docs/payments/cards/charging-saved-cards#off-session-payments-with-saved-cards)
        --   for this customer.
    receiptEmail : Optional Text
        -- ^ Email address that the receipt for the resulting
        --   payment will be sent to.
    paymentMethod : Optional Text
        -- ^ ID of the payment method used in this PaymentIntent.
    application : Optional Text
        -- ^ ID of the Connect application that created the
        --   PaymentIntent.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    amountCapturable : Optional Int
        -- ^ Amount that can be captured from this PaymentIntent.
    clientSecret : Optional Text
        -- ^ The client secret of this PaymentIntent. Used for
        --   client-side retrieval using a publishable key. The
        --   client secret can be used to complete a payment from
        --   your frontend. It should not be stored, logged,
        --   embedded in URLs, or exposed to anyone other than the
        --   customer. Make sure that you have TLS enabled on any
        --   page that includes the client secret. Refer to our
        --   docs to [accept a
        --   payment](https://stripe.com/docs/payments/accept-a-payment)
        --   and learn about how `client_secret` should be
        --   handled.
    nextAction : Optional PaymentIntentNextAction
    invoice : Optional Text
        -- ^ ID of the invoice that created this PaymentIntent, if
        --   it exists.
    charges : Optional PaymentIntentCharges
        -- ^ Charges that were created by this PaymentIntent, if
        --   any.
    transferData : Optional TransferData
    customer : Optional Text
        -- ^ ID of the Customer this PaymentIntent belongs to, if
        --   one exists. If present, payment methods used with
        --   this PaymentIntent can only be attached to this
        --   Customer, and payment methods attached to other
        --   Customers cannot be used with this PaymentIntent.
    amountReceived : Optional Int
        -- ^ Amount that was collected by this PaymentIntent.
    onBehalfOf : Optional Text
        -- ^ The account (if any) for which the funds of the
        --   PaymentIntent are intended. See the PaymentIntents
        --   [use case for connected
        --   accounts](https://stripe.com/docs/payments/connected-accounts)
        --   for details.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    review : Optional Text
        -- ^ ID of the review associated with this PaymentIntent,
        --   if any.
    shipping : Optional Shipping
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : Optional PaymentIntentMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   For more information, see the
        --   [documentation](https://stripe.com/docs/payments/payment-intents/creating-payment-intents#storing-information-in-metadata).
    paymentMethodOptions : Optional PaymentIntentPaymentMethodOptions
    id : Text
        -- ^ Unique identifier for the object.
    canceledAt : Optional Int
        -- ^ Populated when `status` is `canceled`, this is the
        --   time at which the PaymentIntent was canceled.
        --   Measured in seconds since the Unix epoch.
    applicationFeeAmount : Optional Int
        -- ^ The amount of the application fee (if any) for the
        --   resulting payment. See the PaymentIntents [use case
        --   for connected
        --   accounts](https://stripe.com/docs/payments/connected-accounts)
        --   for details.
    lastPaymentError : Optional ApiErrors
    transferGroup : Optional Text
        -- ^ A string that identifies the resulting payment as
        --   part of a group. See the PaymentIntents [use case for
        --   connected
        --   accounts](https://stripe.com/docs/payments/connected-accounts)
        --   for details.
    paymentMethodTypes : [Text]
        -- ^ The list of payment method types (e.g. card) that
        --   this PaymentIntent is allowed to use.
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users.
    statementDescriptor : Optional Text
        -- ^ For non-card charges, you can use this value as the
        --   complete description that appears on your customers’
        --   statements. Must contain at least one letter, maximum
        --   22 characters.
    statementDescriptorSuffix : Optional Text
        -- ^ Provides information about a card payment that
        --   customers see on their statements. Concatenated with
        --   the prefix (shortened descriptor) or statement
        --   descriptor that’s set on the account to form the
        --   complete statement descriptor. Maximum 22 characters
        --   for the concatenated descriptor.
      deriving (Eq, Ord, Show)

data PaymentMethodCardWalletApplePay = PaymentMethodCardWalletApplePay with
      deriving (Eq, Ord, Show)

data PaymentMethodOptionsCardInstallmentsAvailablePlans = PaymentMethodOptionsCardInstallmentsAvailablePlans with
      deriving (Eq, Ord, Show)

data PaymentMethodOptionsCardInstallments = PaymentMethodOptionsCardInstallments with
    enabled : Bool
        -- ^ Whether Installments are enabled for this
        --   PaymentIntent.
    plan : Optional PaymentMethodDetailsCardInstallmentsPlan
    availablePlans : [PaymentMethodDetailsCardInstallmentsPlan]
        -- ^ Installment plans that may be selected for this
        --   PaymentIntent.
      deriving (Eq, Ord, Show)

data SourceTypeAlipay = SourceTypeAlipay with
    nativeUrl : Optional Text
    dataString : Optional Text
    statementDescriptor : Optional Text
      deriving (Eq, Ord, Show)

data ChargeTransferData = ChargeTransferData with
    destination : Text
        -- ^ ID of an existing, connected Stripe account to
        --   transfer funds to if `transfer_data` was specified in
        --   the charge request.
    amount : Optional Int
        -- ^ The amount transferred to the destination account, if
        --   specified. By default, the entire charge amount is
        --   transferred to the destination account.
      deriving (Eq, Ord, Show)

data SourceTransactionPaperCheckData = SourceTransactionPaperCheckData with
    invoices : Optional Text
        -- ^ Comma-separated list of invoice IDs associated with
        --   the paper check.
    availableAt : Optional Text
        -- ^ Time at which the deposited funds will be available
        --   for use. Measured in seconds since the Unix epoch.
      deriving (Eq, Ord, Show)

data InvoiceSettingCustomField = InvoiceSettingCustomField with
    value : Text
        -- ^ The value of the custom field.
    name : Text
        -- ^ The name of the custom field.
      deriving (Eq, Ord, Show)

data SourceCodeVerificationFlow = SourceCodeVerificationFlow with
    status : Text
        -- ^ The status of the code verification, either `pending`
        --   (awaiting verification, `attempts_remaining` should
        --   be greater than 0), `succeeded` (successful
        --   verification) or `failed` (failed verification,
        --   cannot be verified anymore as `attempts_remaining`
        --   should be 0).
    attemptsRemaining : Optional Int
        -- ^ The number of attempts remaining to authenticate the
        --   source object with a verification code.
      deriving (Eq, Ord, Show)

data TransferData = TransferData with
    destination : Text
        -- ^ The account (if any) the payment will be attributed
        --   to for tax reporting, and where funds from the
        --   payment will be transferred to upon payment success.
    amount : Optional Int
        -- ^ Amount intended to be collected by this
        --   PaymentIntent. A positive integer representing how
        --   much to charge in the [smallest currency
        --   unit](https://stripe.com/docs/currencies#zero-decimal)
        --   (e.g., 100 cents to charge $1.00 or 100 to charge
        --   ¥100, a zero-decimal currency). The minimum amount is
        --   $0.50 US or [equivalent in charge
        --   currency](https://stripe.com/docs/currencies#minimum-and-maximum-charge-amounts).
        --   The amount value supports up to eight digits (e.g., a
        --   value of 99999999 for a USD charge of $999,999.99).
      deriving (Eq, Ord, Show)

data IssuingCardholderRequirements = IssuingCardholderRequirements with
    disabledReason : Optional Text
        -- ^ If `disabled_reason` is present, all cards will
        --   decline authorizations with
        --   `cardholder_verification_required` reason.
    pastDue : [Text]
        -- ^ If not empty, this field contains the list of fields
        --   that need to be collected in order to verify and
        --   re-enabled the cardholder.
      deriving (Eq, Ord, Show)

data SubscriptionBillingThresholds = SubscriptionBillingThresholds with
    amountGte : Optional Int
        -- ^ Monetary threshold that triggers the subscription to
        --   create an invoice
    resetBillingCycleAnchor : Optional Bool
        -- ^ Indicates if the `billing_cycle_anchor` should be
        --   reset when a threshold is reached. If true,
        --   `billing_cycle_anchor` will be updated to the
        --   date/time the threshold was last reached; otherwise,
        --   the value will remain unchanged. This value may not
        --   be `true` if the subscription contains items with
        --   plans that have `aggregate_usage=last_ever`.
      deriving (Eq, Ord, Show)

data PaymentMethodDetailsCardWalletApplePay = PaymentMethodDetailsCardWalletApplePay with
      deriving (Eq, Ord, Show)

data LineItemTaxAmounts = LineItemTaxAmounts with
      deriving (Eq, Ord, Show)

data LineItemMetadata = LineItemMetadata with
      deriving (Eq, Ord, Show)

data LineItemTaxRates = LineItemTaxRates with
      deriving (Eq, Ord, Show)

data LineItem = LineItem with
    amount : Int
        -- ^ The amount, in %s.
    proration : Bool
        -- ^ Whether this is a proration.
    taxAmounts : [InvoiceTaxAmount]
        -- ^ The amount of tax calculated per tax rate for this
        --   line item
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    period : InvoiceLineItemPeriod
    quantity : Optional Int
        -- ^ The quantity of the subscription, if the line item is
        --   a subscription or a proration.
    plan : Optional Plan
    subscriptionItem : Optional Text
        -- ^ The subscription item that generated this invoice
        --   item. Left empty if the line item is not an explicit
        --   result of a subscription.
    discountable : Bool
        -- ^ If true, discounts will apply to this line item.
        --   Always false for prorations.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : LineItemMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Note that for line items with `type=subscription`
        --   this will reflect the metadata of the subscription
        --   that caused the line item to be created.
    id : Text
        -- ^ Unique identifier for the object.
    type_ : Optional Text
        -- ^ A string identifying the type of the source of this
        --   line item, either an `invoiceitem` or a
        --   `subscription`.
    invoiceItem : Optional Text
        -- ^ The ID of the [invoice
        --   item](https://stripe.com/docs/api/invoiceitems)
        --   associated with this line item if any.
    taxRates : [TaxRate]
        -- ^ The tax rates which apply to the line item.
    subscription : Optional Text
        -- ^ The subscription that the invoice item pertains to,
        --   if any.
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users.
      deriving (Eq, Ord, Show)

data PaymentMethodCardWalletVisaCheckout = PaymentMethodCardWalletVisaCheckout with
    email : Optional Text
        -- ^ Owner's verified email. Values are verified or
        --   provided by the wallet directly (if supported) at the
        --   time of authorization or settlement. They cannot be
        --   set or mutated.
    billingAddress : Optional Address
    shippingAddress : Optional Address
    name : Optional Text
        -- ^ Owner's verified full name. Values are verified or
        --   provided by the wallet directly (if supported) at the
        --   time of authorization or settlement. They cannot be
        --   set or mutated.
      deriving (Eq, Ord, Show)

data CustomerTaxIdsData = CustomerTaxIdsData with
      deriving (Eq, Ord, Show)

data CustomerTaxIds = CustomerTaxIds with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [TaxId]
        -- ^ Details about each object.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data CustomerSourcesData = CustomerSourcesData with
      deriving (Eq, Ord, Show)

data CustomerSources = CustomerSources with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [AlipayAccount]
        -- ^ Details about each object.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data CustomerMetadata = CustomerMetadata with
      deriving (Eq, Ord, Show)

data CustomerSubscriptionsData = CustomerSubscriptionsData with
      deriving (Eq, Ord, Show)

data CustomerSubscriptions = CustomerSubscriptions with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Subscription]
        -- ^ Details about each object.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data Customer = Customer with
    email : Optional Text
        -- ^ The customer's email address.
    phone : Optional Text
        -- ^ The customer's phone number.
    taxIds : Optional CustomerTaxIds
        -- ^ The customer's tax IDs.
    discount : Optional Discount
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    invoiceSettings : Optional InvoiceSettingCustomerSetting
    balance : Optional Int
        -- ^ Current balance, if any, being stored on the
        --   customer. If negative, the customer has credit to
        --   apply to their next invoice. If positive, the
        --   customer has an amount owed that will be added to
        --   their next invoice. The balance does not refer to any
        --   unpaid invoices; it solely takes into account amounts
        --   that have yet to be successfully applied to any
        --   invoice. This balance is only taken into account as
        --   invoices are finalized.
    sources : CustomerSources
        -- ^ The customer's payment sources, if any.
    address : Optional Address
    name : Optional Text
        -- ^ The customer's full name or business name.
    currency : Optional Text
        -- ^ Three-letter [ISO code for the
        --   currency](https://stripe.com/docs/currencies) the
        --   customer can be charged in for recurring billing
        --   purposes.
    shipping : Optional Shipping
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : Optional CustomerMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    subscriptions : Optional CustomerSubscriptions
        -- ^ The customer's current subscriptions, if any.
    taxExempt : Optional Text
        -- ^ Describes the customer's tax exemption status. One of
        --   `none`, `exempt`, or `reverse`. When set to
        --   `reverse`, invoice and receipt PDFs include the text
        --   **"Reverse charge"**.
    invoicePrefix : Optional Text
        -- ^ The prefix for the customer used to generate unique
        --   invoice numbers.
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users.
    defaultSource : Optional Text
        -- ^ ID of the default payment source for the customer. If
        --   you are using payment methods created via the
        --   PaymentMethods API, see the
        --   [invoice_settings.default_payment_method](https://stripe.com/docs/api/customers/object#customer_object-invoice_settings-default_payment_method)
        --   field instead.
    delinquent : Optional Bool
        -- ^ When the customer's latest invoice is billed by
        --   charging automatically, delinquent is true if the
        --   invoice's latest charge is failed. When the
        --   customer's latest invoice is billed by sending an
        --   invoice, delinquent is true if the invoice is not
        --   paid by its due date.
    preferredLocales : [Text]
        -- ^ The customer's preferred locales (languages), ordered
        --   by preference.
      deriving (Eq, Ord, Show)

data LoginLink = LoginLink with
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    url : Text
        -- ^ The URL for the login link.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
      deriving (Eq, Ord, Show)

data CountrySpecVerificationFieldDetails = CountrySpecVerificationFieldDetails with
    minimum : [Text]
        -- ^ Fields which every account must eventually provide.
    additional : [Text]
        -- ^ Additional fields which are only required for some
        --   users.
      deriving (Eq, Ord, Show)

data SourceOrderItems = SourceOrderItems with
      deriving (Eq, Ord, Show)

data SourceOrder = SourceOrder with
    email : Optional Text
        -- ^ The email address of the customer placing the order.
    amount : Int
        -- ^ A positive integer in the smallest currency unit
        --   (that is, 100 cents for $1.00, or 1 for ¥1, Japanese
        --   Yen being a zero-decimal currency) representing the
        --   total amount for the order.
    items : [SourceOrderItem]
        -- ^ List of items constituting the order.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    shipping : Optional Shipping
      deriving (Eq, Ord, Show)

data InvoiceitemMetadata = InvoiceitemMetadata with
      deriving (Eq, Ord, Show)

data InvoiceitemTaxRates = InvoiceitemTaxRates with
      deriving (Eq, Ord, Show)

data Invoiceitem = Invoiceitem with
    amount : Int
        -- ^ Amount (in the `currency` specified) of the invoice
        --   item. This should always be equal to `unit_amount *
        --   quantity`.
    proration : Bool
        -- ^ Whether the invoice item was created automatically as
        --   a proration adjustment when the customer switched
        --   plans.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    period : InvoiceLineItemPeriod
    quantity : Int
        -- ^ Quantity of units for the invoice item. If the
        --   invoice item is a proration, the quantity of the
        --   subscription that the proration was computed for.
    plan : Optional Plan
    invoice : Optional Text
        -- ^ The ID of the invoice this invoice item belongs to.
    subscriptionItem : Optional Text
        -- ^ The subscription item that this invoice item has been
        --   created for, if any.
    unitAmountDecimal : Optional Text
        -- ^ Same as `unit_amount`, but contains a decimal value
        --   with at most 12 decimal places.
    date : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    customer : Text
        -- ^ The ID of the customer who will be billed when this
        --   invoice item is billed.
    discountable : Bool
        -- ^ If true, discounts will apply to this invoice item.
        --   Always false for prorations.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : InvoiceitemMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    unitAmount : Optional Int
        -- ^ Unit Amount (in the `currency` specified) of the
        --   invoice item.
    taxRates : [TaxRate]
        -- ^ The tax rates which apply to the invoice item. When
        --   set, the `default_tax_rates` on the invoice do not
        --   apply to this invoice item.
    subscription : Optional Text
        -- ^ The subscription that this invoice item has been
        --   created for, if any.
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users.
      deriving (Eq, Ord, Show)

data DeletedRadarValueListItem = DeletedRadarValueListItem with
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    deleted : Bool
        -- ^ Always true for a deleted object
      deriving (Eq, Ord, Show)

data ApiErrors = ApiErrors with
    docUrl : Optional Text
        -- ^ A URL to more information about the [error
        --   code](https://stripe.com/docs/error-codes) reported.
    paymentMethod : Optional PaymentMethod
    declineCode : Optional Text
        -- ^ For card errors resulting from a card issuer decline,
        --   a short string indicating the [card issuer's reason
        --   for the
        --   decline](https://stripe.com/docs/declines#issuer-declines)
        --   if they provide one.
    paymentIntent : Optional PaymentIntent
    setupIntent : Optional SetupIntent
    param : Optional Text
        -- ^ If the error is parameter-specific, the parameter
        --   related to the error. For example, you can use this
        --   to display a message near the correct form field.
    charge : Optional Text
        -- ^ For card errors, the ID of the failed charge.
    code : Optional Text
        -- ^ For some errors that could be handled
        --   programmatically, a short string indicating the
        --   [error code](https://stripe.com/docs/error-codes)
        --   reported.
    source : Optional BankAccount
    type_ : Optional Text
        -- ^ The type of error returned. One of
        --   `api_connection_error`, `api_error`,
        --   `authentication_error`, `card_error`,
        --   `idempotency_error`, `invalid_request_error`, or
        --   `rate_limit_error`
    message : Optional Text
        -- ^ A human-readable message providing more details about
        --   the error. For card errors, these messages can be
        --   shown to your users.
      deriving (Eq, Ord, Show)

data MandateMultiUse = MandateMultiUse with
      deriving (Eq, Ord, Show)

data SetupIntentMetadata = SetupIntentMetadata with
      deriving (Eq, Ord, Show)

data SetupIntent = SetupIntent with
    status : Text
        -- ^ [Status](https://stripe.com/docs/payments/intents#intent-statuses)
        --   of this SetupIntent, one of
        --   `requires_payment_method`, `requires_confirmation`,
        --   `requires_action`, `processing`, `canceled`, or
        --   `succeeded`.
    cancellationReason : Optional Text
        -- ^ Reason for cancellation of this SetupIntent, one of
        --   `abandoned`, `requested_by_customer`, or `duplicate`.
    singleUseMandate : Optional Text
        -- ^ ID of the single_use Mandate generated by the
        --   SetupIntent.
    paymentMethod : Optional Text
        -- ^ ID of the payment method used with this SetupIntent.
    application : Optional Text
        -- ^ ID of the Connect application that created the
        --   SetupIntent.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    clientSecret : Optional Text
        -- ^ The client secret of this SetupIntent. Used for
        --   client-side retrieval using a publishable key. The
        --   client secret can be used to complete payment setup
        --   from your frontend. It should not be stored, logged,
        --   embedded in URLs, or exposed to anyone other than the
        --   customer. Make sure that you have TLS enabled on any
        --   page that includes the client secret.
    nextAction : Optional SetupIntentNextAction
    mandate : Optional Text
        -- ^ ID of the multi use Mandate generated by the
        --   SetupIntent.
    customer : Optional Text
        -- ^ ID of the Customer this SetupIntent belongs to, if
        --   one exists. If present, payment methods used with
        --   this SetupIntent can only be attached to this
        --   Customer, and payment methods attached to other
        --   Customers cannot be used with this SetupIntent.
    onBehalfOf : Optional Text
        -- ^ The account (if any) for which the setup is intended.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : Optional SetupIntentMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    paymentMethodOptions : Optional SetupIntentPaymentMethodOptions
    lastSetupError : Optional ApiErrors
    id : Text
        -- ^ Unique identifier for the object.
    usage : Text
        -- ^ Indicates how the payment method is intended to be
        --   used in the future. Use `on_session` if you intend to
        --   only reuse the payment method when the customer is in
        --   your checkout flow. Use `off_session` if your
        --   customer may or may not be in your checkout flow. If
        --   not provided, this value defaults to `off_session`.
    paymentMethodTypes : [Text]
        -- ^ The list of payment method types (e.g. card) that
        --   this SetupIntent is allowed to set up.
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users.
      deriving (Eq, Ord, Show)

data PaymentMethodDetailsCard = PaymentMethodDetailsCard with
    wallet : Optional PaymentMethodDetailsCardWallet
    checks : Optional PaymentMethodDetailsCardChecks
    country : Optional Text
        -- ^ Two-letter ISO code representing the country of the
        --   card. You could use this attribute to get a sense of
        --   the international breakdown of cards you've
        --   collected.
    installments : Optional PaymentMethodDetailsCardInstallments
    fingerprint : Optional Text
        -- ^ Uniquely identifies this particular card number. You
        --   can use this attribute to check whether two customers
        --   who've signed up with you are using the same card
        --   number, for example.
    brand : Optional Text
        -- ^ Card brand. Can be `amex`, `diners`, `discover`,
        --   `jcb`, `mastercard`, `unionpay`, `visa`, or
        --   `unknown`.
    network : Optional Text
        -- ^ Identifies which network this charge was processed
        --   on. Can be `amex`, `diners`, `discover`, `interac`,
        --   `jcb`, `mastercard`, `unionpay`, `visa`, or
        --   `unknown`.
    funding : Optional Text
        -- ^ Card funding type. Can be `credit`, `debit`,
        --   `prepaid`, or `unknown`.
    expMonth : Optional Int
        -- ^ Two-digit number representing the card's expiration
        --   month.
    expYear : Optional Int
        -- ^ Four-digit number representing the card's expiration
        --   year.
    threeDSecure : Optional ThreeDSecureDetails
    last4 : Optional Text
        -- ^ The last four digits of the card.
      deriving (Eq, Ord, Show)

data SourceMandateNotificationBacsDebitData = SourceMandateNotificationBacsDebitData with
    last4 : Optional Text
        -- ^ Last 4 digits of the account number associated with
        --   the debit.
      deriving (Eq, Ord, Show)

data TransferReversalMetadata = TransferReversalMetadata with
      deriving (Eq, Ord, Show)

data TransferReversal = TransferReversal with
    sourceRefund : Optional Text
        -- ^ ID of the refund responsible for the transfer
        --   reversal.
    amount : Int
        -- ^ Amount, in %s.
    balanceTransaction : Optional Text
        -- ^ Balance transaction that describes the impact on your
        --   account balance.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : TransferReversalMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    destinationPaymentRefund : Optional Text
        -- ^ Linked payment refund for the transfer reversal.
    transfer : Text
        -- ^ ID of the transfer that was reversed.
      deriving (Eq, Ord, Show)

data BitcoinTransaction = BitcoinTransaction with
    amount : Int
        -- ^ The amount of `currency` that the transaction was
        --   converted to in real-time.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    receiver : Text
        -- ^ The receiver to which this transaction was sent.
    bitcoinAmount : Optional Int
        -- ^ The amount of bitcoin contained in the transaction.
    currency : Text
        -- ^ Three-letter [ISO code for the
        --   currency](https://stripe.com/docs/currencies) to
        --   which this transaction was converted.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
      deriving (Eq, Ord, Show)

data FeeRefundMetadata = FeeRefundMetadata with
      deriving (Eq, Ord, Show)

data FeeRefund = FeeRefund with
    amount : Int
        -- ^ Amount, in %s.
    balanceTransaction : Optional Text
        -- ^ Balance transaction that describes the impact on your
        --   account balance.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    fee : Text
        -- ^ ID of the application fee that was refunded.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : FeeRefundMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
      deriving (Eq, Ord, Show)

data TerminalReaderMetadata = TerminalReaderMetadata with
      deriving (Eq, Ord, Show)

data TerminalReader = TerminalReader with
    serialNumber : Optional Text
        -- ^ Serial number of the reader.
    status : Optional Text
        -- ^ The networking status of the reader.
    deviceType : Optional Text
        -- ^ Type of reader, one of `bbpos_chipper2x` or
        --   `verifone_P400`.
    location : Optional Text
        -- ^ The location identifier of the reader.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : TerminalReaderMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    ipAddress : Optional Text
        -- ^ The local IP address of the reader.
    deviceSwVersion : Optional Text
        -- ^ The current software version of the reader.
    label_ : Optional Text
        -- ^ Custom label given to the reader for easier
        --   identification.
      deriving (Eq, Ord, Show)

data MandateSingleUse = MandateSingleUse with
    amount : Int
        -- ^ On a single use mandate, the amount of the payment.
    currency : Text
        -- ^ On a single use mandate, the currency of the payment.
      deriving (Eq, Ord, Show)

data SubscriptionPendingInvoiceItemInterval = SubscriptionPendingInvoiceItemInterval with
    interval : Text
        -- ^ Specifies invoicing frequency. Either `day`, `week`,
        --   `month` or `year`.
    intervalCount : Optional Int
        -- ^ The number of intervals between invoices. For
        --   example, `interval=month` and `interval_count=3`
        --   bills every 3 months. Maximum of one year interval
        --   allowed (1 year, 12 months, or 52 weeks).
      deriving (Eq, Ord, Show)

data PaymentMethodDetailsStripeAccount = PaymentMethodDetailsStripeAccount with
      deriving (Eq, Ord, Show)

data OfflineAcceptance = OfflineAcceptance with
      deriving (Eq, Ord, Show)

data DeletedPerson = DeletedPerson with
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    deleted : Bool
        -- ^ Always true for a deleted object
      deriving (Eq, Ord, Show)

data DeletedDiscount = DeletedDiscount with
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    deleted : Bool
        -- ^ Always true for a deleted object
      deriving (Eq, Ord, Show)

data PaymentMethodDetails = PaymentMethodDetails with
    card : Optional PaymentMethodDetailsCard
    stripeAccount : Optional PaymentMethodDetailsStripeAccount
    ideal : Optional PaymentMethodDetailsIdeal
    bancontact : Optional PaymentMethodDetailsBancontact
    sofort : Optional PaymentMethodDetailsSofort
    klarna : Optional PaymentMethodDetailsKlarna
    achDebit : Optional PaymentMethodDetailsAchDebit
    fpx : Optional PaymentMethodDetailsFpx
    wechat : Optional PaymentMethodDetailsWechat
    alipay : Optional PaymentMethodDetailsAlipay
    achCreditTransfer : Optional PaymentMethodDetailsAchCreditTransfer
    cardPresent : Optional PaymentMethodDetailsCardPresent
    multibanco : Optional PaymentMethodDetailsMultibanco
    type_ : Optional Text
        -- ^ The type of transaction-specific details of the
        --   payment method used in the payment, one of
        --   `ach_credit_transfer`, `ach_debit`, `alipay`,
        --   `bancontact`, `card`, `card_present`, `eps`,
        --   `giropay`, `ideal`, `klarna`, `multibanco`, `p24`,
        --   `sepa_debit`, `sofort`, `stripe_account`, or
        --   `wechat`. An additional hash is included on
        --   `payment_method_details` with a name matching this
        --   value. It contains information specific to the
        --   payment method.
    giropay : Optional PaymentMethodDetailsGiropay
    eps : Optional PaymentMethodDetailsEps
    p24 : Optional PaymentMethodDetailsP24
    sepaDebit : Optional PaymentMethodDetailsSepaDebit
      deriving (Eq, Ord, Show)

data SubscriptionScheduleCurrentPhase = SubscriptionScheduleCurrentPhase with
    endDate : Optional Int
        -- ^ The end of this phase of the subscription schedule.
    startDate : Optional Int
        -- ^ The start of this phase of the subscription schedule.
      deriving (Eq, Ord, Show)

data InvoiceSettingSubscriptionScheduleSetting = InvoiceSettingSubscriptionScheduleSetting with
    daysUntilDue : Optional Int
        -- ^ Number of days within which a customer must pay
        --   invoices generated by this subscription schedule.
        --   This value will be `null` for subscription schedules
        --   where `billing=charge_automatically`.
      deriving (Eq, Ord, Show)

data AccountBrandingSettings = AccountBrandingSettings with
    icon : Optional Text
        -- ^ (ID of a [file
        --   upload](https://stripe.com/docs/guides/file-upload))
        --   An icon for the account. Must be square and at least
        --   128px x 128px.
    primaryColor : Optional Text
        -- ^ A CSS hex color value representing the primary
        --   branding color for this account
    logo : Optional Text
        -- ^ (ID of a [file
        --   upload](https://stripe.com/docs/guides/file-upload))
        --   A logo for the account that will be used in Checkout
        --   instead of the icon and without the account's name
        --   next to it if provided. Must be at least 128px x
        --   128px.
      deriving (Eq, Ord, Show)

data EphemeralKey = EphemeralKey with
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    expires : Int
        -- ^ Time at which the key will expire. Measured in
        --   seconds since the Unix epoch.
    secret : Optional Text
        -- ^ The key's secret. You can use this value to make
        --   authorized requests to the Stripe API.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
      deriving (Eq, Ord, Show)

data LegalEntityCompany = LegalEntityCompany with
    addressKana : Optional LegalEntityJapanAddress
    phone : Optional Text
        -- ^ The company's phone number (used for verification).
    nameKanji : Optional Text
        -- ^ The Kanji variation of the company's legal name
        --   (Japan only).
    taxIdRegistrar : Optional Text
        -- ^ The jurisdiction in which the `tax_id` is registered
        --   (Germany-based companies only).
    ownersProvided : Optional Bool
        -- ^ Whether the company's owners have been provided. This
        --   Boolean will be `true` if you've manually indicated
        --   that all owners are provided via [the
        --   `owners_provided`
        --   parameter](https://stripe.com/docs/api/accounts/update#update_account-company-owners_provided),
        --   or if Stripe determined that sufficient owners were
        --   provided. Stripe determines ownership requirements
        --   using both the number of owners provided and their
        --   total percent ownership (calculated by adding the
        --   `percent_ownership` of each owner together).
    structure : Optional Text
        -- ^ The category identifying the legal structure of the
        --   company or legal entity.
    directorsProvided : Optional Bool
        -- ^ Whether the company's directors have been provided.
        --   This Boolean will be `true` if you've manually
        --   indicated that all directors are provided via [the
        --   `directors_provided`
        --   parameter](https://stripe.com/docs/api/accounts/update#update_account-company-directors_provided).
    address : Optional Address
    vatIdProvided : Optional Bool
        -- ^ Whether the company's business VAT number was
        --   provided.
    name : Optional Text
        -- ^ The company's legal name.
    taxIdProvided : Optional Bool
        -- ^ Whether the company's business ID number was
        --   provided.
    nameKana : Optional Text
        -- ^ The Kana variation of the company's legal name (Japan
        --   only).
    executivesProvided : Optional Bool
        -- ^ Whether the company's executives have been provided.
        --   This Boolean will be `true` if you've manually
        --   indicated that all executives are provided via [the
        --   `executives_provided`
        --   parameter](https://stripe.com/docs/api/accounts/update#update_account-company-executives_provided),
        --   or if Stripe determined that sufficient executives
        --   were provided.
    addressKanji : Optional LegalEntityJapanAddress
    verification : Optional LegalEntityCompanyVerification
      deriving (Eq, Ord, Show)

data IssuingSettlementMetadata = IssuingSettlementMetadata with
      deriving (Eq, Ord, Show)

data IssuingSettlement = IssuingSettlement with
    networkFees : Optional Int
        -- ^ The total amount of fees owed to the network.
    settlementService : Optional Text
        -- ^ One of `international` or `uk_national_net`.
    clearingDate : Optional Int
        -- ^ The date that the transactions are cleared and posted
        --   to user's accounts.
    bin : Text
        -- ^ The Bank Identification Number reflecting this
        --   settlement record.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    interchangeFees : Optional Int
        -- ^ The total interchange received as reimbursement for
        --   the transactions.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    transactionVolume : Optional Int
        -- ^ The total transaction amount reflected in this
        --   settlement.
    network : Text
        -- ^ The card network for this settlement report. One of
        --   ["visa"]
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    netTotal : Optional Int
        -- ^ The total net amount required to settle with the
        --   network.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : IssuingSettlementMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    networkSettlementIdentifier : Optional Text
        -- ^ The Settlement Identification Number assigned by the
        --   network.
    transactionCount : Optional Int
        -- ^ The total number of transactions reflected in this
        --   settlement.
      deriving (Eq, Ord, Show)

data RadarEarlyFraudWarning = RadarEarlyFraudWarning with
    actionable : Bool
        -- ^ An EFW is actionable if it has not received a dispute
        --   and has not been fully refunded. You may wish to
        --   proactively refund a charge that receives an EFW, in
        --   order to avoid receiving a dispute later.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    fraudType : Optional Text
        -- ^ The type of fraud labelled by the issuer. One of
        --   `card_never_received`, `fraudulent_card_application`,
        --   `made_with_counterfeit_card`, `made_with_lost_card`,
        --   `made_with_stolen_card`, `misc`,
        --   `unauthorized_use_of_card`.
    charge : Text
        -- ^ ID of the charge this early fraud warning is for,
        --   optionally expanded.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
      deriving (Eq, Ord, Show)

data SourceTransactionSepaCreditTransferData = SourceTransactionSepaCreditTransferData with
    reference : Optional Text
        -- ^ Reference associated with the transfer.
    senderIban : Optional Text
        -- ^ Sender's bank account IBAN.
    senderName : Optional Text
        -- ^ Sender's name.
      deriving (Eq, Ord, Show)

data ScheduledQueryRun = ScheduledQueryRun with
    status : Text
        -- ^ The query's execution status, which will be
        --   `completed` for successful runs, and `canceled`,
        --   `failed`, or `timed_out` otherwise.
    resultAvailableUntil : Optional Int
        -- ^ Time at which the result expires and is no longer
        --   available for download.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    error : Optional SigmaScheduledQueryRunError
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    dataLoadTime : Optional Int
        -- ^ When the query was run, Sigma contained a snapshot of
        --   your Stripe data at this time.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    title : Text
        -- ^ Title of the query.
    file : Optional File
    sql : Text
        -- ^ SQL for the query.
      deriving (Eq, Ord, Show)

data AccountPayoutSettings = AccountPayoutSettings with
    schedule : TransferSchedule
    debitNegativeBalances : Optional Bool
        -- ^ A Boolean indicating if Stripe should try to reclaim
        --   negative balances from an attached bank account. See
        --   our [Understanding Connect Account
        --   Balances](https://stripe.com/docs/connect/account-balances)
        --   documentation for details. Default value is `true`
        --   for Express accounts and `false` for Custom accounts.
    statementDescriptor : Optional Text
        -- ^ The text that appears on the bank account statement
        --   for payouts. If not set, this defaults to the
        --   platform's bank descriptor as set in the Dashboard.
      deriving (Eq, Ord, Show)

data DisputeMetadata = DisputeMetadata with
      deriving (Eq, Ord, Show)

data DisputeBalanceTransactions = DisputeBalanceTransactions with
      deriving (Eq, Ord, Show)

data Dispute = Dispute with
    status : Text
        -- ^ Current status of dispute. Possible values are
        --   `warning_needs_response`, `warning_under_review`,
        --   `warning_closed`, `needs_response`, `under_review`,
        --   `charge_refunded`, `won`, or `lost`.
    amount : Int
        -- ^ Disputed amount. Usually the amount of the charge,
        --   but can differ (usually because of currency
        --   fluctuation or because only part of the order is
        --   disputed).
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    paymentIntent : Optional Text
        -- ^ ID of the PaymentIntent that was disputed.
    reason : Text
        -- ^ Reason given by cardholder for dispute. Possible
        --   values are `bank_cannot_process`, `check_returned`,
        --   `credit_not_processed`, `customer_initiated`,
        --   `debit_not_authorized`, `duplicate`, `fraudulent`,
        --   `general`, `incorrect_account_details`,
        --   `insufficient_funds`, `product_not_received`,
        --   `product_unacceptable`, `subscription_canceled`, or
        --   `unrecognized`. Read more about [dispute
        --   reasons](https://stripe.com/docs/disputes/categories).
    isChargeRefundable : Optional Bool
        -- ^ If true, it is still possible to refund the disputed
        --   payment. Once the payment has been fully refunded, no
        --   further funds will be withdrawn from your Stripe
        --   account as a result of this dispute.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    charge : Text
        -- ^ ID of the charge that was disputed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : DisputeMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    evidence : DisputeEvidence
    evidenceDetails : Optional DisputeEvidenceDetails
    balanceTransactions : [BalanceTransaction]
        -- ^ List of zero, one, or two balance transactions that
        --   show funds withdrawn and reinstated to your Stripe
        --   account as a result of this dispute.
      deriving (Eq, Ord, Show)

data DeletedCoupon = DeletedCoupon with
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    deleted : Bool
        -- ^ Always true for a deleted object
      deriving (Eq, Ord, Show)

data CreditNoteTaxAmounts = CreditNoteTaxAmounts with
      deriving (Eq, Ord, Show)

data CreditNoteLinesData = CreditNoteLinesData with
      deriving (Eq, Ord, Show)

data CreditNoteLines = CreditNoteLines with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [CreditNoteLineItem]
        -- ^ Details about each object.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data CreditNoteMetadata = CreditNoteMetadata with
      deriving (Eq, Ord, Show)

data CreditNote = CreditNote with
    status : Text
        -- ^ Status of this credit note, one of `issued` or
        --   `void`. Learn more about [voiding credit
        --   notes](https://stripe.com/docs/billing/invoices/credit-notes#voiding).
    amount : Int
        -- ^ The integer amount in **%s** representing the total
        --   amount of the credit note, including tax.
    memo : Optional Text
        -- ^ Customer-facing text that appears on the credit note
        --   PDF.
    taxAmounts : [CreditNoteTaxAmount]
        -- ^ The aggregate amounts calculated per tax rate for all
        --   line items.
    lines : CreditNoteLines
        -- ^ Line items that make up the credit note
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    outOfBandAmount : Optional Int
        -- ^ Amount that was credited outside of Stripe.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    subtotal : Int
        -- ^ The integer amount in **%s** representing the amount
        --   of the credit note, excluding tax and discount.
    customerBalanceTransaction : Optional Text
        -- ^ Customer balance transaction related to this credit
        --   note.
    refund : Optional Text
        -- ^ Refund related to this credit note.
    invoice : Text
        -- ^ ID of the invoice.
    customer : Text
        -- ^ ID of the customer.
    reason : Optional Text
        -- ^ Reason for issuing this credit note, one of
        --   `duplicate`, `fraudulent`, `order_change`, or
        --   `product_unsatisfactory`
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    voidedAt : Optional Int
        -- ^ The time that the credit note was voided.
    total : Int
        -- ^ The integer amount in **%s** representing the total
        --   amount of the credit note, including tax and
        --   discount.
    pdf : Text
        -- ^ The link to download the PDF of the credit note.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : CreditNoteMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    type_ : Optional Text
        -- ^ Type of this credit note, one of `pre_payment` or
        --   `post_payment`. A `pre_payment` credit note means it
        --   was issued when the invoice was open. A
        --   `post_payment` credit note means it was issued when
        --   the invoice was paid.
    number : Text
        -- ^ A unique number that identifies this particular
        --   credit note and appears on the PDF of the credit note
        --   and its associated invoice.
    discountAmount : Optional Int
        -- ^ The integer amount in **%s** representing the amount
        --   of the discount that was credited.
      deriving (Eq, Ord, Show)

data InvoicesStatusTransitions = InvoicesStatusTransitions with
    finalizedAt : Optional Int
        -- ^ The time that the invoice draft was finalized.
    markedUncollectibleAt : Optional Int
        -- ^ The time that the invoice was marked uncollectible.
    voidedAt : Optional Int
        -- ^ The time that the invoice was voided.
    paidAt : Optional Int
        -- ^ The time that the invoice was paid.
      deriving (Eq, Ord, Show)

data DeletedBankAccount = DeletedBankAccount with
    currency : Optional Text
        -- ^ Three-letter [ISO code for the
        --   currency](https://stripe.com/docs/payouts) paid out
        --   to the bank account.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    deleted : Bool
        -- ^ Always true for a deleted object
      deriving (Eq, Ord, Show)

data Review = Review with
    ipAddressLocation : Optional RadarReviewResourceLocation
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    openedReason : Optional Text
        -- ^ The reason the review was opened. One of `rule` or
        --   `manual`.
    paymentIntent : Optional Text
        -- ^ The PaymentIntent ID associated with this review, if
        --   one exists.
    closedReason : Optional Text
        -- ^ The reason the review was closed, or null if it has
        --   not yet been closed. One of `approved`, `refunded`,
        --   `refunded_as_fraud`, or `disputed`.
    open : Bool
        -- ^ If `true`, the review needs action.
    reason : Text
        -- ^ The reason the review is currently open or closed.
        --   One of `rule`, `manual`, `approved`, `refunded`,
        --   `refunded_as_fraud`, or `disputed`.
    billingZip : Optional Text
        -- ^ The ZIP or postal code of the card used, if
        --   applicable.
    charge : Optional Text
        -- ^ The charge associated with this review.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    ipAddress : Optional Text
        -- ^ The IP address where the payment originated.
    session : Optional RadarReviewResourceSession
      deriving (Eq, Ord, Show)

data CustomerAcceptance = CustomerAcceptance with
    online : Optional OnlineAcceptance
    offline : Optional OfflineAcceptance
    type_ : Optional Text
        -- ^ The type of customer acceptance information included
        --   with the Mandate. One of `online` or `offline`.
    acceptedAt : Optional Int
        -- ^ The time at which the customer accepted the Mandate.
      deriving (Eq, Ord, Show)

data Shipping = Shipping with
    carrier : Optional Text
        -- ^ The delivery service that shipped a physical product,
        --   such as Fedex, UPS, USPS, etc.
    phone : Optional Text
        -- ^ Recipient phone (including extension).
    trackingNumber : Optional Text
        -- ^ The tracking number for a physical product, obtained
        --   from the delivery service. If multiple tracking
        --   numbers were generated for this purchase, please
        --   separate them with commas.
    address : Optional Address
    name : Optional Text
        -- ^ Recipient name.
      deriving (Eq, Ord, Show)

data BitcoinReceiverTransactionsData = BitcoinReceiverTransactionsData with
      deriving (Eq, Ord, Show)

data BitcoinReceiverTransactions = BitcoinReceiverTransactions with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [BitcoinTransaction]
        -- ^ Details about each object.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data BitcoinReceiverMetadata = BitcoinReceiverMetadata with
      deriving (Eq, Ord, Show)

data BitcoinReceiver = BitcoinReceiver with
    email : Optional Text
        -- ^ The customer's email address, set by the API call
        --   that creates the receiver.
    amount : Int
        -- ^ The amount of `currency` that you are collecting as
        --   payment.
    transactions : Optional BitcoinReceiverTransactions
        -- ^ A list with one entry for each time that the customer
        --   sent bitcoin to the receiver. Hidden when viewing the
        --   receiver with a publishable key.
    refundAddress : Optional Text
        -- ^ The refund address of this bitcoin receiver.
    payment : Optional Text
        -- ^ The ID of the payment created from the receiver, if
        --   any. Hidden when viewing the receiver with a
        --   publishable key.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    uncapturedFunds : Optional Bool
        -- ^ This receiver contains uncaptured funds that can be
        --   used for a payment or refunded.
    usedForPayment : Optional Bool
        -- ^ Indicate if this source is used for payment.
    active : Bool
        -- ^ True when this bitcoin receiver has received a
        --   non-zero amount of bitcoin.
    customer : Optional Text
        -- ^ The customer ID of the bitcoin receiver.
    amountReceived : Optional Int
        -- ^ The amount of `currency` to which
        --   `bitcoin_amount_received` has been converted.
    inboundAddress : Optional Text
        -- ^ A bitcoin address that is specific to this receiver.
        --   The customer can send bitcoin to this address to fill
        --   the receiver.
    bitcoinAmount : Optional Int
        -- ^ The amount of bitcoin that the customer should send
        --   to fill the receiver. The `bitcoin_amount` is
        --   denominated in Satoshi: there are 10^8 Satoshi in one
        --   bitcoin.
    bitcoinAmountReceived : Optional Int
        -- ^ The amount of bitcoin that has been sent by the
        --   customer to this receiver.
    currency : Text
        -- ^ Three-letter [ISO code for the
        --   currency](https://stripe.com/docs/currencies) to
        --   which the bitcoin will be converted.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : BitcoinReceiverMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    bitcoinUri : Optional Text
        -- ^ This URI can be displayed to the customer as a
        --   clickable link (to activate their bitcoin client) or
        --   as a QR code (for mobile wallets).
    id : Text
        -- ^ Unique identifier for the object.
    filled : Bool
        -- ^ This flag is initially false and updates to true when
        --   the customer sends the `bitcoin_amount` to this
        --   receiver.
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users.
      deriving (Eq, Ord, Show)

data Inventory = Inventory with
    quantity : Optional Int
        -- ^ The count of inventory available. Will be present if
        --   and only if `type` is `finite`.
    value : Optional Text
        -- ^ An indicator of the inventory available. Possible
        --   values are `in_stock`, `limited`, and `out_of_stock`.
        --   Will be present if and only if `type` is `bucket`.
    type_ : Optional Text
        -- ^ Inventory type. Possible values are `finite`,
        --   `bucket` (not quantified), and `infinite`.
      deriving (Eq, Ord, Show)

data IssuerFraudRecord = IssuerFraudRecord with
    actionable : Bool
        -- ^ An IFR is actionable if it has not received a dispute
        --   and has not been fully refunded. You may wish to
        --   proactively refund a charge that receives an IFR, in
        --   order to avoid receiving a dispute later.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    hasLiabilityShift : Optional Bool
        -- ^ If true, the associated charge is subject to
        --   [liability
        --   shift](https://stripe.com/docs/sources/three-d-secure#disputed-payments).
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    fraudType : Optional Text
        -- ^ The type of fraud labelled by the issuer. One of
        --   `card_never_received`, `fraudulent_card_application`,
        --   `made_with_counterfeit_card`, `made_with_lost_card`,
        --   `made_with_stolen_card`, `misc`,
        --   `unauthorized_use_of_card`.
    postDate : Optional Int
        -- ^ The timestamp at which the card issuer posted the
        --   issuer fraud record.
    charge : Text
        -- ^ ID of the charge this issuer fraud record is for,
        --   optionally expanded.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
      deriving (Eq, Ord, Show)

data ShippingMethod = ShippingMethod with
    amount : Int
        -- ^ A positive integer in the smallest currency unit
        --   (that is, 100 cents for $1.00, or 1 for ¥1, Japanese
        --   Yen being a zero-decimal currency) representing the
        --   total amount for the line item.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    deliveryEstimate : Optional DeliveryEstimate
    id : Text
        -- ^ Unique identifier for the object.
    description : Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users.
      deriving (Eq, Ord, Show)

data AlipayAccountMetadata = AlipayAccountMetadata with
      deriving (Eq, Ord, Show)

data AlipayAccount = AlipayAccount with
    used : Bool
        -- ^ Whether this Alipay account object has ever been used
        --   for a payment.
    paymentCurrency : Optional Text
        -- ^ If the Alipay account object is not reusable, the
        --   exact currency that you can create a charge for.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    fingerprint : Text
        -- ^ Uniquely identifies the account and will be the same
        --   across all Alipay account objects that are linked to
        --   the same Alipay account.
    reusable : Bool
        -- ^ True if you can create multiple payments using this
        --   account. If the account is reusable, then you can
        --   freely choose the amount of each payment.
    username : Text
        -- ^ The username for the Alipay account.
    customer : Optional Text
        -- ^ The ID of the customer associated with this Alipay
        --   Account.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : Optional AlipayAccountMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    paymentAmount : Optional Int
        -- ^ If the Alipay account object is not reusable, the
        --   exact amount that you can create a charge for.
      deriving (Eq, Ord, Show)

data SourceTransactionAchCreditTransferData = SourceTransactionAchCreditTransferData with
    fingerprint : Optional Text
        -- ^ Bank account fingerprint associated with the
        --   transfer.
    customerData : Optional Text
        -- ^ Customer data associated with the transfer.
    routingNumber : Optional Text
        -- ^ Routing number associated with the transfer.
    last4 : Optional Text
        -- ^ Last 4 digits of the account number associated with
        --   the transfer.
      deriving (Eq, Ord, Show)

data DeletedPaymentSource = DeletedPaymentSource with
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    deleted : Bool
        -- ^ Always true for a deleted object
      deriving (Eq, Ord, Show)

data ChargeRefundsData = ChargeRefundsData with
      deriving (Eq, Ord, Show)

data ChargeRefunds = ChargeRefunds with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Refund]
        -- ^ Details about each object.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data ChargeMetadata = ChargeMetadata with
      deriving (Eq, Ord, Show)

data Charge = Charge with
    failureMessage : Optional Text
        -- ^ Message to user further explaining reason for charge
        --   failure if available.
    status : Text
        -- ^ The status of the payment is either `succeeded`,
        --   `pending`, or `failed`.
    amount : Int
        -- ^ Amount intended to be collected by this
        --   PaymentIntent. A positive integer representing how
        --   much to charge in the [smallest currency
        --   unit](https://stripe.com/docs/currencies#zero-decimal)
        --   (e.g., 100 cents to charge $1.00 or 100 to charge
        --   ¥100, a zero-decimal currency). The minimum amount is
        --   $0.50 US or [equivalent in charge
        --   currency](https://stripe.com/docs/currencies#minimum-and-maximum-charge-amounts).
        --   The amount value supports up to eight digits (e.g., a
        --   value of 99999999 for a USD charge of $999,999.99).
    receiptEmail : Optional Text
        -- ^ This is the email address that the receipt for this
        --   charge was sent to.
    refunded : Bool
        -- ^ Whether the charge has been fully refunded. If the
        --   charge is only partially refunded, this attribute
        --   will still be false.
    paymentMethod : Optional Text
        -- ^ ID of the payment method used in this charge.
    application : Optional Text
        -- ^ ID of the Connect application that created the
        --   charge.
    balanceTransaction : Optional Text
        -- ^ ID of the balance transaction that describes the
        --   impact of this charge on your account balance (not
        --   including refunds or disputes).
    refunds : ChargeRefunds
        -- ^ A list of refunds that have been applied to the
        --   charge.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    receiptUrl : Optional Text
        -- ^ This is the URL to view the receipt for this charge.
        --   The receipt is kept up-to-date to the latest state of
        --   the charge, including any refunds. If the charge is
        --   for an Invoice, the receipt will be stylized as an
        --   Invoice receipt.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    receiptNumber : Optional Text
        -- ^ This is the transaction number that appears on email
        --   receipts sent for this charge. This attribute will be
        --   `null` until a receipt has been sent.
    invoice : Optional Text
        -- ^ ID of the invoice this charge is for if one exists.
    sourceTransfer : Optional Text
        -- ^ The transfer ID which created this charge. Only
        --   present if the charge came from another Stripe
        --   account. [See the Connect
        --   documentation](https://stripe.com/docs/connect/destination-charges)
        --   for details.
    paymentIntent : Optional Text
        -- ^ ID of the PaymentIntent associated with this charge,
        --   if one exists.
    transferData : Optional ChargeTransferData
    amountRefunded : Optional Int
        -- ^ Amount in %s refunded (can be less than the amount
        --   attribute on the charge if a partial refund was
        --   issued).
    customer : Optional Text
        -- ^ ID of the customer this charge is for if one exists.
    onBehalfOf : Optional Text
        -- ^ The account (if any) the charge was made on behalf of
        --   without triggering an automatic transfer. See the
        --   [Connect
        --   documentation](https://stripe.com/docs/connect/charges-transfers)
        --   for details.
    paid : Bool
        -- ^ `true` if the charge succeeded, or was successfully
        --   authorized for later capture.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    paymentMethodDetails : Optional PaymentMethodDetails
    outcome : Optional ChargeOutcome
    review : Optional Text
        -- ^ ID of the review associated with this charge if one
        --   exists.
    shipping : Optional Shipping
    failureCode : Optional Text
        -- ^ Error code explaining reason for charge failure if
        --   available (see [the errors
        --   section](https://stripe.com/docs/api#errors) for a
        --   list of codes).
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : ChargeMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    applicationFeeAmount : Optional Int
        -- ^ The amount of the application fee (if any) for the
        --   charge. [See the Connect
        --   documentation](https://stripe.com/docs/connect/direct-charges#collecting-fees)
        --   for details.
    captured : Bool
        -- ^ If the charge was created without capturing, this
        --   Boolean represents whether it is still uncaptured or
        --   has since been captured.
    applicationFee : Optional Text
        -- ^ The application fee (if any) for the charge. [See the
        --   Connect
        --   documentation](https://stripe.com/docs/connect/direct-charges#collecting-fees)
        --   for details.
    transferGroup : Optional Text
        -- ^ A string that identifies this transaction as part of
        --   a group. See the [Connect
        --   documentation](https://stripe.com/docs/connect/charges-transfers#grouping-transactions)
        --   for details.
    transfer : Optional Text
        -- ^ ID of the transfer to the `destination` account (only
        --   applicable if the charge was created using the
        --   `destination` parameter).
    billingDetails : Optional BillingDetails
    disputed : Bool
        -- ^ Whether the charge has been disputed.
    order : Optional Text
        -- ^ ID of the order this charge is for if one exists.
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users.
    statementDescriptor : Optional Text
        -- ^ For card charges, use `statement_descriptor_suffix`
        --   instead. Otherwise, you can use this value as the
        --   complete description of a charge on your customers’
        --   statements. Must contain at least one letter, maximum
        --   22 characters.
    fraudDetails : Optional ChargeFraudDetails
    statementDescriptorSuffix : Optional Text
        -- ^ Provides information about the charge that customers
        --   see on their statements. Concatenated with the prefix
        --   (shortened descriptor) or statement descriptor that’s
        --   set on the account to form the complete statement
        --   descriptor. Maximum 22 characters for the
        --   concatenated descriptor.
      deriving (Eq, Ord, Show)

data DeliveryEstimate = DeliveryEstimate with
    date : Optional Text
        -- ^ If `type` is `"exact"`, `date` will be the expected
        --   delivery date in the format YYYY-MM-DD.
    type_ : Optional Text
        -- ^ The type of estimate. Must be either `"range"` or
        --   `"exact"`.
    latest : Optional Text
        -- ^ If `type` is `"range"`, `latest` will be the latest
        --   delivery date in the format YYYY-MM-DD.
    earliest : Optional Text
        -- ^ If `type` is `"range"`, `earliest` will be be the
        --   earliest delivery date in the format YYYY-MM-DD.
      deriving (Eq, Ord, Show)

data DeletedWebhookEndpoint = DeletedWebhookEndpoint with
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    deleted : Bool
        -- ^ Always true for a deleted object
      deriving (Eq, Ord, Show)

data PaymentIntentNextActionRedirectToUrl = PaymentIntentNextActionRedirectToUrl with
    url : Optional Text
        -- ^ The URL you must redirect your customer to in order
        --   to authenticate the payment.
    returnUrl : Optional Text
        -- ^ If the customer does not exit their browser while
        --   authenticating, they will be redirected to this
        --   specified URL after completion.
      deriving (Eq, Ord, Show)

data DeletedTerminalLocation = DeletedTerminalLocation with
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    deleted : Bool
        -- ^ Always true for a deleted object
      deriving (Eq, Ord, Show)

data SourceRedirectFlow = SourceRedirectFlow with
    status : Text
        -- ^ The status of the redirect, either `pending` (ready
        --   to be used by your customer to authenticate the
        --   transaction), `succeeded` (succesful authentication,
        --   cannot be reused) or `not_required` (redirect should
        --   not be used) or `failed` (failed authentication,
        --   cannot be reused).
    url : Text
        -- ^ The URL provided to you to redirect a customer to as
        --   part of a `redirect` authentication flow.
    failureReason : Optional Text
        -- ^ The failure reason for the redirect, either
        --   `user_abort` (the customer aborted or dropped out of
        --   the redirect flow), `declined` (the authentication
        --   failed or the transaction was declined), or
        --   `processing_error` (the redirect failed due to a
        --   technical error). Present only if the redirect status
        --   is `failed`.
    returnUrl : Optional Text
        -- ^ The URL you provide to redirect the customer to after
        --   they authenticated their payment.
      deriving (Eq, Ord, Show)

data SourceTransactionChfCreditTransferData = SourceTransactionChfCreditTransferData with
    senderAddressLine1 : Optional Text
        -- ^ Sender's line 1 address.
    reference : Optional Text
        -- ^ Reference associated with the transfer.
    senderAddressCountry : Optional Text
        -- ^ Sender's country address.
    senderIban : Optional Text
        -- ^ Sender's bank account IBAN.
    senderName : Optional Text
        -- ^ Sender's name.
      deriving (Eq, Ord, Show)

data PaymentMethodDetailsBancontact = PaymentMethodDetailsBancontact with
    bankCode : Optional Text
        -- ^ Bank code of bank associated with the bank account.
    bic : Optional Text
        -- ^ Bank Identifier Code of the bank associated with the
        --   bank account.
    verifiedName : Optional Text
        -- ^ Owner's verified full name. Values are verified or
        --   provided by Bancontact directly (if supported) at the
        --   time of authorization or settlement. They cannot be
        --   set or mutated.
    ibanLast4 : Optional Text
        -- ^ Last four characters of the IBAN.
    preferredLanguage : Optional Text
        -- ^ Preferred language of the Bancontact authorization
        --   page that the customer is redirected to. Can be one
        --   of `en`, `de`, `fr`, or `nl`
    bankName : Optional Text
        -- ^ Name of the bank associated with the bank account.
      deriving (Eq, Ord, Show)

data SourceMetadata = SourceMetadata with
      deriving (Eq, Ord, Show)

data Source = Source with
    card : Optional SourceTypeCard
    status : Text
        -- ^ The status of the source, one of `canceled`,
        --   `chargeable`, `consumed`, `failed`, or `pending`.
        --   Only `chargeable` sources can be used to create a
        --   charge.
    amount : Optional Int
        -- ^ A positive integer in the smallest currency unit
        --   (that is, 100 cents for $1.00, or 1 for ¥1, Japanese
        --   Yen being a zero-decimal currency) representing the
        --   total amount associated with the source. This is the
        --   amount for which the source will be chargeable once
        --   ready. Required for `single_use` sources.
    flow : Text
        -- ^ The authentication `flow` of the source. `flow` is
        --   one of `redirect`, `receiver`, `code_verification`,
        --   `none`.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    ideal : Optional SourceTypeIdeal
    bancontact : Optional SourceTypeBancontact
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    clientSecret : Optional Text
        -- ^ The client secret of the source. Used for client-side
        --   retrieval using a publishable key.
    sofort : Optional SourceTypeSofort
    klarna : Optional SourceTypeKlarna
    codeVerification : Optional SourceCodeVerificationFlow
    achDebit : Optional SourceTypeAchDebit
    owner : Optional SourceOwner
    receiver : Optional SourceReceiverFlow
    customer : Optional Text
        -- ^ The ID of the customer to which this source is
        --   attached. This will not be present when the source
        --   has not been attached to a customer.
    wechat : Optional SourceTypeWechat
    sourceOrder : Optional SourceOrder
    alipay : Optional SourceTypeAlipay
    achCreditTransfer : Optional SourceTypeAchCreditTransfer
    currency : Optional Text
        -- ^ Three-letter [ISO code for the
        --   currency](https://stripe.com/docs/currencies)
        --   associated with the source. This is the currency for
        --   which the source will be chargeable once ready.
        --   Required for `single_use` sources.
    redirect : Optional SourceRedirectFlow
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : Optional SourceMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    threeDSecure : Optional SourceTypeThreeDSecure
    usage : Optional Text
        -- ^ Either `reusable` or `single_use`. Whether this
        --   source should be reusable or not. Some source types
        --   may or may not be reusable by construction, while
        --   others may leave the option at creation. If an
        --   incompatible value is passed, an error will be
        --   returned.
    cardPresent : Optional SourceTypeCardPresent
    multibanco : Optional SourceTypeMultibanco
    type_ : Optional Text
        -- ^ The `type` of the source. The `type` is a payment
        --   method, one of `ach_credit_transfer`, `ach_debit`,
        --   `alipay`, `bancontact`, `card`, `card_present`,
        --   `eps`, `giropay`, `ideal`, `multibanco`, `klarna`,
        --   `p24`, `sepa_debit`, `sofort`, `three_d_secure`, or
        --   `wechat`. An additional hash is included on the
        --   source with a name matching this value. It contains
        --   additional information specific to the [payment
        --   method](https://stripe.com/docs/sources) used.
    giropay : Optional SourceTypeGiropay
    eps : Optional SourceTypeEps
    p24 : Optional SourceTypeP24
    sepaDebit : Optional SourceTypeSepaDebit
    statementDescriptor : Optional Text
        -- ^ Extra information about a source. This will appear on
        --   your customer's statement every time you charge the
        --   source.
      deriving (Eq, Ord, Show)

data AccountPaymentsSettings = AccountPaymentsSettings with
    statementDescriptorKanji : Optional Text
        -- ^ The Kanji variation of the default text that appears
        --   on credit card statements when a charge is made
        --   (Japan only)
    statementDescriptorKana : Optional Text
        -- ^ The Kana variation of the default text that appears
        --   on credit card statements when a charge is made
        --   (Japan only)
    statementDescriptor : Optional Text
        -- ^ The default text that appears on credit card
        --   statements when a charge is made. This field prefixes
        --   any dynamic `statement_descriptor` specified on the
        --   charge.
      deriving (Eq, Ord, Show)

data SkuAttributes = SkuAttributes with
      deriving (Eq, Ord, Show)

data SkuMetadata = SkuMetadata with
      deriving (Eq, Ord, Show)

data Sku = Sku with
    image : Optional Text
        -- ^ The URL of an image for this SKU, meant to be
        --   displayable to the customer.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    packageDimensions : Optional PackageDimensions
    active : Bool
        -- ^ Whether the SKU is available for purchase.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    attributes : SkuAttributes
        -- ^ A dictionary of attributes and values for the
        --   attributes defined by the product. If, for example, a
        --   product's attributes are `["size", "gender"]`, a
        --   valid SKU has the following dictionary of attributes:
        --   `{"size": "Medium", "gender": "Unisex"}`.
    inventory : Inventory
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : SkuMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    price : Int
        -- ^ The cost of the item as a positive integer in the
        --   smallest currency unit (that is, 100 cents to charge
        --   $1.00, or 100 to charge ¥100, Japanese Yen being a
        --   zero-decimal currency).
    updated : Int
        -- ^ Time at which the object was last updated. Measured
        --   in seconds since the Unix epoch.
    product : Text
        -- ^ The ID of the product this SKU is associated with.
        --   The product must be currently active.
      deriving (Eq, Ord, Show)

data DeletedTaxId = DeletedTaxId with
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    deleted : Bool
        -- ^ Always true for a deleted object
      deriving (Eq, Ord, Show)

data PaymentMethodDetailsIdeal = PaymentMethodDetailsIdeal with
    bic : Optional Text
        -- ^ The Bank Identifier Code of the customer's bank.
    verifiedName : Optional Text
        -- ^ Owner's verified full name. Values are verified or
        --   provided by iDEAL directly (if supported) at the time
        --   of authorization or settlement. They cannot be set or
        --   mutated.
    ibanLast4 : Optional Text
        -- ^ Last four characters of the IBAN.
    bank : Optional Text
        -- ^ The customer's bank. Can be one of `abn_amro`,
        --   `asn_bank`, `bunq`, `handelsbanken`, `ing`, `knab`,
        --   `moneyou`, `rabobank`, `regiobank`, `sns_bank`,
        --   `triodos_bank`, or `van_lanschot`.
      deriving (Eq, Ord, Show)

data ThreeDSecure = ThreeDSecure with
    card : Card
    status : Text
        -- ^ Possible values are `redirect_pending`, `succeeded`,
        --   or `failed`. When the cardholder can be
        --   authenticated, the object starts with status
        --   `redirect_pending`. When liability will be shifted to
        --   the cardholder's bank (either because the cardholder
        --   was successfully authenticated, or because the bank
        --   has not implemented 3D Secure, the object wlil be in
        --   status `succeeded`. `failed` indicates that
        --   authentication was attempted unsuccessfully.
    amount : Int
        -- ^ Amount of the charge that you will create when
        --   authentication completes.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    redirectUrl : Optional Text
        -- ^ If present, this is the URL that you should send the
        --   cardholder to for authentication. If you are going to
        --   use Stripe.js to display the authentication page in
        --   an iframe, you should use the value "_callback".
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    authenticated : Bool
        -- ^ True if the cardholder went through the
        --   authentication flow and their bank indicated that
        --   authentication succeeded.
      deriving (Eq, Ord, Show)

data LightAccountLogout = LightAccountLogout with
      deriving (Eq, Ord, Show)

data IssuingCardholderIdDocument = IssuingCardholderIdDocument with
    front : Optional Text
        -- ^ The front of a document returned by a [file
        --   upload](https://stripe.com/docs/api#create_file) with
        --   a `purpose` value of `identity_document`.
    back : Optional Text
        -- ^ The back of a document returned by a [file
        --   upload](https://stripe.com/docs/api#create_file) with
        --   a `purpose` value of `identity_document`.
      deriving (Eq, Ord, Show)

data PaymentIntentPaymentMethodOptionsCard = PaymentIntentPaymentMethodOptionsCard with
    installments : Optional PaymentMethodOptionsCardInstallments
    requestThreeDSecure : Optional Text
        -- ^ We strongly recommend that you rely on our SCA Engine
        --   to automatically prompt your customers for
        --   authentication based on risk level and [other
        --   requirements](https://stripe.com/docs/strong-customer-authentication).
        --   However, if you wish to request 3D Secure based on
        --   logic from your own fraud engine, provide this
        --   option. Permitted values include: `automatic` or
        --   `any`. If not provided, defaults to `automatic`. Read
        --   our guide on [manually requesting 3D
        --   Secure](https://stripe.com/docs/payments/3d-secure#manual-three-ds)
        --   for more information on how this configuration
        --   interacts with Radar and our SCA Engine.
      deriving (Eq, Ord, Show)

data PaymentIntentNextActionUseStripeSdk = PaymentIntentNextActionUseStripeSdk with
      deriving (Eq, Ord, Show)

data PaymentIntentNextAction = PaymentIntentNextAction with
    redirectToUrl : Optional PaymentIntentNextActionRedirectToUrl
    type_ : Optional Text
        -- ^ Type of the next action to perform, one of
        --   `redirect_to_url` or `use_stripe_sdk`.
    useStripeSdk : Optional PaymentIntentNextActionUseStripeSdk
        -- ^ When confirming a PaymentIntent with Stripe.js,
        --   Stripe.js depends on the contents of this dictionary
        --   to invoke authentication flows. The shape of the
        --   contents is subject to change and is only intended to
        --   be used by Stripe.js.
      deriving (Eq, Ord, Show)

data InvoiceItemThresholdReason = InvoiceItemThresholdReason with
    lineItemIds : [Text]
        -- ^ The IDs of the line items that triggered the
        --   threshold invoice.
    usageGte : Optional Int
        -- ^ The quantity threshold boundary that applied to the
        --   given line item.
      deriving (Eq, Ord, Show)

data IssuingAuthorizationViolatedAuthControl = IssuingAuthorizationViolatedAuthControl with
    name : Text
        -- ^ Name of the authorization control. One of
        --   `allowed_categories`, `blocked_categories`,
        --   `spending_limits`, `max_approvals`, or `max_amount`.
    entity : Text
        -- ^ Entity which the authorization control acts on. One
        --   of `card`, `cardholder`, or `account`.
      deriving (Eq, Ord, Show)

data NotificationEventRequest = NotificationEventRequest with
    idempotencyKey : Optional Text
        -- ^ The idempotency key transmitted during the request,
        --   if any. *Note: This property is populated only for
        --   events on or after May 23, 2017*.
    id : Optional Text
        -- ^ ID of the API request that caused the event. If null,
        --   the event was automatic (e.g., Stripe's automatic
        --   subscription handling). Request logs are available in
        --   the [dashboard](https://dashboard.stripe.com/logs),
        --   but currently not in the API.
      deriving (Eq, Ord, Show)

data ReportingReportRun = ReportingReportRun with
    status : Text
        -- ^ Status of this report run. This will be `pending`
        --   when the run is initially created. When the run
        --   finishes, this will be set to `succeeded` and the
        --   `result` field will be populated. Rarely, we may
        --   encounter an error, at which point this will be set
        --   to `failed` and the `error` field will be populated.
    livemode : Bool
        -- ^ Always `true`: reports can only be run on live-mode
        --   data.
    error : Optional Text
        -- ^ If something should go wrong during the run, a
        --   message about the failure (populated when
        --   `status=failed`).
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    result : Optional File
    parameters : FinancialReportingFinanceReportRunRunParameters
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    reportType : Optional Text
        -- ^ The ID of the [report
        --   type](https://stripe.com/docs/reporting/statements/api#report-types)
        --   to run, such as `"balance.summary.1"`.
    succeededAt : Optional Int
        -- ^ Timestamp at which this run successfully finished
        --   (populated when `status=succeeded`). Measured in
        --   seconds since the Unix epoch.
      deriving (Eq, Ord, Show)

data Capability = Capability with
    requirements : Optional AccountCapabilityRequirements
    status : Text
        -- ^ The status of the capability. Can be `active`,
        --   `inactive`, `pending`, or `unrequested`.
    requested : Bool
        -- ^ Whether the capability has been requested.
    account : Text
        -- ^ The account for which the capability enables
        --   functionality.
    requestedAt : Optional Int
        -- ^ Time at which the capability was requested. Measured
        --   in seconds since the Unix epoch.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ The identifier for the capability.
      deriving (Eq, Ord, Show)

data PaymentMethodDetailsCardWalletAmexExpressCheckout = PaymentMethodDetailsCardWalletAmexExpressCheckout with
      deriving (Eq, Ord, Show)

data IssuingCardholderAuthorizationControlsSpendingLimits = IssuingCardholderAuthorizationControlsSpendingLimits with
      deriving (Eq, Ord, Show)

data IssuingCardholderAuthorizationControls = IssuingCardholderAuthorizationControls with
    spendingLimits : [IssuingCardholderSpendingLimit]
        -- ^ Limit the spending with rules based on time intervals
        --   and categories.
    spendingLimitsCurrency : Optional Text
        -- ^ Currency for the amounts within spending_limits.
    allowedCategories : [Text]
        -- ^ Array of strings containing
        --   [categories](https://stripe.com/docs/api#issuing_authorization_object-merchant_data-category)
        --   of authorizations permitted on this cardholder's
        --   cards.
    blockedCategories : [Text]
        -- ^ Array of strings containing
        --   [categories](https://stripe.com/docs/api#issuing_authorization_object-merchant_data-category)
        --   of authorizations to always decline on this
        --   cardholder's cards.
      deriving (Eq, Ord, Show)

data IssuingCardAuthorizationControlsSpendingLimits = IssuingCardAuthorizationControlsSpendingLimits with
      deriving (Eq, Ord, Show)

data IssuingCardAuthorizationControls = IssuingCardAuthorizationControls with
    spendingLimits : [IssuingCardSpendingLimit]
        -- ^ Limit the spending with rules based on time intervals
        --   and categories.
    maxApprovals : Optional Int
        -- ^ Maximum count of approved authorizations on this
        --   card. Counts all authorizations retroactively.
    currency : Optional Text
        -- ^ The currency of the card. See
        --   [max_amount](https://stripe.com/docs/api#issuing_card_object-authorization_controls-max_amount)
    spendingLimitsCurrency : Optional Text
        -- ^ Currency for the amounts within spending_limits.
        --   Locked to the currency of the card.
    allowedCategories : [Text]
        -- ^ Array of strings containing
        --   [categories](https://stripe.com/docs/api#issuing_authorization_object-merchant_data-category)
        --   of authorizations permitted on this card.
    blockedCategories : [Text]
        -- ^ Array of strings containing
        --   [categories](https://stripe.com/docs/api#issuing_authorization_object-merchant_data-category)
        --   of authorizations to always decline on this card.
      deriving (Eq, Ord, Show)

data TerminalConnectionToken = TerminalConnectionToken with
    location : Optional Text
        -- ^ The id of the location that this connection token is
        --   scoped to.
    secret : Text
        -- ^ Your application should pass this token to the Stripe
        --   Terminal SDK.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
      deriving (Eq, Ord, Show)

data IssuingDisputeFraudulentEvidence = IssuingDisputeFraudulentEvidence with
    disputeExplanation : Optional Text
        -- ^ Brief freeform text explaining why you are disputing
        --   this transaction.
    uncategorizedFile : Optional Text
        -- ^ (ID of a [file
        --   upload](https://stripe.com/docs/guides/file-upload))
        --   Additional file evidence supporting your dispute.
      deriving (Eq, Ord, Show)

data ApplePayDomain = ApplePayDomain with
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    domainName : Optional Text
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
      deriving (Eq, Ord, Show)

data SourceTypeBancontact = SourceTypeBancontact with
    bankCode : Optional Text
    bic : Optional Text
    ibanLast4 : Optional Text
    preferredLanguage : Optional Text
    bankName : Optional Text
    statementDescriptor : Optional Text
      deriving (Eq, Ord, Show)

data InvoiceTaxAmount = InvoiceTaxAmount with
    inclusive : Bool
        -- ^ Whether this tax amount is inclusive or exclusive.
    amount : Int
        -- ^ The amount, in %s, of the tax.
    taxRate : Optional Text
        -- ^ The tax rate that was applied to get this tax amount.
      deriving (Eq, Ord, Show)

data ReserveTransaction = ReserveTransaction with
    amount : Int
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users.
      deriving (Eq, Ord, Show)

data PaymentMethodFpx = PaymentMethodFpx with
    bank : Text
        -- ^ The customer's bank, if provided. Can be one of
        --   `affin_bank`, `alliance_bank`, `ambank`,
        --   `bank_islam`, `bank_muamalat`, `bank_rakyat`, `bsn`,
        --   `cimb`, `hong_leong_bank`, `hsbc`, `kfh`,
        --   `maybank2u`, `ocbc`, `public_bank`, `rhb`,
        --   `standard_chartered`, `uob`, `deutsche_bank`,
        --   `maybank2e`, `pb_enterprise`, or `uob_regional`.
      deriving (Eq, Ord, Show)

data ApplicationFeeRefundsData = ApplicationFeeRefundsData with
      deriving (Eq, Ord, Show)

data ApplicationFeeRefunds = ApplicationFeeRefunds with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [FeeRefund]
        -- ^ Details about each object.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data ApplicationFee = ApplicationFee with
    amount : Int
        -- ^ Amount earned, in %s.
    refunded : Bool
        -- ^ Whether the fee has been fully refunded. If the fee
        --   is only partially refunded, this attribute will still
        --   be false.
    application : Text
        -- ^ ID of the Connect application that earned the fee.
    originatingTransaction : Optional Text
        -- ^ ID of the corresponding charge on the platform
        --   account, if this fee was the result of a charge using
        --   the `destination` parameter.
    balanceTransaction : Optional Text
        -- ^ Balance transaction that describes the impact of this
        --   collected application fee on your account balance
        --   (not including refunds).
    refunds : ApplicationFeeRefunds
        -- ^ A list of refunds that have been applied to the fee.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    account : Text
        -- ^ ID of the Stripe account this fee was taken from.
    amountRefunded : Optional Int
        -- ^ Amount in %s refunded (can be less than the amount
        --   attribute on the fee if a partial refund was issued)
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    charge : Text
        -- ^ ID of the charge that the application fee was taken
        --   from.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
      deriving (Eq, Ord, Show)

data ProductMetadata = ProductMetadata with
      deriving (Eq, Ord, Show)

data Product = Product with
    images : [Text]
        -- ^ A list of up to 8 URLs of images for this product,
        --   meant to be displayable to the customer. Only
        --   applicable to products of `type=good`.
    unitLabel : Optional Text
        -- ^ A label that represents units of this product in
        --   Stripe and on customers’ receipts and invoices. When
        --   set, this will be included in associated invoice line
        --   item descriptions.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    shippable : Optional Bool
        -- ^ Whether this product is a shipped good. Only
        --   applicable to products of `type=good`.
    url : Optional Text
        -- ^ A URL of a publicly-accessible webpage for this
        --   product. Only applicable to products of `type=good`.
    caption : Optional Text
        -- ^ A short one-line description of the product, meant to
        --   be displayable to the customer. Only applicable to
        --   products of `type=good`.
    packageDimensions : Optional PackageDimensions
    active : Bool
        -- ^ Whether the product is currently available for
        --   purchase.
    name : Text
        -- ^ The product's name, meant to be displayable to the
        --   customer. Whenever this product is sold via a
        --   subscription, name will show up on associated invoice
        --   line item descriptions.
    attributes : [Text]
        -- ^ A list of up to 5 attributes that each SKU can
        --   provide values for (e.g., `["color", "size"]`).
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : ProductMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    updated : Int
        -- ^ Time at which the object was last updated. Measured
        --   in seconds since the Unix epoch.
    type_ : Optional Text
        -- ^ The type of the product. The product is either of
        --   type `good`, which is eligible for use with Orders
        --   and SKUs, or `service`, which is eligible for use
        --   with Subscriptions and Plans.
    deactivateOn : [Text]
        -- ^ An array of connect application identifiers that
        --   cannot purchase this product. Only applicable to
        --   products of `type=good`.
    description : Optional Text
        -- ^ The product's description, meant to be displayable to
        --   the customer. Use this field to optionally store a
        --   long form explanation of the product being sold for
        --   your own rendering purposes.
    statementDescriptor : Optional Text
        -- ^ Extra information about a product which will appear
        --   on your customer's credit card statement. In the case
        --   that multiple products are billed at once, the first
        --   statement descriptor will be used.
      deriving (Eq, Ord, Show)

data IssuingDisputeProductNotReceivedEvidence = IssuingDisputeProductNotReceivedEvidence with
    disputeExplanation : Optional Text
        -- ^ Brief freeform text explaining why you are disputing
        --   this transaction.
    uncategorizedFile : Optional Text
        -- ^ (ID of a [file
        --   upload](https://stripe.com/docs/guides/file-upload))
        --   Additional file evidence supporting your dispute.
      deriving (Eq, Ord, Show)

data CreditNoteTaxAmount = CreditNoteTaxAmount with
    inclusive : Bool
        -- ^ Whether this tax amount is inclusive or exclusive.
    amount : Int
        -- ^ The amount, in %s, of the tax.
    taxRate : Optional Text
        -- ^ The tax rate that was applied to get this tax amount.
      deriving (Eq, Ord, Show)

data FinancialReportingFinanceReportRunRunParameters = FinancialReportingFinanceReportRunRunParameters with
    intervalEnd : Optional Int
        -- ^ Ending timestamp of data to be included in the report
        --   run (exclusive).
    connectedAccount : Optional Text
        -- ^ Connected account ID by which to filter the report
        --   run.
    reportingCategory : Optional Text
        -- ^ Category of balance transactions to be included in
        --   the report run.
    currency : Optional Text
        -- ^ Currency of objects to be included in the report run.
    columns : [Text]
        -- ^ The set of output columns requested for inclusion in
        --   the report run.
    intervalStart : Optional Int
        -- ^ Starting timestamp of data to be included in the
        --   report run.
    timezone : Optional Text
        -- ^ Defaults to `Etc/UTC`. The output timezone for all
        --   timestamps in the report. A list of possible time
        --   zone values is maintained at the [IANA Time Zone
        --   Database](http://www.iana.org/time-zones). Has no
        --   effect on `interval_start` or `interval_end`.
    payout : Optional Text
        -- ^ Payout ID by which to filter the report run.
      deriving (Eq, Ord, Show)

data DeletedInvoice = DeletedInvoice with
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    deleted : Bool
        -- ^ Always true for a deleted object
      deriving (Eq, Ord, Show)

data PaymentIntentPaymentMethodOptions = PaymentIntentPaymentMethodOptions with
    card : Optional PaymentIntentPaymentMethodOptionsCard
      deriving (Eq, Ord, Show)

data PaymentMethodDetailsSofort = PaymentMethodDetailsSofort with
    bankCode : Optional Text
        -- ^ Bank code of bank associated with the bank account.
    country : Optional Text
        -- ^ Two-letter ISO code representing the country the bank
        --   account is located in.
    bic : Optional Text
        -- ^ Bank Identifier Code of the bank associated with the
        --   bank account.
    verifiedName : Optional Text
        -- ^ Owner's verified full name. Values are verified or
        --   provided by SOFORT directly (if supported) at the
        --   time of authorization or settlement. They cannot be
        --   set or mutated.
    ibanLast4 : Optional Text
        -- ^ Last four characters of the IBAN.
    bankName : Optional Text
        -- ^ Name of the bank associated with the bank account.
      deriving (Eq, Ord, Show)

data RecipientCardsData = RecipientCardsData with
      deriving (Eq, Ord, Show)

data RecipientCards = RecipientCards with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Card]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data RecipientMetadata = RecipientMetadata with
      deriving (Eq, Ord, Show)

data Recipient = Recipient with
    email : Optional Text
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    defaultCard : Optional Text
        -- ^ The default card to use for creating transfers to
        --   this recipient.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    name : Optional Text
        -- ^ Full, legal name of the recipient.
    cards : Optional RecipientCards
    activeAccount : Optional BankAccount
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : RecipientMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    type_ : Optional Text
        -- ^ Type of the recipient, one of `individual` or
        --   `corporation`.
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users.
    migratedTo : Optional Text
        -- ^ The ID of the [Custom
        --   account](https://stripe.com/docs/connect/custom-accounts)
        --   this recipient was migrated to. If set, the recipient
        --   can no longer be updated, nor can transfers be made
        --   to it: use the Custom account instead.
    rolledBackFrom : Optional Text
      deriving (Eq, Ord, Show)

data PaymentMethodDetailsKlarna = PaymentMethodDetailsKlarna with
      deriving (Eq, Ord, Show)

data TransferMetadata = TransferMetadata with
      deriving (Eq, Ord, Show)

data TransferReversalsData = TransferReversalsData with
      deriving (Eq, Ord, Show)

data TransferReversals = TransferReversals with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [TransferReversal]
        -- ^ Details about each object.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data Transfer = Transfer with
    destination : Optional Text
        -- ^ ID of the Stripe account the transfer was sent to.
    reversed : Bool
        -- ^ Whether the transfer has been fully reversed. If the
        --   transfer is only partially reversed, this attribute
        --   will still be false.
    amount : Int
        -- ^ Amount in %s to be transferred.
    sourceType : Optional Text
        -- ^ The source balance this transfer came from. One of
        --   `card`, `fpx`, or `bank_account`.
    balanceTransaction : Optional Text
        -- ^ Balance transaction that describes the impact of this
        --   transfer on your account balance.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time that this record of the transfer was first
        --   created.
    destinationPayment : Optional Text
        -- ^ If the destination is a Stripe account, this will be
        --   the ID of the payment that the destination account
        --   received for the transfer.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    amountReversed : Optional Int
        -- ^ Amount in %s reversed (can be less than the amount
        --   attribute on the transfer if a partial reversal was
        --   issued).
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : TransferMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    transferGroup : Optional Text
        -- ^ A string that identifies this transaction as part of
        --   a group. See the [Connect
        --   documentation](https://stripe.com/docs/connect/charges-transfers#grouping-transactions)
        --   for details.
    reversals : TransferReversals
        -- ^ A list of reversals that have been applied to the
        --   transfer.
    sourceTransaction : Optional Text
        -- ^ ID of the charge or payment that was used to fund the
        --   transfer. If null, the transfer was funded from the
        --   available balance.
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users.
      deriving (Eq, Ord, Show)

data LegalEntityPersonVerification = LegalEntityPersonVerification with
    status : Text
        -- ^ The state of verification for the person. Possible
        --   values are `unverified`, `pending`, or `verified`.
    document : Optional LegalEntityPersonVerificationDocument
    details : Optional Text
        -- ^ A user-displayable string describing the verification
        --   state for the person. For example, this may say
        --   "Provided identity information could not be
        --   verified".
    detailsCode : Optional Text
        -- ^ One of `document_address_mismatch`,
        --   `document_dob_mismatch`, `document_duplicate_type`,
        --   `document_id_number_mismatch`,
        --   `document_name_mismatch`,
        --   `document_nationality_mismatch`,
        --   `failed_keyed_identity`, or `failed_other`. A
        --   machine-readable code specifying the verification
        --   state for the person.
    additionalDocument : Optional LegalEntityPersonVerificationDocument
      deriving (Eq, Ord, Show)

data OrderReturnItems = OrderReturnItems with
      deriving (Eq, Ord, Show)

data OrderReturn = OrderReturn with
    amount : Int
        -- ^ A positive integer in the smallest currency unit
        --   (that is, 100 cents for $1.00, or 1 for ¥1, Japanese
        --   Yen being a zero-decimal currency) representing the
        --   total amount for the returned line item.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    items : [OrderItem]
        -- ^ The items included in this order return.
    refund : Optional Text
        -- ^ The ID of the refund issued for this return.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    order : Optional Text
        -- ^ The order that this return includes items from.
      deriving (Eq, Ord, Show)

data PaymentMethodCardWalletMasterpass = PaymentMethodCardWalletMasterpass with
    email : Optional Text
        -- ^ Owner's verified email. Values are verified or
        --   provided by the wallet directly (if supported) at the
        --   time of authorization or settlement. They cannot be
        --   set or mutated.
    billingAddress : Optional Address
    shippingAddress : Optional Address
    name : Optional Text
        -- ^ Owner's verified full name. Values are verified or
        --   provided by the wallet directly (if supported) at the
        --   time of authorization or settlement. They cannot be
        --   set or mutated.
      deriving (Eq, Ord, Show)

data CheckoutSessionCustomDisplayItemDescription = CheckoutSessionCustomDisplayItemDescription with
    images : [Text]
        -- ^ The images of the line item.
    name : Text
        -- ^ The name of the line item.
    description : Optional Text
        -- ^ The description of the line item.
      deriving (Eq, Ord, Show)

data PaymentMethodSepaDebit = PaymentMethodSepaDebit with
    bankCode : Optional Text
        -- ^ Bank code of bank associated with the bank account.
    country : Optional Text
        -- ^ Two-letter ISO code representing the country the bank
        --   account is located in.
    fingerprint : Optional Text
        -- ^ Uniquely identifies this particular bank account. You
        --   can use this attribute to check whether two bank
        --   accounts are the same.
    branchCode : Optional Text
        -- ^ Branch code of bank associated with the bank account.
    last4 : Optional Text
        -- ^ Last four characters of the IBAN.
      deriving (Eq, Ord, Show)

data SourceTypeSepaDebit = SourceTypeSepaDebit with
    bankCode : Optional Text
    country : Optional Text
    fingerprint : Optional Text
    branchCode : Optional Text
    mandateUrl : Optional Text
    mandateReference : Optional Text
    last4 : Optional Text
      deriving (Eq, Ord, Show)

data DeletedAccount = DeletedAccount with
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    deleted : Bool
        -- ^ Always true for a deleted object
      deriving (Eq, Ord, Show)

data SubscriptionItemsData = SubscriptionItemsData with
      deriving (Eq, Ord, Show)

data SubscriptionItems = SubscriptionItems with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [SubscriptionItem]
        -- ^ Details about each object.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data SubscriptionDefaultTaxRates = SubscriptionDefaultTaxRates with
      deriving (Eq, Ord, Show)

data SubscriptionMetadata = SubscriptionMetadata with
      deriving (Eq, Ord, Show)

data Subscription = Subscription with
    currentPeriodStart : Optional Int
        -- ^ Start of the current period that the subscription has
        --   been invoiced for.
    status : Text
        -- ^ Possible values are `incomplete`,
        --   `incomplete_expired`, `trialing`, `active`,
        --   `past_due`, `canceled`, or `unpaid`. For
        --   `collection_method=charge_automatically` a
        --   subscription moves into `incomplete` if the initial
        --   payment attempt fails. A subscription in this state
        --   can only have metadata and default_source updated.
        --   Once the first invoice is paid, the subscription
        --   moves into an `active` state. If the first invoice is
        --   not paid within 23 hours, the subscription
        --   transitions to `incomplete_expired`. This is a
        --   terminal state, the open invoice will be voided and
        --   no further invoices will be generated. A subscription
        --   that is currently in a trial period is `trialing` and
        --   moves to `active` when the trial period is over. If
        --   subscription `collection_method=charge_automatically`
        --   it becomes `past_due` when payment to renew it fails
        --   and `canceled` or `unpaid` (depending on your
        --   subscriptions settings) when Stripe has exhausted all
        --   payment retry attempts. If subscription
        --   `collection_method=send_invoice` it becomes
        --   `past_due` when its invoice is not paid by the due
        --   date, and `canceled` or `unpaid` if it is still not
        --   paid by an additional deadline after that. Note that
        --   when a subscription has a status of `unpaid`, no
        --   subsequent invoices will be attempted (invoices will
        --   be created, but then immediately automatically
        --   closed). After receiving updated payment information
        --   from a customer, you may choose to reopen and pay
        --   their closed invoices.
    trialStart : Optional Int
        -- ^ If the subscription has a trial, the beginning of
        --   that trial.
    schedule : Optional Text
        -- ^ The schedule attached to the subscription
    discount : Optional Discount
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    billingThresholds : Optional SubscriptionBillingThresholds
    pendingInvoiceItemInterval : Optional SubscriptionPendingInvoiceItemInterval
    quantity : Optional Int
        -- ^ The quantity of the plan to which the customer is
        --   subscribed. For example, if your plan is
        --   $10/user/month, and your customer has 5 users, you
        --   could pass 5 as the quantity to have the customer
        --   charged $50 (5 x $10) monthly. Only set if the
        --   subscription contains a single plan.
    startDate : Optional Int
        -- ^ Date when the subscription was first created. The
        --   date might differ from the `created` date due to
        --   backdating.
    plan : Optional Plan
    cancelAtPeriodEnd : Optional Bool
        -- ^ If the subscription has been canceled with the
        --   `at_period_end` flag set to `true`,
        --   `cancel_at_period_end` on the subscription will be
        --   true. You can use this attribute to determine whether
        --   a subscription that has a status of active is
        --   scheduled to be canceled at the end of the current
        --   period.
    items : SubscriptionItems
        -- ^ List of subscription items, each with an attached
        --   plan.
    collectionMethod : Optional Text
        -- ^ Either `charge_automatically`, or `send_invoice`.
        --   When charging automatically, Stripe will attempt to
        --   pay this subscription at the end of the cycle using
        --   the default source attached to the customer. When
        --   sending an invoice, Stripe will email your customer
        --   an invoice with payment instructions.
    pendingSetupIntent : Optional Text
        -- ^ You can use this
        --   [SetupIntent](https://stripe.com/docs/api/setup_intents)
        --   to collect user authentication when creating a
        --   subscription without immediate payment or updating a
        --   subscription's payment method, allowing you to
        --   optimize for off-session payments. Learn more in the
        --   [SCA Migration
        --   Guide](https://stripe.com/docs/billing/migration/strong-customer-authentication#scenario-2).
    customer : Text
        -- ^ ID of the customer who owns the subscription.
    endedAt : Optional Int
        -- ^ If the subscription has ended, the date the
        --   subscription ended.
    cancelAt : Optional Int
        -- ^ A date in the future at which the subscription will
        --   automatically get canceled
    defaultTaxRates : [TaxRate]
        -- ^ The tax rates that will apply to any subscription
        --   item that does not have `tax_rates` set. Invoices
        --   created will have their `default_tax_rates` populated
        --   from the subscription.
    applicationFeePercent : Optional Decimal
        -- ^ A non-negative decimal between 0 and 100, with at
        --   most two decimal places. This represents the
        --   percentage of the subscription invoice subtotal that
        --   will be transferred to the application owner's Stripe
        --   account.
    trialEnd : Optional Int
        -- ^ If the subscription has a trial, the end of that
        --   trial.
    billingCycleAnchor : Optional Int
        -- ^ Determines the date of the first full invoice, and,
        --   for plans with `month` or `year` intervals, the day
        --   of the month for subsequent invoices.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : SubscriptionMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    canceledAt : Optional Int
        -- ^ If the subscription has been canceled, the date of
        --   that cancellation. If the subscription was canceled
        --   with `cancel_at_period_end`, `canceled_at` will still
        --   reflect the date of the initial cancellation request,
        --   not the end of the subscription period when the
        --   subscription is automatically moved to a canceled
        --   state.
    latestInvoice : Optional Text
        -- ^ The most recent invoice this subscription has
        --   generated.
    nextPendingInvoiceItemInvoice : Optional Int
        -- ^ Specifies the approximate timestamp on which any
        --   pending invoice items will be billed according to the
        --   schedule provided at `pending_invoice_item_interval`.
    currentPeriodEnd : Optional Int
        -- ^ End of the current period that the subscription has
        --   been invoiced for. At the end of this period, a new
        --   invoice will be created.
    defaultPaymentMethod : Optional Text
        -- ^ ID of the default payment method for the
        --   subscription. It must belong to the customer
        --   associated with the subscription. If not set,
        --   invoices will use the default payment method in the
        --   customer's invoice settings.
    pendingUpdate : Optional SubscriptionsResourcePendingUpdate
    defaultSource : Optional Text
        -- ^ ID of the default payment source for the
        --   subscription. It must belong to the customer
        --   associated with the subscription and be in a
        --   chargeable state. If not set, defaults to the
        --   customer's default source.
    daysUntilDue : Optional Int
        -- ^ Number of days a customer has to pay invoices
        --   generated by this subscription. This value will be
        --   `null` for subscriptions where
        --   `collection_method=charge_automatically`.
    taxPercent : Optional Decimal
        -- ^ If provided, each invoice created by this
        --   subscription will apply the tax rate, increasing the
        --   amount billed to the customer.
      deriving (Eq, Ord, Show)

data PaymentMethodCardChecks = PaymentMethodCardChecks with
    addressPostalCodeCheck : Optional Text
        -- ^ If a address postal code was provided, results of the
        --   check, one of `pass`, `fail`, `unavailable`, or
        --   `unchecked`.
    addressLine1Check : Optional Text
        -- ^ If a address line1 was provided, results of the
        --   check, one of `pass`, `fail`, `unavailable`, or
        --   `unchecked`.
    cvcCheck : Optional Text
        -- ^ If a CVC was provided, results of the check, one of
        --   `pass`, `fail`, `unavailable`, or `unchecked`.
      deriving (Eq, Ord, Show)

data SigmaScheduledQueryRunError = SigmaScheduledQueryRunError with
    message : Text
        -- ^ Information about the run failure.
      deriving (Eq, Ord, Show)

data DeletedSubscriptionItem = DeletedSubscriptionItem with
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    deleted : Bool
        -- ^ Always true for a deleted object
      deriving (Eq, Ord, Show)

data SourceOwner = SourceOwner with
    email : Optional Text
        -- ^ Owner's email address.
    phone : Optional Text
        -- ^ Owner's phone number (including extension).
    verifiedAddress : Optional Address
    verifiedName : Optional Text
        -- ^ Verified owner's full name. Verified values are
        --   verified or provided by the payment method directly
        --   (and if supported) at the time of authorization or
        --   settlement. They cannot be set or mutated.
    address : Optional Address
    name : Optional Text
        -- ^ Owner's full name.
    verifiedEmail : Optional Text
        -- ^ Verified owner's email address. Verified values are
        --   verified or provided by the payment method directly
        --   (and if supported) at the time of authorization or
        --   settlement. They cannot be set or mutated.
    verifiedPhone : Optional Text
        -- ^ Verified owner's phone number (including extension).
        --   Verified values are verified or provided by the
        --   payment method directly (and if supported) at the
        --   time of authorization or settlement. They cannot be
        --   set or mutated.
      deriving (Eq, Ord, Show)

data PaymentMethodDetailsAchDebit = PaymentMethodDetailsAchDebit with
    country : Optional Text
        -- ^ Two-letter ISO code representing the country the bank
        --   account is located in.
    fingerprint : Optional Text
        -- ^ Uniquely identifies this particular bank account. You
        --   can use this attribute to check whether two bank
        --   accounts are the same.
    accountHolderType : Optional Text
        -- ^ Type of entity that holds the account. This can be
        --   either `individual` or `company`.
    routingNumber : Optional Text
        -- ^ Routing transit number of the bank account.
    bankName : Optional Text
        -- ^ Name of the bank associated with the bank account.
    last4 : Optional Text
        -- ^ Last four digits of the bank account number.
      deriving (Eq, Ord, Show)

data IssuingCardholderAddress = IssuingCardholderAddress with
    address : Address
    name : Optional Text
        -- ^ The cardholder’s billing name.
      deriving (Eq, Ord, Show)

data BillingDetails = BillingDetails with
    email : Optional Text
        -- ^ Email address.
    phone : Optional Text
        -- ^ Billing phone number (including extension).
    address : Optional Address
    name : Optional Text
        -- ^ Full name.
      deriving (Eq, Ord, Show)

data PaymentMethodDetailsCardChecks = PaymentMethodDetailsCardChecks with
    addressPostalCodeCheck : Optional Text
        -- ^ If a address postal code was provided, results of the
        --   check, one of `pass`, `fail`, `unavailable`, or
        --   `unchecked`.
    addressLine1Check : Optional Text
        -- ^ If a address line1 was provided, results of the
        --   check, one of `pass`, `fail`, `unavailable`, or
        --   `unchecked`.
    cvcCheck : Optional Text
        -- ^ If a CVC was provided, results of the check, one of
        --   `pass`, `fail`, `unavailable`, or `unchecked`.
      deriving (Eq, Ord, Show)

data PersonRequirements = PersonRequirements with
    currentlyDue : [Text]
        -- ^ Fields that need to be collected to keep the person's
        --   account enabled. If not collected by the account's
        --   `current_deadline`, these fields appear in `past_due`
        --   as well, and the account is disabled.
    eventuallyDue : [Text]
        -- ^ Fields that need to be collected assuming all volume
        --   thresholds are reached. As fields are needed, they
        --   are moved to `currently_due` and the account's
        --   `current_deadline` is set.
    pastDue : [Text]
        -- ^ Fields that weren't collected by the account's
        --   `current_deadline`. These fields need to be collected
        --   to enable payouts for the person's account.
    pendingVerification : [Text]
        -- ^ Fields that may become required depending on the
        --   results of verification or review. An empty array
        --   unless an asynchronous verification is pending. If
        --   verification fails, the fields in this array become
        --   required and move to `currently_due` or `past_due`.
      deriving (Eq, Ord, Show)

data IssuingCardSpendingLimit = IssuingCardSpendingLimit with
    amount : Int
        -- ^ Maximum amount allowed to spend per time interval.
    categories : [Text]
        -- ^ Array of strings containing
        --   [categories](https://stripe.com/docs/api#issuing_authorization_object-merchant_data-category)
        --   on which to apply the spending limit. Leave this
        --   blank to limit all charges.
    interval : Text
        -- ^ The time interval or event with which to apply this
        --   spending limit towards.
      deriving (Eq, Ord, Show)

data IssuingCardholderSpendingLimit = IssuingCardholderSpendingLimit with
    amount : Int
        -- ^ Maximum amount allowed to spend per time interval.
    categories : [Text]
        -- ^ Array of strings containing
        --   [categories](https://stripe.com/docs/api#issuing_authorization_object-merchant_data-category)
        --   on which to apply the spending limit. Leave this
        --   blank to limit all charges.
    interval : Text
        -- ^ The time interval or event with which to apply this
        --   spending limit towards.
      deriving (Eq, Ord, Show)

data PayoutMetadata = PayoutMetadata with
      deriving (Eq, Ord, Show)

data Payout = Payout with
    destination : Optional Text
        -- ^ ID of the bank account or card the payout was sent
        --   to.
    failureMessage : Optional Text
        -- ^ Message to user further explaining reason for payout
        --   failure if available.
    status : Text
        -- ^ Current status of the payout (`paid`, `pending`,
        --   `in_transit`, `canceled` or `failed`). A payout will
        --   be `pending` until it is submitted to the bank, at
        --   which point it becomes `in_transit`. It will then
        --   change to `paid` if the transaction goes through. If
        --   it does not go through successfully, its status will
        --   change to `failed` or `canceled`.
    amount : Int
        -- ^ Amount (in %s) to be transferred to your bank account
        --   or debit card.
    sourceType : Optional Text
        -- ^ The source balance this payout came from. One of
        --   `card`, `fpx`, or `bank_account`.
    balanceTransaction : Optional Text
        -- ^ ID of the balance transaction that describes the
        --   impact of this payout on your account balance.
    automatic : Bool
        -- ^ Returns `true` if the payout was created by an
        --   [automated payout
        --   schedule](https://stripe.com/docs/payouts#payout-schedule),
        --   and `false` if it was [requested
        --   manually](https://stripe.com/docs/payouts#manual-payouts).
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    arrivalDate : Optional Int
        -- ^ Date the payout is expected to arrive in the bank.
        --   This factors in delays like weekends or bank
        --   holidays.
    method : Text
        -- ^ The method used to send this payout, which can be
        --   `standard` or `instant`. `instant` is only supported
        --   for payouts to debit cards. (See [Instant payouts for
        --   marketplaces](https://stripe.com/blog/instant-payouts-for-marketplaces)
        --   for more information.)
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    failureBalanceTransaction : Optional Text
        -- ^ If the payout failed or was canceled, this will be
        --   the ID of the balance transaction that reversed the
        --   initial balance transaction, and puts the funds from
        --   the failed payout back in your balance.
    failureCode : Optional Text
        -- ^ Error code explaining reason for payout failure if
        --   available. See [Types of payout
        --   failures](https://stripe.com/docs/api#payout_failures)
        --   for a list of failure codes.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : PayoutMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    type_ : Optional Text
        -- ^ Can be `bank_account` or `card`.
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users.
    statementDescriptor : Optional Text
        -- ^ Extra information about a payout to be displayed on
        --   the user's bank statement.
      deriving (Eq, Ord, Show)

data OrderItems = OrderItems with
      deriving (Eq, Ord, Show)

data OrderShippingMethods = OrderShippingMethods with
      deriving (Eq, Ord, Show)

data OrderMetadata = OrderMetadata with
      deriving (Eq, Ord, Show)

data OrderReturnsData = OrderReturnsData with
      deriving (Eq, Ord, Show)

data OrderReturns = OrderReturns with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [OrderReturn]
        -- ^ Details about each object.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data Order = Order with
    email : Optional Text
        -- ^ The email address of the customer placing the order.
    status : Text
        -- ^ Current order status. One of `created`, `paid`,
        --   `canceled`, `fulfilled`, or `returned`. More details
        --   in the [Orders
        --   Guide](https://stripe.com/docs/orders/guide#understanding-order-statuses).
    amount : Int
        -- ^ A positive integer in the smallest currency unit
        --   (that is, 100 cents for $1.00, or 1 for ¥1, Japanese
        --   Yen being a zero-decimal currency) representing the
        --   total amount for the order.
    statusTransitions : Optional StatusTransitions
    application : Optional Text
        -- ^ ID of the Connect Application that created the order.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    externalCouponCode : Optional Text
        -- ^ External coupon code to load for this order.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    amountReturned : Optional Int
        -- ^ The total amount that was returned to the customer.
    items : [OrderItem]
        -- ^ List of items constituting the order. An order can
        --   have up to 25 items.
    shippingMethods : [ShippingMethod]
        -- ^ A list of supported shipping methods for this order.
        --   The desired shipping method can be specified either
        --   by updating the order, or when paying it.
    selectedShippingMethod : Optional Text
        -- ^ The shipping method that is currently selected for
        --   this order, if any. If present, it is equal to one of
        --   the `id`s of shipping methods in the
        --   `shipping_methods` array. At order creation time, if
        --   there are multiple shipping methods, Stripe will
        --   automatically selected the first method.
    customer : Optional Text
        -- ^ The customer used for the order.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    shipping : Optional Shipping
    charge : Optional Text
        -- ^ The ID of the payment used to pay for the order.
        --   Present if the order status is `paid`, `fulfilled`,
        --   or `refunded`.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : OrderMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    updated : Optional Int
        -- ^ Time at which the object was last updated. Measured
        --   in seconds since the Unix epoch.
    applicationFee : Optional Int
        -- ^ A fee in cents that will be applied to the order and
        --   transferred to the application owner’s Stripe
        --   account. The request must be made with an OAuth key
        --   or the Stripe-Account header in order to take an
        --   application fee. For more information, see the
        --   application fees documentation.
    upstreamId : Optional Text
        -- ^ The user's order ID if it is different from the
        --   Stripe order ID.
    returns : Optional OrderReturns
        -- ^ A list of returns that have taken place for this
        --   order.
      deriving (Eq, Ord, Show)

data FileLinksData = FileLinksData with
      deriving (Eq, Ord, Show)

data FileLinks = FileLinks with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [FileLink]
        -- ^ Details about each object.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data File = File with
    size : Int
        -- ^ The size in bytes of the file object.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    url : Optional Text
        -- ^ The URL from which the file can be downloaded using
        --   your live secret API key.
    purpose : Text
        -- ^ The purpose of the file. Possible values are
        --   `additional_verification`, `business_icon`,
        --   `business_logo`, `customer_signature`,
        --   `dispute_evidence`, `finance_report_run`,
        --   `identity_document`, `pci_document`,
        --   `sigma_scheduled_query`, or
        --   `tax_document_user_upload`.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    title : Optional Text
        -- ^ A user friendly title for the document.
    type_ : Optional Text
        -- ^ The type of the file returned (e.g., `csv`, `pdf`,
        --   `jpg`, or `png`).
    links : Optional FileLinks
        -- ^ A list of [file
        --   links](https://stripe.com/docs/api#file_links) that
        --   point at this file.
    filename : Optional Text
        -- ^ A filename for the file, suitable for saving to a
        --   filesystem.
      deriving (Eq, Ord, Show)

data AccountLink = AccountLink with
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    url : Text
        -- ^ The URL for the account link.
    expiresAt : Optional Int
        -- ^ The timestamp at which this account link will expire.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
      deriving (Eq, Ord, Show)

data SourceTypeAchDebit = SourceTypeAchDebit with
    country : Optional Text
    fingerprint : Optional Text
    routingNumber : Optional Text
    type_ : Optional Text
    bankName : Optional Text
    last4 : Optional Text
      deriving (Eq, Ord, Show)

data RadarValueListItem = RadarValueListItem with
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    value : Text
        -- ^ The value of the item.
    createdBy : Optional Text
        -- ^ The name or email address of the user who added this
        --   item to the value list.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    valueList : Optional Text
        -- ^ The identifier of the value list this item belongs
        --   to.
      deriving (Eq, Ord, Show)

data SourceTransaction = SourceTransaction with
    status : Text
        -- ^ The status of the transaction, one of `succeeded`,
        --   `pending`, or `failed`.
    amount : Int
        -- ^ A positive integer in the smallest currency unit
        --   (that is, 100 cents for $1.00, or 1 for ¥1, Japanese
        --   Yen being a zero-decimal currency) representing the
        --   amount your customer has pushed to the receiver.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    chfCreditTransfer : Optional SourceTransactionChfCreditTransferData
    achCreditTransfer : Optional SourceTransactionAchCreditTransferData
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    paperCheck : Optional SourceTransactionPaperCheckData
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    source : Text
        -- ^ The ID of the source this transaction is attached to.
    id : Text
        -- ^ Unique identifier for the object.
    type_ : Optional Text
        -- ^ The type of source this transaction is attached to.
    gbpCreditTransfer : Optional SourceTransactionGbpCreditTransferData
    sepaCreditTransfer : Optional SourceTransactionSepaCreditTransferData
      deriving (Eq, Ord, Show)

data DeletedCustomer = DeletedCustomer with
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    deleted : Bool
        -- ^ Always true for a deleted object
      deriving (Eq, Ord, Show)

data IssuingTransactionMetadata = IssuingTransactionMetadata with
      deriving (Eq, Ord, Show)

data IssuingTransaction = IssuingTransaction with
    card : Text
        -- ^ The card used to make this transaction.
    cardholder : Optional Text
        -- ^ The cardholder to whom this transaction belongs.
    amount : Int
        -- ^ The amount of this transaction in your currency. This
        --   is the amount that your balance will be updated by.
    balanceTransaction : Optional Text
        -- ^ ID of the [balance
        --   transaction](https://stripe.com/docs/api/balance_transactions)
        --   associated with this transaction.
    merchantData : Optional IssuingAuthorizationMerchantData
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    authorization : Optional Text
        -- ^ The `Authorization` object that led to this
        --   transaction.
    merchantAmount : Optional Int
        -- ^ The amount that the merchant will receive,
        --   denominated in `merchant_currency`. It will be
        --   different from `amount` if the merchant is taking
        --   payment in a different currency.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    dispute : Optional Text
        -- ^ If you've disputed the transaction, the ID of the
        --   [dispute
        --   object](https://stripe.com/docs/api/issuing/disputes/object).
    merchantCurrency : Optional Text
        -- ^ The currency with which the merchant is taking
        --   payment.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : IssuingTransactionMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    type_ : Optional Text
        -- ^ The nature of the transaction.
      deriving (Eq, Ord, Show)

data SubscriptionSchedulePhaseConfigurationPlans = SubscriptionSchedulePhaseConfigurationPlans with
      deriving (Eq, Ord, Show)

data SubscriptionSchedulePhaseConfigurationDefaultTaxRates = SubscriptionSchedulePhaseConfigurationDefaultTaxRates with
      deriving (Eq, Ord, Show)

data SubscriptionSchedulePhaseConfiguration = SubscriptionSchedulePhaseConfiguration with
    endDate : Optional Int
        -- ^ The end of this phase of the subscription schedule.
    coupon : Optional Text
        -- ^ ID of the coupon to use during this phase of the
        --   subscription schedule.
    billingThresholds : Optional SubscriptionBillingThresholds
    plans : [SubscriptionScheduleConfigurationItem]
        -- ^ Plans to subscribe during this phase of the
        --   subscription schedule.
    invoiceSettings : Optional InvoiceSettingSubscriptionScheduleSetting
    startDate : Optional Int
        -- ^ The start of this phase of the subscription schedule.
    collectionMethod : Optional Text
        -- ^ Either `charge_automatically`, or `send_invoice`.
        --   When charging automatically, Stripe will attempt to
        --   pay the underlying subscription at the end of each
        --   billing cycle using the default source attached to
        --   the customer. When sending an invoice, Stripe will
        --   email your customer an invoice with payment
        --   instructions.
    prorationBehavior : Optional Text
        -- ^ Controls whether or not the subscription schedule
        --   will prorate when transitioning to this phase. Values
        --   are `create_prorations` and `none`.
    defaultTaxRates : [TaxRate]
        -- ^ The default tax rates to apply to the subscription
        --   during this phase of the subscription schedule.
    applicationFeePercent : Optional Decimal
        -- ^ A non-negative decimal between 0 and 100, with at
        --   most two decimal places. This represents the
        --   percentage of the subscription invoice subtotal that
        --   will be transferred to the application owner's Stripe
        --   account during this phase of the schedule.
    trialEnd : Optional Int
        -- ^ When the trial ends within the phase.
    defaultPaymentMethod : Optional Text
        -- ^ ID of the default payment method for the subscription
        --   schedule. It must belong to the customer associated
        --   with the subscription schedule. If not set, invoices
        --   will use the default payment method in the customer's
        --   invoice settings.
    taxPercent : Optional Decimal
        -- ^ If provided, each invoice created during this phase
        --   of the subscription schedule will apply the tax rate,
        --   increasing the amount billed to the customer.
      deriving (Eq, Ord, Show)

data SubscriptionItemBillingThresholds = SubscriptionItemBillingThresholds with
    usageGte : Optional Int
        -- ^ Usage threshold that triggers the subscription to
        --   create an invoice
      deriving (Eq, Ord, Show)

data PersonRelationship = PersonRelationship with
    director : Optional Bool
        -- ^ Whether the person is a director of the account's
        --   legal entity. Currently only required for accounts in
        --   the EU. Directors are typically members of the
        --   governing board of the company, or responsible for
        --   ensuring the company meets its regulatory
        --   obligations.
    owner : Optional Bool
        -- ^ Whether the person is an owner of the account’s legal
        --   entity.
    percentOwnership : Optional Decimal
        -- ^ The percent owned by the person of the account's
        --   legal entity.
    title : Optional Text
        -- ^ The person's title (e.g., CEO, Support Engineer).
    executive : Optional Bool
        -- ^ Whether the person has significant responsibility to
        --   control, manage, or direct the organization.
    representative : Optional Bool
        -- ^ Whether the person is authorized as the primary
        --   representative of the account. This is the person
        --   nominated by the business to provide information
        --   about themselves, and general information about the
        --   account. There can only be one representative at any
        --   given time. At the time the account is created, this
        --   person should be set to the person responsible for
        --   opening the account.
      deriving (Eq, Ord, Show)

data PaymentMethodDetailsFpx = PaymentMethodDetailsFpx with
    transactionId : Optional Text
        -- ^ Unique transaction id generated by FPX for every
        --   request from the merchant
    bank : Text
        -- ^ The customer's bank. Can be one of `affin_bank`,
        --   `alliance_bank`, `ambank`, `bank_islam`,
        --   `bank_muamalat`, `bank_rakyat`, `bsn`, `cimb`,
        --   `hong_leong_bank`, `hsbc`, `kfh`, `maybank2u`,
        --   `ocbc`, `public_bank`, `rhb`, `standard_chartered`,
        --   `uob`, `deutsche_bank`, `maybank2e`, `pb_enterprise`,
        --   or `uob_regional`.
      deriving (Eq, Ord, Show)

data DeletedInvoiceitem = DeletedInvoiceitem with
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    deleted : Bool
        -- ^ Always true for a deleted object
      deriving (Eq, Ord, Show)

data TransferSchedule = TransferSchedule with
    monthlyAnchor : Optional Int
        -- ^ The day of the month funds will be paid out. Only
        --   shown if `interval` is monthly. Payouts scheduled
        --   between the 29th and 31st of the month are sent on
        --   the last day of shorter months.
    interval : Text
        -- ^ How frequently funds will be paid out. One of
        --   `manual` (payouts only created via API call),
        --   `daily`, `weekly`, or `monthly`.
    weeklyAnchor : Optional Text
        -- ^ The day of the week funds will be paid out, of the
        --   style 'monday', 'tuesday', etc. Only shown if
        --   `interval` is weekly.
    delayDays : Optional Int
        -- ^ The number of days charges for the account will be
        --   held before being paid out.
      deriving (Eq, Ord, Show)

data IssuingAuthorizationVerificationData = IssuingAuthorizationVerificationData with
    authentication : Text
        -- ^ Whether 3DS authentication was performed.
    addressZipCheck : Optional Text
        -- ^ Whether the cardholder provided a zip (or postal
        --   code) and if it matched the cardholder’s
        --   `billing.address.postal_code`.
    addressLine1Check : Optional Text
        -- ^ Whether the cardholder provided an address first line
        --   and if it matched the cardholder’s
        --   `billing.address.line1`.
    cvcCheck : Optional Text
        -- ^ Whether the cardholder provided a CVC and if it
        --   matched Stripe’s record.
      deriving (Eq, Ord, Show)

data ReportingReportType = ReportingReportType with
    name : Text
        -- ^ Human-readable name of the Report Type
    version : Int
        -- ^ Version of the Report Type. Different versions report
        --   with the same ID will have the same purpose, but may
        --   take different run parameters or have different
        --   result schemas.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ The [ID of the Report
        --   Type](https://stripe.com/docs/reporting/statements/api#available-report-types),
        --   such as `balance.summary.1`.
    updated : Int
        -- ^ When this Report Type was latest updated. Measured in
        --   seconds since the Unix epoch.
    dataAvailableEnd : Optional Int
        -- ^ Most recent time for which this Report Type is
        --   available. Measured in seconds since the Unix epoch.
    dataAvailableStart : Optional Int
        -- ^ Earliest time for which this Report Type is
        --   available. Measured in seconds since the Unix epoch.
    defaultColumns : [Text]
        -- ^ List of column names that are included by default
        --   when this Report Type gets run. (If the Report Type
        --   doesn't support the `columns` parameter, this will be
        --   null.)
      deriving (Eq, Ord, Show)

data SourceTypeAchCreditTransfer = SourceTypeAchCreditTransfer with
    fingerprint : Optional Text
    refundAccountHolderName : Optional Text
    swiftCode : Optional Text
    accountNumber : Optional Text
    routingNumber : Optional Text
    refundAccountHolderType : Optional Text
    bankName : Optional Text
    refundRoutingNumber : Optional Text
      deriving (Eq, Ord, Show)

data SubscriptionSchedulesResourceDefaultSettings = SubscriptionSchedulesResourceDefaultSettings with
    billingThresholds : Optional SubscriptionBillingThresholds
    invoiceSettings : Optional InvoiceSettingSubscriptionScheduleSetting
    collectionMethod : Optional Text
        -- ^ Either `charge_automatically`, or `send_invoice`.
        --   When charging automatically, Stripe will attempt to
        --   pay the underlying subscription at the end of each
        --   billing cycle using the default source attached to
        --   the customer. When sending an invoice, Stripe will
        --   email your customer an invoice with payment
        --   instructions.
    defaultPaymentMethod : Optional Text
        -- ^ ID of the default payment method for the subscription
        --   schedule. If not set, invoices will use the default
        --   payment method in the customer's invoice settings.
      deriving (Eq, Ord, Show)

data PaymentMethodCardWallet = PaymentMethodCardWallet with
    dynamicLast4 : Optional Text
        -- ^ (For tokenized numbers only.) The last four digits of
        --   the device account number.
    samsungPay : Optional PaymentMethodCardWalletSamsungPay
    visaCheckout : Optional PaymentMethodCardWalletVisaCheckout
    masterpass : Optional PaymentMethodCardWalletMasterpass
    googlePay : Optional PaymentMethodCardWalletGooglePay
    applePay : Optional PaymentMethodCardWalletApplePay
    type_ : Optional Text
        -- ^ The type of the card wallet, one of
        --   `amex_express_checkout`, `apple_pay`, `google_pay`,
        --   `masterpass`, `samsung_pay`, or `visa_checkout`. An
        --   additional hash is included on the Wallet subhash
        --   with a name matching this value. It contains
        --   additional information specific to the card wallet
        --   type.
    amexExpressCheckout : Optional PaymentMethodCardWalletAmexExpressCheckout
      deriving (Eq, Ord, Show)

data IssuingAuthorizationTransactions = IssuingAuthorizationTransactions with
      deriving (Eq, Ord, Show)

data IssuingAuthorizationRequestHistory = IssuingAuthorizationRequestHistory with
      deriving (Eq, Ord, Show)

data IssuingAuthorizationMetadata = IssuingAuthorizationMetadata with
      deriving (Eq, Ord, Show)

data IssuingAuthorizationBalanceTransactions = IssuingAuthorizationBalanceTransactions with
      deriving (Eq, Ord, Show)

data IssuingAuthorization = IssuingAuthorization with
    card : IssuingCard
    pendingAuthorizedAmount : Optional Int
        -- ^ The amount the user is requesting to be authorized.
        --   This field will only be non-zero during an
        --   `issuing.authorization.request` webhook.
    cardholder : Optional Text
        -- ^ The cardholder to whom this authorization belongs.
    status : Text
        -- ^ The current status of the authorization in its
        --   lifecycle.
    authorizedAmount : Optional Int
        -- ^ The amount that has been authorized. This will be `0`
        --   when the object is created, and increase after it has
        --   been approved.
    transactions : [IssuingTransaction]
        -- ^ List of
        --   [transactions](https://stripe.com/docs/api/issuing/transactions)
        --   associated with this authorization.
    merchantData : Optional IssuingAuthorizationMerchantData
    isHeldAmountControllable : Optional Bool
        -- ^ If set `true`, you may provide
        --   [held_amount](https://stripe.com/docs/api/issuing/authorizations/approve#approve_issuing_authorization-held_amount)
        --   to control how much to hold for the authorization.
    heldCurrency : Optional Text
        -- ^ The currency of the [held
        --   amount](https://stripe.com/docs/api#issuing_authorization_object-held_amount).
        --   This will always be the card currency.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    verificationData : Optional IssuingAuthorizationVerificationData
    pendingHeldAmount : Optional Int
        -- ^ The additional amount Stripe will hold if the
        --   authorization is approved. This field will only be
        --   non-zero during an `issuing.authorization.request`
        --   webhook.
    heldAmount : Optional Int
        -- ^ The amount the authorization is expected to be in
        --   `held_currency`. When Stripe holds funds from you,
        --   this is the amount reserved for the authorization.
        --   This will be `0` when the object is created, and
        --   increase after it has been approved. For
        --   multi-currency transactions, `held_amount` can be
        --   used to determine the expected exchange rate.
    requestHistory : [IssuingAuthorizationRequest]
        -- ^ History of every time the authorization was
        --   approved/denied (whether approved/denied by you
        --   directly, or by Stripe based on your
        --   authorization_controls). If the merchant changes the
        --   authorization by performing an [incremental
        --   authorization or partial
        --   capture](https://stripe.com/docs/issuing/authorizations/special_scenarios#incremental-authorizations),
        --   you can look at request_history to see the previous
        --   states of the authorization.
    authorizedCurrency : Optional Text
        -- ^ The currency that was presented to the cardholder for
        --   the authorization. Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : IssuingAuthorizationMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    authorizationMethod : Optional Text
        -- ^ How the card details were provided.
    approved : Bool
        -- ^ Whether the authorization has been approved.
    balanceTransactions : [BalanceTransaction]
        -- ^ List of balance transactions associated with this
        --   authorization.
    walletProvider : Optional Text
        -- ^ What, if any, digital wallet was used for this
        --   authorization. One of `apple_pay`, `google_pay`, or
        --   `samsung_pay`.
      deriving (Eq, Ord, Show)

data LegalEntityCompanyVerificationDocument = LegalEntityCompanyVerificationDocument with
    front : Optional Text
        -- ^ The front of a document returned by a [file
        --   upload](https://stripe.com/docs/api#create_file) with
        --   a `purpose` value of `additional_verification`.
    details : Optional Text
        -- ^ A user-displayable string describing the verification
        --   state of this document.
    detailsCode : Optional Text
        -- ^ One of `document_corrupt`, `document_expired`,
        --   `document_failed_copy`, `document_failed_greyscale`,
        --   `document_failed_other`, `document_failed_test_mode`,
        --   `document_fraudulent`, `document_incomplete`,
        --   `document_invalid`, `document_manipulated`,
        --   `document_not_readable`, `document_not_uploaded`,
        --   `document_type_not_supported`, or
        --   `document_too_large`. A machine-readable code
        --   specifying the verification state for this document.
    back : Optional Text
        -- ^ The back of a document returned by a [file
        --   upload](https://stripe.com/docs/api#create_file) with
        --   a `purpose` value of `additional_verification`.
      deriving (Eq, Ord, Show)

data PaymentMethodCardWalletSamsungPay = PaymentMethodCardWalletSamsungPay with
      deriving (Eq, Ord, Show)

data DeletedExternalAccount = DeletedExternalAccount with
    currency : Optional Text
        -- ^ Three-letter [ISO code for the
        --   currency](https://stripe.com/docs/payouts) paid out
        --   to the bank account.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    deleted : Bool
        -- ^ Always true for a deleted object
      deriving (Eq, Ord, Show)

data TaxIdVerification = TaxIdVerification with
    status : Text
        -- ^ Verification status, one of `pending`, `verified`,
        --   `unverified`, or `unavailable`.
    verifiedAddress : Optional Text
        -- ^ Verified address.
    verifiedName : Optional Text
        -- ^ Verified name.
      deriving (Eq, Ord, Show)

data SubscriptionSchedulePhases = SubscriptionSchedulePhases with
      deriving (Eq, Ord, Show)

data SubscriptionScheduleMetadata = SubscriptionScheduleMetadata with
      deriving (Eq, Ord, Show)

data SubscriptionSchedule = SubscriptionSchedule with
    phases : [SubscriptionSchedulePhaseConfiguration]
        -- ^ Configuration for the subscription schedule's phases.
    endBehavior : Optional Text
        -- ^ Behavior of the subscription schedule and underlying
        --   subscription when it ends.
    status : Text
        -- ^ The present status of the subscription schedule.
        --   Possible values are `not_started`, `active`,
        --   `completed`, `released`, and `canceled`. You can read
        --   more about the different states in our [behavior
        --   guide](https://stripe.com/docs/billing/subscriptions/subscription-schedules).
    releasedAt : Optional Int
        -- ^ Time at which the subscription schedule was released.
        --   Measured in seconds since the Unix epoch.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    defaultSettings : Optional SubscriptionSchedulesResourceDefaultSettings
    currentPhase : Optional SubscriptionScheduleCurrentPhase
    customer : Text
        -- ^ ID of the customer who owns the subscription
        --   schedule.
    completedAt : Optional Int
        -- ^ Time at which the subscription schedule was
        --   completed. Measured in seconds since the Unix epoch.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : Optional SubscriptionScheduleMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    canceledAt : Optional Int
        -- ^ Time at which the subscription schedule was canceled.
        --   Measured in seconds since the Unix epoch.
    subscription : Optional Text
        -- ^ ID of the subscription managed by the subscription
        --   schedule.
    releasedSubscription : Optional Text
        -- ^ ID of the subscription once managed by the
        --   subscription schedule (if it is released).
      deriving (Eq, Ord, Show)

data PaymentMethodCard = PaymentMethodCard with
    threeDSecureUsage : Optional ThreeDSecureUsage
    wallet : Optional PaymentMethodCardWallet
    checks : Optional PaymentMethodCardChecks
    country : Optional Text
        -- ^ Two-letter ISO code representing the country of the
        --   card. You could use this attribute to get a sense of
        --   the international breakdown of cards you've
        --   collected.
    fingerprint : Optional Text
        -- ^ Uniquely identifies this particular card number. You
        --   can use this attribute to check whether two customers
        --   who've signed up with you are using the same card
        --   number, for example.
    brand : Text
        -- ^ Card brand. Can be `amex`, `diners`, `discover`,
        --   `jcb`, `mastercard`, `unionpay`, `visa`, or
        --   `unknown`.
    funding : Text
        -- ^ Card funding type. Can be `credit`, `debit`,
        --   `prepaid`, or `unknown`.
    expMonth : Optional Int
        -- ^ Two-digit number representing the card's expiration
        --   month.
    expYear : Optional Int
        -- ^ Four-digit number representing the card's expiration
        --   year.
    generatedFrom : Optional PaymentMethodCardGeneratedCard
    last4 : Text
        -- ^ The last four digits of the card.
      deriving (Eq, Ord, Show)

data SourceTypeCard = SourceTypeCard with
    dynamicLast4 : Optional Text
    tokenizationMethod : Optional Text
    country : Optional Text
    fingerprint : Optional Text
    addressZipCheck : Optional Text
    brand : Optional Text
    funding : Optional Text
    name : Optional Text
    addressLine1Check : Optional Text
    cvcCheck : Optional Text
    expMonth : Optional Int
    expYear : Optional Int
    threeDSecure : Optional Text
    last4 : Optional Text
      deriving (Eq, Ord, Show)

data NotificationEventDataPreviousAttributes = NotificationEventDataPreviousAttributes with
      deriving (Eq, Ord, Show)

data NotificationEventDataObject = NotificationEventDataObject with
      deriving (Eq, Ord, Show)

data NotificationEventData = NotificationEventData with
    previousAttributes : Optional NotificationEventDataPreviousAttributes
        -- ^ Object containing the names of the attributes that
        --   have changed, and their previous values (sent along
        --   only with *.updated events).
    object : NotificationEventDataObject
        -- ^ Object containing the API resource relevant to the
        --   event. For example, an `invoice.created` event will
        --   have a full [invoice
        --   object](https://stripe.com/docs/api#invoice_object)
        --   as the value of the object key.
      deriving (Eq, Ord, Show)

data OnlineAcceptance = OnlineAcceptance with
    userAgent : Optional Text
        -- ^ The user agent of the browser from which the Mandate
        --   was accepted by the customer.
    ipAddress : Optional Text
        -- ^ The IP address from which the Mandate was accepted by
        --   the customer.
      deriving (Eq, Ord, Show)

data FileLinkMetadata = FileLinkMetadata with
      deriving (Eq, Ord, Show)

data FileLink = FileLink with
    expired : Bool
        -- ^ Whether this link is already expired.
    livemode : Bool
        -- ^ Has the value `true` if the object exists in live
        --   mode or the value `false` if the object exists in
        --   test mode.
    created : Int
        -- ^ Time at which the object was created. Measured in
        --   seconds since the Unix epoch.
    url : Optional Text
        -- ^ The publicly accessible URL to download the file.
    expiresAt : Optional Int
        -- ^ Time at which the link expires.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    metadata : FileLinkMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
    id : Text
        -- ^ Unique identifier for the object.
    file : Text
        -- ^ The file object this link points to.
      deriving (Eq, Ord, Show)

data DeletedPlan = DeletedPlan with
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value.
    id : Text
        -- ^ Unique identifier for the object.
    deleted : Bool
        -- ^ Always true for a deleted object
      deriving (Eq, Ord, Show)

-- | <p>Returns a full list of Report Types. (Requires a
--   <a
--   href="https://stripe.com/docs/keys#test-live-modes">live-mode
--   API key</a>.)</p>
template GetReportingReportTypes with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
  where
    signatory requestor

template GetReportingReportTypesResponse with
    requestor : Party
    requestId : ContractId GetReportingReportTypes
    body : GetReportingReportTypesResponseBody
  where
    signatory requestor

data GetReportingReportTypesResponseBody
    = GetReportingReportTypesResponseBody_200 GetReportingReportTypesResponseBody200
    | GetReportingReportTypesResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>A PaymentIntent object can be canceled when it is
--   in one of these statuses:
--   <code>requires_payment_method</code>,
--   <code>requires_capture</code>,
--   <code>requires_confirmation</code>,
--   <code>requires_action</code>. </p> <p>Once canceled,
--   no additional charges will be made by the
--   PaymentIntent and any operations on the PaymentIntent
--   will fail with an error. For PaymentIntents with
--   <code>status='requires_capture'</code>, the remaining
--   <code>amount_capturable</code> will automatically be
--   refunded.</p>
template PostPaymentIntentsIntentCancel with
    requestor : Party
    intent : Text
    payload : PostPaymentIntentsIntentCancelPayload
  where
    signatory requestor

template PostPaymentIntentsIntentCancelResponse with
    requestor : Party
    requestId : ContractId PostPaymentIntentsIntentCancel
    body : PostPaymentIntentsIntentCancelResponseBody
  where
    signatory requestor

data PostPaymentIntentsIntentCancelResponseBody
    = PostPaymentIntentsIntentCancelResponseBody_200 PaymentIntent
    | PostPaymentIntentsIntentCancelResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the details of an existing product.
--   Supply the unique product ID from either a product
--   creation request or the product list, and Stripe will
--   return the corresponding product information.</p>
template GetProductsId with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    id : Text
  where
    signatory requestor

template GetProductsIdResponse with
    requestor : Party
    requestId : ContractId GetProductsId
    body : GetProductsIdResponseBody
  where
    signatory requestor

data GetProductsIdResponseBody
    = GetProductsIdResponseBody_200 Product
    | GetProductsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the specific product by setting the values
--   of the parameters passed. Any parameters not provided
--   will be left unchanged.</p>
template PostProductsId with
    requestor : Party
    id : Text
    payload : PostProductsIdPayload
  where
    signatory requestor

template PostProductsIdResponse with
    requestor : Party
    requestId : ContractId PostProductsId
    body : PostProductsIdResponseBody
  where
    signatory requestor

data PostProductsIdResponseBody
    = PostProductsIdResponseBody_200 Product
    | PostProductsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Delete a product. Deleting a product with
--   type=<code>good</code> is only possible if it has no
--   SKUs associated with it. Deleting a product with
--   type=<code>service</code> is only possible if it has
--   no plans associated with it.</p>
template DeleteProductsId with
    requestor : Party
    id : Text
  where
    signatory requestor

template DeleteProductsIdResponse with
    requestor : Party
    requestId : ContractId DeleteProductsId
    body : DeleteProductsIdResponseBody
  where
    signatory requestor

data DeleteProductsIdResponseBody
    = DeleteProductsIdResponseBody_200 DeletedProduct
    | DeleteProductsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the invoice item with the given ID.</p>
template GetSubscriptionItemsItem with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    item : Text
  where
    signatory requestor

template GetSubscriptionItemsItemResponse with
    requestor : Party
    requestId : ContractId GetSubscriptionItemsItem
    body : GetSubscriptionItemsItemResponseBody
  where
    signatory requestor

data GetSubscriptionItemsItemResponseBody
    = GetSubscriptionItemsItemResponseBody_200 SubscriptionItem
    | GetSubscriptionItemsItemResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the plan or quantity of an item on a
--   current subscription.</p>
template PostSubscriptionItemsItem with
    requestor : Party
    item : Text
    payload : PostSubscriptionItemsItemPayload
  where
    signatory requestor

template PostSubscriptionItemsItemResponse with
    requestor : Party
    requestId : ContractId PostSubscriptionItemsItem
    body : PostSubscriptionItemsItemResponseBody
  where
    signatory requestor

data PostSubscriptionItemsItemResponseBody
    = PostSubscriptionItemsItemResponseBody_200 SubscriptionItem
    | PostSubscriptionItemsItemResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Deletes an item from the subscription. Removing a
--   subscription item from a subscription will not cancel
--   the subscription.</p>
template DeleteSubscriptionItemsItem with
    requestor : Party
    item : Text
    payload : DeleteSubscriptionItemsItemPayload
  where
    signatory requestor

template DeleteSubscriptionItemsItemResponse with
    requestor : Party
    requestId : ContractId DeleteSubscriptionItemsItem
    body : DeleteSubscriptionItemsItemResponseBody
  where
    signatory requestor

data DeleteSubscriptionItemsItemResponseBody
    = DeleteSubscriptionItemsItemResponseBody_200 DeletedSubscriptionItem
    | DeleteSubscriptionItemsItemResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of top-ups.</p>
template GetTopups with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    amount : Int
        -- ^ A positive integer representing how much to transfer.
    created : Int
        -- ^ A filter on the list, based on the object `created`
        --   field. The value can be a string with an integer Unix
        --   timestamp, or it can be a dictionary with a number of
        --   different query options.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
    status : Text
        -- ^ Only return top-ups that have the given status. One
        --   of `canceled`, `failed`, `pending` or `succeeded`.
  where
    signatory requestor

template GetTopupsResponse with
    requestor : Party
    requestId : ContractId GetTopups
    body : GetTopupsResponseBody
  where
    signatory requestor

data GetTopupsResponseBody
    = GetTopupsResponseBody_200 GetTopupsResponseBody200
    | GetTopupsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Top up the balance of an account</p>
template PostTopups with
    requestor : Party
    payload : PostTopupsPayload
  where
    signatory requestor

template PostTopupsResponse with
    requestor : Party
    requestId : ContractId PostTopups
    body : PostTopupsResponseBody
  where
    signatory requestor

data PostTopupsResponseBody
    = PostTopupsResponseBody_200 Topup
    | PostTopupsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the Bitcoin receiver with the given
--   ID.</p>
template GetBitcoinReceiversId with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    id : Text
  where
    signatory requestor

template GetBitcoinReceiversIdResponse with
    requestor : Party
    requestId : ContractId GetBitcoinReceiversId
    body : GetBitcoinReceiversIdResponseBody
  where
    signatory requestor

data GetBitcoinReceiversIdResponseBody
    = GetBitcoinReceiversIdResponseBody_200 BitcoinReceiver
    | GetBitcoinReceiversIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a usage record for a specified
--   subscription item and date, and fills it with a
--   quantity.</p> <p>Usage records provide
--   <code>quantity</code> information that Stripe uses to
--   track how much a customer is using your service. With
--   usage information and the pricing model set up by the
--   <a
--   href="https://stripe.com/docs/billing/subscriptions/metered-billing">metered
--   billing</a> plan, Stripe helps you send accurate
--   invoices to your customers.</p> <p>The default
--   calculation for usage is to add up all the
--   <code>quantity</code> values of the usage records
--   within a billing period. You can change this default
--   behavior with the billing plan’s
--   <code>aggregate_usage</code> <a
--   href="/docs/api/plans/create#create_plan-aggregate_usage">parameter</a>.
--   When there is more than one usage record with the
--   same timestamp, Stripe adds the <code>quantity</code>
--   values together. In most cases, this is the desired
--   resolution, however, you can change this behavior
--   with the <code>action</code> parameter.</p> <p>The
--   default pricing model for metered billing is <a
--   href="/docs/api/plans/object#plan_object-billing_scheme">per-unit
--   pricing</a>. For finer granularity, you can configure
--   metered billing to have a <a
--   href="https://stripe.com/docs/billing/subscriptions/tiers">tiered
--   pricing</a> model.</p>
template PostSubscriptionItemsSubscriptionItemUsageRecords with
    requestor : Party
    subscriptionItem : Text
    payload : PostSubscriptionItemsSubscriptionItemUsageRecordsPayload
  where
    signatory requestor

template PostSubscriptionItemsSubscriptionItemUsageRecordsResponse with
    requestor : Party
    requestId : ContractId PostSubscriptionItemsSubscriptionItemUsageRecords
    body : PostSubscriptionItemsSubscriptionItemUsageRecordsResponseBody
  where
    signatory requestor

data PostSubscriptionItemsSubscriptionItemUsageRecordsResponseBody
    = PostSubscriptionItemsSubscriptionItemUsageRecordsResponseBody_200 UsageRecord
    | PostSubscriptionItemsSubscriptionItemUsageRecordsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Verify a specified bank account for a given
--   customer.</p>
template PostCustomersCustomerSourcesIdVerify with
    requestor : Party
    customer : Text
    id : Text
    payload : PostCustomersCustomerSourcesIdVerifyPayload
  where
    signatory requestor

template PostCustomersCustomerSourcesIdVerifyResponse with
    requestor : Party
    requestId : ContractId PostCustomersCustomerSourcesIdVerify
    body : PostCustomersCustomerSourcesIdVerifyResponseBody
  where
    signatory requestor

data PostCustomersCustomerSourcesIdVerifyResponseBody
    = PostCustomersCustomerSourcesIdVerifyResponseBody_200 BankAccount
    | PostCustomersCustomerSourcesIdVerifyResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of early fraud warnings.</p>
template GetRadarEarlyFraudWarnings with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    charge : Text
        -- ^ Only return early fraud warnings for the charge
        --   specified by this charge ID.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetRadarEarlyFraudWarningsResponse with
    requestor : Party
    requestId : ContractId GetRadarEarlyFraudWarnings
    body : GetRadarEarlyFraudWarningsResponseBody
  where
    signatory requestor

data GetRadarEarlyFraudWarningsResponseBody
    = GetRadarEarlyFraudWarningsResponseBody_200 GetRadarEarlyFraudWarningsResponseBody200
    | GetRadarEarlyFraudWarningsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of transactions that have
--   contributed to the Stripe account balance (e.g.,
--   charges, transfers, and so forth). The transactions
--   are returned in sorted order, with the most recent
--   transactions appearing first.</p> <p>Note that this
--   endpoint was previously called “Balance history” and
--   used the path <code>/v1/balance/history</code>.</p>
template GetBalanceTransactions with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    availableOn : Int
    created : Int
    currency : Text
        -- ^ Only return transactions in a certain currency.
        --   Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    payout : Text
        -- ^ For automatic Stripe payouts only, only returns
        --   transactions that were paid out on the specified
        --   payout ID.
    source : Text
        -- ^ Only returns the original transaction.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
    type_ : Text
        -- ^ Only returns transactions of the given type. One of:
        --   `charge`, `refund`, `adjustment`, `application_fee`,
        --   `application_fee_refund`, `transfer`, `payment`,
        --   `payout`, `payout_failure`, `stripe_fee`, or
        --   `network_cost`.
  where
    signatory requestor

template GetBalanceTransactionsResponse with
    requestor : Party
    requestId : ContractId GetBalanceTransactions
    body : GetBalanceTransactionsResponseBody
  where
    signatory requestor

data GetBalanceTransactionsResponseBody
    = GetBalanceTransactionsResponseBody_200 GetBalanceTransactionsResponseBody200
    | GetBalanceTransactionsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the credit note object with the given
--   identifier.</p>
template GetCreditNotesId with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    id : Text
  where
    signatory requestor

template GetCreditNotesIdResponse with
    requestor : Party
    requestId : ContractId GetCreditNotesId
    body : GetCreditNotesIdResponseBody
  where
    signatory requestor

data GetCreditNotesIdResponseBody
    = GetCreditNotesIdResponseBody_200 CreditNote
    | GetCreditNotesIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates an existing credit note.</p>
template PostCreditNotesId with
    requestor : Party
    id : Text
    payload : PostCreditNotesIdPayload
  where
    signatory requestor

template PostCreditNotesIdResponse with
    requestor : Party
    requestId : ContractId PostCreditNotesId
    body : PostCreditNotesIdResponseBody
  where
    signatory requestor

data PostCreditNotesIdResponseBody
    = PostCreditNotesIdResponseBody_200 CreditNote
    | PostCreditNotesIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of your subscription items for a
--   given subscription.</p>
template GetSubscriptionItems with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
    subscription : Text
        -- ^ The ID of the subscription whose items will be
        --   retrieved.
  where
    signatory requestor

template GetSubscriptionItemsResponse with
    requestor : Party
    requestId : ContractId GetSubscriptionItems
    body : GetSubscriptionItemsResponseBody
  where
    signatory requestor

data GetSubscriptionItemsResponseBody
    = GetSubscriptionItemsResponseBody_200 GetSubscriptionItemsResponseBody200
    | GetSubscriptionItemsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Adds a new item to an existing subscription. No
--   existing items will be changed or replaced.</p>
template PostSubscriptionItems with
    requestor : Party
    payload : PostSubscriptionItemsPayload
  where
    signatory requestor

template PostSubscriptionItemsResponse with
    requestor : Party
    requestId : ContractId PostSubscriptionItems
    body : PostSubscriptionItemsResponseBody
  where
    signatory requestor

data PostSubscriptionItemsResponseBody
    = PostSubscriptionItemsResponseBody_200 SubscriptionItem
    | PostSubscriptionItemsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of charges you’ve previously
--   created. The charges are returned in sorted order,
--   with the most recent charges appearing first.</p>
template GetCharges with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    created : Int
    customer : Text
        -- ^ Only return charges for the customer specified by
        --   this customer ID.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    paymentIntent : Text
        -- ^ Only return charges that were created by the
        --   PaymentIntent specified by this PaymentIntent ID.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
    transferGroup : Text
        -- ^ Only return charges for this transfer group.
  where
    signatory requestor

template GetChargesResponse with
    requestor : Party
    requestId : ContractId GetCharges
    body : GetChargesResponseBody
  where
    signatory requestor

data GetChargesResponseBody
    = GetChargesResponseBody_200 GetChargesResponseBody200
    | GetChargesResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>To charge a credit card or other payment source,
--   you create a <code>Charge</code> object. If your API
--   key is in test mode, the supplied payment source
--   (e.g., card) won’t actually be charged, although
--   everything else will occur as if in live mode.
--   (Stripe assumes that the charge would have completed
--   successfully).</p>
template PostCharges with
    requestor : Party
    payload : PostChargesPayload
  where
    signatory requestor

template PostChargesResponse with
    requestor : Party
    requestId : ContractId PostCharges
    body : PostChargesResponseBody
  where
    signatory requestor

data PostChargesResponseBody
    = PostChargesResponseBody_200 Charge
    | PostChargesResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the exchange rates from the given
--   currency to every supported currency.</p>
template GetExchangeRatesCurrency with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    currency : Text
  where
    signatory requestor

template GetExchangeRatesCurrencyResponse with
    requestor : Party
    requestId : ContractId GetExchangeRatesCurrency
    body : GetExchangeRatesCurrencyResponseBody
  where
    signatory requestor

data GetExchangeRatesCurrencyResponseBody
    = GetExchangeRatesCurrencyResponseBody_200 ExchangeRate
    | GetExchangeRatesCurrencyResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Invalidates a short-lived API key for a given
--   resource.</p>
template DeleteEphemeralKeysKey with
    requestor : Party
    key_ : Text
    payload : DeleteEphemeralKeysKeyPayload
  where
    signatory requestor

template DeleteEphemeralKeysKeyResponse with
    requestor : Party
    requestId : ContractId DeleteEphemeralKeysKey
    body : DeleteEphemeralKeysKeyResponseBody
  where
    signatory requestor

data DeleteEphemeralKeysKeyResponseBody
    = DeleteEphemeralKeysKeyResponseBody_200 EphemeralKey
    | DeleteEphemeralKeysKeyResponseBody_default_ Error
    deriving (Eq, Ord, Show)

template PostChargesChargeDisputeClose with
    requestor : Party
    charge : Text
    payload : PostChargesChargeDisputeClosePayload
  where
    signatory requestor

template PostChargesChargeDisputeCloseResponse with
    requestor : Party
    requestId : ContractId PostChargesChargeDisputeClose
    body : PostChargesChargeDisputeCloseResponseBody
  where
    signatory requestor

data PostChargesChargeDisputeCloseResponseBody
    = PostChargesChargeDisputeCloseResponseBody_200 Dispute
    | PostChargesChargeDisputeCloseResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Invalidates all sessions for a light account, for
--   a platform to use during platform logout.</p>
--   <p><strong>You may only log out <a
--   href="/docs/connect/express-accounts">Express
--   accounts</a> connected to your platform</strong>.</p>
template PutAccountsAccountLogout with
    requestor : Party
    account : Text
    payload : PutAccountsAccountLogoutPayload
  where
    signatory requestor

template PutAccountsAccountLogoutResponse with
    requestor : Party
    requestId : ContractId PutAccountsAccountLogout
    body : PutAccountsAccountLogoutResponseBody
  where
    signatory requestor

data PutAccountsAccountLogoutResponseBody
    = PutAccountsAccountLogoutResponseBody_200 LightAccountLogout
    | PutAccountsAccountLogoutResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>For the specified subscription item, returns a
--   list of summary objects. Each object in the list
--   provides usage information that’s been summarized
--   from multiple usage records and over a subscription
--   billing period (e.g., 15 usage records in the billing
--   plan’s month of September).</p> <p>The list is sorted
--   in reverse-chronological order (newest first). The
--   first list item represents the most current usage
--   period that hasn’t ended yet. Since new usage records
--   can still be added, the returned summary information
--   for the subscription item’s ID should be seen as
--   unstable until the subscription billing period
--   ends.</p>
template GetSubscriptionItemsSubscriptionItemUsageRecordSummaries with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
    subscriptionItem : Text
  where
    signatory requestor

template GetSubscriptionItemsSubscriptionItemUsageRecordSummariesResponse with
    requestor : Party
    requestId : ContractId GetSubscriptionItemsSubscriptionItemUsageRecordSummaries
    body : GetSubscriptionItemsSubscriptionItemUsageRecordSummariesResponseBody
  where
    signatory requestor

data GetSubscriptionItemsSubscriptionItemUsageRecordSummariesResponseBody
    = GetSubscriptionItemsSubscriptionItemUsageRecordSummariesResponseBody_200 GetSubscriptionItemsSubscriptionItemUsageRecordSummariesResponseBody200
    | GetSubscriptionItemsSubscriptionItemUsageRecordSummariesResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the plan with the given ID.</p>
template GetPlansPlan with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    plan : Text
  where
    signatory requestor

template GetPlansPlanResponse with
    requestor : Party
    requestId : ContractId GetPlansPlan
    body : GetPlansPlanResponseBody
  where
    signatory requestor

data GetPlansPlanResponseBody
    = GetPlansPlanResponseBody_200 Plan
    | GetPlansPlanResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the specified plan by setting the values
--   of the parameters passed. Any parameters not provided
--   are left unchanged. By design, you cannot change a
--   plan’s ID, amount, currency, or billing cycle.</p>
template PostPlansPlan with
    requestor : Party
    plan : Text
    payload : PostPlansPlanPayload
  where
    signatory requestor

template PostPlansPlanResponse with
    requestor : Party
    requestId : ContractId PostPlansPlan
    body : PostPlansPlanResponseBody
  where
    signatory requestor

data PostPlansPlanResponseBody
    = PostPlansPlanResponseBody_200 Plan
    | PostPlansPlanResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Deleting plans means new subscribers can’t be
--   added. Existing subscribers aren’t affected.</p>
template DeletePlansPlan with
    requestor : Party
    plan : Text
  where
    signatory requestor

template DeletePlansPlanResponse with
    requestor : Party
    requestId : ContractId DeletePlansPlan
    body : DeletePlansPlanResponseBody
  where
    signatory requestor

data DeletePlansPlanResponseBody
    = DeletePlansPlanResponseBody_200 DeletedPlan
    | DeletePlansPlanResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Invalidates all sessions for a light account, for
--   a platform to use during platform logout.</p>
--   <p><strong>You may only log out <a
--   href="/docs/connect/express-accounts">Express
--   accounts</a> connected to your platform</strong>.</p>
template PutAccountLogout with
    requestor : Party
    payload : PutAccountLogoutPayload
  where
    signatory requestor

template PutAccountLogoutResponse with
    requestor : Party
    requestId : ContractId PutAccountLogout
    body : PutAccountLogoutResponseBody
  where
    signatory requestor

data PutAccountLogoutResponseBody
    = PutAccountLogoutResponseBody_200 LightAccountLogout
    | PutAccountLogoutResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the details of an scheduled query
--   run.</p>
template GetSigmaScheduledQueryRunsScheduledQueryRun with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    scheduledQueryRun : Text
  where
    signatory requestor

template GetSigmaScheduledQueryRunsScheduledQueryRunResponse with
    requestor : Party
    requestId : ContractId GetSigmaScheduledQueryRunsScheduledQueryRun
    body : GetSigmaScheduledQueryRunsScheduledQueryRunResponseBody
  where
    signatory requestor

data GetSigmaScheduledQueryRunsScheduledQueryRunResponseBody
    = GetSigmaScheduledQueryRunsScheduledQueryRunResponseBody_200 ScheduledQueryRun
    | GetSigmaScheduledQueryRunsScheduledQueryRunResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the subscription with the given ID.</p>
template GetSubscriptionsSubscriptionExposedId with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    subscriptionExposedId : Text
  where
    signatory requestor

template GetSubscriptionsSubscriptionExposedIdResponse with
    requestor : Party
    requestId : ContractId GetSubscriptionsSubscriptionExposedId
    body : GetSubscriptionsSubscriptionExposedIdResponseBody
  where
    signatory requestor

data GetSubscriptionsSubscriptionExposedIdResponseBody
    = GetSubscriptionsSubscriptionExposedIdResponseBody_200 Subscription
    | GetSubscriptionsSubscriptionExposedIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates an existing subscription on a customer to
--   match the specified parameters. When changing plans
--   or quantities, we will optionally prorate the price
--   we charge next month to make up for any price
--   changes. To preview how the proration will be
--   calculated, use the <a
--   href="#upcoming_invoice">upcoming invoice</a>
--   endpoint.</p>
template PostSubscriptionsSubscriptionExposedId with
    requestor : Party
    subscriptionExposedId : Text
    payload : PostSubscriptionsSubscriptionExposedIdPayload
  where
    signatory requestor

template PostSubscriptionsSubscriptionExposedIdResponse with
    requestor : Party
    requestId : ContractId PostSubscriptionsSubscriptionExposedId
    body : PostSubscriptionsSubscriptionExposedIdResponseBody
  where
    signatory requestor

data PostSubscriptionsSubscriptionExposedIdResponseBody
    = PostSubscriptionsSubscriptionExposedIdResponseBody_200 Subscription
    | PostSubscriptionsSubscriptionExposedIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Cancels a customer’s subscription immediately. The
--   customer will not be charged again for the
--   subscription.</p> <p>Note, however, that any pending
--   invoice items that you’ve created will still be
--   charged for at the end of the period, unless manually
--   <a href="#delete_invoiceitem">deleted</a>. If you’ve
--   set the subscription to cancel at the end of the
--   period, any pending prorations will also be left in
--   place and collected at the end of the period. But if
--   the subscription is set to cancel immediately,
--   pending prorations will be removed.</p> <p>By
--   default, upon subscription cancellation, Stripe will
--   stop automatic collection of all finalized invoices
--   for the customer. This is intended to prevent
--   unexpected payment attempts after the customer has
--   canceled a subscription. However, you can resume
--   automatic collection of the invoices manually after
--   subscription cancellation to have us proceed. Or, you
--   could check for unpaid invoices before allowing the
--   customer to cancel the subscription at all.</p>
template DeleteSubscriptionsSubscriptionExposedId with
    requestor : Party
    subscriptionExposedId : Text
    payload : DeleteSubscriptionsSubscriptionExposedIdPayload
  where
    signatory requestor

template DeleteSubscriptionsSubscriptionExposedIdResponse with
    requestor : Party
    requestId : ContractId DeleteSubscriptionsSubscriptionExposedId
    body : DeleteSubscriptionsSubscriptionExposedIdResponseBody
  where
    signatory requestor

data DeleteSubscriptionsSubscriptionExposedIdResponseBody
    = DeleteSubscriptionsSubscriptionExposedIdResponseBody_200 Subscription
    | DeleteSubscriptionsSubscriptionExposedIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of Issuing <code>Transaction</code>
--   objects. The objects are sorted in descending order
--   by creation date, with the most recently created
--   object appearing first.</p>
template GetIssuingTransactions with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    card : Text
        -- ^ Only return transactions that belong to the given
        --   card.
    cardholder : Text
        -- ^ Only return transactions that belong to the given
        --   cardholder.
    created : Int
        -- ^ Only return transactions that were created during the
        --   given date interval.
    dispute : Text
        -- ^ Only return transactions that originate from a given
        --   dispute.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    settlement : Text
        -- ^ Only return transactions that are associated with the
        --   given settlement.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetIssuingTransactionsResponse with
    requestor : Party
    requestId : ContractId GetIssuingTransactions
    body : GetIssuingTransactionsResponseBody
  where
    signatory requestor

data GetIssuingTransactionsResponseBody
    = GetIssuingTransactionsResponseBody_200 GetIssuingTransactionsResponseBody200
    | GetIssuingTransactionsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of your receivers. Receivers are
--   returned sorted by creation date, with the most
--   recently created receivers appearing first.</p>
template GetBitcoinReceivers with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    active : Bool
        -- ^ Filter for active receivers.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    filled : Bool
        -- ^ Filter for filled receivers.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
    uncapturedFunds : Bool
        -- ^ Filter for receivers with uncaptured funds.
  where
    signatory requestor

template GetBitcoinReceiversResponse with
    requestor : Party
    requestId : ContractId GetBitcoinReceivers
    body : GetBitcoinReceiversResponseBody
  where
    signatory requestor

data GetBitcoinReceiversResponseBody
    = GetBitcoinReceiversResponseBody_200 GetBitcoinReceiversResponseBody200
    | GetBitcoinReceiversResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the details of an existing transfer.
--   Supply the unique transfer ID from either a transfer
--   creation request or the transfer list, and Stripe
--   will return the corresponding transfer
--   information.</p>
template GetTransfersTransfer with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    transfer : Text
  where
    signatory requestor

template GetTransfersTransferResponse with
    requestor : Party
    requestId : ContractId GetTransfersTransfer
    body : GetTransfersTransferResponseBody
  where
    signatory requestor

data GetTransfersTransferResponseBody
    = GetTransfersTransferResponseBody_200 Transfer
    | GetTransfersTransferResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the specified transfer by setting the
--   values of the parameters passed. Any parameters not
--   provided will be left unchanged.</p> <p>This request
--   accepts only metadata as an argument.</p>
template PostTransfersTransfer with
    requestor : Party
    transfer : Text
    payload : PostTransfersTransferPayload
  where
    signatory requestor

template PostTransfersTransferResponse with
    requestor : Party
    requestId : ContractId PostTransfersTransfer
    body : PostTransfersTransferResponseBody
  where
    signatory requestor

data PostTransfersTransferResponseBody
    = PostTransfersTransferResponseBody_200 Transfer
    | PostTransfersTransferResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the details of an existing order return.
--   Supply the unique order ID from either an order
--   return creation request or the order return list, and
--   Stripe will return the corresponding order
--   information.</p>
template GetOrderReturnsId with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    id : Text
  where
    signatory requestor

template GetOrderReturnsIdResponse with
    requestor : Party
    requestId : ContractId GetOrderReturnsId
    body : GetOrderReturnsIdResponseBody
  where
    signatory requestor

data GetOrderReturnsIdResponseBody
    = GetOrderReturnsIdResponseBody_200 OrderReturn
    | GetOrderReturnsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the details of an event. Supply the
--   unique identifier of the event, which you might have
--   received in a webhook.</p>
template GetEventsId with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    id : Text
  where
    signatory requestor

template GetEventsIdResponse with
    requestor : Party
    requestId : ContractId GetEventsId
    body : GetEventsIdResponseBody
  where
    signatory requestor

data GetEventsIdResponseBody
    = GetEventsIdResponseBody_200 Event
    | GetEventsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of <code>Review</code> objects that
--   have <code>open</code> set to <code>true</code>. The
--   objects are sorted in descending order by creation
--   date, with the most recently created object appearing
--   first.</p>
template GetReviews with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    created : Int
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetReviewsResponse with
    requestor : Party
    requestId : ContractId GetReviews
    body : GetReviewsResponseBody
  where
    signatory requestor

data GetReviewsResponseBody
    = GetReviewsResponseBody_200 GetReviewsResponseBody200
    | GetReviewsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Get a preview of a credit note without creating
--   it.</p>
template GetCreditNotesPreview with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    amount : Int
        -- ^ The integer amount in **%s** representing the total
        --   amount of the credit note.
    creditAmount : Int
        -- ^ The integer amount in **%s** representing the amount
        --   to credit the customer's balance, which will be
        --   automatically applied to their next invoice.
    invoice : Text
        -- ^ ID of the invoice.
    lines : [Text]
        -- ^ Line items that make up the credit note.
    memo : Text
        -- ^ The credit note's memo appears on the credit note
        --   PDF.
    metadata : Text
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    outOfBandAmount : Int
        -- ^ The integer amount in **%s** representing the amount
        --   that is credited outside of Stripe.
    reason : Text
        -- ^ Reason for issuing this credit note, one of
        --   `duplicate`, `fraudulent`, `order_change`, or
        --   `product_unsatisfactory`
    refund : Text
        -- ^ ID of an existing refund to link this credit note to.
    refundAmount : Int
        -- ^ The integer amount in **%s** representing the amount
        --   to refund. If set, a refund will be created for the
        --   charge associated with the invoice.
  where
    signatory requestor

template GetCreditNotesPreviewResponse with
    requestor : Party
    requestId : ContractId GetCreditNotesPreview
    body : GetCreditNotesPreviewResponseBody
  where
    signatory requestor

data GetCreditNotesPreviewResponseBody
    = GetCreditNotesPreviewResponseBody_200 CreditNote
    | GetCreditNotesPreviewResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>List events, going back up to 30 days. Each event
--   data is rendered according to Stripe API version at
--   its creation time, specified in <a
--   href="/docs/api/events/object">event object</a>
--   <code>api_version</code> attribute (not according to
--   your current Stripe API version or
--   <code>Stripe-Version</code> header).</p>
template GetEvents with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    created : Int
    deliverySuccess : Bool
        -- ^ Filter events by whether all webhooks were
        --   successfully delivered. If false, events which are
        --   still pending or have failed all delivery attempts to
        --   a webhook endpoint will be returned.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
    type_ : Text
        -- ^ A string containing a specific event name, or group
        --   of events using * as a wildcard. The list will be
        --   filtered to include only events with a matching event
        --   property.
    types : [Text]
        -- ^ An array of up to 20 strings containing specific
        --   event names. The list will be filtered to include
        --   only events with a matching event property. You may
        --   pass either `type` or `types`, but not both.
  where
    signatory requestor

template GetEventsResponse with
    requestor : Party
    requestId : ContractId GetEvents
    body : GetEventsResponseBody
  where
    signatory requestor

data GetEventsResponseBody
    = GetEventsResponseBody_200 GetEventsResponseBody200
    | GetEventsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves an Issuing <code>Dispute</code>
--   object.</p>
template GetIssuingDisputesDispute with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    dispute : Text
  where
    signatory requestor

template GetIssuingDisputesDisputeResponse with
    requestor : Party
    requestId : ContractId GetIssuingDisputesDispute
    body : GetIssuingDisputesDisputeResponseBody
  where
    signatory requestor

data GetIssuingDisputesDisputeResponseBody
    = GetIssuingDisputesDisputeResponseBody_200 IssuingDispute
    | GetIssuingDisputesDisputeResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the specified Issuing <code>Dispute</code>
--   object by setting the values of the parameters
--   passed. Any parameters not provided will be left
--   unchanged.</p>
template PostIssuingDisputesDispute with
    requestor : Party
    dispute : Text
    payload : PostIssuingDisputesDisputePayload
  where
    signatory requestor

template PostIssuingDisputesDisputeResponse with
    requestor : Party
    requestId : ContractId PostIssuingDisputesDispute
    body : PostIssuingDisputesDisputeResponseBody
  where
    signatory requestor

data PostIssuingDisputesDisputeResponseBody
    = PostIssuingDisputesDisputeResponseBody_200 IssuingDispute
    | PostIssuingDisputesDisputeResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves a 3D Secure object.</p>
template Get3dSecureThreeDSecure with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    threeDSecure : Text
  where
    signatory requestor

template Get3dSecureThreeDSecureResponse with
    requestor : Party
    requestId : ContractId Get3dSecureThreeDSecure
    body : Get3dSecureThreeDSecureResponseBody
  where
    signatory requestor

data Get3dSecureThreeDSecureResponseBody
    = Get3dSecureThreeDSecureResponseBody_200 ThreeDSecure
    | Get3dSecureThreeDSecureResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of <code>Reader</code> objects.</p>
template GetTerminalReaders with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    deviceType : Text
        -- ^ Filters readers by device type
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    location : Text
        -- ^ A location ID to filter the response list to only
        --   readers at the specific location
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
    status : Text
        -- ^ A status filter to filter readers to only offline or
        --   online readers
  where
    signatory requestor

template GetTerminalReadersResponse with
    requestor : Party
    requestId : ContractId GetTerminalReaders
    body : GetTerminalReadersResponseBody
  where
    signatory requestor

data GetTerminalReadersResponseBody
    = GetTerminalReadersResponseBody_200 GetTerminalReadersResponseBody200
    | GetTerminalReadersResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a new <code>Reader</code> object.</p>
template PostTerminalReaders with
    requestor : Party
    payload : PostTerminalReadersPayload
  where
    signatory requestor

template PostTerminalReadersResponse with
    requestor : Party
    requestId : ContractId PostTerminalReaders
    body : PostTerminalReadersResponseBody
  where
    signatory requestor

data PostTerminalReadersResponseBody
    = PostTerminalReadersResponseBody_200 TerminalReader
    | PostTerminalReadersResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of <code>ValueList</code> objects.
--   The objects are sorted in descending order by
--   creation date, with the most recently created object
--   appearing first.</p>
template GetRadarValueLists with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    alias : Text
        -- ^ The alias used to reference the value list when
        --   writing rules.
    contains : Text
        -- ^ A value contained within a value list - returns all
        --   value lists containing this value.
    created : Int
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetRadarValueListsResponse with
    requestor : Party
    requestId : ContractId GetRadarValueLists
    body : GetRadarValueListsResponseBody
  where
    signatory requestor

data GetRadarValueListsResponseBody
    = GetRadarValueListsResponseBody_200 GetRadarValueListsResponseBody200
    | GetRadarValueListsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a new <code>ValueList</code> object, which
--   can then be referenced in rules.</p>
template PostRadarValueLists with
    requestor : Party
    payload : PostRadarValueListsPayload
  where
    signatory requestor

template PostRadarValueListsResponse with
    requestor : Party
    requestId : ContractId PostRadarValueLists
    body : PostRadarValueListsResponseBody
  where
    signatory requestor

data PostRadarValueListsResponseBody
    = PostRadarValueListsResponseBody_200 RadarValueList
    | PostRadarValueListsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves a specific transaction that updated the
--   customer’s <a
--   href="/docs/api/customers/object#customer_object-balance"><code>balance</code></a>.</p>
template GetCustomersCustomerBalanceTransactionsTransaction with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    customer : Text
    transaction : Text
  where
    signatory requestor

template GetCustomersCustomerBalanceTransactionsTransactionResponse with
    requestor : Party
    requestId : ContractId GetCustomersCustomerBalanceTransactionsTransaction
    body : GetCustomersCustomerBalanceTransactionsTransactionResponseBody
  where
    signatory requestor

data GetCustomersCustomerBalanceTransactionsTransactionResponseBody
    = GetCustomersCustomerBalanceTransactionsTransactionResponseBody_200 CustomerBalanceTransaction
    | GetCustomersCustomerBalanceTransactionsTransactionResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Most customer balance transaction fields are
--   immutable, but you may update its
--   <code>description</code> and
--   <code>metadata</code>.</p>
template PostCustomersCustomerBalanceTransactionsTransaction with
    requestor : Party
    customer : Text
    transaction : Text
    payload : PostCustomersCustomerBalanceTransactionsTransactionPayload
  where
    signatory requestor

template PostCustomersCustomerBalanceTransactionsTransactionResponse with
    requestor : Party
    requestId : ContractId PostCustomersCustomerBalanceTransactionsTransaction
    body : PostCustomersCustomerBalanceTransactionsTransactionResponseBody
  where
    signatory requestor

data PostCustomersCustomerBalanceTransactionsTransactionResponseBody
    = PostCustomersCustomerBalanceTransactionsTransactionResponseBody_200 CustomerBalanceTransaction
    | PostCustomersCustomerBalanceTransactionsTransactionResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of Report Runs, with the most
--   recent appearing first. (Requires a <a
--   href="https://stripe.com/docs/keys#test-live-modes">live-mode
--   API key</a>.)</p>
template GetReportingReportRuns with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    created : Int
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetReportingReportRunsResponse with
    requestor : Party
    requestId : ContractId GetReportingReportRuns
    body : GetReportingReportRunsResponseBody
  where
    signatory requestor

data GetReportingReportRunsResponseBody
    = GetReportingReportRunsResponseBody_200 GetReportingReportRunsResponseBody200
    | GetReportingReportRunsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a new object and begin running the report.
--   (Requires a <a
--   href="https://stripe.com/docs/keys#test-live-modes">live-mode
--   API key</a>.)</p>
template PostReportingReportRuns with
    requestor : Party
    payload : PostReportingReportRunsPayload
  where
    signatory requestor

template PostReportingReportRunsResponse with
    requestor : Party
    requestId : ContractId PostReportingReportRuns
    body : PostReportingReportRunsResponseBody
  where
    signatory requestor

data PostReportingReportRunsResponseBody
    = PostReportingReportRunsResponseBody_200 ReportingReportRun
    | PostReportingReportRunsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of people associated with the
--   account’s legal entity. The people are returned
--   sorted by creation date, with the most recent people
--   appearing first.</p>
template GetAccountPersons with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    relationship : Text
        -- ^ Filters on the list of people returned based on the
        --   person's relationship to the account's company.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetAccountPersonsResponse with
    requestor : Party
    requestId : ContractId GetAccountPersons
    body : GetAccountPersonsResponseBody
  where
    signatory requestor

data GetAccountPersonsResponseBody
    = GetAccountPersonsResponseBody_200 GetAccountPersonsResponseBody200
    | GetAccountPersonsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a new person.</p>
template PostAccountPersons with
    requestor : Party
    payload : PostAccountPersonsPayload
  where
    signatory requestor

template PostAccountPersonsResponse with
    requestor : Party
    requestId : ContractId PostAccountPersons
    body : PostAccountPersonsResponseBody
  where
    signatory requestor

data PostAccountPersonsResponseBody
    = PostAccountPersonsResponseBody_200 Person
    | PostAccountPersonsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves an Issuing <code>Card</code> object.</p>
template GetIssuingCardsCard with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    card : Text
  where
    signatory requestor

template GetIssuingCardsCardResponse with
    requestor : Party
    requestId : ContractId GetIssuingCardsCard
    body : GetIssuingCardsCardResponseBody
  where
    signatory requestor

data GetIssuingCardsCardResponseBody
    = GetIssuingCardsCardResponseBody_200 IssuingCard
    | GetIssuingCardsCardResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the specified Issuing <code>Card</code>
--   object by setting the values of the parameters
--   passed. Any parameters not provided will be left
--   unchanged.</p>
template PostIssuingCardsCard with
    requestor : Party
    card : Text
    payload : PostIssuingCardsCardPayload
  where
    signatory requestor

template PostIssuingCardsCardResponse with
    requestor : Party
    requestId : ContractId PostIssuingCardsCard
    body : PostIssuingCardsCardResponseBody
  where
    signatory requestor

data PostIssuingCardsCardResponseBody
    = PostIssuingCardsCardResponseBody_200 IssuingCard
    | PostIssuingCardsCardResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>List external accounts for an account.</p>
template GetAccountsAccountExternalAccounts with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    account : Text
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetAccountsAccountExternalAccountsResponse with
    requestor : Party
    requestId : ContractId GetAccountsAccountExternalAccounts
    body : GetAccountsAccountExternalAccountsResponseBody
  where
    signatory requestor

data GetAccountsAccountExternalAccountsResponseBody
    = GetAccountsAccountExternalAccountsResponseBody_200 GetAccountsAccountExternalAccountsResponseBody200
    | GetAccountsAccountExternalAccountsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Create an external account for a given
--   account.</p>
template PostAccountsAccountExternalAccounts with
    requestor : Party
    account : Text
    payload : PostAccountsAccountExternalAccountsPayload
  where
    signatory requestor

template PostAccountsAccountExternalAccountsResponse with
    requestor : Party
    requestId : ContractId PostAccountsAccountExternalAccounts
    body : PostAccountsAccountExternalAccountsResponseBody
  where
    signatory requestor

data PostAccountsAccountExternalAccountsResponseBody
    = PostAccountsAccountExternalAccountsResponseBody_200 ExternalAccount
    | PostAccountsAccountExternalAccountsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of issuer fraud records.</p>
template GetIssuerFraudRecords with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    charge : Text
        -- ^ Only return issuer fraud records for the charge
        --   specified by this charge ID.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetIssuerFraudRecordsResponse with
    requestor : Party
    requestId : ContractId GetIssuerFraudRecords
    body : GetIssuerFraudRecordsResponseBody
  where
    signatory requestor

data GetIssuerFraudRecordsResponseBody
    = GetIssuerFraudRecordsResponseBody_200 GetIssuerFraudRecordsResponseBody200
    | GetIssuerFraudRecordsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves information about the specified Account
--   Capability.</p>
template GetAccountsAccountCapabilitiesCapability with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    account : Text
    capability : Text
  where
    signatory requestor

template GetAccountsAccountCapabilitiesCapabilityResponse with
    requestor : Party
    requestId : ContractId GetAccountsAccountCapabilitiesCapability
    body : GetAccountsAccountCapabilitiesCapabilityResponseBody
  where
    signatory requestor

data GetAccountsAccountCapabilitiesCapabilityResponseBody
    = GetAccountsAccountCapabilitiesCapabilityResponseBody_200 Capability
    | GetAccountsAccountCapabilitiesCapabilityResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates an existing Account Capability.</p>
template PostAccountsAccountCapabilitiesCapability with
    requestor : Party
    account : Text
    capability : Text
    payload : PostAccountsAccountCapabilitiesCapabilityPayload
  where
    signatory requestor

template PostAccountsAccountCapabilitiesCapabilityResponse with
    requestor : Party
    requestId : ContractId PostAccountsAccountCapabilitiesCapability
    body : PostAccountsAccountCapabilitiesCapabilityResponseBody
  where
    signatory requestor

data PostAccountsAccountCapabilitiesCapabilityResponseBody
    = PostAccountsAccountCapabilitiesCapabilityResponseBody_200 Capability
    | PostAccountsAccountCapabilitiesCapabilityResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the invoice item with the given ID.</p>
template GetInvoiceitemsInvoiceitem with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    invoiceitem : Text
  where
    signatory requestor

template GetInvoiceitemsInvoiceitemResponse with
    requestor : Party
    requestId : ContractId GetInvoiceitemsInvoiceitem
    body : GetInvoiceitemsInvoiceitemResponseBody
  where
    signatory requestor

data GetInvoiceitemsInvoiceitemResponseBody
    = GetInvoiceitemsInvoiceitemResponseBody_200 Invoiceitem
    | GetInvoiceitemsInvoiceitemResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the amount or description of an invoice
--   item on an upcoming invoice. Updating an invoice item
--   is only possible before the invoice it’s attached to
--   is closed.</p>
template PostInvoiceitemsInvoiceitem with
    requestor : Party
    invoiceitem : Text
    payload : PostInvoiceitemsInvoiceitemPayload
  where
    signatory requestor

template PostInvoiceitemsInvoiceitemResponse with
    requestor : Party
    requestId : ContractId PostInvoiceitemsInvoiceitem
    body : PostInvoiceitemsInvoiceitemResponseBody
  where
    signatory requestor

data PostInvoiceitemsInvoiceitemResponseBody
    = PostInvoiceitemsInvoiceitemResponseBody_200 Invoiceitem
    | PostInvoiceitemsInvoiceitemResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Deletes an invoice item, removing it from an
--   invoice. Deleting invoice items is only possible when
--   they’re not attached to invoices, or if it’s attached
--   to a draft invoice.</p>
template DeleteInvoiceitemsInvoiceitem with
    requestor : Party
    invoiceitem : Text
  where
    signatory requestor

template DeleteInvoiceitemsInvoiceitemResponse with
    requestor : Party
    requestId : ContractId DeleteInvoiceitemsInvoiceitem
    body : DeleteInvoiceitemsInvoiceitemResponseBody
  where
    signatory requestor

data DeleteInvoiceitemsInvoiceitemResponseBody
    = DeleteInvoiceitemsInvoiceitemResponseBody_200 DeletedInvoiceitem
    | DeleteInvoiceitemsInvoiceitemResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves a tax rate with the given ID</p>
template GetTaxRatesTaxRate with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    taxRate : Text
  where
    signatory requestor

template GetTaxRatesTaxRateResponse with
    requestor : Party
    requestId : ContractId GetTaxRatesTaxRate
    body : GetTaxRatesTaxRateResponseBody
  where
    signatory requestor

data GetTaxRatesTaxRateResponseBody
    = GetTaxRatesTaxRateResponseBody_200 TaxRate
    | GetTaxRatesTaxRateResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates an existing tax rate.</p>
template PostTaxRatesTaxRate with
    requestor : Party
    taxRate : Text
    payload : PostTaxRatesTaxRatePayload
  where
    signatory requestor

template PostTaxRatesTaxRateResponse with
    requestor : Party
    requestId : ContractId PostTaxRatesTaxRate
    body : PostTaxRatesTaxRateResponseBody
  where
    signatory requestor

data PostTaxRatesTaxRateResponseBody
    = PostTaxRatesTaxRateResponseBody_200 TaxRate
    | PostTaxRatesTaxRateResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of your recipients. The recipients
--   are returned sorted by creation date, with the most
--   recently created recipients appearing first.</p>
template GetRecipients with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    created : Int
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
    type_ : Text
    verified : Bool
        -- ^ Only return recipients that are verified or
        --   unverified.
  where
    signatory requestor

template GetRecipientsResponse with
    requestor : Party
    requestId : ContractId GetRecipients
    body : GetRecipientsResponseBody
  where
    signatory requestor

data GetRecipientsResponseBody
    = GetRecipientsResponseBody_200 GetRecipientsResponseBody200
    | GetRecipientsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a new <code>Recipient</code> object and
--   verifies the recipient’s identity. Also verifies the
--   recipient’s bank account information or debit card,
--   if either is provided.</p>
template PostRecipients with
    requestor : Party
    payload : PostRecipientsPayload
  where
    signatory requestor

template PostRecipientsResponse with
    requestor : Party
    requestId : ContractId PostRecipients
    body : PostRecipientsResponseBody
  where
    signatory requestor

data PostRecipientsResponseBody
    = PostRecipientsResponseBody_200 Recipient
    | PostRecipientsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a single-use login link for an Express
--   account to access their Stripe dashboard.</p>
--   <p><strong>You may only create login links for <a
--   href="/docs/connect/express-accounts">Express
--   accounts</a> connected to your platform</strong>.</p>
template PostAccountsAccountLoginLinks with
    requestor : Party
    account : Text
    payload : PostAccountsAccountLoginLinksPayload
  where
    signatory requestor

template PostAccountsAccountLoginLinksResponse with
    requestor : Party
    requestId : ContractId PostAccountsAccountLoginLinks
    body : PostAccountsAccountLoginLinksResponseBody
  where
    signatory requestor

data PostAccountsAccountLoginLinksResponseBody
    = PostAccountsAccountLoginLinksResponseBody_200 LoginLink
    | PostAccountsAccountLoginLinksResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the token with the given ID.</p>
template GetTokensToken with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    token : Text
  where
    signatory requestor

template GetTokensTokenResponse with
    requestor : Party
    requestId : ContractId GetTokensToken
    body : GetTokensTokenResponseBody
  where
    signatory requestor

data GetTokensTokenResponseBody
    = GetTokensTokenResponseBody_200 Token
    | GetTokensTokenResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Confirm that your customer intends to pay with
--   current or provided payment method. Upon
--   confirmation, the PaymentIntent will attempt to
--   initiate a payment.</p> <p>If the selected payment
--   method requires additional authentication steps, the
--   PaymentIntent will transition to the
--   <code>requires_action</code> status and suggest
--   additional actions via <code>next_action</code>. If
--   payment fails, the PaymentIntent will transition to
--   the <code>requires_payment_method</code> status. If
--   payment succeeds, the PaymentIntent will transition
--   to the <code>succeeded</code> status (or
--   <code>requires_capture</code>, if
--   <code>capture_method</code> is set to
--   <code>manual</code>).</p> <p>If the
--   <code>confirmation_method</code> is
--   <code>automatic</code>, payment may be attempted
--   using our <a
--   href="/docs/stripe-js/reference#stripe-handle-card-payment">client
--   SDKs</a> and the PaymentIntent’s <a
--   href="#payment_intent_object-client_secret">client_secret</a>.
--   After <code>next_action</code>s are handled by the
--   client, no additional confirmation is required to
--   complete the payment.</p> <p>If the
--   <code>confirmation_method</code> is
--   <code>manual</code>, all payment attempts must be
--   initiated using a secret key. If any actions are
--   required for the payment, the PaymentIntent will
--   return to the <code>requires_confirmation</code>
--   state after those actions are completed. Your server
--   needs to then explicitly re-confirm the PaymentIntent
--   to initiate the next payment attempt. Read the <a
--   href="/docs/payments/payment-intents/web-manual">expanded
--   documentation</a> to learn more about manual
--   confirmation.</p>
template PostPaymentIntentsIntentConfirm with
    requestor : Party
    intent : Text
    payload : PostPaymentIntentsIntentConfirmPayload
  where
    signatory requestor

template PostPaymentIntentsIntentConfirmResponse with
    requestor : Party
    requestId : ContractId PostPaymentIntentsIntentConfirm
    body : PostPaymentIntentsIntentConfirmResponseBody
  where
    signatory requestor

data PostPaymentIntentsIntentConfirmResponseBody
    = PostPaymentIntentsIntentConfirmResponseBody_200 PaymentIntent
    | PostPaymentIntentsIntentConfirmResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of transactions that have
--   contributed to the Stripe account balance (e.g.,
--   charges, transfers, and so forth). The transactions
--   are returned in sorted order, with the most recent
--   transactions appearing first.</p> <p>Note that this
--   endpoint was previously called “Balance history” and
--   used the path <code>/v1/balance/history</code>.</p>
template GetBalanceHistory with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    availableOn : Int
    created : Int
    currency : Text
        -- ^ Only return transactions in a certain currency.
        --   Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    payout : Text
        -- ^ For automatic Stripe payouts only, only returns
        --   transactions that were paid out on the specified
        --   payout ID.
    source : Text
        -- ^ Only returns the original transaction.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
    type_ : Text
        -- ^ Only returns transactions of the given type. One of:
        --   `charge`, `refund`, `adjustment`, `application_fee`,
        --   `application_fee_refund`, `transfer`, `payment`,
        --   `payout`, `payout_failure`, `stripe_fee`, or
        --   `network_cost`.
  where
    signatory requestor

template GetBalanceHistoryResponse with
    requestor : Party
    requestId : ContractId GetBalanceHistory
    body : GetBalanceHistoryResponseBody
  where
    signatory requestor

data GetBalanceHistoryResponseBody
    = GetBalanceHistoryResponseBody_200 GetBalanceHistoryResponseBody200
    | GetBalanceHistoryResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>List bitcoin transacitons for a given
--   receiver.</p>
template GetBitcoinReceiversReceiverTransactions with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    customer : Text
        -- ^ Only return transactions for the customer specified
        --   by this customer ID.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    receiver : Text
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetBitcoinReceiversReceiverTransactionsResponse with
    requestor : Party
    requestId : ContractId GetBitcoinReceiversReceiverTransactions
    body : GetBitcoinReceiversReceiverTransactionsResponseBody
  where
    signatory requestor

data GetBitcoinReceiversReceiverTransactionsResponseBody
    = GetBitcoinReceiversReceiverTransactionsResponseBody_200 GetBitcoinReceiversReceiverTransactionsResponseBody200
    | GetBitcoinReceiversReceiverTransactionsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves a <code>Review</code> object.</p>
template GetReviewsReview with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    review : Text
  where
    signatory requestor

template GetReviewsReviewResponse with
    requestor : Party
    requestId : ContractId GetReviewsReview
    body : GetReviewsReviewResponseBody
  where
    signatory requestor

data GetReviewsReviewResponseBody
    = GetReviewsReviewResponseBody_200 Review
    | GetReviewsReviewResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of PaymentMethods for a given
--   Customer</p>
template GetPaymentMethods with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    customer : Text
        -- ^ The ID of the customer whose PaymentMethods will be
        --   retrieved.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
    type_ : Text
        -- ^ A required filter on the list, based on the object
        --   `type` field.
  where
    signatory requestor

template GetPaymentMethodsResponse with
    requestor : Party
    requestId : ContractId GetPaymentMethods
    body : GetPaymentMethodsResponseBody
  where
    signatory requestor

data GetPaymentMethodsResponseBody
    = GetPaymentMethodsResponseBody_200 GetPaymentMethodsResponseBody200
    | GetPaymentMethodsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a PaymentMethod object. Read the <a
--   href="/docs/stripe-js/reference#stripe-create-payment-method">Stripe.js
--   reference</a> to learn how to create PaymentMethods
--   via Stripe.js.</p>
template PostPaymentMethods with
    requestor : Party
    payload : PostPaymentMethodsPayload
  where
    signatory requestor

template PostPaymentMethodsResponse with
    requestor : Party
    requestId : ContractId PostPaymentMethods
    body : PostPaymentMethodsResponseBody
  where
    signatory requestor

data PostPaymentMethodsResponseBody
    = PostPaymentMethodsResponseBody_200 PaymentMethod
    | PostPaymentMethodsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves a PaymentMethod object.</p>
template GetPaymentMethodsPaymentMethod with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    paymentMethod : Text
  where
    signatory requestor

template GetPaymentMethodsPaymentMethodResponse with
    requestor : Party
    requestId : ContractId GetPaymentMethodsPaymentMethod
    body : GetPaymentMethodsPaymentMethodResponseBody
  where
    signatory requestor

data GetPaymentMethodsPaymentMethodResponseBody
    = GetPaymentMethodsPaymentMethodResponseBody_200 PaymentMethod
    | GetPaymentMethodsPaymentMethodResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates a PaymentMethod object. A PaymentMethod
--   must be attached a customer to be updated.</p>
template PostPaymentMethodsPaymentMethod with
    requestor : Party
    paymentMethod : Text
    payload : PostPaymentMethodsPaymentMethodPayload
  where
    signatory requestor

template PostPaymentMethodsPaymentMethodResponse with
    requestor : Party
    requestId : ContractId PostPaymentMethodsPaymentMethod
    body : PostPaymentMethodsPaymentMethodResponseBody
  where
    signatory requestor

data PostPaymentMethodsPaymentMethodResponseBody
    = PostPaymentMethodsPaymentMethodResponseBody_200 PaymentMethod
    | PostPaymentMethodsPaymentMethodResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves an existing person.</p>
template GetAccountPeoplePerson with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    person : Text
  where
    signatory requestor

template GetAccountPeoplePersonResponse with
    requestor : Party
    requestId : ContractId GetAccountPeoplePerson
    body : GetAccountPeoplePersonResponseBody
  where
    signatory requestor

data GetAccountPeoplePersonResponseBody
    = GetAccountPeoplePersonResponseBody_200 Person
    | GetAccountPeoplePersonResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates an existing person.</p>
template PostAccountPeoplePerson with
    requestor : Party
    person : Text
    payload : PostAccountPeoplePersonPayload
  where
    signatory requestor

template PostAccountPeoplePersonResponse with
    requestor : Party
    requestId : ContractId PostAccountPeoplePerson
    body : PostAccountPeoplePersonResponseBody
  where
    signatory requestor

data PostAccountPeoplePersonResponseBody
    = PostAccountPeoplePersonResponseBody_200 Person
    | PostAccountPeoplePersonResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Deletes an existing person’s relationship to the
--   account’s legal entity. Any person with a
--   relationship for an account can be deleted through
--   the API, except if the person is the
--   <code>account_opener</code>. If your integration is
--   using the <code>executive</code> parameter, you
--   cannot delete the only verified
--   <code>executive</code> on file.</p>
template DeleteAccountPeoplePerson with
    requestor : Party
    person : Text
  where
    signatory requestor

template DeleteAccountPeoplePersonResponse with
    requestor : Party
    requestId : ContractId DeleteAccountPeoplePerson
    body : DeleteAccountPeoplePersonResponseBody
  where
    signatory requestor

data DeleteAccountPeoplePersonResponseBody
    = DeleteAccountPeoplePersonResponseBody_200 DeletedPerson
    | DeleteAccountPeoplePersonResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieve a specified external account for a given
--   account.</p>
template GetAccountBankAccountsId with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    id : Text
  where
    signatory requestor

template GetAccountBankAccountsIdResponse with
    requestor : Party
    requestId : ContractId GetAccountBankAccountsId
    body : GetAccountBankAccountsIdResponseBody
  where
    signatory requestor

data GetAccountBankAccountsIdResponseBody
    = GetAccountBankAccountsIdResponseBody_200 ExternalAccount
    | GetAccountBankAccountsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the metadata, account holder name, and
--   account holder type of a bank account belonging to a
--   <a href="/docs/connect/custom-accounts">Custom
--   account</a>, and optionally sets it as the default
--   for its currency. Other bank account details are not
--   editable by design.</p> <p>You can re-enable a
--   disabled bank account by performing an update call
--   without providing any arguments or changes.</p>
template PostAccountBankAccountsId with
    requestor : Party
    id : Text
    payload : PostAccountBankAccountsIdPayload
  where
    signatory requestor

template PostAccountBankAccountsIdResponse with
    requestor : Party
    requestId : ContractId PostAccountBankAccountsId
    body : PostAccountBankAccountsIdResponseBody
  where
    signatory requestor

data PostAccountBankAccountsIdResponseBody
    = PostAccountBankAccountsIdResponseBody_200 ExternalAccount
    | PostAccountBankAccountsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Delete a specified external account for a given
--   account.</p>
template DeleteAccountBankAccountsId with
    requestor : Party
    id : Text
  where
    signatory requestor

template DeleteAccountBankAccountsIdResponse with
    requestor : Party
    requestId : ContractId DeleteAccountBankAccountsId
    body : DeleteAccountBankAccountsIdResponseBody
  where
    signatory requestor

data DeleteAccountBankAccountsIdResponseBody
    = DeleteAccountBankAccountsIdResponseBody_200 DeletedExternalAccount
    | DeleteAccountBankAccountsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieve a dispute for a specified charge.</p>
template GetChargesChargeDispute with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    charge : Text
  where
    signatory requestor

template GetChargesChargeDisputeResponse with
    requestor : Party
    requestId : ContractId GetChargesChargeDispute
    body : GetChargesChargeDisputeResponseBody
  where
    signatory requestor

data GetChargesChargeDisputeResponseBody
    = GetChargesChargeDisputeResponseBody_200 Dispute
    | GetChargesChargeDisputeResponseBody_default_ Error
    deriving (Eq, Ord, Show)

template PostChargesChargeDispute with
    requestor : Party
    charge : Text
    payload : PostChargesChargeDisputePayload
  where
    signatory requestor

template PostChargesChargeDisputeResponse with
    requestor : Party
    requestId : ContractId PostChargesChargeDispute
    body : PostChargesChargeDisputeResponseBody
  where
    signatory requestor

data PostChargesChargeDisputeResponseBody
    = PostChargesChargeDisputeResponseBody_200 Dispute
    | PostChargesChargeDisputeResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves an Issuing <code>Settlement</code>
--   object.</p>
template GetIssuingSettlementsSettlement with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    settlement : Text
  where
    signatory requestor

template GetIssuingSettlementsSettlementResponse with
    requestor : Party
    requestId : ContractId GetIssuingSettlementsSettlement
    body : GetIssuingSettlementsSettlementResponseBody
  where
    signatory requestor

data GetIssuingSettlementsSettlementResponseBody
    = GetIssuingSettlementsSettlementResponseBody_200 IssuingSettlement
    | GetIssuingSettlementsSettlementResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the specified Issuing
--   <code>Settlement</code> object by setting the values
--   of the parameters passed. Any parameters not provided
--   will be left unchanged.</p>
template PostIssuingSettlementsSettlement with
    requestor : Party
    settlement : Text
    payload : PostIssuingSettlementsSettlementPayload
  where
    signatory requestor

template PostIssuingSettlementsSettlementResponse with
    requestor : Party
    requestId : ContractId PostIssuingSettlementsSettlement
    body : PostIssuingSettlementsSettlementResponseBody
  where
    signatory requestor

data PostIssuingSettlementsSettlementResponseBody
    = PostIssuingSettlementsSettlementResponseBody_200 IssuingSettlement
    | PostIssuingSettlementsSettlementResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>You can always see the 10 most recent cards
--   directly on a customer; this method lets you retrieve
--   details about a specific card stored on the
--   customer.</p>
template GetCustomersCustomerCardsId with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    customer : Text
    id : Text
  where
    signatory requestor

template GetCustomersCustomerCardsIdResponse with
    requestor : Party
    requestId : ContractId GetCustomersCustomerCardsId
    body : GetCustomersCustomerCardsIdResponseBody
  where
    signatory requestor

data GetCustomersCustomerCardsIdResponseBody
    = GetCustomersCustomerCardsIdResponseBody_200 Card
    | GetCustomersCustomerCardsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Update a specified source for a given
--   customer.</p>
template PostCustomersCustomerCardsId with
    requestor : Party
    customer : Text
    id : Text
    payload : PostCustomersCustomerCardsIdPayload
  where
    signatory requestor

template PostCustomersCustomerCardsIdResponse with
    requestor : Party
    requestId : ContractId PostCustomersCustomerCardsId
    body : PostCustomersCustomerCardsIdResponseBody
  where
    signatory requestor

data PostCustomersCustomerCardsIdResponseBody
    = PostCustomersCustomerCardsIdResponseBody_200 BankAccount
    | PostCustomersCustomerCardsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Delete a specified source for a given
--   customer.</p>
template DeleteCustomersCustomerCardsId with
    requestor : Party
    customer : Text
    id : Text
    payload : DeleteCustomersCustomerCardsIdPayload
  where
    signatory requestor

template DeleteCustomersCustomerCardsIdResponse with
    requestor : Party
    requestId : ContractId DeleteCustomersCustomerCardsId
    body : DeleteCustomersCustomerCardsIdResponseBody
  where
    signatory requestor

data DeleteCustomersCustomerCardsIdResponseBody
    = DeleteCustomersCustomerCardsIdResponseBody_200 DeletedPaymentSource
    | DeleteCustomersCustomerCardsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>With <a href="/docs/connect">Connect</a>, you may
--   flag accounts as suspicious.</p> <p>Test-mode Custom
--   and Express accounts can be rejected at any time.
--   Accounts created using live-mode keys may only be
--   rejected once all balances are zero.</p>
template PostAccountsAccountReject with
    requestor : Party
    account : Text
    payload : PostAccountsAccountRejectPayload
  where
    signatory requestor

template PostAccountsAccountRejectResponse with
    requestor : Party
    requestId : ContractId PostAccountsAccountReject
    body : PostAccountsAccountRejectResponseBody
  where
    signatory requestor

data PostAccountsAccountRejectResponseBody
    = PostAccountsAccountRejectResponseBody_200 Account
    | PostAccountsAccountRejectResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of <code>Location</code>
--   objects.</p>
template GetTerminalLocations with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetTerminalLocationsResponse with
    requestor : Party
    requestId : ContractId GetTerminalLocations
    body : GetTerminalLocationsResponseBody
  where
    signatory requestor

data GetTerminalLocationsResponseBody
    = GetTerminalLocationsResponseBody_200 GetTerminalLocationsResponseBody200
    | GetTerminalLocationsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a new <code>Location</code> object.</p>
template PostTerminalLocations with
    requestor : Party
    payload : PostTerminalLocationsPayload
  where
    signatory requestor

template PostTerminalLocationsResponse with
    requestor : Party
    requestId : ContractId PostTerminalLocations
    body : PostTerminalLocationsResponseBody
  where
    signatory requestor

data PostTerminalLocationsResponseBody
    = PostTerminalLocationsResponseBody_200 TerminalLocation
    | PostTerminalLocationsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Detaches a PaymentMethod object from a
--   Customer.</p>
template PostPaymentMethodsPaymentMethodDetach with
    requestor : Party
    paymentMethod : Text
    payload : PostPaymentMethodsPaymentMethodDetachPayload
  where
    signatory requestor

template PostPaymentMethodsPaymentMethodDetachResponse with
    requestor : Party
    requestId : ContractId PostPaymentMethodsPaymentMethodDetach
    body : PostPaymentMethodsPaymentMethodDetachResponseBody
  where
    signatory requestor

data PostPaymentMethodsPaymentMethodDetachResponseBody
    = PostPaymentMethodsPaymentMethodDetachResponseBody_200 PaymentMethod
    | PostPaymentMethodsPaymentMethodDetachResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of people associated with the
--   account’s legal entity. The people are returned
--   sorted by creation date, with the most recent people
--   appearing first.</p>
template GetAccountPeople with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    relationship : Text
        -- ^ Filters on the list of people returned based on the
        --   person's relationship to the account's company.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetAccountPeopleResponse with
    requestor : Party
    requestId : ContractId GetAccountPeople
    body : GetAccountPeopleResponseBody
  where
    signatory requestor

data GetAccountPeopleResponseBody
    = GetAccountPeopleResponseBody_200 GetAccountPeopleResponseBody200
    | GetAccountPeopleResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a new person.</p>
template PostAccountPeople with
    requestor : Party
    payload : PostAccountPeoplePayload
  where
    signatory requestor

template PostAccountPeopleResponse with
    requestor : Party
    requestId : ContractId PostAccountPeople
    body : PostAccountPeopleResponseBody
  where
    signatory requestor

data PostAccountPeopleResponseBody
    = PostAccountPeopleResponseBody_200 Person
    | PostAccountPeopleResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>When retrieving an invoice, you’ll get a
--   <strong>lines</strong> property containing the total
--   count of line items and the first handful of those
--   items. There is also a URL where you can retrieve the
--   full (paginated) list of line items.</p>
template GetInvoicesInvoiceLines with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    invoice : Text
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetInvoicesInvoiceLinesResponse with
    requestor : Party
    requestId : ContractId GetInvoicesInvoiceLines
    body : GetInvoicesInvoiceLinesResponseBody
  where
    signatory requestor

data GetInvoicesInvoiceLinesResponseBody
    = GetInvoicesInvoiceLinesResponseBody_200 GetInvoicesInvoiceLinesResponseBody200
    | GetInvoicesInvoiceLinesResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Approves a <code>Review</code> object, closing it
--   and removing it from the list of reviews.</p>
template PostReviewsReviewApprove with
    requestor : Party
    review : Text
    payload : PostReviewsReviewApprovePayload
  where
    signatory requestor

template PostReviewsReviewApproveResponse with
    requestor : Party
    requestId : ContractId PostReviewsReviewApprove
    body : PostReviewsReviewApproveResponseBody
  where
    signatory requestor

data PostReviewsReviewApproveResponseBody
    = PostReviewsReviewApproveResponseBody_200 Review
    | PostReviewsReviewApproveResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of the files that your account has
--   access to. The files are returned sorted by creation
--   date, with the most recently created files appearing
--   first.</p>
template GetFiles with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    created : Int
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    purpose : Text
        -- ^ The file purpose to filter queries by. If none is
        --   provided, files will not be filtered by purpose.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetFilesResponse with
    requestor : Party
    requestId : ContractId GetFiles
    body : GetFilesResponseBody
  where
    signatory requestor

data GetFilesResponseBody
    = GetFilesResponseBody_200 GetFilesResponseBody200
    | GetFilesResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>To upload a file to Stripe, you’ll need to send a
--   request of type <code>multipart/form-data</code>. The
--   request should contain the file you would like to
--   upload, as well as the parameters for creating a
--   file.</p> <p>All of Stripe’s officially supported
--   Client libraries should have support for sending
--   <code>multipart/form-data</code>.</p>
template PostFiles with
    requestor : Party
    payload : PostFilesPayload
  where
    signatory requestor

template PostFilesResponse with
    requestor : Party
    requestId : ContractId PostFiles
    body : PostFilesResponseBody
  where
    signatory requestor

data PostFilesResponseBody
    = PostFilesResponseBody_200 File
    | PostFilesResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>List apple pay domains.</p>
template GetApplePayDomains with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    domainName : Text
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetApplePayDomainsResponse with
    requestor : Party
    requestId : ContractId GetApplePayDomains
    body : GetApplePayDomainsResponseBody
  where
    signatory requestor

data GetApplePayDomainsResponseBody
    = GetApplePayDomainsResponseBody_200 GetApplePayDomainsResponseBody200
    | GetApplePayDomainsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Create an apple pay domain.</p>
template PostApplePayDomains with
    requestor : Party
    payload : PostApplePayDomainsPayload
  where
    signatory requestor

template PostApplePayDomainsResponse with
    requestor : Party
    requestId : ContractId PostApplePayDomains
    body : PostApplePayDomainsResponseBody
  where
    signatory requestor

data PostApplePayDomainsResponseBody
    = PostApplePayDomainsResponseBody_200 ApplePayDomain
    | PostApplePayDomainsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves an existing person.</p>
template GetAccountPersonsPerson with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    person : Text
  where
    signatory requestor

template GetAccountPersonsPersonResponse with
    requestor : Party
    requestId : ContractId GetAccountPersonsPerson
    body : GetAccountPersonsPersonResponseBody
  where
    signatory requestor

data GetAccountPersonsPersonResponseBody
    = GetAccountPersonsPersonResponseBody_200 Person
    | GetAccountPersonsPersonResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates an existing person.</p>
template PostAccountPersonsPerson with
    requestor : Party
    person : Text
    payload : PostAccountPersonsPersonPayload
  where
    signatory requestor

template PostAccountPersonsPersonResponse with
    requestor : Party
    requestId : ContractId PostAccountPersonsPerson
    body : PostAccountPersonsPersonResponseBody
  where
    signatory requestor

data PostAccountPersonsPersonResponseBody
    = PostAccountPersonsPersonResponseBody_200 Person
    | PostAccountPersonsPersonResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Deletes an existing person’s relationship to the
--   account’s legal entity. Any person with a
--   relationship for an account can be deleted through
--   the API, except if the person is the
--   <code>account_opener</code>. If your integration is
--   using the <code>executive</code> parameter, you
--   cannot delete the only verified
--   <code>executive</code> on file.</p>
template DeleteAccountPersonsPerson with
    requestor : Party
    person : Text
  where
    signatory requestor

template DeleteAccountPersonsPersonResponse with
    requestor : Party
    requestId : ContractId DeleteAccountPersonsPerson
    body : DeleteAccountPersonsPersonResponseBody
  where
    signatory requestor

data DeleteAccountPersonsPersonResponseBody
    = DeleteAccountPersonsPersonResponseBody_200 DeletedPerson
    | DeleteAccountPersonsPersonResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a single-use token that represents a bank
--   account’s details. This token can be used with any
--   API method in place of a bank account dictionary.
--   This token can be used only once, by attaching it to
--   a <a href="#accounts">Custom account</a>.</p>
template PostTokens with
    requestor : Party
    payload : PostTokensPayload
  where
    signatory requestor

template PostTokensResponse with
    requestor : Party
    requestId : ContractId PostTokens
    body : PostTokensResponseBody
  where
    signatory requestor

data PostTokensResponseBody
    = PostTokensResponseBody_200 Token
    | PostTokensResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>You can see a list of the cards belonging to a
--   customer. Note that the 10 most recent sources are
--   always available on the <code>Customer</code> object.
--   If you need more than those 10, you can use this API
--   method and the <code>limit</code> and
--   <code>starting_after</code> parameters to page
--   through additional cards.</p>
template GetCustomersCustomerCards with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    customer : Text
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetCustomersCustomerCardsResponse with
    requestor : Party
    requestId : ContractId GetCustomersCustomerCards
    body : GetCustomersCustomerCardsResponseBody
  where
    signatory requestor

data GetCustomersCustomerCardsResponseBody
    = GetCustomersCustomerCardsResponseBody_200 GetCustomersCustomerCardsResponseBody200
    | GetCustomersCustomerCardsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>When you create a new credit card, you must
--   specify a customer or recipient on which to create
--   it.</p> <p>If the card’s owner has no default card,
--   then the new card will become the default. However,
--   if the owner already has a default, then it will not
--   change. To change the default, you should <a
--   href="/docs/api#update_customer">update the
--   customer</a> to have a new
--   <code>default_source</code>.</p>
template PostCustomersCustomerCards with
    requestor : Party
    customer : Text
    payload : PostCustomersCustomerCardsPayload
  where
    signatory requestor

template PostCustomersCustomerCardsResponse with
    requestor : Party
    requestId : ContractId PostCustomersCustomerCards
    body : PostCustomersCustomerCardsResponseBody
  where
    signatory requestor

data PostCustomersCustomerCardsResponseBody
    = PostCustomersCustomerCardsResponseBody_200 PaymentSource
    | PostCustomersCustomerCardsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>A SetupIntent object can be canceled when it is in
--   one of these statuses:
--   <code>requires_payment_method</code>,
--   <code>requires_capture</code>,
--   <code>requires_confirmation</code>,
--   <code>requires_action</code>. </p> <p>Once canceled,
--   setup is abandoned and any operations on the
--   SetupIntent will fail with an error.</p>
template PostSetupIntentsIntentCancel with
    requestor : Party
    intent : Text
    payload : PostSetupIntentsIntentCancelPayload
  where
    signatory requestor

template PostSetupIntentsIntentCancelResponse with
    requestor : Party
    requestId : ContractId PostSetupIntentsIntentCancel
    body : PostSetupIntentsIntentCancelResponseBody
  where
    signatory requestor

data PostSetupIntentsIntentCancelResponseBody
    = PostSetupIntentsIntentCancelResponseBody_200 SetupIntent
    | PostSetupIntentsIntentCancelResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of accounts connected to your
--   platform via <a href="/docs/connect">Connect</a>. If
--   you’re not a platform, the list is empty.</p>
template GetAccounts with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    created : Int
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetAccountsResponse with
    requestor : Party
    requestId : ContractId GetAccounts
    body : GetAccountsResponseBody
  where
    signatory requestor

data GetAccountsResponseBody
    = GetAccountsResponseBody_200 GetAccountsResponseBody200
    | GetAccountsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>With <a href="/docs/connect">Connect</a>, you can
--   create Stripe accounts for your users. To do this,
--   you’ll first need to <a
--   href="https://dashboard.stripe.com/account/applications/settings">register
--   your platform</a>.</p> <p>For Standard accounts,
--   parameters other than <code>country</code>,
--   <code>email</code>, and <code>type</code> are used to
--   prefill the account application that we ask the
--   account holder to complete.</p>
template PostAccounts with
    requestor : Party
    payload : PostAccountsPayload
  where
    signatory requestor

template PostAccountsResponse with
    requestor : Party
    requestId : ContractId PostAccounts
    body : PostAccountsResponseBody
  where
    signatory requestor

data PostAccountsResponseBody
    = PostAccountsResponseBody_200 Account
    | PostAccountsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of your products. The products are
--   returned sorted by creation date, with the most
--   recently created products appearing first.</p>
template GetProducts with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    active : Bool
        -- ^ Only return products that are active or inactive
        --   (e.g., pass `false` to list all inactive products).
    created : Int
        -- ^ Only return products that were created during the
        --   given date interval.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    ids : [Text]
        -- ^ Only return products with the given IDs.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    shippable : Bool
        -- ^ Only return products that can be shipped (i.e.,
        --   physical, not digital products).
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
    type_ : Text
        -- ^ Only return products of this type.
    url : Text
        -- ^ Only return products with the given url.
  where
    signatory requestor

template GetProductsResponse with
    requestor : Party
    requestId : ContractId GetProducts
    body : GetProductsResponseBody
  where
    signatory requestor

data GetProductsResponseBody
    = GetProductsResponseBody_200 GetProductsResponseBody200
    | GetProductsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a new product object. To create a product
--   for use with orders, see <a
--   href="#create_product">Products</a>.</p>
template PostProducts with
    requestor : Party
    payload : PostProductsPayload
  where
    signatory requestor

template PostProductsResponse with
    requestor : Party
    requestId : ContractId PostProducts
    body : PostProductsResponseBody
  where
    signatory requestor

data PostProductsResponseBody
    = PostProductsResponseBody_200 Product
    | PostProductsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>A previously created payout can be canceled if it
--   has not yet been paid out. Funds will be refunded to
--   your available balance. You may not cancel automatic
--   Stripe payouts.</p>
template PostPayoutsPayoutCancel with
    requestor : Party
    payout : Text
    payload : PostPayoutsPayoutCancelPayload
  where
    signatory requestor

template PostPayoutsPayoutCancelResponse with
    requestor : Party
    requestId : ContractId PostPayoutsPayoutCancel
    body : PostPayoutsPayoutCancelResponseBody
  where
    signatory requestor

data PostPayoutsPayoutCancelResponseBody
    = PostPayoutsPayoutCancelResponseBody_200 Payout
    | PostPayoutsPayoutCancelResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieve a specified source for a given
--   customer.</p>
template GetCustomersCustomerSourcesId with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    customer : Text
    id : Text
  where
    signatory requestor

template GetCustomersCustomerSourcesIdResponse with
    requestor : Party
    requestId : ContractId GetCustomersCustomerSourcesId
    body : GetCustomersCustomerSourcesIdResponseBody
  where
    signatory requestor

data GetCustomersCustomerSourcesIdResponseBody
    = GetCustomersCustomerSourcesIdResponseBody_200 PaymentSource
    | GetCustomersCustomerSourcesIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Update a specified source for a given
--   customer.</p>
template PostCustomersCustomerSourcesId with
    requestor : Party
    customer : Text
    id : Text
    payload : PostCustomersCustomerSourcesIdPayload
  where
    signatory requestor

template PostCustomersCustomerSourcesIdResponse with
    requestor : Party
    requestId : ContractId PostCustomersCustomerSourcesId
    body : PostCustomersCustomerSourcesIdResponseBody
  where
    signatory requestor

data PostCustomersCustomerSourcesIdResponseBody
    = PostCustomersCustomerSourcesIdResponseBody_200 BankAccount
    | PostCustomersCustomerSourcesIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Delete a specified source for a given
--   customer.</p>
template DeleteCustomersCustomerSourcesId with
    requestor : Party
    customer : Text
    id : Text
    payload : DeleteCustomersCustomerSourcesIdPayload
  where
    signatory requestor

template DeleteCustomersCustomerSourcesIdResponse with
    requestor : Party
    requestId : ContractId DeleteCustomersCustomerSourcesId
    body : DeleteCustomersCustomerSourcesIdResponseBody
  where
    signatory requestor

data DeleteCustomersCustomerSourcesIdResponseBody
    = DeleteCustomersCustomerSourcesIdResponseBody_200 DeletedPaymentSource
    | DeleteCustomersCustomerSourcesIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Marking an invoice as uncollectible is useful for
--   keeping track of bad debts that can be written off
--   for accounting purposes.</p>
template PostInvoicesInvoiceMarkUncollectible with
    requestor : Party
    invoice : Text
    payload : PostInvoicesInvoiceMarkUncollectiblePayload
  where
    signatory requestor

template PostInvoicesInvoiceMarkUncollectibleResponse with
    requestor : Party
    requestId : ContractId PostInvoicesInvoiceMarkUncollectible
    body : PostInvoicesInvoiceMarkUncollectibleResponseBody
  where
    signatory requestor

data PostInvoicesInvoiceMarkUncollectibleResponseBody
    = PostInvoicesInvoiceMarkUncollectibleResponseBody_200 Invoice
    | PostInvoicesInvoiceMarkUncollectibleResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>List sources for a specified customer.</p>
template GetCustomersCustomerSources with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    customer : Text
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    object : Text
        -- ^ Filter sources according to a particular object type.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetCustomersCustomerSourcesResponse with
    requestor : Party
    requestId : ContractId GetCustomersCustomerSources
    body : GetCustomersCustomerSourcesResponseBody
  where
    signatory requestor

data GetCustomersCustomerSourcesResponseBody
    = GetCustomersCustomerSourcesResponseBody_200 GetCustomersCustomerSourcesResponseBody200
    | GetCustomersCustomerSourcesResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>When you create a new credit card, you must
--   specify a customer or recipient on which to create
--   it.</p> <p>If the card’s owner has no default card,
--   then the new card will become the default. However,
--   if the owner already has a default, then it will not
--   change. To change the default, you should <a
--   href="/docs/api#update_customer">update the
--   customer</a> to have a new
--   <code>default_source</code>.</p>
template PostCustomersCustomerSources with
    requestor : Party
    customer : Text
    payload : PostCustomersCustomerSourcesPayload
  where
    signatory requestor

template PostCustomersCustomerSourcesResponse with
    requestor : Party
    requestId : ContractId PostCustomersCustomerSources
    body : PostCustomersCustomerSourcesResponseBody
  where
    signatory requestor

data PostCustomersCustomerSourcesResponseBody
    = PostCustomersCustomerSourcesResponseBody_200 PaymentSource
    | PostCustomersCustomerSourcesResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the details of an existing recipient.
--   You need only supply the unique recipient identifier
--   that was returned upon recipient creation.</p>
template GetRecipientsId with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    id : Text
  where
    signatory requestor

template GetRecipientsIdResponse with
    requestor : Party
    requestId : ContractId GetRecipientsId
    body : GetRecipientsIdResponseBody
  where
    signatory requestor

data GetRecipientsIdResponseBody
    = GetRecipientsIdResponseBody_200 DeletedRecipient
    | GetRecipientsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the specified recipient by setting the
--   values of the parameters passed. Any parameters not
--   provided will be left unchanged.</p> <p>If you update
--   the name or tax ID, the identity verification will
--   automatically be rerun. If you update the bank
--   account, the bank account validation will
--   automatically be rerun.</p>
template PostRecipientsId with
    requestor : Party
    id : Text
    payload : PostRecipientsIdPayload
  where
    signatory requestor

template PostRecipientsIdResponse with
    requestor : Party
    requestId : ContractId PostRecipientsId
    body : PostRecipientsIdResponseBody
  where
    signatory requestor

data PostRecipientsIdResponseBody
    = PostRecipientsIdResponseBody_200 Recipient
    | PostRecipientsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Permanently deletes a recipient. It cannot be
--   undone.</p>
template DeleteRecipientsId with
    requestor : Party
    id : Text
  where
    signatory requestor

template DeleteRecipientsIdResponse with
    requestor : Party
    requestId : ContractId DeleteRecipientsId
    body : DeleteRecipientsIdResponseBody
  where
    signatory requestor

data DeleteRecipientsIdResponseBody
    = DeleteRecipientsIdResponseBody_200 DeletedRecipient
    | DeleteRecipientsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves an Issuing <code>Transaction</code>
--   object.</p>
template GetIssuingTransactionsTransaction with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    transaction : Text
  where
    signatory requestor

template GetIssuingTransactionsTransactionResponse with
    requestor : Party
    requestId : ContractId GetIssuingTransactionsTransaction
    body : GetIssuingTransactionsTransactionResponseBody
  where
    signatory requestor

data GetIssuingTransactionsTransactionResponseBody
    = GetIssuingTransactionsTransactionResponseBody_200 IssuingTransaction
    | GetIssuingTransactionsTransactionResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the specified Issuing
--   <code>Transaction</code> object by setting the values
--   of the parameters passed. Any parameters not provided
--   will be left unchanged.</p>
template PostIssuingTransactionsTransaction with
    requestor : Party
    transaction : Text
    payload : PostIssuingTransactionsTransactionPayload
  where
    signatory requestor

template PostIssuingTransactionsTransactionResponse with
    requestor : Party
    requestId : ContractId PostIssuingTransactionsTransaction
    body : PostIssuingTransactionsTransactionResponseBody
  where
    signatory requestor

data PostIssuingTransactionsTransactionResponseBody
    = PostIssuingTransactionsTransactionResponseBody_200 IssuingTransaction
    | PostIssuingTransactionsTransactionResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves an Issuing <code>Authorization</code>
--   object.</p>
template GetIssuingAuthorizationsAuthorization with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    authorization : Text
  where
    signatory requestor

template GetIssuingAuthorizationsAuthorizationResponse with
    requestor : Party
    requestId : ContractId GetIssuingAuthorizationsAuthorization
    body : GetIssuingAuthorizationsAuthorizationResponseBody
  where
    signatory requestor

data GetIssuingAuthorizationsAuthorizationResponseBody
    = GetIssuingAuthorizationsAuthorizationResponseBody_200 IssuingAuthorization
    | GetIssuingAuthorizationsAuthorizationResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the specified Issuing
--   <code>Authorization</code> object by setting the
--   values of the parameters passed. Any parameters not
--   provided will be left unchanged.</p>
template PostIssuingAuthorizationsAuthorization with
    requestor : Party
    authorization : Text
    payload : PostIssuingAuthorizationsAuthorizationPayload
  where
    signatory requestor

template PostIssuingAuthorizationsAuthorizationResponse with
    requestor : Party
    requestId : ContractId PostIssuingAuthorizationsAuthorization
    body : PostIssuingAuthorizationsAuthorizationResponseBody
  where
    signatory requestor

data PostIssuingAuthorizationsAuthorizationResponseBody
    = PostIssuingAuthorizationsAuthorizationResponseBody_200 IssuingAuthorization
    | PostIssuingAuthorizationsAuthorizationResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of Checkout Sessions.</p>
template GetCheckoutSessions with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    paymentIntent : Text
        -- ^ Only return the Checkout Session for the
        --   PaymentIntent specified.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
    subscription : Text
        -- ^ Only return the Checkout Session for the subscription
        --   specified.
  where
    signatory requestor

template GetCheckoutSessionsResponse with
    requestor : Party
    requestId : ContractId GetCheckoutSessions
    body : GetCheckoutSessionsResponseBody
  where
    signatory requestor

data GetCheckoutSessionsResponseBody
    = GetCheckoutSessionsResponseBody_200 GetCheckoutSessionsResponseBody200
    | GetCheckoutSessionsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a Session object.</p>
template PostCheckoutSessions with
    requestor : Party
    payload : PostCheckoutSessionsPayload
  where
    signatory requestor

template PostCheckoutSessionsResponse with
    requestor : Party
    requestId : ContractId PostCheckoutSessions
    body : PostCheckoutSessionsResponseBody
  where
    signatory requestor

data PostCheckoutSessionsResponseBody
    = PostCheckoutSessionsResponseBody_200 CheckoutSession
    | PostCheckoutSessionsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Attaches a PaymentMethod object to a Customer.</p>
--   <p>To use this PaymentMethod as the default for
--   invoice or subscription payments, set <a
--   href="/docs/api/customers/update#update_customer-invoice_settings-default_payment_method"><code>invoice_settings.default_payment_method</code></a>,
--   on the Customer to the PaymentMethod’s ID.</p>
template PostPaymentMethodsPaymentMethodAttach with
    requestor : Party
    paymentMethod : Text
    payload : PostPaymentMethodsPaymentMethodAttachPayload
  where
    signatory requestor

template PostPaymentMethodsPaymentMethodAttachResponse with
    requestor : Party
    requestId : ContractId PostPaymentMethodsPaymentMethodAttach
    body : PostPaymentMethodsPaymentMethodAttachResponseBody
  where
    signatory requestor

data PostPaymentMethodsPaymentMethodAttachResponseBody
    = PostPaymentMethodsPaymentMethodAttachResponseBody_200 PaymentMethod
    | PostPaymentMethodsPaymentMethodAttachResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves an Issuing <code>Cardholder</code>
--   object.</p>
template GetIssuingCardholdersCardholder with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    cardholder : Text
  where
    signatory requestor

template GetIssuingCardholdersCardholderResponse with
    requestor : Party
    requestId : ContractId GetIssuingCardholdersCardholder
    body : GetIssuingCardholdersCardholderResponseBody
  where
    signatory requestor

data GetIssuingCardholdersCardholderResponseBody
    = GetIssuingCardholdersCardholderResponseBody_200 IssuingCardholder
    | GetIssuingCardholdersCardholderResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the specified Issuing
--   <code>Cardholder</code> object by setting the values
--   of the parameters passed. Any parameters not provided
--   will be left unchanged.</p>
template PostIssuingCardholdersCardholder with
    requestor : Party
    cardholder : Text
    payload : PostIssuingCardholdersCardholderPayload
  where
    signatory requestor

template PostIssuingCardholdersCardholderResponse with
    requestor : Party
    requestId : ContractId PostIssuingCardholdersCardholder
    body : PostIssuingCardholdersCardholderResponseBody
  where
    signatory requestor

data PostIssuingCardholdersCardholderResponseBody
    = PostIssuingCardholdersCardholderResponseBody_200 IssuingCardholder
    | PostIssuingCardholdersCardholderResponseBody_default_ Error
    deriving (Eq, Ord, Show)

template GetCustomersCustomerDiscount with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    customer : Text
  where
    signatory requestor

template GetCustomersCustomerDiscountResponse with
    requestor : Party
    requestId : ContractId GetCustomersCustomerDiscount
    body : GetCustomersCustomerDiscountResponseBody
  where
    signatory requestor

data GetCustomersCustomerDiscountResponseBody
    = GetCustomersCustomerDiscountResponseBody_200 Discount
    | GetCustomersCustomerDiscountResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Removes the currently applied discount on a
--   customer.</p>
template DeleteCustomersCustomerDiscount with
    requestor : Party
    customer : Text
  where
    signatory requestor

template DeleteCustomersCustomerDiscountResponse with
    requestor : Party
    requestId : ContractId DeleteCustomersCustomerDiscount
    body : DeleteCustomersCustomerDiscountResponseBody
  where
    signatory requestor

data DeleteCustomersCustomerDiscountResponseBody
    = DeleteCustomersCustomerDiscountResponseBody_200 DeletedDiscount
    | DeleteCustomersCustomerDiscountResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the details of an existing SKU. Supply
--   the unique SKU identifier from either a SKU creation
--   request or from the product, and Stripe will return
--   the corresponding SKU information.</p>
template GetSkusId with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    id : Text
  where
    signatory requestor

template GetSkusIdResponse with
    requestor : Party
    requestId : ContractId GetSkusId
    body : GetSkusIdResponseBody
  where
    signatory requestor

data GetSkusIdResponseBody
    = GetSkusIdResponseBody_200 DeletedSku
    | GetSkusIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the specific SKU by setting the values of
--   the parameters passed. Any parameters not provided
--   will be left unchanged.</p> <p>Note that a SKU’s
--   <code>attributes</code> are not editable. Instead,
--   you would need to deactivate the existing SKU and
--   create a new one with the new attribute values.</p>
template PostSkusId with
    requestor : Party
    id : Text
    payload : PostSkusIdPayload
  where
    signatory requestor

template PostSkusIdResponse with
    requestor : Party
    requestId : ContractId PostSkusId
    body : PostSkusIdResponseBody
  where
    signatory requestor

data PostSkusIdResponseBody
    = PostSkusIdResponseBody_200 Sku
    | PostSkusIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Delete a SKU. Deleting a SKU is only possible
--   until it has been used in an order.</p>
template DeleteSkusId with
    requestor : Party
    id : Text
  where
    signatory requestor

template DeleteSkusIdResponse with
    requestor : Party
    requestId : ContractId DeleteSkusId
    body : DeleteSkusIdResponseBody
  where
    signatory requestor

data DeleteSkusIdResponseBody
    = DeleteSkusIdResponseBody_200 DeletedSku
    | DeleteSkusIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of your webhook endpoints.</p>
template GetWebhookEndpoints with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetWebhookEndpointsResponse with
    requestor : Party
    requestId : ContractId GetWebhookEndpoints
    body : GetWebhookEndpointsResponseBody
  where
    signatory requestor

data GetWebhookEndpointsResponseBody
    = GetWebhookEndpointsResponseBody_200 GetWebhookEndpointsResponseBody200
    | GetWebhookEndpointsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>A webhook endpoint must have a <code>url</code>
--   and a list of <code>enabled_events</code>. You may
--   optionally specify the Boolean <code>connect</code>
--   parameter. If set to true, then a Connect webhook
--   endpoint that notifies the specified <code>url</code>
--   about events from all connected accounts is created;
--   otherwise an account webhook endpoint that notifies
--   the specified <code>url</code> only about events from
--   your account is created. You can also create webhook
--   endpoints in the <a
--   href="https://dashboard.stripe.com/account/webhooks">webhooks
--   settings</a> section of the Dashboard.</p>
template PostWebhookEndpoints with
    requestor : Party
    payload : PostWebhookEndpointsPayload
  where
    signatory requestor

template PostWebhookEndpointsResponse with
    requestor : Party
    requestId : ContractId PostWebhookEndpoints
    body : PostWebhookEndpointsResponseBody
  where
    signatory requestor

data PostWebhookEndpointsResponseBody
    = PostWebhookEndpointsResponseBody_200 WebhookEndpoint
    | PostWebhookEndpointsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a new source object.</p>
template PostSources with
    requestor : Party
    payload : PostSourcesPayload
  where
    signatory requestor

template PostSourcesResponse with
    requestor : Party
    requestId : ContractId PostSources
    body : PostSourcesResponseBody
  where
    signatory requestor

data PostSourcesResponseBody
    = PostSourcesResponseBody_200 Source
    | PostSourcesResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a Country Spec for a given Country
--   code.</p>
template GetCountrySpecsCountry with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    country : Text
  where
    signatory requestor

template GetCountrySpecsCountryResponse with
    requestor : Party
    requestId : ContractId GetCountrySpecsCountry
    body : GetCountrySpecsCountryResponseBody
  where
    signatory requestor

data GetCountrySpecsCountryResponseBody
    = GetCountrySpecsCountryResponseBody_200 CountrySpec
    | GetCountrySpecsCountryResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a single-use login link for an Express
--   account to access their Stripe dashboard.</p>
--   <p><strong>You may only create login links for <a
--   href="/docs/connect/express-accounts">Express
--   accounts</a> connected to your platform</strong>.</p>
template PostAccountLoginLinks with
    requestor : Party
    payload : PostAccountLoginLinksPayload
  where
    signatory requestor

template PostAccountLoginLinksResponse with
    requestor : Party
    requestId : ContractId PostAccountLoginLinks
    body : PostAccountLoginLinksResponseBody
  where
    signatory requestor

data PostAccountLoginLinksResponseBody
    = PostAccountLoginLinksResponseBody_200 LoginLink
    | PostAccountLoginLinksResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves information about the specified Account
--   Capability.</p>
template GetAccountCapabilitiesCapability with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    capability : Text
  where
    signatory requestor

template GetAccountCapabilitiesCapabilityResponse with
    requestor : Party
    requestId : ContractId GetAccountCapabilitiesCapability
    body : GetAccountCapabilitiesCapabilityResponseBody
  where
    signatory requestor

data GetAccountCapabilitiesCapabilityResponseBody
    = GetAccountCapabilitiesCapabilityResponseBody_200 Capability
    | GetAccountCapabilitiesCapabilityResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates an existing Account Capability.</p>
template PostAccountCapabilitiesCapability with
    requestor : Party
    capability : Text
    payload : PostAccountCapabilitiesCapabilityPayload
  where
    signatory requestor

template PostAccountCapabilitiesCapabilityResponse with
    requestor : Party
    requestId : ContractId PostAccountCapabilitiesCapability
    body : PostAccountCapabilitiesCapabilityResponseBody
  where
    signatory requestor

data PostAccountCapabilitiesCapabilityResponseBody
    = PostAccountCapabilitiesCapabilityResponseBody_200 Capability
    | PostAccountCapabilitiesCapabilityResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Verify a specified bank account for a given
--   customer.</p>
template PostCustomersCustomerBankAccountsIdVerify with
    requestor : Party
    customer : Text
    id : Text
    payload : PostCustomersCustomerBankAccountsIdVerifyPayload
  where
    signatory requestor

template PostCustomersCustomerBankAccountsIdVerifyResponse with
    requestor : Party
    requestId : ContractId PostCustomersCustomerBankAccountsIdVerify
    body : PostCustomersCustomerBankAccountsIdVerifyResponseBody
  where
    signatory requestor

data PostCustomersCustomerBankAccountsIdVerifyResponseBody
    = PostCustomersCustomerBankAccountsIdVerifyResponseBody_200 BankAccount
    | PostCustomersCustomerBankAccountsIdVerifyResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>When retrieving a credit note preview, you’ll get
--   a <strong>lines</strong> property containing the
--   first handful of those items. This URL you can
--   retrieve the full (paginated) list of line items.</p>
template GetCreditNotesPreviewLines with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    amount : Int
        -- ^ The integer amount in **%s** representing the total
        --   amount of the credit note.
    creditAmount : Int
        -- ^ The integer amount in **%s** representing the amount
        --   to credit the customer's balance, which will be
        --   automatically applied to their next invoice.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    invoice : Text
        -- ^ ID of the invoice.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    lines : [Text]
        -- ^ Line items that make up the credit note.
    memo : Text
        -- ^ The credit note's memo appears on the credit note
        --   PDF.
    metadata : Text
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    outOfBandAmount : Int
        -- ^ The integer amount in **%s** representing the amount
        --   that is credited outside of Stripe.
    reason : Text
        -- ^ Reason for issuing this credit note, one of
        --   `duplicate`, `fraudulent`, `order_change`, or
        --   `product_unsatisfactory`
    refund : Text
        -- ^ ID of an existing refund to link this credit note to.
    refundAmount : Int
        -- ^ The integer amount in **%s** representing the amount
        --   to refund. If set, a refund will be created for the
        --   charge associated with the invoice.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetCreditNotesPreviewLinesResponse with
    requestor : Party
    requestId : ContractId GetCreditNotesPreviewLines
    body : GetCreditNotesPreviewLinesResponseBody
  where
    signatory requestor

data GetCreditNotesPreviewLinesResponseBody
    = GetCreditNotesPreviewLinesResponseBody_200 GetCreditNotesPreviewLinesResponseBody200
    | GetCreditNotesPreviewLinesResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Stripe automatically finalizes drafts before
--   sending and attempting payment on invoices. However,
--   if you’d like to finalize a draft invoice manually,
--   you can do so using this method.</p>
template PostInvoicesInvoiceFinalize with
    requestor : Party
    invoice : Text
    payload : PostInvoicesInvoiceFinalizePayload
  where
    signatory requestor

template PostInvoicesInvoiceFinalizeResponse with
    requestor : Party
    requestId : ContractId PostInvoicesInvoiceFinalize
    body : PostInvoicesInvoiceFinalizeResponseBody
  where
    signatory requestor

data PostInvoicesInvoiceFinalizeResponseBody
    = PostInvoicesInvoiceFinalizeResponseBody_200 Invoice
    | PostInvoicesInvoiceFinalizeResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of your SKUs. The SKUs are returned
--   sorted by creation date, with the most recently
--   created SKUs appearing first.</p>
template GetSkus with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    active : Bool
        -- ^ Only return SKUs that are active or inactive (e.g.,
        --   pass `false` to list all inactive products).
    attributes : Text
        -- ^ Only return SKUs that have the specified key-value
        --   pairs in this partially constructed dictionary. Can
        --   be specified only if `product` is also supplied. For
        --   instance, if the associated product has attributes
        --   `["color", "size"]`, passing in
        --   `attributes[color]=red` returns all the SKUs for this
        --   product that have `color` set to `red`.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    ids : [Text]
        -- ^ Only return SKUs with the given IDs.
    inStock : Bool
        -- ^ Only return SKUs that are either in stock or out of
        --   stock (e.g., pass `false` to list all SKUs that are
        --   out of stock). If no value is provided, all SKUs are
        --   returned.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    product : Text
        -- ^ The ID of the product whose SKUs will be retrieved.
        --   Must be a product with type `good`.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetSkusResponse with
    requestor : Party
    requestId : ContractId GetSkus
    body : GetSkusResponseBody
  where
    signatory requestor

data GetSkusResponseBody
    = GetSkusResponseBody_200 GetSkusResponseBody200
    | GetSkusResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a new SKU associated with a product.</p>
template PostSkus with
    requestor : Party
    payload : PostSkusPayload
  where
    signatory requestor

template PostSkusResponse with
    requestor : Party
    requestId : ContractId PostSkus
    body : PostSkusResponseBody
  where
    signatory requestor

data PostSkusResponseBody
    = PostSkusResponseBody_200 Sku
    | PostSkusResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of existing payouts sent to
--   third-party bank accounts or that Stripe has sent
--   you. The payouts are returned in sorted order, with
--   the most recently created payouts appearing
--   first.</p>
template GetPayouts with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    arrivalDate : Int
    created : Int
    destination : Text
        -- ^ The ID of an external account - only return payouts
        --   sent to this external account.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
    status : Text
        -- ^ Only return payouts that have the given status:
        --   `pending`, `paid`, `failed`, or `canceled`.
  where
    signatory requestor

template GetPayoutsResponse with
    requestor : Party
    requestId : ContractId GetPayouts
    body : GetPayoutsResponseBody
  where
    signatory requestor

data GetPayoutsResponseBody
    = GetPayoutsResponseBody_200 GetPayoutsResponseBody200
    | GetPayoutsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>To send funds to your own bank account, you create
--   a new payout object. Your <a href="#balance">Stripe
--   balance</a> must be able to cover the payout amount,
--   or you’ll receive an “Insufficient Funds” error.</p>
--   <p>If your API key is in test mode, money won’t
--   actually be sent, though everything else will occur
--   as if in live mode.</p> <p>If you are creating a
--   manual payout on a Stripe account that uses multiple
--   payment source types, you’ll need to specify the
--   source type balance that the payout should draw from.
--   The <a href="#balance_object">balance object</a>
--   details available and pending amounts by source
--   type.</p>
template PostPayouts with
    requestor : Party
    payload : PostPayoutsPayload
  where
    signatory requestor

template PostPayoutsResponse with
    requestor : Party
    requestId : ContractId PostPayouts
    body : PostPayoutsResponseBody
  where
    signatory requestor

data PostPayoutsResponseBody
    = PostPayoutsResponseBody_200 Payout
    | PostPayoutsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>For virtual cards only. Retrieves an Issuing
--   <code>card_details</code> object that contains <a
--   href="/docs/issuing/cards/management#virtual-card-info">the
--   sensitive details</a> of a virtual card.</p>
template GetIssuingCardsCardDetails with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    card : Text
  where
    signatory requestor

template GetIssuingCardsCardDetailsResponse with
    requestor : Party
    requestId : ContractId GetIssuingCardsCardDetails
    body : GetIssuingCardsCardDetailsResponseBody
  where
    signatory requestor

data GetIssuingCardsCardDetailsResponseBody
    = GetIssuingCardsCardDetailsResponseBody_200 IssuingCardDetails
    | GetIssuingCardsCardDetailsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Mark a finalized invoice as void. This cannot be
--   undone. Voiding an invoice is similar to <a
--   href="#delete_invoice">deletion</a>, however it only
--   applies to finalized invoices and maintains a
--   papertrail where the invoice can still be found.</p>
template PostInvoicesInvoiceVoid with
    requestor : Party
    invoice : Text
    payload : PostInvoicesInvoiceVoidPayload
  where
    signatory requestor

template PostInvoicesInvoiceVoidResponse with
    requestor : Party
    requestId : ContractId PostInvoicesInvoiceVoid
    body : PostInvoicesInvoiceVoidResponseBody
  where
    signatory requestor

data PostInvoicesInvoiceVoidResponseBody
    = PostInvoicesInvoiceVoidResponseBody_200 Invoice
    | PostInvoicesInvoiceVoidResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>You can see a list of the reversals belonging to a
--   specific transfer. Note that the 10 most recent
--   reversals are always available by default on the
--   transfer object. If you need more than those 10, you
--   can use this API method and the <code>limit</code>
--   and <code>starting_after</code> parameters to page
--   through additional reversals.</p>
template GetTransfersIdReversals with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    id : Text
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetTransfersIdReversalsResponse with
    requestor : Party
    requestId : ContractId GetTransfersIdReversals
    body : GetTransfersIdReversalsResponseBody
  where
    signatory requestor

data GetTransfersIdReversalsResponseBody
    = GetTransfersIdReversalsResponseBody_200 GetTransfersIdReversalsResponseBody200
    | GetTransfersIdReversalsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>When you create a new reversal, you must specify a
--   transfer to create it on.</p> <p>When reversing
--   transfers, you can optionally reverse part of the
--   transfer. You can do so as many times as you wish
--   until the entire transfer has been reversed.</p>
--   <p>Once entirely reversed, a transfer can’t be
--   reversed again. This method will return an error when
--   called on an already-reversed transfer, or when
--   trying to reverse more money than is left on a
--   transfer.</p>
template PostTransfersIdReversals with
    requestor : Party
    id : Text
    payload : PostTransfersIdReversalsPayload
  where
    signatory requestor

template PostTransfersIdReversalsResponse with
    requestor : Party
    requestId : ContractId PostTransfersIdReversals
    body : PostTransfersIdReversalsResponseBody
  where
    signatory requestor

data PostTransfersIdReversalsResponseBody
    = PostTransfersIdReversalsResponseBody_200 TransferReversal
    | PostTransfersIdReversalsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>When retrieving an upcoming invoice, you’ll get a
--   <strong>lines</strong> property containing the total
--   count of line items and the first handful of those
--   items. There is also a URL where you can retrieve the
--   full (paginated) list of line items.</p>
template GetInvoicesUpcomingLines with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    coupon : Text
        -- ^ The code of the coupon to apply. If `subscription` or
        --   `subscription_items` is provided, the invoice
        --   returned will preview updating or creating a
        --   subscription with that coupon. Otherwise, it will
        --   preview applying that coupon to the customer for the
        --   next upcoming invoice from among the customer's
        --   subscriptions. The invoice can be previewed without a
        --   coupon by passing this value as an empty string.
    customer : Text
        -- ^ The identifier of the customer whose upcoming invoice
        --   you'd like to retrieve.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    invoiceItems : [Text]
        -- ^ List of invoice items to add or update in the
        --   upcoming invoice preview.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    schedule : Text
        -- ^ The identifier of the unstarted schedule whose
        --   upcoming invoice you'd like to retrieve. Cannot be
        --   used with subscription or subscription fields.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
    subscription : Text
        -- ^ The identifier of the subscription for which you'd
        --   like to retrieve the upcoming invoice. If not
        --   provided, but a `subscription_items` is provided, you
        --   will preview creating a subscription with those
        --   items. If neither `subscription` nor
        --   `subscription_items` is provided, you will retrieve
        --   the next upcoming invoice from among the customer's
        --   subscriptions.
    subscriptionBillingCycleAnchor : Text
        -- ^ For new subscriptions, a future timestamp to anchor
        --   the subscription's [billing
        --   cycle](https://stripe.com/docs/subscriptions/billing-cycle).
        --   This is used to determine the date of the first full
        --   invoice, and, for plans with `month` or `year`
        --   intervals, the day of the month for subsequent
        --   invoices. For existing subscriptions, the value can
        --   only be set to `now` or `unchanged`.
    subscriptionCancelAt : Text
        -- ^ Timestamp indicating when the subscription should be
        --   scheduled to cancel. Will prorate if within the
        --   current period and prorations have been enabled using
        --   `proration_behavior`.`
    subscriptionCancelAtPeriodEnd : Bool
        -- ^ Boolean indicating whether this subscription should
        --   cancel at the end of the current period.
    subscriptionCancelNow : Bool
        -- ^ This simulates the subscription being canceled or
        --   expired immediately.
    subscriptionDefaultTaxRates : Text
        -- ^ If provided, the invoice returned will preview
        --   updating or creating a subscription with these
        --   default tax rates. The default tax rates will apply
        --   to any line item that does not have `tax_rates` set.
    subscriptionItems : [Text]
        -- ^ List of subscription items, each with an attached
        --   plan.
    subscriptionProrate : Bool
        -- ^ If previewing an update to a subscription, this
        --   decides whether the preview will show the result of
        --   applying prorations or not. If set, one of
        --   `subscription_items` or `subscription`, and one of
        --   `subscription_items` or `subscription_trial_end` are
        --   required.
    subscriptionProrationBehavior : Text
        -- ^ Determines how to handle
        --   [prorations](https://stripe.com/docs/subscriptions/billing-cycle#prorations)
        --   when the billing cycle changes (e.g., when switching
        --   plans, resetting `billing_cycle_anchor=now`, or
        --   starting a trial), or if an item's `quantity`
        --   changes. Valid values are `create_prorations`,
        --   `none`, or `always_invoice`. Passing
        --   `create_prorations` will cause proration invoice
        --   items to be created when applicable. These proration
        --   items will only be invoiced immediately under
        --   [certain
        --   conditions](https://stripe.com/docs/subscriptions/upgrading-downgrading#immediate-payment).
        --   In order to always invoice immediately for
        --   prorations, pass `always_invoice`. Prorations can be
        --   disabled by passing `none`.
    subscriptionProrationDate : Int
        -- ^ If previewing an update to a subscription, and doing
        --   proration, `subscription_proration_date` forces the
        --   proration to be calculated as though the update was
        --   done at the specified time. The time given must be
        --   within the current subscription period, and cannot be
        --   before the subscription was on its current plan. If
        --   set, `subscription`, and one of `subscription_items`,
        --   or `subscription_trial_end` are required. Also,
        --   `subscription_proration` cannot be set to false.
    subscriptionStartDate : Int
        -- ^ Date a subscription is intended to start (can be
        --   future or past)
    subscriptionTaxPercent : Decimal
        -- ^ If provided, the invoice returned will preview
        --   updating or creating a subscription with that tax
        --   percent. If set, one of `subscription_items` or
        --   `subscription` is required. This field has been
        --   deprecated and will be removed in a future API
        --   version, for further information view the [migration
        --   docs](https://stripe.com/docs/billing/migration/taxes)
        --   for `tax_rates`.
    subscriptionTrialEnd : Text
        -- ^ If provided, the invoice returned will preview
        --   updating or creating a subscription with that trial
        --   end. If set, one of `subscription_items` or
        --   `subscription` is required.
    subscriptionTrialFromPlan : Bool
        -- ^ Indicates if a plan's `trial_period_days` should be
        --   applied to the subscription. Setting
        --   `subscription_trial_end` per subscription is
        --   preferred, and this defaults to `false`. Setting this
        --   flag to `true` together with `subscription_trial_end`
        --   is not allowed.
  where
    signatory requestor

template GetInvoicesUpcomingLinesResponse with
    requestor : Party
    requestId : ContractId GetInvoicesUpcomingLines
    body : GetInvoicesUpcomingLinesResponseBody
  where
    signatory requestor

data GetInvoicesUpcomingLinesResponseBody
    = GetInvoicesUpcomingLinesResponseBody_200 GetInvoicesUpcomingLinesResponseBody200
    | GetInvoicesUpcomingLinesResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of your customers. The customers
--   are returned sorted by creation date, with the most
--   recent customers appearing first.</p>
template GetCustomers with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    created : Int
    email : Text
        -- ^ A filter on the list based on the customer's `email`
        --   field. The value must be a string.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetCustomersResponse with
    requestor : Party
    requestId : ContractId GetCustomers
    body : GetCustomersResponseBody
  where
    signatory requestor

data GetCustomersResponseBody
    = GetCustomersResponseBody_200 GetCustomersResponseBody200
    | GetCustomersResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a new customer object.</p>
template PostCustomers with
    requestor : Party
    payload : PostCustomersPayload
  where
    signatory requestor

template PostCustomersResponse with
    requestor : Party
    requestId : ContractId PostCustomers
    body : PostCustomersResponseBody
  where
    signatory requestor

data PostCustomersResponseBody
    = PostCustomersResponseBody_200 Customer
    | PostCustomersResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Initiate 3D Secure authentication.</p>
template Post3dSecure with
    requestor : Party
    payload : Post3dSecurePayload
  where
    signatory requestor

template Post3dSecureResponse with
    requestor : Party
    requestId : ContractId Post3dSecure
    body : Post3dSecureResponseBody
  where
    signatory requestor

data Post3dSecureResponseBody
    = Post3dSecureResponseBody_200 ThreeDSecure
    | Post3dSecureResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates an AccountLink object that returns a
--   single-use Stripe URL that the user can redirect
--   their user to in order to take them through the
--   Connect Onboarding flow.</p>
template PostAccountLinks with
    requestor : Party
    payload : PostAccountLinksPayload
  where
    signatory requestor

template PostAccountLinksResponse with
    requestor : Party
    requestId : ContractId PostAccountLinks
    body : PostAccountLinksResponseBody
  where
    signatory requestor

data PostAccountLinksResponseBody
    = PostAccountLinksResponseBody_200 AccountLink
    | PostAccountLinksResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves a <code>Reader</code> object.</p>
template GetTerminalReadersReader with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    reader : Text
  where
    signatory requestor

template GetTerminalReadersReaderResponse with
    requestor : Party
    requestId : ContractId GetTerminalReadersReader
    body : GetTerminalReadersReaderResponseBody
  where
    signatory requestor

data GetTerminalReadersReaderResponseBody
    = GetTerminalReadersReaderResponseBody_200 TerminalReader
    | GetTerminalReadersReaderResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates a <code>Reader</code> object by setting
--   the values of the parameters passed. Any parameters
--   not provided will be left unchanged.</p>
template PostTerminalReadersReader with
    requestor : Party
    reader : Text
    payload : PostTerminalReadersReaderPayload
  where
    signatory requestor

template PostTerminalReadersReaderResponse with
    requestor : Party
    requestId : ContractId PostTerminalReadersReader
    body : PostTerminalReadersReaderResponseBody
  where
    signatory requestor

data PostTerminalReadersReaderResponseBody
    = PostTerminalReadersReaderResponseBody_200 TerminalReader
    | PostTerminalReadersReaderResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Deletes a <code>Reader</code> object.</p>
template DeleteTerminalReadersReader with
    requestor : Party
    reader : Text
  where
    signatory requestor

template DeleteTerminalReadersReaderResponse with
    requestor : Party
    requestId : ContractId DeleteTerminalReadersReader
    body : DeleteTerminalReadersReaderResponseBody
  where
    signatory requestor

data DeleteTerminalReadersReaderResponseBody
    = DeleteTerminalReadersReaderResponseBody_200 DeletedTerminalReader
    | DeleteTerminalReadersReaderResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the details of a top-up that has
--   previously been created. Supply the unique top-up ID
--   that was returned from your previous request, and
--   Stripe will return the corresponding top-up
--   information.</p>
template GetTopupsTopup with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    topup : Text
  where
    signatory requestor

template GetTopupsTopupResponse with
    requestor : Party
    requestId : ContractId GetTopupsTopup
    body : GetTopupsTopupResponseBody
  where
    signatory requestor

data GetTopupsTopupResponseBody
    = GetTopupsTopupResponseBody_200 Topup
    | GetTopupsTopupResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the metadata of a top-up. Other top-up
--   details are not editable by design.</p>
template PostTopupsTopup with
    requestor : Party
    topup : Text
    payload : PostTopupsTopupPayload
  where
    signatory requestor

template PostTopupsTopupResponse with
    requestor : Party
    requestId : ContractId PostTopupsTopup
    body : PostTopupsTopupResponseBody
  where
    signatory requestor

data PostTopupsTopupResponseBody
    = PostTopupsTopupResponseBody_200 Topup
    | PostTopupsTopupResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the <code>TaxID</code> object with the
--   given identifier.</p>
template GetCustomersCustomerTaxIdsId with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    customer : Text
    id : Text
  where
    signatory requestor

template GetCustomersCustomerTaxIdsIdResponse with
    requestor : Party
    requestId : ContractId GetCustomersCustomerTaxIdsId
    body : GetCustomersCustomerTaxIdsIdResponseBody
  where
    signatory requestor

data GetCustomersCustomerTaxIdsIdResponseBody
    = GetCustomersCustomerTaxIdsIdResponseBody_200 TaxId
    | GetCustomersCustomerTaxIdsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Deletes an existing <code>TaxID</code> object.</p>
template DeleteCustomersCustomerTaxIdsId with
    requestor : Party
    customer : Text
    id : Text
  where
    signatory requestor

template DeleteCustomersCustomerTaxIdsIdResponse with
    requestor : Party
    requestId : ContractId DeleteCustomersCustomerTaxIdsId
    body : DeleteCustomersCustomerTaxIdsIdResponseBody
  where
    signatory requestor

data DeleteCustomersCustomerTaxIdsIdResponseBody
    = DeleteCustomersCustomerTaxIdsIdResponseBody_200 DeletedTaxId
    | DeleteCustomersCustomerTaxIdsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of your coupons.</p>
template GetCoupons with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    created : Int
        -- ^ A filter on the list, based on the object `created`
        --   field. The value can be a string with an integer Unix
        --   timestamp, or it can be a dictionary with a number of
        --   different query options.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetCouponsResponse with
    requestor : Party
    requestId : ContractId GetCoupons
    body : GetCouponsResponseBody
  where
    signatory requestor

data GetCouponsResponseBody
    = GetCouponsResponseBody_200 GetCouponsResponseBody200
    | GetCouponsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>You can create coupons easily via the <a
--   href="https://dashboard.stripe.com/coupons">coupon
--   management</a> page of the Stripe dashboard. Coupon
--   creation is also accessible via the API if you need
--   to create coupons on the fly.</p> <p>A coupon has
--   either a <code>percent_off</code> or an
--   <code>amount_off</code> and <code>currency</code>. If
--   you set an <code>amount_off</code>, that amount will
--   be subtracted from any invoice’s subtotal. For
--   example, an invoice with a subtotal of
--   <currency>100</currency> will have a final total of
--   <currency>0</currency> if a coupon with an
--   <code>amount_off</code> of <amount>200</amount> is
--   applied to it and an invoice with a subtotal of
--   <currency>300</currency> will have a final total of
--   <currency>100</currency> if a coupon with an
--   <code>amount_off</code> of <amount>200</amount> is
--   applied to it.</p>
template PostCoupons with
    requestor : Party
    payload : PostCouponsPayload
  where
    signatory requestor

template PostCouponsResponse with
    requestor : Party
    requestId : ContractId PostCoupons
    body : PostCouponsResponseBody
  where
    signatory requestor

data PostCouponsResponseBody
    = PostCouponsResponseBody_200 Coupon
    | PostCouponsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the details of an existing file object.
--   Supply the unique file ID from a file, and Stripe
--   will return the corresponding file object. To access
--   file contents, see the <a
--   href="/docs/file-upload#download-file-contents">File
--   Upload Guide</a>.</p>
template GetFilesFile with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    file : Text
  where
    signatory requestor

template GetFilesFileResponse with
    requestor : Party
    requestId : ContractId GetFilesFile
    body : GetFilesFileResponseBody
  where
    signatory requestor

data GetFilesFileResponseBody
    = GetFilesFileResponseBody_200 File
    | GetFilesFileResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the dispute with the given ID.</p>
template GetDisputesDispute with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    dispute : Text
  where
    signatory requestor

template GetDisputesDisputeResponse with
    requestor : Party
    requestId : ContractId GetDisputesDispute
    body : GetDisputesDisputeResponseBody
  where
    signatory requestor

data GetDisputesDisputeResponseBody
    = GetDisputesDisputeResponseBody_200 Dispute
    | GetDisputesDisputeResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>When you get a dispute, contacting your customer
--   is always the best first step. If that doesn’t work,
--   you can submit evidence to help us resolve the
--   dispute in your favor. You can do this in your <a
--   href="https://dashboard.stripe.com/disputes">dashboard</a>,
--   but if you prefer, you can use the API to submit
--   evidence programmatically.</p> <p>Depending on your
--   dispute type, different evidence fields will give you
--   a better chance of winning your dispute. To figure
--   out which evidence fields to provide, see our <a
--   href="/docs/disputes/categories">guide to dispute
--   types</a>.</p>
template PostDisputesDispute with
    requestor : Party
    dispute : Text
    payload : PostDisputesDisputePayload
  where
    signatory requestor

template PostDisputesDisputeResponse with
    requestor : Party
    requestId : ContractId PostDisputesDispute
    body : PostDisputesDisputeResponseBody
  where
    signatory requestor

data PostDisputesDisputeResponseBody
    = PostDisputesDisputeResponseBody_200 Dispute
    | PostDisputesDisputeResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of your disputes.</p>
template GetDisputes with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    charge : Text
        -- ^ Only return disputes associated to the charge
        --   specified by this charge ID.
    created : Int
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    paymentIntent : Text
        -- ^ Only return disputes associated to the PaymentIntent
        --   specified by this PaymentIntent ID.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetDisputesResponse with
    requestor : Party
    requestId : ContractId GetDisputes
    body : GetDisputesResponseBody
  where
    signatory requestor

data GetDisputesResponseBody
    = GetDisputesResponseBody_200 GetDisputesResponseBody200
    | GetDisputesResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the details of an early fraud warning
--   that has previously been created. </p> <p>Please
--   refer to the <a
--   href="#early_fraud_warning_object">early fraud
--   warning</a> object reference for more details.</p>
template GetRadarEarlyFraudWarningsEarlyFraudWarning with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    earlyFraudWarning : Text
  where
    signatory requestor

template GetRadarEarlyFraudWarningsEarlyFraudWarningResponse with
    requestor : Party
    requestId : ContractId GetRadarEarlyFraudWarningsEarlyFraudWarning
    body : GetRadarEarlyFraudWarningsEarlyFraudWarningResponseBody
  where
    signatory requestor

data GetRadarEarlyFraudWarningsEarlyFraudWarningResponseBody
    = GetRadarEarlyFraudWarningsEarlyFraudWarningResponseBody_200 RadarEarlyFraudWarning
    | GetRadarEarlyFraudWarningsEarlyFraudWarningResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieve a specified external account for a given
--   account.</p>
template GetAccountsAccountBankAccountsId with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    account : Text
    id : Text
  where
    signatory requestor

template GetAccountsAccountBankAccountsIdResponse with
    requestor : Party
    requestId : ContractId GetAccountsAccountBankAccountsId
    body : GetAccountsAccountBankAccountsIdResponseBody
  where
    signatory requestor

data GetAccountsAccountBankAccountsIdResponseBody
    = GetAccountsAccountBankAccountsIdResponseBody_200 ExternalAccount
    | GetAccountsAccountBankAccountsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the metadata, account holder name, and
--   account holder type of a bank account belonging to a
--   <a href="/docs/connect/custom-accounts">Custom
--   account</a>, and optionally sets it as the default
--   for its currency. Other bank account details are not
--   editable by design.</p> <p>You can re-enable a
--   disabled bank account by performing an update call
--   without providing any arguments or changes.</p>
template PostAccountsAccountBankAccountsId with
    requestor : Party
    account : Text
    id : Text
    payload : PostAccountsAccountBankAccountsIdPayload
  where
    signatory requestor

template PostAccountsAccountBankAccountsIdResponse with
    requestor : Party
    requestId : ContractId PostAccountsAccountBankAccountsId
    body : PostAccountsAccountBankAccountsIdResponseBody
  where
    signatory requestor

data PostAccountsAccountBankAccountsIdResponseBody
    = PostAccountsAccountBankAccountsIdResponseBody_200 ExternalAccount
    | PostAccountsAccountBankAccountsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Delete a specified external account for a given
--   account.</p>
template DeleteAccountsAccountBankAccountsId with
    requestor : Party
    account : Text
    id : Text
  where
    signatory requestor

template DeleteAccountsAccountBankAccountsIdResponse with
    requestor : Party
    requestId : ContractId DeleteAccountsAccountBankAccountsId
    body : DeleteAccountsAccountBankAccountsIdResponseBody
  where
    signatory requestor

data DeleteAccountsAccountBankAccountsIdResponseBody
    = DeleteAccountsAccountBankAccountsIdResponseBody_200 DeletedExternalAccount
    | DeleteAccountsAccountBankAccountsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of scheduled query runs.</p>
template GetSigmaScheduledQueryRuns with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetSigmaScheduledQueryRunsResponse with
    requestor : Party
    requestId : ContractId GetSigmaScheduledQueryRuns
    body : GetSigmaScheduledQueryRunsResponseBody
  where
    signatory requestor

data GetSigmaScheduledQueryRunsResponseBody
    = GetSigmaScheduledQueryRunsResponseBody_200 GetSigmaScheduledQueryRunsResponseBody200
    | GetSigmaScheduledQueryRunsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Pay an order by providing a <code>source</code> to
--   create a payment.</p>
template PostOrdersIdPay with
    requestor : Party
    id : Text
    payload : PostOrdersIdPayPayload
  where
    signatory requestor

template PostOrdersIdPayResponse with
    requestor : Party
    requestId : ContractId PostOrdersIdPay
    body : PostOrdersIdPayResponseBody
  where
    signatory requestor

data PostOrdersIdPayResponseBody
    = PostOrdersIdPayResponseBody_200 Order
    | PostOrdersIdPayResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>By default, returns a list of subscriptions that
--   have not been canceled. In order to list canceled
--   subscriptions, specify
--   <code>status=canceled</code>.</p>
template GetSubscriptions with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    collectionMethod : Text
        -- ^ The collection method of the subscriptions to
        --   retrieve. Either `charge_automatically` or
        --   `send_invoice`.
    created : Int
    currentPeriodEnd : Int
    currentPeriodStart : Int
    customer : Text
        -- ^ The ID of the customer whose subscriptions will be
        --   retrieved.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    plan : Text
        -- ^ The ID of the plan whose subscriptions will be
        --   retrieved.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
    status : Text
        -- ^ The status of the subscriptions to retrieve. One of:
        --   `incomplete`, `incomplete_expired`, `trialing`,
        --   `active`, `past_due`, `unpaid`, `canceled`, or `all`.
        --   Passing in a value of `canceled` will return all
        --   canceled subscriptions, including those belonging to
        --   deleted customers. Passing in a value of `all` will
        --   return subscriptions of all statuses.
  where
    signatory requestor

template GetSubscriptionsResponse with
    requestor : Party
    requestId : ContractId GetSubscriptions
    body : GetSubscriptionsResponseBody
  where
    signatory requestor

data GetSubscriptionsResponseBody
    = GetSubscriptionsResponseBody_200 GetSubscriptionsResponseBody200
    | GetSubscriptionsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a new subscription on an existing
--   customer. Each customer can have up to 25 active or
--   scheduled subscriptions.</p>
template PostSubscriptions with
    requestor : Party
    payload : PostSubscriptionsPayload
  where
    signatory requestor

template PostSubscriptionsResponse with
    requestor : Party
    requestId : ContractId PostSubscriptions
    body : PostSubscriptionsResponseBody
  where
    signatory requestor

data PostSubscriptionsResponseBody
    = PostSubscriptionsResponseBody_200 Subscription
    | PostSubscriptionsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

template GetCustomersCustomerSubscriptionsSubscriptionExposedIdDiscount with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    customer : Text
    subscriptionExposedId : Text
  where
    signatory requestor

template GetCustomersCustomerSubscriptionsSubscriptionExposedIdDiscountResponse with
    requestor : Party
    requestId : ContractId GetCustomersCustomerSubscriptionsSubscriptionExposedIdDiscount
    body : GetCustomersCustomerSubscriptionsSubscriptionExposedIdDiscountResponseBody
  where
    signatory requestor

data GetCustomersCustomerSubscriptionsSubscriptionExposedIdDiscountResponseBody
    = GetCustomersCustomerSubscriptionsSubscriptionExposedIdDiscountResponseBody_200 Discount
    | GetCustomersCustomerSubscriptionsSubscriptionExposedIdDiscountResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Removes the currently applied discount on a
--   customer.</p>
template DeleteCustomersCustomerSubscriptionsSubscriptionExposedIdDiscount with
    requestor : Party
    customer : Text
    subscriptionExposedId : Text
  where
    signatory requestor

template DeleteCustomersCustomerSubscriptionsSubscriptionExposedIdDiscountResponse with
    requestor : Party
    requestId : ContractId DeleteCustomersCustomerSubscriptionsSubscriptionExposedIdDiscount
    body : DeleteCustomersCustomerSubscriptionsSubscriptionExposedIdDiscountResponseBody
  where
    signatory requestor

data DeleteCustomersCustomerSubscriptionsSubscriptionExposedIdDiscountResponseBody
    = DeleteCustomersCustomerSubscriptionsSubscriptionExposedIdDiscountResponseBody_200 DeletedDiscount
    | DeleteCustomersCustomerSubscriptionsSubscriptionExposedIdDiscountResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of your invoice items. Invoice
--   items are returned sorted by creation date, with the
--   most recently created invoice items appearing
--   first.</p>
template GetInvoiceitems with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    created : Int
    customer : Text
        -- ^ The identifier of the customer whose invoice items to
        --   return. If none is provided, all invoice items will
        --   be returned.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    invoice : Text
        -- ^ Only return invoice items belonging to this invoice.
        --   If none is provided, all invoice items will be
        --   returned. If specifying an invoice, no customer
        --   identifier is needed.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    pending : Bool
        -- ^ Set to `true` to only show pending invoice items,
        --   which are not yet attached to any invoices. Set to
        --   `false` to only show invoice items already attached
        --   to invoices. If unspecified, no filter is applied.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetInvoiceitemsResponse with
    requestor : Party
    requestId : ContractId GetInvoiceitems
    body : GetInvoiceitemsResponseBody
  where
    signatory requestor

data GetInvoiceitemsResponseBody
    = GetInvoiceitemsResponseBody_200 GetInvoiceitemsResponseBody200
    | GetInvoiceitemsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates an item to be added to a draft invoice. If
--   no invoice is specified, the item will be on the next
--   invoice created for the customer specified.</p>
template PostInvoiceitems with
    requestor : Party
    payload : PostInvoiceitemsPayload
  where
    signatory requestor

template PostInvoiceitemsResponse with
    requestor : Party
    requestId : ContractId PostInvoiceitems
    body : PostInvoiceitemsResponseBody
  where
    signatory requestor

data PostInvoiceitemsResponseBody
    = PostInvoiceitemsResponseBody_200 Invoiceitem
    | PostInvoiceitemsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves a <code>ValueList</code> object.</p>
template GetRadarValueListsValueList with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    valueList : Text
  where
    signatory requestor

template GetRadarValueListsValueListResponse with
    requestor : Party
    requestId : ContractId GetRadarValueListsValueList
    body : GetRadarValueListsValueListResponseBody
  where
    signatory requestor

data GetRadarValueListsValueListResponseBody
    = GetRadarValueListsValueListResponseBody_200 RadarValueList
    | GetRadarValueListsValueListResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates a <code>ValueList</code> object by setting
--   the values of the parameters passed. Any parameters
--   not provided will be left unchanged. Note that
--   <code>item_type</code> is immutable.</p>
template PostRadarValueListsValueList with
    requestor : Party
    valueList : Text
    payload : PostRadarValueListsValueListPayload
  where
    signatory requestor

template PostRadarValueListsValueListResponse with
    requestor : Party
    requestId : ContractId PostRadarValueListsValueList
    body : PostRadarValueListsValueListResponseBody
  where
    signatory requestor

data PostRadarValueListsValueListResponseBody
    = PostRadarValueListsValueListResponseBody_200 RadarValueList
    | PostRadarValueListsValueListResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Deletes a <code>ValueList</code> object, also
--   deleting any items contained within the value list.
--   To be deleted, a value list must not be referenced in
--   any rules.</p>
template DeleteRadarValueListsValueList with
    requestor : Party
    valueList : Text
  where
    signatory requestor

template DeleteRadarValueListsValueListResponse with
    requestor : Party
    requestId : ContractId DeleteRadarValueListsValueList
    body : DeleteRadarValueListsValueListResponseBody
  where
    signatory requestor

data DeleteRadarValueListsValueListResponseBody
    = DeleteRadarValueListsValueListResponseBody_200 DeletedRadarValueList
    | DeleteRadarValueListsValueListResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>You can list all invoices, or list the invoices
--   for a specific customer. The invoices are returned
--   sorted by creation date, with the most recently
--   created invoices appearing first.</p>
template GetInvoices with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    collectionMethod : Text
        -- ^ The collection method of the invoice to retrieve.
        --   Either `charge_automatically` or `send_invoice`.
    created : Int
    customer : Text
        -- ^ Only return invoices for the customer specified by
        --   this customer ID.
    dueDate : Int
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
    status : Text
        -- ^ The status of the invoice, one of `draft`, `open`,
        --   `paid`, `uncollectible`, or `void`. [Learn
        --   more](https://stripe.com/docs/billing/invoices/workflow#workflow-overview)
    subscription : Text
        -- ^ Only return invoices for the subscription specified
        --   by this subscription ID.
  where
    signatory requestor

template GetInvoicesResponse with
    requestor : Party
    requestId : ContractId GetInvoices
    body : GetInvoicesResponseBody
  where
    signatory requestor

data GetInvoicesResponseBody
    = GetInvoicesResponseBody_200 GetInvoicesResponseBody200
    | GetInvoicesResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>This endpoint creates a draft invoice for a given
--   customer. The draft invoice created pulls in all
--   pending invoice items on that customer, including
--   prorations.</p>
template PostInvoices with
    requestor : Party
    payload : PostInvoicesPayload
  where
    signatory requestor

template PostInvoicesResponse with
    requestor : Party
    requestId : ContractId PostInvoices
    body : PostInvoicesResponseBody
  where
    signatory requestor

data PostInvoicesResponseBody
    = PostInvoicesResponseBody_200 Invoice
    | PostInvoicesResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves a new Source MandateNotification.</p>
template GetSourcesSourceMandateNotificationsMandateNotification with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    mandateNotification : Text
    source : Text
  where
    signatory requestor

template GetSourcesSourceMandateNotificationsMandateNotificationResponse with
    requestor : Party
    requestId : ContractId GetSourcesSourceMandateNotificationsMandateNotification
    body : GetSourcesSourceMandateNotificationsMandateNotificationResponseBody
  where
    signatory requestor

data GetSourcesSourceMandateNotificationsMandateNotificationResponseBody
    = GetSourcesSourceMandateNotificationsMandateNotificationResponseBody_200 SourceMandateNotification
    | GetSourcesSourceMandateNotificationsMandateNotificationResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves an existing source object. Supply the
--   unique source ID from a source creation request and
--   Stripe will return the corresponding up-to-date
--   source object information.</p>
template GetSourcesSource with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    clientSecret : Text
        -- ^ The client secret of the source. Required if a
        --   publishable key is used to retrieve the source.
    source : Text
  where
    signatory requestor

template GetSourcesSourceResponse with
    requestor : Party
    requestId : ContractId GetSourcesSource
    body : GetSourcesSourceResponseBody
  where
    signatory requestor

data GetSourcesSourceResponseBody
    = GetSourcesSourceResponseBody_200 Source
    | GetSourcesSourceResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the specified source by setting the values
--   of the parameters passed. Any parameters not provided
--   will be left unchanged.</p> <p>This request accepts
--   the <code>metadata</code> and <code>owner</code> as
--   arguments. It is also possible to update type
--   specific information for selected payment methods.
--   Please refer to our <a href="/docs/sources">payment
--   method guides</a> for more detail.</p>
template PostSourcesSource with
    requestor : Party
    source : Text
    payload : PostSourcesSourcePayload
  where
    signatory requestor

template PostSourcesSourceResponse with
    requestor : Party
    requestId : ContractId PostSourcesSource
    body : PostSourcesSourceResponseBody
  where
    signatory requestor

data PostSourcesSourceResponseBody
    = PostSourcesSourceResponseBody_200 Source
    | PostSourcesSourceResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieve an existing source transaction object.
--   Supply the unique source ID from a source creation
--   request and the source transaction ID and Stripe will
--   return the corresponding up-to-date source object
--   information.</p>
template GetSourcesSourceSourceTransactionsSourceTransaction with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    source : Text
    sourceTransaction : Text
  where
    signatory requestor

template GetSourcesSourceSourceTransactionsSourceTransactionResponse with
    requestor : Party
    requestId : ContractId GetSourcesSourceSourceTransactionsSourceTransaction
    body : GetSourcesSourceSourceTransactionsSourceTransactionResponseBody
  where
    signatory requestor

data GetSourcesSourceSourceTransactionsSourceTransactionResponseBody
    = GetSourcesSourceSourceTransactionsSourceTransactionResponseBody_200 SourceTransaction
    | GetSourcesSourceSourceTransactionsSourceTransactionResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieve a specified external account for a given
--   account.</p>
template GetAccountsAccountExternalAccountsId with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    account : Text
    id : Text
  where
    signatory requestor

template GetAccountsAccountExternalAccountsIdResponse with
    requestor : Party
    requestId : ContractId GetAccountsAccountExternalAccountsId
    body : GetAccountsAccountExternalAccountsIdResponseBody
  where
    signatory requestor

data GetAccountsAccountExternalAccountsIdResponseBody
    = GetAccountsAccountExternalAccountsIdResponseBody_200 ExternalAccount
    | GetAccountsAccountExternalAccountsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the metadata, account holder name, and
--   account holder type of a bank account belonging to a
--   <a href="/docs/connect/custom-accounts">Custom
--   account</a>, and optionally sets it as the default
--   for its currency. Other bank account details are not
--   editable by design.</p> <p>You can re-enable a
--   disabled bank account by performing an update call
--   without providing any arguments or changes.</p>
template PostAccountsAccountExternalAccountsId with
    requestor : Party
    account : Text
    id : Text
    payload : PostAccountsAccountExternalAccountsIdPayload
  where
    signatory requestor

template PostAccountsAccountExternalAccountsIdResponse with
    requestor : Party
    requestId : ContractId PostAccountsAccountExternalAccountsId
    body : PostAccountsAccountExternalAccountsIdResponseBody
  where
    signatory requestor

data PostAccountsAccountExternalAccountsIdResponseBody
    = PostAccountsAccountExternalAccountsIdResponseBody_200 ExternalAccount
    | PostAccountsAccountExternalAccountsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Delete a specified external account for a given
--   account.</p>
template DeleteAccountsAccountExternalAccountsId with
    requestor : Party
    account : Text
    id : Text
  where
    signatory requestor

template DeleteAccountsAccountExternalAccountsIdResponse with
    requestor : Party
    requestId : ContractId DeleteAccountsAccountExternalAccountsId
    body : DeleteAccountsAccountExternalAccountsIdResponseBody
  where
    signatory requestor

data DeleteAccountsAccountExternalAccountsIdResponseBody
    = DeleteAccountsAccountExternalAccountsIdResponseBody_200 DeletedExternalAccount
    | DeleteAccountsAccountExternalAccountsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>When you create a new refund, you must specify a
--   Charge or a PaymentIntent object on which to create
--   it.</p> <p>Creating a new refund will refund a charge
--   that has previously been created but not yet
--   refunded. Funds will be refunded to the credit or
--   debit card that was originally charged.</p> <p>You
--   can optionally refund only part of a charge. You can
--   do so multiple times, until the entire charge has
--   been refunded.</p> <p>Once entirely refunded, a
--   charge can’t be refunded again. This method will
--   raise an error when called on an already-refunded
--   charge, or when trying to refund more money than is
--   left on a charge.</p>
template PostChargesChargeRefund with
    requestor : Party
    charge : Text
    payload : PostChargesChargeRefundPayload
  where
    signatory requestor

template PostChargesChargeRefundResponse with
    requestor : Party
    requestId : ContractId PostChargesChargeRefund
    body : PostChargesChargeRefundResponseBody
  where
    signatory requestor

data PostChargesChargeRefundResponseBody
    = PostChargesChargeRefundResponseBody_200 Charge
    | PostChargesChargeRefundResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Marks a credit note as void. Learn more about <a
--   href="/docs/billing/invoices/credit-notes#voiding">voiding
--   credit notes</a>.</p>
template PostCreditNotesIdVoid with
    requestor : Party
    id : Text
    payload : PostCreditNotesIdVoidPayload
  where
    signatory requestor

template PostCreditNotesIdVoidResponse with
    requestor : Party
    requestId : ContractId PostCreditNotesIdVoid
    body : PostCreditNotesIdVoidResponseBody
  where
    signatory requestor

data PostCreditNotesIdVoidResponseBody
    = PostCreditNotesIdVoidResponseBody_200 CreditNote
    | PostCreditNotesIdVoidResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Return all or part of an order. The order must
--   have a status of <code>paid</code> or
--   <code>fulfilled</code> before it can be returned.
--   Once all items have been returned, the order will
--   become <code>canceled</code> or <code>returned</code>
--   depending on which status the order started in.</p>
template PostOrdersIdReturns with
    requestor : Party
    id : Text
    payload : PostOrdersIdReturnsPayload
  where
    signatory requestor

template PostOrdersIdReturnsResponse with
    requestor : Party
    requestId : ContractId PostOrdersIdReturns
    body : PostOrdersIdReturnsResponseBody
  where
    signatory requestor

data PostOrdersIdReturnsResponseBody
    = PostOrdersIdReturnsResponseBody_200 OrderReturn
    | PostOrdersIdReturnsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the webhook endpoint with the given
--   ID.</p>
template GetWebhookEndpointsWebhookEndpoint with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    webhookEndpoint : Text
  where
    signatory requestor

template GetWebhookEndpointsWebhookEndpointResponse with
    requestor : Party
    requestId : ContractId GetWebhookEndpointsWebhookEndpoint
    body : GetWebhookEndpointsWebhookEndpointResponseBody
  where
    signatory requestor

data GetWebhookEndpointsWebhookEndpointResponseBody
    = GetWebhookEndpointsWebhookEndpointResponseBody_200 WebhookEndpoint
    | GetWebhookEndpointsWebhookEndpointResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the webhook endpoint. You may edit the
--   <code>url</code>, the list of
--   <code>enabled_events</code>, and the status of your
--   endpoint.</p>
template PostWebhookEndpointsWebhookEndpoint with
    requestor : Party
    webhookEndpoint : Text
    payload : PostWebhookEndpointsWebhookEndpointPayload
  where
    signatory requestor

template PostWebhookEndpointsWebhookEndpointResponse with
    requestor : Party
    requestId : ContractId PostWebhookEndpointsWebhookEndpoint
    body : PostWebhookEndpointsWebhookEndpointResponseBody
  where
    signatory requestor

data PostWebhookEndpointsWebhookEndpointResponseBody
    = PostWebhookEndpointsWebhookEndpointResponseBody_200 WebhookEndpoint
    | PostWebhookEndpointsWebhookEndpointResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>You can also delete webhook endpoints via the <a
--   href="https://dashboard.stripe.com/account/webhooks">webhook
--   endpoint management</a> page of the Stripe
--   dashboard.</p>
template DeleteWebhookEndpointsWebhookEndpoint with
    requestor : Party
    webhookEndpoint : Text
  where
    signatory requestor

template DeleteWebhookEndpointsWebhookEndpointResponse with
    requestor : Party
    requestId : ContractId DeleteWebhookEndpointsWebhookEndpoint
    body : DeleteWebhookEndpointsWebhookEndpointResponseBody
  where
    signatory requestor

data DeleteWebhookEndpointsWebhookEndpointResponseBody
    = DeleteWebhookEndpointsWebhookEndpointResponseBody_200 DeletedWebhookEndpoint
    | DeleteWebhookEndpointsWebhookEndpointResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves a Session object.</p>
template GetCheckoutSessionsSession with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    session : Text
  where
    signatory requestor

template GetCheckoutSessionsSessionResponse with
    requestor : Party
    requestId : ContractId GetCheckoutSessionsSession
    body : GetCheckoutSessionsSessionResponseBody
  where
    signatory requestor

data GetCheckoutSessionsSessionResponseBody
    = GetCheckoutSessionsSessionResponseBody_200 CheckoutSession
    | GetCheckoutSessionsSessionResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Some actions (eg: updating a PIN) need
--   confirmation from the cardholder</p>
template PostIssuingVerifications with
    requestor : Party
    payload : PostIssuingVerificationsPayload
  where
    signatory requestor

template PostIssuingVerificationsResponse with
    requestor : Party
    requestId : ContractId PostIssuingVerifications
    body : PostIssuingVerificationsResponseBody
  where
    signatory requestor

data PostIssuingVerificationsResponseBody
    = PostIssuingVerificationsResponseBody_200 IssuingVerification
    | PostIssuingVerificationsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the details of an existing order. Supply
--   the unique order ID from either an order creation
--   request or the order list, and Stripe will return the
--   corresponding order information.</p>
template GetOrdersId with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    id : Text
  where
    signatory requestor

template GetOrdersIdResponse with
    requestor : Party
    requestId : ContractId GetOrdersId
    body : GetOrdersIdResponseBody
  where
    signatory requestor

data GetOrdersIdResponseBody
    = GetOrdersIdResponseBody_200 Order
    | GetOrdersIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the specific order by setting the values
--   of the parameters passed. Any parameters not provided
--   will be left unchanged.</p>
template PostOrdersId with
    requestor : Party
    id : Text
    payload : PostOrdersIdPayload
  where
    signatory requestor

template PostOrdersIdResponse with
    requestor : Party
    requestId : ContractId PostOrdersId
    body : PostOrdersIdResponseBody
  where
    signatory requestor

data PostOrdersIdResponseBody
    = PostOrdersIdResponseBody_200 Order
    | PostOrdersIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of your orders. The orders are
--   returned sorted by creation date, with the most
--   recently created orders appearing first.</p>
template GetOrders with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    created : Int
        -- ^ Date this order was created.
    customer : Text
        -- ^ Only return orders for the given customer.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    ids : [Text]
        -- ^ Only return orders with the given IDs.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
    status : Text
        -- ^ Only return orders that have the given status. One of
        --   `created`, `paid`, `fulfilled`, or `refunded`.
    statusTransitions : Text
        -- ^ Filter orders based on when they were paid,
        --   fulfilled, canceled, or returned.
    upstreamIds : [Text]
        -- ^ Only return orders with the given upstream order IDs.
  where
    signatory requestor

template GetOrdersResponse with
    requestor : Party
    requestId : ContractId GetOrders
    body : GetOrdersResponseBody
  where
    signatory requestor

data GetOrdersResponseBody
    = GetOrdersResponseBody_200 GetOrdersResponseBody200
    | GetOrdersResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a new order object.</p>
template PostOrders with
    requestor : Party
    payload : PostOrdersPayload
  where
    signatory requestor

template PostOrdersResponse with
    requestor : Party
    requestId : ContractId PostOrders
    body : PostOrdersResponseBody
  where
    signatory requestor

data PostOrdersResponseBody
    = PostOrdersResponseBody_200 Order
    | PostOrdersResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>By default, you can see the 10 most recent
--   reversals stored directly on the transfer object, but
--   you can also retrieve details about a specific
--   reversal stored on the transfer.</p>
template GetTransfersTransferReversalsId with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    id : Text
    transfer : Text
  where
    signatory requestor

template GetTransfersTransferReversalsIdResponse with
    requestor : Party
    requestId : ContractId GetTransfersTransferReversalsId
    body : GetTransfersTransferReversalsIdResponseBody
  where
    signatory requestor

data GetTransfersTransferReversalsIdResponseBody
    = GetTransfersTransferReversalsIdResponseBody_200 TransferReversal
    | GetTransfersTransferReversalsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the specified reversal by setting the
--   values of the parameters passed. Any parameters not
--   provided will be left unchanged.</p> <p>This request
--   only accepts metadata and description as
--   arguments.</p>
template PostTransfersTransferReversalsId with
    requestor : Party
    id : Text
    transfer : Text
    payload : PostTransfersTransferReversalsIdPayload
  where
    signatory requestor

template PostTransfersTransferReversalsIdResponse with
    requestor : Party
    requestId : ContractId PostTransfersTransferReversalsId
    body : PostTransfersTransferReversalsIdResponseBody
  where
    signatory requestor

data PostTransfersTransferReversalsIdResponseBody
    = PostTransfersTransferReversalsIdResponseBody_200 TransferReversal
    | PostTransfersTransferReversalsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

template PostApplicationFeesIdRefund with
    requestor : Party
    id : Text
    payload : PostApplicationFeesIdRefundPayload
  where
    signatory requestor

template PostApplicationFeesIdRefundResponse with
    requestor : Party
    requestId : ContractId PostApplicationFeesIdRefund
    body : PostApplicationFeesIdRefundResponseBody
  where
    signatory requestor

data PostApplicationFeesIdRefundResponseBody
    = PostApplicationFeesIdRefundResponseBody_200 ApplicationFee
    | PostApplicationFeesIdRefundResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of objects that contain the rates
--   at which foreign currencies are converted to one
--   another. Only shows the currencies for which Stripe
--   supports.</p>
template GetExchangeRates with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is
        --   the currency that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with the exchange rate for currency
        --   X your subsequent call can include
        --   `ending_before=obj_bar` in order to fetch the
        --   previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and total number of
        --   supported payout currencies, and the default is the
        --   max.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   the currency that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with the exchange rate for currency
        --   X, your subsequent call can include
        --   `starting_after=X` in order to fetch the next page of
        --   the list.
  where
    signatory requestor

template GetExchangeRatesResponse with
    requestor : Party
    requestId : ContractId GetExchangeRates
    body : GetExchangeRatesResponseBody
  where
    signatory requestor

data GetExchangeRatesResponseBody
    = GetExchangeRatesResponseBody_200 GetExchangeRatesResponseBody200
    | GetExchangeRatesResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the details of an existing Report Run.
--   (Requires a <a
--   href="https://stripe.com/docs/keys#test-live-modes">live-mode
--   API key</a>.)</p>
template GetReportingReportRunsReportRun with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    reportRun : Text
  where
    signatory requestor

template GetReportingReportRunsReportRunResponse with
    requestor : Party
    requestId : ContractId GetReportingReportRunsReportRun
    body : GetReportingReportRunsReportRunResponseBody
  where
    signatory requestor

data GetReportingReportRunsReportRunResponseBody
    = GetReportingReportRunsReportRunResponseBody_200 ReportingReportRun
    | GetReportingReportRunsReportRunResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>You can see a list of the refunds belonging to a
--   specific application fee. Note that the 10 most
--   recent refunds are always available by default on the
--   application fee object. If you need more than those
--   10, you can use this API method and the
--   <code>limit</code> and <code>starting_after</code>
--   parameters to page through additional refunds.</p>
template GetApplicationFeesIdRefunds with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    id : Text
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetApplicationFeesIdRefundsResponse with
    requestor : Party
    requestId : ContractId GetApplicationFeesIdRefunds
    body : GetApplicationFeesIdRefundsResponseBody
  where
    signatory requestor

data GetApplicationFeesIdRefundsResponseBody
    = GetApplicationFeesIdRefundsResponseBody_200 GetApplicationFeesIdRefundsResponseBody200
    | GetApplicationFeesIdRefundsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Refunds an application fee that has previously
--   been collected but not yet refunded. Funds will be
--   refunded to the Stripe account from which the fee was
--   originally collected.</p> <p>You can optionally
--   refund only part of an application fee. You can do so
--   multiple times, until the entire fee has been
--   refunded.</p> <p>Once entirely refunded, an
--   application fee can’t be refunded again. This method
--   will raise an error when called on an
--   already-refunded application fee, or when trying to
--   refund more money than is left on an application
--   fee.</p>
template PostApplicationFeesIdRefunds with
    requestor : Party
    id : Text
    payload : PostApplicationFeesIdRefundsPayload
  where
    signatory requestor

template PostApplicationFeesIdRefundsResponse with
    requestor : Party
    requestId : ContractId PostApplicationFeesIdRefunds
    body : PostApplicationFeesIdRefundsResponseBody
  where
    signatory requestor

data PostApplicationFeesIdRefundsResponseBody
    = PostApplicationFeesIdRefundsResponseBody_200 FeeRefund
    | PostApplicationFeesIdRefundsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the details of a Report Type. (Requires
--   a <a
--   href="https://stripe.com/docs/keys#test-live-modes">live-mode
--   API key</a>.)</p>
template GetReportingReportTypesReportType with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    reportType : Text
  where
    signatory requestor

template GetReportingReportTypesReportTypeResponse with
    requestor : Party
    requestId : ContractId GetReportingReportTypesReportType
    body : GetReportingReportTypesReportTypeResponseBody
  where
    signatory requestor

data GetReportingReportTypesReportTypeResponseBody
    = GetReportingReportTypesReportTypeResponseBody_200 ReportingReportType
    | GetReportingReportTypesReportTypeResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of Issuing <code>Cardholder</code>
--   objects. The objects are sorted in descending order
--   by creation date, with the most recently created
--   object appearing first.</p>
template GetIssuingCardholders with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    created : Int
        -- ^ Only return cardholders that were created during the
        --   given date interval.
    email : Text
        -- ^ Only return cardholders that have the given email
        --   address.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    isDefault : Bool
        -- ^ Only return the default cardholder.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    phoneNumber : Text
        -- ^ Only return cardholders that have the given phone
        --   number.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
    status : Text
        -- ^ Only return cardholders that have the given status.
        --   One of `active`, `inactive`, or `blocked`.
    type_ : Text
        -- ^ Only return cardholders that have the given type. One
        --   of `individual` or `business_entity`.
  where
    signatory requestor

template GetIssuingCardholdersResponse with
    requestor : Party
    requestId : ContractId GetIssuingCardholders
    body : GetIssuingCardholdersResponseBody
  where
    signatory requestor

data GetIssuingCardholdersResponseBody
    = GetIssuingCardholdersResponseBody_200 GetIssuingCardholdersResponseBody200
    | GetIssuingCardholdersResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a new Issuing <code>Cardholder</code>
--   object that can be issued cards.</p>
template PostIssuingCardholders with
    requestor : Party
    payload : PostIssuingCardholdersPayload
  where
    signatory requestor

template PostIssuingCardholdersResponse with
    requestor : Party
    requestId : ContractId PostIssuingCardholders
    body : PostIssuingCardholdersResponseBody
  where
    signatory requestor

data PostIssuingCardholdersResponseBody
    = PostIssuingCardholdersResponseBody_200 IssuingCardholder
    | PostIssuingCardholdersResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the PIN for a card object, subject to
--   cardholder verification, see <a
--   href="/docs/issuing/pin_management">Retrieve and
--   update cardholder PIN</a></p>
template GetIssuingCardsCardPin with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    card : Text
    verification : Text
        -- ^ The id of the `Verification` that was sent and the
        --   code entered by the cardholder
  where
    signatory requestor

template GetIssuingCardsCardPinResponse with
    requestor : Party
    requestId : ContractId GetIssuingCardsCardPin
    body : GetIssuingCardsCardPinResponseBody
  where
    signatory requestor

data GetIssuingCardsCardPinResponseBody
    = GetIssuingCardsCardPinResponseBody_200 IssuingCardPin
    | GetIssuingCardsCardPinResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the PIN for a card, subject to cardholder
--   verification. See <a
--   href="/docs/issuing/pin_management">Retrieve and
--   update cardholder PIN</a></p>
template PostIssuingCardsCardPin with
    requestor : Party
    card : Text
    payload : PostIssuingCardsCardPinPayload
  where
    signatory requestor

template PostIssuingCardsCardPinResponse with
    requestor : Party
    requestId : ContractId PostIssuingCardsCardPin
    body : PostIssuingCardsCardPinResponseBody
  where
    signatory requestor

data PostIssuingCardsCardPinResponseBody
    = PostIssuingCardsCardPinResponseBody_200 IssuingCardPin
    | PostIssuingCardsCardPinResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Closing the dispute for a charge indicates that
--   you do not have any evidence to submit and are
--   essentially dismissing the dispute, acknowledging it
--   as lost.</p> <p>The status of the dispute will change
--   from <code>needs_response</code> to
--   <code>lost</code>. <em>Closing a dispute is
--   irreversible</em>.</p>
template PostDisputesDisputeClose with
    requestor : Party
    dispute : Text
    payload : PostDisputesDisputeClosePayload
  where
    signatory requestor

template PostDisputesDisputeCloseResponse with
    requestor : Party
    requestId : ContractId PostDisputesDisputeClose
    body : PostDisputesDisputeCloseResponseBody
  where
    signatory requestor

data PostDisputesDisputeCloseResponseBody
    = PostDisputesDisputeCloseResponseBody_200 Dispute
    | PostDisputesDisputeCloseResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of people associated with the
--   account’s legal entity. The people are returned
--   sorted by creation date, with the most recent people
--   appearing first.</p>
template GetAccountsAccountPersons with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    account : Text
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    relationship : Text
        -- ^ Filters on the list of people returned based on the
        --   person's relationship to the account's company.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetAccountsAccountPersonsResponse with
    requestor : Party
    requestId : ContractId GetAccountsAccountPersons
    body : GetAccountsAccountPersonsResponseBody
  where
    signatory requestor

data GetAccountsAccountPersonsResponseBody
    = GetAccountsAccountPersonsResponseBody_200 GetAccountsAccountPersonsResponseBody200
    | GetAccountsAccountPersonsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a new person.</p>
template PostAccountsAccountPersons with
    requestor : Party
    account : Text
    payload : PostAccountsAccountPersonsPayload
  where
    signatory requestor

template PostAccountsAccountPersonsResponse with
    requestor : Party
    requestId : ContractId PostAccountsAccountPersons
    body : PostAccountsAccountPersonsResponseBody
  where
    signatory requestor

data PostAccountsAccountPersonsResponseBody
    = PostAccountsAccountPersonsResponseBody_200 Person
    | PostAccountsAccountPersonsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of your order returns. The returns
--   are returned sorted by creation date, with the most
--   recently created return appearing first.</p>
template GetOrderReturns with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    created : Int
        -- ^ Date this return was created.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    order : Text
        -- ^ The order to retrieve returns for.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetOrderReturnsResponse with
    requestor : Party
    requestId : ContractId GetOrderReturns
    body : GetOrderReturnsResponseBody
  where
    signatory requestor

data GetOrderReturnsResponseBody
    = GetOrderReturnsResponseBody_200 GetOrderReturnsResponseBody200
    | GetOrderReturnsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of credit notes.</p>
template GetCreditNotes with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    customer : Text
        -- ^ Only return credit notes for the customer specified
        --   by this customer ID.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    invoice : Text
        -- ^ Only return credit notes for the invoice specified by
        --   this invoice ID.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetCreditNotesResponse with
    requestor : Party
    requestId : ContractId GetCreditNotes
    body : GetCreditNotesResponseBody
  where
    signatory requestor

data GetCreditNotesResponseBody
    = GetCreditNotesResponseBody_200 GetCreditNotesResponseBody200
    | GetCreditNotesResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Issue a credit note to adjust the amount of a
--   finalized invoice. For a <code>status=open</code>
--   invoice, a credit note reduces its
--   <code>amount_due</code>. For a
--   <code>status=paid</code> invoice, a credit note does
--   not affect its <code>amount_due</code>. Instead, it
--   can result in any combination of the following:</p>
--   <ul> <li>Refund: create a new refund (using
--   <code>refund_amount</code>) or link an existing
--   refund (using <code>refund</code>).</li> <li>Customer
--   balance credit: credit the customer’s balance (using
--   <code>credit_amount</code>) which will be
--   automatically applied to their next invoice when it’s
--   finalized.</li> <li>Outside of Stripe credit: record
--   the amount that is or will be credited outside of
--   Stripe (using <code>out_of_band_amount</code>).</li>
--   </ul> <p>For post-payment credit notes the sum of the
--   refund, credit and outside of Stripe amounts must
--   equal the credit note total.</p> <p>You may issue
--   multiple credit notes for an invoice. Each credit
--   note will increment the invoice’s
--   <code>pre_payment_credit_notes_amount</code> or
--   <code>post_payment_credit_notes_amount</code>
--   depending on its <code>status</code> at the time of
--   credit note creation.</p>
template PostCreditNotes with
    requestor : Party
    payload : PostCreditNotesPayload
  where
    signatory requestor

template PostCreditNotesResponse with
    requestor : Party
    requestId : ContractId PostCreditNotes
    body : PostCreditNotesResponseBody
  where
    signatory requestor

data PostCreditNotesResponseBody
    = PostCreditNotesResponseBody_200 CreditNote
    | PostCreditNotesResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the details of an existing customer. You
--   need only supply the unique customer identifier that
--   was returned upon customer creation.</p>
template GetCustomersCustomer with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    customer : Text
  where
    signatory requestor

template GetCustomersCustomerResponse with
    requestor : Party
    requestId : ContractId GetCustomersCustomer
    body : GetCustomersCustomerResponseBody
  where
    signatory requestor

data GetCustomersCustomerResponseBody
    = GetCustomersCustomerResponseBody_200 Customer
    | GetCustomersCustomerResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the specified customer by setting the
--   values of the parameters passed. Any parameters not
--   provided will be left unchanged. For example, if you
--   pass the <strong>source</strong> parameter, that
--   becomes the customer’s active source (e.g., a card)
--   to be used for all charges in the future. When you
--   update a customer to a new valid card source by
--   passing the <strong>source</strong> parameter: for
--   each of the customer’s current subscriptions, if the
--   subscription bills automatically and is in the
--   <code>past_due</code> state, then the latest open
--   invoice for the subscription with automatic
--   collection enabled will be retried. This retry will
--   not count as an automatic retry, and will not affect
--   the next regularly scheduled payment for the invoice.
--   Changing the <strong>default_source</strong> for a
--   customer will not trigger this behavior.</p> <p>This
--   request accepts mostly the same arguments as the
--   customer creation call.</p>
template PostCustomersCustomer with
    requestor : Party
    customer : Text
    payload : PostCustomersCustomerPayload
  where
    signatory requestor

template PostCustomersCustomerResponse with
    requestor : Party
    requestId : ContractId PostCustomersCustomer
    body : PostCustomersCustomerResponseBody
  where
    signatory requestor

data PostCustomersCustomerResponseBody
    = PostCustomersCustomerResponseBody_200 Customer
    | PostCustomersCustomerResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Permanently deletes a customer. It cannot be
--   undone. Also immediately cancels any active
--   subscriptions on the customer.</p>
template DeleteCustomersCustomer with
    requestor : Party
    customer : Text
  where
    signatory requestor

template DeleteCustomersCustomerResponse with
    requestor : Party
    requestId : ContractId DeleteCustomersCustomer
    body : DeleteCustomersCustomerResponseBody
  where
    signatory requestor

data DeleteCustomersCustomerResponseBody
    = DeleteCustomersCustomerResponseBody_200 DeletedCustomer
    | DeleteCustomersCustomerResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>By default, you can see the 10 most recent refunds
--   stored directly on the application fee object, but
--   you can also retrieve details about a specific refund
--   stored on the application fee.</p>
template GetApplicationFeesFeeRefundsId with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    fee : Text
    id : Text
  where
    signatory requestor

template GetApplicationFeesFeeRefundsIdResponse with
    requestor : Party
    requestId : ContractId GetApplicationFeesFeeRefundsId
    body : GetApplicationFeesFeeRefundsIdResponseBody
  where
    signatory requestor

data GetApplicationFeesFeeRefundsIdResponseBody
    = GetApplicationFeesFeeRefundsIdResponseBody_200 FeeRefund
    | GetApplicationFeesFeeRefundsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the specified application fee refund by
--   setting the values of the parameters passed. Any
--   parameters not provided will be left unchanged.</p>
--   <p>This request only accepts metadata as an
--   argument.</p>
template PostApplicationFeesFeeRefundsId with
    requestor : Party
    fee : Text
    id : Text
    payload : PostApplicationFeesFeeRefundsIdPayload
  where
    signatory requestor

template PostApplicationFeesFeeRefundsIdResponse with
    requestor : Party
    requestId : ContractId PostApplicationFeesFeeRefundsId
    body : PostApplicationFeesFeeRefundsIdResponseBody
  where
    signatory requestor

data PostApplicationFeesFeeRefundsIdResponseBody
    = PostApplicationFeesFeeRefundsIdResponseBody_200 FeeRefund
    | PostApplicationFeesFeeRefundsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Stripe will automatically send invoices to
--   customers according to your <a
--   href="https://dashboard.stripe.com/account/billing/automatic">subscriptions
--   settings</a>. However, if you’d like to manually send
--   an invoice to your customer out of the normal
--   schedule, you can do so. When sending invoices that
--   have already been paid, there will be no reference to
--   the payment in the email.</p> <p>Requests made in
--   test-mode result in no emails being sent, despite
--   sending an <code>invoice.sent</code> event.</p>
template PostInvoicesInvoiceSend with
    requestor : Party
    invoice : Text
    payload : PostInvoicesInvoiceSendPayload
  where
    signatory requestor

template PostInvoicesInvoiceSendResponse with
    requestor : Party
    requestId : ContractId PostInvoicesInvoiceSend
    body : PostInvoicesInvoiceSendResponseBody
  where
    signatory requestor

data PostInvoicesInvoiceSendResponseBody
    = PostInvoicesInvoiceSendResponseBody_200 Invoice
    | PostInvoicesInvoiceSendResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of all refunds you’ve previously
--   created. The refunds are returned in sorted order,
--   with the most recent refunds appearing first. For
--   convenience, the 10 most recent refunds are always
--   available by default on the charge object.</p>
template GetRefunds with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    charge : Text
        -- ^ Only return refunds for the charge specified by this
        --   charge ID.
    created : Int
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    paymentIntent : Text
        -- ^ Only return refunds for the PaymentIntent specified
        --   by this ID.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetRefundsResponse with
    requestor : Party
    requestId : ContractId GetRefunds
    body : GetRefundsResponseBody
  where
    signatory requestor

data GetRefundsResponseBody
    = GetRefundsResponseBody_200 GetRefundsResponseBody200
    | GetRefundsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Create a refund.</p>
template PostRefunds with
    requestor : Party
    payload : PostRefundsPayload
  where
    signatory requestor

template PostRefundsResponse with
    requestor : Party
    requestId : ContractId PostRefunds
    body : PostRefundsResponseBody
  where
    signatory requestor

data PostRefundsResponseBody
    = PostRefundsResponseBody_200 Refund
    | PostRefundsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the details of an account.</p>
template GetAccount with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
  where
    signatory requestor

template GetAccountResponse with
    requestor : Party
    requestId : ContractId GetAccount
    body : GetAccountResponseBody
  where
    signatory requestor

data GetAccountResponseBody
    = GetAccountResponseBody_200 Account
    | GetAccountResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates a connected <a
--   href="/docs/connect/accounts">Express or Custom
--   account</a> by setting the values of the parameters
--   passed. Any parameters not provided are left
--   unchanged. Most parameters can be changed only for
--   Custom accounts. (These are marked <strong>Custom
--   Only</strong> below.) Parameters marked
--   <strong>Custom and Express</strong> are supported by
--   both account types.</p> <p>To update your own
--   account, use the <a
--   href="https://dashboard.stripe.com/account">Dashboard</a>.
--   Refer to our <a
--   href="/docs/connect/updating-accounts">Connect</a>
--   documentation to learn more about updating
--   accounts.</p>
template PostAccount with
    requestor : Party
    payload : PostAccountPayload
  where
    signatory requestor

template PostAccountResponse with
    requestor : Party
    requestId : ContractId PostAccount
    body : PostAccountResponseBody
  where
    signatory requestor

data PostAccountResponseBody
    = PostAccountResponseBody_200 Account
    | PostAccountResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>With <a href="/docs/connect">Connect</a>, you can
--   delete Custom or Express accounts you manage.</p>
--   <p>Accounts created using test-mode keys can be
--   deleted at any time. Accounts created using live-mode
--   keys can only be deleted once all balances are
--   zero.</p> <p>If you want to delete your own account,
--   use the <a
--   href="https://dashboard.stripe.com/account">account
--   information tab in your account settings</a>
--   instead.</p>
template DeleteAccount with
    requestor : Party
    payload : DeleteAccountPayload
  where
    signatory requestor

template DeleteAccountResponse with
    requestor : Party
    requestId : ContractId DeleteAccount
    body : DeleteAccountResponseBody
  where
    signatory requestor

data DeleteAccountResponseBody
    = DeleteAccountResponseBody_200 DeletedAccount
    | DeleteAccountResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the details of an existing refund.</p>
template GetChargesChargeRefundsRefund with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    charge : Text
    refund : Text
  where
    signatory requestor

template GetChargesChargeRefundsRefundResponse with
    requestor : Party
    requestId : ContractId GetChargesChargeRefundsRefund
    body : GetChargesChargeRefundsRefundResponseBody
  where
    signatory requestor

data GetChargesChargeRefundsRefundResponseBody
    = GetChargesChargeRefundsRefundResponseBody_200 Refund
    | GetChargesChargeRefundsRefundResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Update a specified refund.</p>
template PostChargesChargeRefundsRefund with
    requestor : Party
    charge : Text
    refund : Text
    payload : PostChargesChargeRefundsRefundPayload
  where
    signatory requestor

template PostChargesChargeRefundsRefundResponse with
    requestor : Party
    requestId : ContractId PostChargesChargeRefundsRefund
    body : PostChargesChargeRefundsRefundResponseBody
  where
    signatory requestor

data PostChargesChargeRefundsRefundResponseBody
    = PostChargesChargeRefundsRefundResponseBody_200 Refund
    | PostChargesChargeRefundsRefundResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the details of an existing refund.</p>
template GetRefundsRefund with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    refund : Text
  where
    signatory requestor

template GetRefundsRefundResponse with
    requestor : Party
    requestId : ContractId GetRefundsRefund
    body : GetRefundsRefundResponseBody
  where
    signatory requestor

data GetRefundsRefundResponseBody
    = GetRefundsRefundResponseBody_200 Refund
    | GetRefundsRefundResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the specified refund by setting the values
--   of the parameters passed. Any parameters not provided
--   will be left unchanged.</p> <p>This request only
--   accepts <code>metadata</code> as an argument.</p>
template PostRefundsRefund with
    requestor : Party
    refund : Text
    payload : PostRefundsRefundPayload
  where
    signatory requestor

template PostRefundsRefundResponse with
    requestor : Party
    requestId : ContractId PostRefundsRefund
    body : PostRefundsRefundResponseBody
  where
    signatory requestor

data PostRefundsRefundResponseBody
    = PostRefundsRefundResponseBody_200 Refund
    | PostRefundsRefundResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of your tax rates. Tax rates are
--   returned sorted by creation date, with the most
--   recently created tax rates appearing first.</p>
template GetTaxRates with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    active : Bool
        -- ^ Optional flag to filter by tax rates that are either
        --   active or not active (archived)
    created : Int
        -- ^ Optional range for filtering created date
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    inclusive : Bool
        -- ^ Optional flag to filter by tax rates that are
        --   inclusive (or those that are not inclusive)
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetTaxRatesResponse with
    requestor : Party
    requestId : ContractId GetTaxRates
    body : GetTaxRatesResponseBody
  where
    signatory requestor

data GetTaxRatesResponseBody
    = GetTaxRatesResponseBody_200 GetTaxRatesResponseBody200
    | GetTaxRatesResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a new tax rate.</p>
template PostTaxRates with
    requestor : Party
    payload : PostTaxRatesPayload
  where
    signatory requestor

template PostTaxRatesResponse with
    requestor : Party
    requestId : ContractId PostTaxRates
    body : PostTaxRatesResponseBody
  where
    signatory requestor

data PostTaxRatesResponseBody
    = PostTaxRatesResponseBody_200 TaxRate
    | PostTaxRatesResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves a <code>Location</code> object.</p>
template GetTerminalLocationsLocation with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    location : Text
  where
    signatory requestor

template GetTerminalLocationsLocationResponse with
    requestor : Party
    requestId : ContractId GetTerminalLocationsLocation
    body : GetTerminalLocationsLocationResponseBody
  where
    signatory requestor

data GetTerminalLocationsLocationResponseBody
    = GetTerminalLocationsLocationResponseBody_200 TerminalLocation
    | GetTerminalLocationsLocationResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates a <code>Location</code> object by setting
--   the values of the parameters passed. Any parameters
--   not provided will be left unchanged.</p>
template PostTerminalLocationsLocation with
    requestor : Party
    location : Text
    payload : PostTerminalLocationsLocationPayload
  where
    signatory requestor

template PostTerminalLocationsLocationResponse with
    requestor : Party
    requestId : ContractId PostTerminalLocationsLocation
    body : PostTerminalLocationsLocationResponseBody
  where
    signatory requestor

data PostTerminalLocationsLocationResponseBody
    = PostTerminalLocationsLocationResponseBody_200 TerminalLocation
    | PostTerminalLocationsLocationResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Deletes a <code>Location</code> object.</p>
template DeleteTerminalLocationsLocation with
    requestor : Party
    location : Text
  where
    signatory requestor

template DeleteTerminalLocationsLocationResponse with
    requestor : Party
    requestId : ContractId DeleteTerminalLocationsLocation
    body : DeleteTerminalLocationsLocationResponseBody
  where
    signatory requestor

data DeleteTerminalLocationsLocationResponseBody
    = DeleteTerminalLocationsLocationResponseBody_200 DeletedTerminalLocation
    | DeleteTerminalLocationsLocationResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>To connect to a reader the Stripe Terminal SDK
--   needs to retrieve a short-lived connection token from
--   Stripe, proxied through your server. On your backend,
--   add an endpoint that creates and returns a connection
--   token.</p>
template PostTerminalConnectionTokens with
    requestor : Party
    payload : PostTerminalConnectionTokensPayload
  where
    signatory requestor

template PostTerminalConnectionTokensResponse with
    requestor : Party
    requestId : ContractId PostTerminalConnectionTokens
    body : PostTerminalConnectionTokensResponseBody
  where
    signatory requestor

data PostTerminalConnectionTokensResponseBody
    = PostTerminalConnectionTokensResponseBody_200 TerminalConnectionToken
    | PostTerminalConnectionTokensResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the coupon with the given ID.</p>
template GetCouponsCoupon with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    coupon : Text
  where
    signatory requestor

template GetCouponsCouponResponse with
    requestor : Party
    requestId : ContractId GetCouponsCoupon
    body : GetCouponsCouponResponseBody
  where
    signatory requestor

data GetCouponsCouponResponseBody
    = GetCouponsCouponResponseBody_200 Coupon
    | GetCouponsCouponResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the metadata of a coupon. Other coupon
--   details (currency, duration, amount_off) are, by
--   design, not editable.</p>
template PostCouponsCoupon with
    requestor : Party
    coupon : Text
    payload : PostCouponsCouponPayload
  where
    signatory requestor

template PostCouponsCouponResponse with
    requestor : Party
    requestId : ContractId PostCouponsCoupon
    body : PostCouponsCouponResponseBody
  where
    signatory requestor

data PostCouponsCouponResponseBody
    = PostCouponsCouponResponseBody_200 Coupon
    | PostCouponsCouponResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>You can delete coupons via the <a
--   href="https://dashboard.stripe.com/coupons">coupon
--   management</a> page of the Stripe dashboard. However,
--   deleting a coupon does not affect any customers who
--   have already applied the coupon; it means that new
--   customers can’t redeem the coupon. You can also
--   delete coupons via the API.</p>
template DeleteCouponsCoupon with
    requestor : Party
    coupon : Text
  where
    signatory requestor

template DeleteCouponsCouponResponse with
    requestor : Party
    requestId : ContractId DeleteCouponsCoupon
    body : DeleteCouponsCouponResponseBody
  where
    signatory requestor

data DeleteCouponsCouponResponseBody
    = DeleteCouponsCouponResponseBody_200 DeletedCoupon
    | DeleteCouponsCouponResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the file link with the given ID.</p>
template GetFileLinksLink with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    link : Text
  where
    signatory requestor

template GetFileLinksLinkResponse with
    requestor : Party
    requestId : ContractId GetFileLinksLink
    body : GetFileLinksLinkResponseBody
  where
    signatory requestor

data GetFileLinksLinkResponseBody
    = GetFileLinksLinkResponseBody_200 FileLink
    | GetFileLinksLinkResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates an existing file link object. Expired
--   links can no longer be updated.</p>
template PostFileLinksLink with
    requestor : Party
    link : Text
    payload : PostFileLinksLinkPayload
  where
    signatory requestor

template PostFileLinksLinkResponse with
    requestor : Party
    requestId : ContractId PostFileLinksLink
    body : PostFileLinksLinkResponseBody
  where
    signatory requestor

data PostFileLinksLinkResponseBody
    = PostFileLinksLinkResponseBody_200 FileLink
    | PostFileLinksLinkResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the details of a charge that has
--   previously been created. Supply the unique charge ID
--   that was returned from your previous request, and
--   Stripe will return the corresponding charge
--   information. The same information is returned when
--   creating or refunding the charge.</p>
template GetChargesCharge with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    charge : Text
  where
    signatory requestor

template GetChargesChargeResponse with
    requestor : Party
    requestId : ContractId GetChargesCharge
    body : GetChargesChargeResponseBody
  where
    signatory requestor

data GetChargesChargeResponseBody
    = GetChargesChargeResponseBody_200 Charge
    | GetChargesChargeResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the specified charge by setting the values
--   of the parameters passed. Any parameters not provided
--   will be left unchanged.</p>
template PostChargesCharge with
    requestor : Party
    charge : Text
    payload : PostChargesChargePayload
  where
    signatory requestor

template PostChargesChargeResponse with
    requestor : Party
    requestId : ContractId PostChargesCharge
    body : PostChargesChargeResponseBody
  where
    signatory requestor

data PostChargesChargeResponseBody
    = PostChargesChargeResponseBody_200 Charge
    | PostChargesChargeResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of SetupIntents.</p>
template GetSetupIntents with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    created : Int
        -- ^ A filter on the list, based on the object `created`
        --   field. The value can be a string with an integer Unix
        --   timestamp, or it can be a dictionary with a number of
        --   different query options.
    customer : Text
        -- ^ Only return SetupIntents for the customer specified
        --   by this customer ID.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    paymentMethod : Text
        -- ^ Only return SetupIntents associated with the
        --   specified payment method.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetSetupIntentsResponse with
    requestor : Party
    requestId : ContractId GetSetupIntents
    body : GetSetupIntentsResponseBody
  where
    signatory requestor

data GetSetupIntentsResponseBody
    = GetSetupIntentsResponseBody_200 GetSetupIntentsResponseBody200
    | GetSetupIntentsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a SetupIntent object.</p> <p>After the
--   SetupIntent is created, attach a payment method and
--   <a href="/docs/api/setup_intents/confirm">confirm</a>
--   to collect any required permissions to charge the
--   payment method later.</p>
template PostSetupIntents with
    requestor : Party
    payload : PostSetupIntentsPayload
  where
    signatory requestor

template PostSetupIntentsResponse with
    requestor : Party
    requestId : ContractId PostSetupIntents
    body : PostSetupIntentsResponseBody
  where
    signatory requestor

data PostSetupIntentsResponseBody
    = PostSetupIntentsResponseBody_200 SetupIntent
    | PostSetupIntentsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of people associated with the
--   account’s legal entity. The people are returned
--   sorted by creation date, with the most recent people
--   appearing first.</p>
template GetAccountsAccountPeople with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    account : Text
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    relationship : Text
        -- ^ Filters on the list of people returned based on the
        --   person's relationship to the account's company.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetAccountsAccountPeopleResponse with
    requestor : Party
    requestId : ContractId GetAccountsAccountPeople
    body : GetAccountsAccountPeopleResponseBody
  where
    signatory requestor

data GetAccountsAccountPeopleResponseBody
    = GetAccountsAccountPeopleResponseBody_200 GetAccountsAccountPeopleResponseBody200
    | GetAccountsAccountPeopleResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a new person.</p>
template PostAccountsAccountPeople with
    requestor : Party
    account : Text
    payload : PostAccountsAccountPeoplePayload
  where
    signatory requestor

template PostAccountsAccountPeopleResponse with
    requestor : Party
    requestId : ContractId PostAccountsAccountPeople
    body : PostAccountsAccountPeopleResponseBody
  where
    signatory requestor

data PostAccountsAccountPeopleResponseBody
    = PostAccountsAccountPeopleResponseBody_200 Person
    | PostAccountsAccountPeopleResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the current account balance, based on
--   the authentication that was used to make the request.
--   For a sample request, see <a
--   href="/docs/connect/account-balances#accounting-for-negative-balances">Accounting
--   for negative balances</a>.</p>
template GetBalance with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
  where
    signatory requestor

template GetBalanceResponse with
    requestor : Party
    requestId : ContractId GetBalance
    body : GetBalanceResponseBody
  where
    signatory requestor

data GetBalanceResponseBody
    = GetBalanceResponseBody_200 Balance
    | GetBalanceResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>By default, you can see the 10 most recent sources
--   stored on a Customer directly on the object, but you
--   can also retrieve details about a specific bank
--   account stored on the Stripe account.</p>
template GetCustomersCustomerBankAccountsId with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    customer : Text
    id : Text
  where
    signatory requestor

template GetCustomersCustomerBankAccountsIdResponse with
    requestor : Party
    requestId : ContractId GetCustomersCustomerBankAccountsId
    body : GetCustomersCustomerBankAccountsIdResponseBody
  where
    signatory requestor

data GetCustomersCustomerBankAccountsIdResponseBody
    = GetCustomersCustomerBankAccountsIdResponseBody_200 BankAccount
    | GetCustomersCustomerBankAccountsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Update a specified source for a given
--   customer.</p>
template PostCustomersCustomerBankAccountsId with
    requestor : Party
    customer : Text
    id : Text
    payload : PostCustomersCustomerBankAccountsIdPayload
  where
    signatory requestor

template PostCustomersCustomerBankAccountsIdResponse with
    requestor : Party
    requestId : ContractId PostCustomersCustomerBankAccountsId
    body : PostCustomersCustomerBankAccountsIdResponseBody
  where
    signatory requestor

data PostCustomersCustomerBankAccountsIdResponseBody
    = PostCustomersCustomerBankAccountsIdResponseBody_200 BankAccount
    | PostCustomersCustomerBankAccountsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Delete a specified source for a given
--   customer.</p>
template DeleteCustomersCustomerBankAccountsId with
    requestor : Party
    customer : Text
    id : Text
    payload : DeleteCustomersCustomerBankAccountsIdPayload
  where
    signatory requestor

template DeleteCustomersCustomerBankAccountsIdResponse with
    requestor : Party
    requestId : ContractId DeleteCustomersCustomerBankAccountsId
    body : DeleteCustomersCustomerBankAccountsIdResponseBody
  where
    signatory requestor

data DeleteCustomersCustomerBankAccountsIdResponseBody
    = DeleteCustomersCustomerBankAccountsIdResponseBody_200 DeletedPaymentSource
    | DeleteCustomersCustomerBankAccountsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of Issuing
--   <code>Authorization</code> objects. The objects are
--   sorted in descending order by creation date, with the
--   most recently created object appearing first.</p>
template GetIssuingAuthorizations with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    card : Text
        -- ^ Only return issuing transactions that belong to the
        --   given card.
    cardholder : Text
        -- ^ Only return authorizations belonging to the given
        --   cardholder.
    created : Int
        -- ^ Only return authorizations that were created during
        --   the given date interval.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
    status : Text
        -- ^ Only return authorizations with the given status. One
        --   of `pending`, `closed`, or `reversed`.
  where
    signatory requestor

template GetIssuingAuthorizationsResponse with
    requestor : Party
    requestId : ContractId GetIssuingAuthorizations
    body : GetIssuingAuthorizationsResponseBody
  where
    signatory requestor

data GetIssuingAuthorizationsResponseBody
    = GetIssuingAuthorizationsResponseBody_200 GetIssuingAuthorizationsResponseBody200
    | GetIssuingAuthorizationsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Approves a pending Issuing
--   <code>Authorization</code> object.</p>
template PostIssuingAuthorizationsAuthorizationApprove with
    requestor : Party
    authorization : Text
    payload : PostIssuingAuthorizationsAuthorizationApprovePayload
  where
    signatory requestor

template PostIssuingAuthorizationsAuthorizationApproveResponse with
    requestor : Party
    requestId : ContractId PostIssuingAuthorizationsAuthorizationApprove
    body : PostIssuingAuthorizationsAuthorizationApproveResponseBody
  where
    signatory requestor

data PostIssuingAuthorizationsAuthorizationApproveResponseBody
    = PostIssuingAuthorizationsAuthorizationApproveResponseBody_200 IssuingAuthorization
    | PostIssuingAuthorizationsAuthorizationApproveResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>You can see a list of the customer’s active
--   subscriptions. Note that the 10 most recent active
--   subscriptions are always available by default on the
--   customer object. If you need more than those 10, you
--   can use the limit and starting_after parameters to
--   page through additional subscriptions.</p>
template GetCustomersCustomerSubscriptions with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    customer : Text
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetCustomersCustomerSubscriptionsResponse with
    requestor : Party
    requestId : ContractId GetCustomersCustomerSubscriptions
    body : GetCustomersCustomerSubscriptionsResponseBody
  where
    signatory requestor

data GetCustomersCustomerSubscriptionsResponseBody
    = GetCustomersCustomerSubscriptionsResponseBody_200 GetCustomersCustomerSubscriptionsResponseBody200
    | GetCustomersCustomerSubscriptionsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a new subscription on an existing
--   customer.</p>
template PostCustomersCustomerSubscriptions with
    requestor : Party
    customer : Text
    payload : PostCustomersCustomerSubscriptionsPayload
  where
    signatory requestor

template PostCustomersCustomerSubscriptionsResponse with
    requestor : Party
    requestId : ContractId PostCustomersCustomerSubscriptions
    body : PostCustomersCustomerSubscriptionsResponseBody
  where
    signatory requestor

data PostCustomersCustomerSubscriptionsResponseBody
    = PostCustomersCustomerSubscriptionsResponseBody_200 Subscription
    | PostCustomersCustomerSubscriptionsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Removes the currently applied discount on a
--   subscription.</p>
template DeleteSubscriptionsSubscriptionExposedIdDiscount with
    requestor : Party
    subscriptionExposedId : Text
  where
    signatory requestor

template DeleteSubscriptionsSubscriptionExposedIdDiscountResponse with
    requestor : Party
    requestId : ContractId DeleteSubscriptionsSubscriptionExposedIdDiscount
    body : DeleteSubscriptionsSubscriptionExposedIdDiscountResponseBody
  where
    signatory requestor

data DeleteSubscriptionsSubscriptionExposedIdDiscountResponseBody
    = DeleteSubscriptionsSubscriptionExposedIdDiscountResponseBody_200 DeletedDiscount
    | DeleteSubscriptionsSubscriptionExposedIdDiscountResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of tax IDs for a customer.</p>
template GetCustomersCustomerTaxIds with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    customer : Text
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetCustomersCustomerTaxIdsResponse with
    requestor : Party
    requestId : ContractId GetCustomersCustomerTaxIds
    body : GetCustomersCustomerTaxIdsResponseBody
  where
    signatory requestor

data GetCustomersCustomerTaxIdsResponseBody
    = GetCustomersCustomerTaxIdsResponseBody_200 GetCustomersCustomerTaxIdsResponseBody200
    | GetCustomersCustomerTaxIdsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a new <code>TaxID</code> object for a
--   customer.</p>
template PostCustomersCustomerTaxIds with
    requestor : Party
    customer : Text
    payload : PostCustomersCustomerTaxIdsPayload
  where
    signatory requestor

template PostCustomersCustomerTaxIdsResponse with
    requestor : Party
    requestId : ContractId PostCustomersCustomerTaxIds
    body : PostCustomersCustomerTaxIdsResponseBody
  where
    signatory requestor

data PostCustomersCustomerTaxIdsResponseBody
    = PostCustomersCustomerTaxIdsResponseBody_200 TaxId
    | PostCustomersCustomerTaxIdsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the details of a PaymentIntent that has
--   previously been created. </p> <p>Client-side
--   retrieval using a publishable key is allowed when the
--   <code>client_secret</code> is provided in the query
--   string. </p> <p>When retrieved with a publishable
--   key, only a subset of properties will be returned.
--   Please refer to the <a
--   href="#payment_intent_object">payment intent</a>
--   object reference for more details.</p>
template GetPaymentIntentsIntent with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    clientSecret : Text
        -- ^ The client secret of the PaymentIntent. Required if a
        --   publishable key is used to retrieve the source.
    intent : Text
  where
    signatory requestor

template GetPaymentIntentsIntentResponse with
    requestor : Party
    requestId : ContractId GetPaymentIntentsIntent
    body : GetPaymentIntentsIntentResponseBody
  where
    signatory requestor

data GetPaymentIntentsIntentResponseBody
    = GetPaymentIntentsIntentResponseBody_200 PaymentIntent
    | GetPaymentIntentsIntentResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates properties on a PaymentIntent object
--   without confirming.</p> <p>Depending on which
--   properties you update, you may need to confirm the
--   PaymentIntent again. For example, updating the
--   <code>payment_method</code> will always require you
--   to confirm the PaymentIntent again. If you prefer to
--   update and confirm at the same time, we recommend
--   updating properties via the <a
--   href="/docs/api/payment_intents/confirm">confirm
--   API</a> instead.</p>
template PostPaymentIntentsIntent with
    requestor : Party
    intent : Text
    payload : PostPaymentIntentsIntentPayload
  where
    signatory requestor

template PostPaymentIntentsIntentResponse with
    requestor : Party
    requestId : ContractId PostPaymentIntentsIntent
    body : PostPaymentIntentsIntentResponseBody
  where
    signatory requestor

data PostPaymentIntentsIntentResponseBody
    = PostPaymentIntentsIntentResponseBody_200 PaymentIntent
    | PostPaymentIntentsIntentResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the balance transaction with the given
--   ID.</p> <p>Note that this endpoint previously used
--   the path <code>/v1/balance/history/:id</code>.</p>
template GetBalanceTransactionsId with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    id : Text
  where
    signatory requestor

template GetBalanceTransactionsIdResponse with
    requestor : Party
    requestId : ContractId GetBalanceTransactionsId
    body : GetBalanceTransactionsIdResponseBody
  where
    signatory requestor

data GetBalanceTransactionsIdResponseBody
    = GetBalanceTransactionsIdResponseBody_200 BalanceTransaction
    | GetBalanceTransactionsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Cancels a subscription schedule and its associated
--   subscription immediately (if the subscription
--   schedule has an active subscription). A subscription
--   schedule can only be canceled if its status is
--   <code>not_started</code> or <code>active</code>.</p>
template PostSubscriptionSchedulesScheduleCancel with
    requestor : Party
    schedule : Text
    payload : PostSubscriptionSchedulesScheduleCancelPayload
  where
    signatory requestor

template PostSubscriptionSchedulesScheduleCancelResponse with
    requestor : Party
    requestId : ContractId PostSubscriptionSchedulesScheduleCancel
    body : PostSubscriptionSchedulesScheduleCancelResponseBody
  where
    signatory requestor

data PostSubscriptionSchedulesScheduleCancelResponseBody
    = PostSubscriptionSchedulesScheduleCancelResponseBody_200 SubscriptionSchedule
    | PostSubscriptionSchedulesScheduleCancelResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the details of an existing subscription
--   schedule. You only need to supply the unique
--   subscription schedule identifier that was returned
--   upon subscription schedule creation.</p>
template GetSubscriptionSchedulesSchedule with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    schedule : Text
  where
    signatory requestor

template GetSubscriptionSchedulesScheduleResponse with
    requestor : Party
    requestId : ContractId GetSubscriptionSchedulesSchedule
    body : GetSubscriptionSchedulesScheduleResponseBody
  where
    signatory requestor

data GetSubscriptionSchedulesScheduleResponseBody
    = GetSubscriptionSchedulesScheduleResponseBody_200 SubscriptionSchedule
    | GetSubscriptionSchedulesScheduleResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates an existing subscription schedule.</p>
template PostSubscriptionSchedulesSchedule with
    requestor : Party
    schedule : Text
    payload : PostSubscriptionSchedulesSchedulePayload
  where
    signatory requestor

template PostSubscriptionSchedulesScheduleResponse with
    requestor : Party
    requestId : ContractId PostSubscriptionSchedulesSchedule
    body : PostSubscriptionSchedulesScheduleResponseBody
  where
    signatory requestor

data PostSubscriptionSchedulesScheduleResponseBody
    = PostSubscriptionSchedulesScheduleResponseBody_200 SubscriptionSchedule
    | PostSubscriptionSchedulesScheduleResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Verify a given source.</p>
template PostSourcesSourceVerify with
    requestor : Party
    source : Text
    payload : PostSourcesSourceVerifyPayload
  where
    signatory requestor

template PostSourcesSourceVerifyResponse with
    requestor : Party
    requestId : ContractId PostSourcesSourceVerify
    body : PostSourcesSourceVerifyResponseBody
  where
    signatory requestor

data PostSourcesSourceVerifyResponseBody
    = PostSourcesSourceVerifyResponseBody_200 Source
    | PostSourcesSourceVerifyResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Confirm that your customer intends to set up the
--   current or provided payment method. For example, you
--   would confirm a SetupIntent when a customer hits the
--   “Save” button on a payment method management page on
--   your website.</p> <p>If the selected payment method
--   does not require any additional steps from the
--   customer, the SetupIntent will transition to the
--   <code>succeeded</code> status.</p> <p>Otherwise, it
--   will transition to the <code>requires_action</code>
--   status and suggest additional actions via
--   <code>next_action</code>. If setup fails, the
--   SetupIntent will transition to the
--   <code>requires_payment_method</code> status.</p>
template PostSetupIntentsIntentConfirm with
    requestor : Party
    intent : Text
    payload : PostSetupIntentsIntentConfirmPayload
  where
    signatory requestor

template PostSetupIntentsIntentConfirmResponse with
    requestor : Party
    requestId : ContractId PostSetupIntentsIntentConfirm
    body : PostSetupIntentsIntentConfirmResponseBody
  where
    signatory requestor

data PostSetupIntentsIntentConfirmResponseBody
    = PostSetupIntentsIntentConfirmResponseBody_200 SetupIntent
    | PostSetupIntentsIntentConfirmResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>At any time, you can preview the upcoming invoice
--   for a customer. This will show you all the charges
--   that are pending, including subscription renewal
--   charges, invoice item charges, etc. It will also show
--   you any discount that is applicable to the
--   customer.</p> <p>Note that when you are viewing an
--   upcoming invoice, you are simply viewing a preview –
--   the invoice has not yet been created. As such, the
--   upcoming invoice will not show up in invoice listing
--   calls, and you cannot use the API to pay or edit the
--   invoice. If you want to change the amount that your
--   customer will be billed, you can add, remove, or
--   update pending invoice items, or update the
--   customer’s discount.</p> <p>You can preview the
--   effects of updating a subscription, including a
--   preview of what proration will take place. To ensure
--   that the actual proration is calculated exactly the
--   same as the previewed proration, you should pass a
--   <code>proration_date</code> parameter when doing the
--   actual subscription update. The value passed in
--   should be the same as the
--   <code>subscription_proration_date</code> returned on
--   the upcoming invoice resource. The recommended way to
--   get only the prorations being previewed is to
--   consider only proration line items where
--   <code>period[start]</code> is equal to the
--   <code>subscription_proration_date</code> on the
--   upcoming invoice resource.</p>
template GetInvoicesUpcoming with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    coupon : Text
        -- ^ The code of the coupon to apply. If `subscription` or
        --   `subscription_items` is provided, the invoice
        --   returned will preview updating or creating a
        --   subscription with that coupon. Otherwise, it will
        --   preview applying that coupon to the customer for the
        --   next upcoming invoice from among the customer's
        --   subscriptions. The invoice can be previewed without a
        --   coupon by passing this value as an empty string.
    customer : Text
        -- ^ The identifier of the customer whose upcoming invoice
        --   you'd like to retrieve.
    invoiceItems : [Text]
        -- ^ List of invoice items to add or update in the
        --   upcoming invoice preview.
    schedule : Text
        -- ^ The identifier of the unstarted schedule whose
        --   upcoming invoice you'd like to retrieve. Cannot be
        --   used with subscription or subscription fields.
    subscription : Text
        -- ^ The identifier of the subscription for which you'd
        --   like to retrieve the upcoming invoice. If not
        --   provided, but a `subscription_items` is provided, you
        --   will preview creating a subscription with those
        --   items. If neither `subscription` nor
        --   `subscription_items` is provided, you will retrieve
        --   the next upcoming invoice from among the customer's
        --   subscriptions.
    subscriptionBillingCycleAnchor : Text
        -- ^ For new subscriptions, a future timestamp to anchor
        --   the subscription's [billing
        --   cycle](https://stripe.com/docs/subscriptions/billing-cycle).
        --   This is used to determine the date of the first full
        --   invoice, and, for plans with `month` or `year`
        --   intervals, the day of the month for subsequent
        --   invoices. For existing subscriptions, the value can
        --   only be set to `now` or `unchanged`.
    subscriptionCancelAt : Text
        -- ^ Timestamp indicating when the subscription should be
        --   scheduled to cancel. Will prorate if within the
        --   current period and prorations have been enabled using
        --   `proration_behavior`.`
    subscriptionCancelAtPeriodEnd : Bool
        -- ^ Boolean indicating whether this subscription should
        --   cancel at the end of the current period.
    subscriptionCancelNow : Bool
        -- ^ This simulates the subscription being canceled or
        --   expired immediately.
    subscriptionDefaultTaxRates : Text
        -- ^ If provided, the invoice returned will preview
        --   updating or creating a subscription with these
        --   default tax rates. The default tax rates will apply
        --   to any line item that does not have `tax_rates` set.
    subscriptionItems : [Text]
        -- ^ List of subscription items, each with an attached
        --   plan.
    subscriptionProrate : Bool
        -- ^ If previewing an update to a subscription, this
        --   decides whether the preview will show the result of
        --   applying prorations or not. If set, one of
        --   `subscription_items` or `subscription`, and one of
        --   `subscription_items` or `subscription_trial_end` are
        --   required.
    subscriptionProrationBehavior : Text
        -- ^ Determines how to handle
        --   [prorations](https://stripe.com/docs/subscriptions/billing-cycle#prorations)
        --   when the billing cycle changes (e.g., when switching
        --   plans, resetting `billing_cycle_anchor=now`, or
        --   starting a trial), or if an item's `quantity`
        --   changes. Valid values are `create_prorations`,
        --   `none`, or `always_invoice`. Passing
        --   `create_prorations` will cause proration invoice
        --   items to be created when applicable. These proration
        --   items will only be invoiced immediately under
        --   [certain
        --   conditions](https://stripe.com/docs/subscriptions/upgrading-downgrading#immediate-payment).
        --   In order to always invoice immediately for
        --   prorations, pass `always_invoice`. Prorations can be
        --   disabled by passing `none`.
    subscriptionProrationDate : Int
        -- ^ If previewing an update to a subscription, and doing
        --   proration, `subscription_proration_date` forces the
        --   proration to be calculated as though the update was
        --   done at the specified time. The time given must be
        --   within the current subscription period, and cannot be
        --   before the subscription was on its current plan. If
        --   set, `subscription`, and one of `subscription_items`,
        --   or `subscription_trial_end` are required. Also,
        --   `subscription_proration` cannot be set to false.
    subscriptionStartDate : Int
        -- ^ Date a subscription is intended to start (can be
        --   future or past)
    subscriptionTaxPercent : Decimal
        -- ^ If provided, the invoice returned will preview
        --   updating or creating a subscription with that tax
        --   percent. If set, one of `subscription_items` or
        --   `subscription` is required. This field has been
        --   deprecated and will be removed in a future API
        --   version, for further information view the [migration
        --   docs](https://stripe.com/docs/billing/migration/taxes)
        --   for `tax_rates`.
    subscriptionTrialEnd : Text
        -- ^ If provided, the invoice returned will preview
        --   updating or creating a subscription with that trial
        --   end. If set, one of `subscription_items` or
        --   `subscription` is required.
    subscriptionTrialFromPlan : Bool
        -- ^ Indicates if a plan's `trial_period_days` should be
        --   applied to the subscription. Setting
        --   `subscription_trial_end` per subscription is
        --   preferred, and this defaults to `false`. Setting this
        --   flag to `true` together with `subscription_trial_end`
        --   is not allowed.
  where
    signatory requestor

template GetInvoicesUpcomingResponse with
    requestor : Party
    requestId : ContractId GetInvoicesUpcoming
    body : GetInvoicesUpcomingResponseBody
  where
    signatory requestor

data GetInvoicesUpcomingResponseBody
    = GetInvoicesUpcomingResponseBody_200 Invoice
    | GetInvoicesUpcomingResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of transactions that updated the
--   customer’s <a
--   href="/docs/api/customers/object#customer_object-balance"><code>balance</code></a>.</p>
template GetCustomersCustomerBalanceTransactions with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    customer : Text
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetCustomersCustomerBalanceTransactionsResponse with
    requestor : Party
    requestId : ContractId GetCustomersCustomerBalanceTransactions
    body : GetCustomersCustomerBalanceTransactionsResponseBody
  where
    signatory requestor

data GetCustomersCustomerBalanceTransactionsResponseBody
    = GetCustomersCustomerBalanceTransactionsResponseBody_200 GetCustomersCustomerBalanceTransactionsResponseBody200
    | GetCustomersCustomerBalanceTransactionsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates an immutable transaction that updates the
--   customer’s <a
--   href="/docs/api/customers/object#customer_object-balance"><code>balance</code></a>.</p>
template PostCustomersCustomerBalanceTransactions with
    requestor : Party
    customer : Text
    payload : PostCustomersCustomerBalanceTransactionsPayload
  where
    signatory requestor

template PostCustomersCustomerBalanceTransactionsResponse with
    requestor : Party
    requestId : ContractId PostCustomersCustomerBalanceTransactions
    body : PostCustomersCustomerBalanceTransactionsResponseBody
  where
    signatory requestor

data PostCustomersCustomerBalanceTransactionsResponseBody
    = PostCustomersCustomerBalanceTransactionsResponseBody_200 CustomerBalanceTransaction
    | PostCustomersCustomerBalanceTransactionsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Cancels a top-up. Only pending top-ups can be
--   canceled.</p>
template PostTopupsTopupCancel with
    requestor : Party
    topup : Text
    payload : PostTopupsTopupCancelPayload
  where
    signatory requestor

template PostTopupsTopupCancelResponse with
    requestor : Party
    requestId : ContractId PostTopupsTopupCancel
    body : PostTopupsTopupCancelResponseBody
  where
    signatory requestor

data PostTopupsTopupCancelResponseBody
    = PostTopupsTopupCancelResponseBody_200 Topup
    | PostTopupsTopupCancelResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>List source transactions for a given source.</p>
template GetSourcesSourceSourceTransactions with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    source : Text
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetSourcesSourceSourceTransactionsResponse with
    requestor : Party
    requestId : ContractId GetSourcesSourceSourceTransactions
    body : GetSourcesSourceSourceTransactionsResponseBody
  where
    signatory requestor

data GetSourcesSourceSourceTransactionsResponseBody
    = GetSourcesSourceSourceTransactionsResponseBody_200 GetSourcesSourceSourceTransactionsResponseBody200
    | GetSourcesSourceSourceTransactionsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Create an external account for a given
--   account.</p>
template PostAccountBankAccounts with
    requestor : Party
    payload : PostAccountBankAccountsPayload
  where
    signatory requestor

template PostAccountBankAccountsResponse with
    requestor : Party
    requestId : ContractId PostAccountBankAccounts
    body : PostAccountBankAccountsResponseBody
  where
    signatory requestor

data PostAccountBankAccountsResponseBody
    = PostAccountBankAccountsResponseBody_200 ExternalAccount
    | PostAccountBankAccountsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of Issuing <code>Dispute</code>
--   objects. The objects are sorted in descending order
--   by creation date, with the most recently created
--   object appearing first.</p>
template GetIssuingDisputes with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    created : Int
        -- ^ Only return issuing disputes that were created during
        --   the given date interval.
    disputedTransaction : Text
        -- ^ Only return issuing disputes for the given
        --   transaction.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetIssuingDisputesResponse with
    requestor : Party
    requestId : ContractId GetIssuingDisputes
    body : GetIssuingDisputesResponseBody
  where
    signatory requestor

data GetIssuingDisputesResponseBody
    = GetIssuingDisputesResponseBody_200 GetIssuingDisputesResponseBody200
    | GetIssuingDisputesResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates an Issuing <code>Dispute</code>
--   object.</p>
template PostIssuingDisputes with
    requestor : Party
    payload : PostIssuingDisputesPayload
  where
    signatory requestor

template PostIssuingDisputesResponse with
    requestor : Party
    requestId : ContractId PostIssuingDisputes
    body : PostIssuingDisputesResponseBody
  where
    signatory requestor

data PostIssuingDisputesResponseBody
    = PostIssuingDisputesResponseBody_200 IssuingDispute
    | PostIssuingDisputesResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Capture the funds of an existing uncaptured
--   PaymentIntent when its status is
--   <code>requires_capture</code>.</p> <p>Uncaptured
--   PaymentIntents will be canceled exactly seven days
--   after they are created.</p> <p>Learn more about <a
--   href="/docs/payments/capture-later">separate
--   authorization and capture</a>.</p>
template PostPaymentIntentsIntentCapture with
    requestor : Party
    intent : Text
    payload : PostPaymentIntentsIntentCapturePayload
  where
    signatory requestor

template PostPaymentIntentsIntentCaptureResponse with
    requestor : Party
    requestId : ContractId PostPaymentIntentsIntentCapture
    body : PostPaymentIntentsIntentCaptureResponseBody
  where
    signatory requestor

data PostPaymentIntentsIntentCaptureResponseBody
    = PostPaymentIntentsIntentCaptureResponseBody_200 PaymentIntent
    | PostPaymentIntentsIntentCaptureResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves an existing person.</p>
template GetAccountsAccountPersonsPerson with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    account : Text
    person : Text
  where
    signatory requestor

template GetAccountsAccountPersonsPersonResponse with
    requestor : Party
    requestId : ContractId GetAccountsAccountPersonsPerson
    body : GetAccountsAccountPersonsPersonResponseBody
  where
    signatory requestor

data GetAccountsAccountPersonsPersonResponseBody
    = GetAccountsAccountPersonsPersonResponseBody_200 Person
    | GetAccountsAccountPersonsPersonResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates an existing person.</p>
template PostAccountsAccountPersonsPerson with
    requestor : Party
    account : Text
    person : Text
    payload : PostAccountsAccountPersonsPersonPayload
  where
    signatory requestor

template PostAccountsAccountPersonsPersonResponse with
    requestor : Party
    requestId : ContractId PostAccountsAccountPersonsPerson
    body : PostAccountsAccountPersonsPersonResponseBody
  where
    signatory requestor

data PostAccountsAccountPersonsPersonResponseBody
    = PostAccountsAccountPersonsPersonResponseBody_200 Person
    | PostAccountsAccountPersonsPersonResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Deletes an existing person’s relationship to the
--   account’s legal entity. Any person with a
--   relationship for an account can be deleted through
--   the API, except if the person is the
--   <code>account_opener</code>. If your integration is
--   using the <code>executive</code> parameter, you
--   cannot delete the only verified
--   <code>executive</code> on file.</p>
template DeleteAccountsAccountPersonsPerson with
    requestor : Party
    account : Text
    person : Text
  where
    signatory requestor

template DeleteAccountsAccountPersonsPersonResponse with
    requestor : Party
    requestId : ContractId DeleteAccountsAccountPersonsPerson
    body : DeleteAccountsAccountPersonsPersonResponseBody
  where
    signatory requestor

data DeleteAccountsAccountPersonsPersonResponseBody
    = DeleteAccountsAccountPersonsPersonResponseBody_200 DeletedPerson
    | DeleteAccountsAccountPersonsPersonResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieve a specified external account for a given
--   account.</p>
template GetAccountExternalAccountsId with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    id : Text
  where
    signatory requestor

template GetAccountExternalAccountsIdResponse with
    requestor : Party
    requestId : ContractId GetAccountExternalAccountsId
    body : GetAccountExternalAccountsIdResponseBody
  where
    signatory requestor

data GetAccountExternalAccountsIdResponseBody
    = GetAccountExternalAccountsIdResponseBody_200 ExternalAccount
    | GetAccountExternalAccountsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the metadata, account holder name, and
--   account holder type of a bank account belonging to a
--   <a href="/docs/connect/custom-accounts">Custom
--   account</a>, and optionally sets it as the default
--   for its currency. Other bank account details are not
--   editable by design.</p> <p>You can re-enable a
--   disabled bank account by performing an update call
--   without providing any arguments or changes.</p>
template PostAccountExternalAccountsId with
    requestor : Party
    id : Text
    payload : PostAccountExternalAccountsIdPayload
  where
    signatory requestor

template PostAccountExternalAccountsIdResponse with
    requestor : Party
    requestId : ContractId PostAccountExternalAccountsId
    body : PostAccountExternalAccountsIdResponseBody
  where
    signatory requestor

data PostAccountExternalAccountsIdResponseBody
    = PostAccountExternalAccountsIdResponseBody_200 ExternalAccount
    | PostAccountExternalAccountsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Delete a specified external account for a given
--   account.</p>
template DeleteAccountExternalAccountsId with
    requestor : Party
    id : Text
  where
    signatory requestor

template DeleteAccountExternalAccountsIdResponse with
    requestor : Party
    requestId : ContractId DeleteAccountExternalAccountsId
    body : DeleteAccountExternalAccountsIdResponseBody
  where
    signatory requestor

data DeleteAccountExternalAccountsIdResponseBody
    = DeleteAccountExternalAccountsIdResponseBody_200 DeletedExternalAccount
    | DeleteAccountExternalAccountsIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Create an external account for a given
--   account.</p>
template PostAccountsAccountBankAccounts with
    requestor : Party
    account : Text
    payload : PostAccountsAccountBankAccountsPayload
  where
    signatory requestor

template PostAccountsAccountBankAccountsResponse with
    requestor : Party
    requestId : ContractId PostAccountsAccountBankAccounts
    body : PostAccountsAccountBankAccountsResponseBody
  where
    signatory requestor

data PostAccountsAccountBankAccountsResponseBody
    = PostAccountsAccountBankAccountsResponseBody_200 ExternalAccount
    | PostAccountsAccountBankAccountsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>When retrieving a credit note, you’ll get a
--   <strong>lines</strong> property containing the the
--   first handful of those items. There is also a URL
--   where you can retrieve the full (paginated) list of
--   line items.</p>
template GetCreditNotesCreditNoteLines with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    creditNote : Text
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetCreditNotesCreditNoteLinesResponse with
    requestor : Party
    requestId : ContractId GetCreditNotesCreditNoteLines
    body : GetCreditNotesCreditNoteLinesResponseBody
  where
    signatory requestor

data GetCreditNotesCreditNoteLinesResponseBody
    = GetCreditNotesCreditNoteLinesResponseBody_200 GetCreditNotesCreditNoteLinesResponseBody200
    | GetCreditNotesCreditNoteLinesResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the subscription with the given ID.</p>
template GetCustomersCustomerSubscriptionsSubscriptionExposedId with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    customer : Text
    subscriptionExposedId : Text
  where
    signatory requestor

template GetCustomersCustomerSubscriptionsSubscriptionExposedIdResponse with
    requestor : Party
    requestId : ContractId GetCustomersCustomerSubscriptionsSubscriptionExposedId
    body : GetCustomersCustomerSubscriptionsSubscriptionExposedIdResponseBody
  where
    signatory requestor

data GetCustomersCustomerSubscriptionsSubscriptionExposedIdResponseBody
    = GetCustomersCustomerSubscriptionsSubscriptionExposedIdResponseBody_200 Subscription
    | GetCustomersCustomerSubscriptionsSubscriptionExposedIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates an existing subscription on a customer to
--   match the specified parameters. When changing plans
--   or quantities, we will optionally prorate the price
--   we charge next month to make up for any price
--   changes. To preview how the proration will be
--   calculated, use the <a
--   href="#upcoming_invoice">upcoming invoice</a>
--   endpoint.</p>
template PostCustomersCustomerSubscriptionsSubscriptionExposedId with
    requestor : Party
    customer : Text
    subscriptionExposedId : Text
    payload : PostCustomersCustomerSubscriptionsSubscriptionExposedIdPayload
  where
    signatory requestor

template PostCustomersCustomerSubscriptionsSubscriptionExposedIdResponse with
    requestor : Party
    requestId : ContractId PostCustomersCustomerSubscriptionsSubscriptionExposedId
    body : PostCustomersCustomerSubscriptionsSubscriptionExposedIdResponseBody
  where
    signatory requestor

data PostCustomersCustomerSubscriptionsSubscriptionExposedIdResponseBody
    = PostCustomersCustomerSubscriptionsSubscriptionExposedIdResponseBody_200 Subscription
    | PostCustomersCustomerSubscriptionsSubscriptionExposedIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Cancels a customer’s subscription. If you set the
--   <code>at_period_end</code> parameter to
--   <code>true</code>, the subscription will remain
--   active until the end of the period, at which point it
--   will be canceled and not renewed. Otherwise, with the
--   default <code>false</code> value, the subscription is
--   terminated immediately. In either case, the customer
--   will not be charged again for the subscription.</p>
--   <p>Note, however, that any pending invoice items that
--   you’ve created will still be charged for at the end
--   of the period, unless manually <a
--   href="#delete_invoiceitem">deleted</a>. If you’ve set
--   the subscription to cancel at the end of the period,
--   any pending prorations will also be left in place and
--   collected at the end of the period. But if the
--   subscription is set to cancel immediately, pending
--   prorations will be removed.</p> <p>By default, upon
--   subscription cancellation, Stripe will stop automatic
--   collection of all finalized invoices for the
--   customer. This is intended to prevent unexpected
--   payment attempts after the customer has canceled a
--   subscription. However, you can resume automatic
--   collection of the invoices manually after
--   subscription cancellation to have us proceed. Or, you
--   could check for unpaid invoices before allowing the
--   customer to cancel the subscription at all.</p>
template DeleteCustomersCustomerSubscriptionsSubscriptionExposedId with
    requestor : Party
    customer : Text
    subscriptionExposedId : Text
    payload : DeleteCustomersCustomerSubscriptionsSubscriptionExposedIdPayload
  where
    signatory requestor

template DeleteCustomersCustomerSubscriptionsSubscriptionExposedIdResponse with
    requestor : Party
    requestId : ContractId DeleteCustomersCustomerSubscriptionsSubscriptionExposedId
    body : DeleteCustomersCustomerSubscriptionsSubscriptionExposedIdResponseBody
  where
    signatory requestor

data DeleteCustomersCustomerSubscriptionsSubscriptionExposedIdResponseBody
    = DeleteCustomersCustomerSubscriptionsSubscriptionExposedIdResponseBody_200 Subscription
    | DeleteCustomersCustomerSubscriptionsSubscriptionExposedIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>You can see a list of the bank accounts belonging
--   to a Customer. Note that the 10 most recent sources
--   are always available by default on the Customer. If
--   you need more than those 10, you can use this API
--   method and the <code>limit</code> and
--   <code>starting_after</code> parameters to page
--   through additional bank accounts.</p>
template GetCustomersCustomerBankAccounts with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    customer : Text
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetCustomersCustomerBankAccountsResponse with
    requestor : Party
    requestId : ContractId GetCustomersCustomerBankAccounts
    body : GetCustomersCustomerBankAccountsResponseBody
  where
    signatory requestor

data GetCustomersCustomerBankAccountsResponseBody
    = GetCustomersCustomerBankAccountsResponseBody_200 GetCustomersCustomerBankAccountsResponseBody200
    | GetCustomersCustomerBankAccountsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>When you create a new credit card, you must
--   specify a customer or recipient on which to create
--   it.</p> <p>If the card’s owner has no default card,
--   then the new card will become the default. However,
--   if the owner already has a default, then it will not
--   change. To change the default, you should <a
--   href="/docs/api#update_customer">update the
--   customer</a> to have a new
--   <code>default_source</code>.</p>
template PostCustomersCustomerBankAccounts with
    requestor : Party
    customer : Text
    payload : PostCustomersCustomerBankAccountsPayload
  where
    signatory requestor

template PostCustomersCustomerBankAccountsResponse with
    requestor : Party
    requestId : ContractId PostCustomersCustomerBankAccounts
    body : PostCustomersCustomerBankAccountsResponseBody
  where
    signatory requestor

data PostCustomersCustomerBankAccountsResponseBody
    = PostCustomersCustomerBankAccountsResponseBody_200 PaymentSource
    | PostCustomersCustomerBankAccountsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Stripe automatically creates and then attempts to
--   collect payment on invoices for customers on
--   subscriptions according to your <a
--   href="https://dashboard.stripe.com/account/billing/automatic">subscriptions
--   settings</a>. However, if you’d like to attempt
--   payment on an invoice out of the normal collection
--   schedule or for some other reason, you can do so.</p>
template PostInvoicesInvoicePay with
    requestor : Party
    invoice : Text
    payload : PostInvoicesInvoicePayPayload
  where
    signatory requestor

template PostInvoicesInvoicePayResponse with
    requestor : Party
    requestId : ContractId PostInvoicesInvoicePay
    body : PostInvoicesInvoicePayResponseBody
  where
    signatory requestor

data PostInvoicesInvoicePayResponseBody
    = PostInvoicesInvoicePayResponseBody_200 Invoice
    | PostInvoicesInvoicePayResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the details of an application fee that
--   your account has collected. The same information is
--   returned when refunding the application fee.</p>
template GetApplicationFeesId with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    id : Text
  where
    signatory requestor

template GetApplicationFeesIdResponse with
    requestor : Party
    requestId : ContractId GetApplicationFeesId
    body : GetApplicationFeesIdResponseBody
  where
    signatory requestor

data GetApplicationFeesIdResponseBody
    = GetApplicationFeesIdResponseBody_200 ApplicationFee
    | GetApplicationFeesIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>List external accounts for an account.</p>
template GetAccountExternalAccounts with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetAccountExternalAccountsResponse with
    requestor : Party
    requestId : ContractId GetAccountExternalAccounts
    body : GetAccountExternalAccountsResponseBody
  where
    signatory requestor

data GetAccountExternalAccountsResponseBody
    = GetAccountExternalAccountsResponseBody_200 GetAccountExternalAccountsResponseBody200
    | GetAccountExternalAccountsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Create an external account for a given
--   account.</p>
template PostAccountExternalAccounts with
    requestor : Party
    payload : PostAccountExternalAccountsPayload
  where
    signatory requestor

template PostAccountExternalAccountsResponse with
    requestor : Party
    requestId : ContractId PostAccountExternalAccounts
    body : PostAccountExternalAccountsResponseBody
  where
    signatory requestor

data PostAccountExternalAccountsResponseBody
    = PostAccountExternalAccountsResponseBody_200 ExternalAccount
    | PostAccountExternalAccountsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the balance transaction with the given
--   ID.</p> <p>Note that this endpoint previously used
--   the path <code>/v1/balance/history/:id</code>.</p>
template GetBalanceHistoryId with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    id : Text
  where
    signatory requestor

template GetBalanceHistoryIdResponse with
    requestor : Party
    requestId : ContractId GetBalanceHistoryId
    body : GetBalanceHistoryIdResponseBody
  where
    signatory requestor

data GetBalanceHistoryIdResponseBody
    = GetBalanceHistoryIdResponseBody_200 BalanceTransaction
    | GetBalanceHistoryIdResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Releases the subscription schedule immediately,
--   which will stop scheduling of its phases, but leave
--   any existing subscription in place. A schedule can
--   only be released if its status is
--   <code>not_started</code> or <code>active</code>. If
--   the subscription schedule is currently associated
--   with a subscription, releasing it will remove its
--   <code>subscription</code> property and set the
--   subscription’s ID to the
--   <code>released_subscription</code> property.</p>
template PostSubscriptionSchedulesScheduleRelease with
    requestor : Party
    schedule : Text
    payload : PostSubscriptionSchedulesScheduleReleasePayload
  where
    signatory requestor

template PostSubscriptionSchedulesScheduleReleaseResponse with
    requestor : Party
    requestId : ContractId PostSubscriptionSchedulesScheduleRelease
    body : PostSubscriptionSchedulesScheduleReleaseResponseBody
  where
    signatory requestor

data PostSubscriptionSchedulesScheduleReleaseResponseBody
    = PostSubscriptionSchedulesScheduleReleaseResponseBody_200 SubscriptionSchedule
    | PostSubscriptionSchedulesScheduleReleaseResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of PaymentIntents.</p>
template GetPaymentIntents with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    created : Int
        -- ^ A filter on the list, based on the object `created`
        --   field. The value can be a string with an integer Unix
        --   timestamp, or it can be a dictionary with a number of
        --   different query options.
    customer : Text
        -- ^ Only return PaymentIntents for the customer specified
        --   by this customer ID.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetPaymentIntentsResponse with
    requestor : Party
    requestId : ContractId GetPaymentIntents
    body : GetPaymentIntentsResponseBody
  where
    signatory requestor

data GetPaymentIntentsResponseBody
    = GetPaymentIntentsResponseBody_200 GetPaymentIntentsResponseBody200
    | GetPaymentIntentsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a PaymentIntent object.</p> <p>After the
--   PaymentIntent is created, attach a payment method and
--   <a
--   href="/docs/api/payment_intents/confirm">confirm</a>
--   to continue the payment. You can read more about the
--   different payment flows available via the Payment
--   Intents API <a
--   href="/docs/payments/payment-intents">here</a>.</p>
--   <p>When <code>confirm=true</code> is used during
--   creation, it is equivalent to creating and confirming
--   the PaymentIntent in the same call. You may use any
--   parameters available in the <a
--   href="/docs/api/payment_intents/confirm">confirm
--   API</a> when <code>confirm=true</code> is
--   supplied.</p>
template PostPaymentIntents with
    requestor : Party
    payload : PostPaymentIntentsPayload
  where
    signatory requestor

template PostPaymentIntentsResponse with
    requestor : Party
    requestId : ContractId PostPaymentIntents
    body : PostPaymentIntentsResponseBody
  where
    signatory requestor

data PostPaymentIntentsResponseBody
    = PostPaymentIntentsResponseBody_200 PaymentIntent
    | PostPaymentIntentsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of existing transfers sent to
--   connected accounts. The transfers are returned in
--   sorted order, with the most recently created
--   transfers appearing first.</p>
template GetTransfers with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    created : Int
    destination : Text
        -- ^ Only return transfers for the destination specified
        --   by this account ID.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
    transferGroup : Text
        -- ^ Only return transfers with the specified transfer
        --   group.
  where
    signatory requestor

template GetTransfersResponse with
    requestor : Party
    requestId : ContractId GetTransfers
    body : GetTransfersResponseBody
  where
    signatory requestor

data GetTransfersResponseBody
    = GetTransfersResponseBody_200 GetTransfersResponseBody200
    | GetTransfersResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>To send funds from your Stripe account to a
--   connected account, you create a new transfer object.
--   Your <a href="#balance">Stripe balance</a> must be
--   able to cover the transfer amount, or you’ll receive
--   an “Insufficient Funds” error.</p>
template PostTransfers with
    requestor : Party
    payload : PostTransfersPayload
  where
    signatory requestor

template PostTransfersResponse with
    requestor : Party
    requestId : ContractId PostTransfers
    body : PostTransfersResponseBody
  where
    signatory requestor

data PostTransfersResponseBody
    = PostTransfersResponseBody_200 Transfer
    | PostTransfersResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Capture the payment of an existing, uncaptured,
--   charge. This is the second half of the two-step
--   payment flow, where first you <a
--   href="#create_charge">created a charge</a> with the
--   capture option set to false.</p> <p>Uncaptured
--   payments expire exactly seven days after they are
--   created. If they are not captured by that point in
--   time, they will be marked as refunded and will no
--   longer be capturable.</p>
template PostChargesChargeCapture with
    requestor : Party
    charge : Text
    payload : PostChargesChargeCapturePayload
  where
    signatory requestor

template PostChargesChargeCaptureResponse with
    requestor : Party
    requestId : ContractId PostChargesChargeCapture
    body : PostChargesChargeCaptureResponseBody
  where
    signatory requestor

data PostChargesChargeCaptureResponseBody
    = PostChargesChargeCaptureResponseBody_200 Charge
    | PostChargesChargeCaptureResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of Issuing <code>Card</code>
--   objects. The objects are sorted in descending order
--   by creation date, with the most recently created
--   object appearing first.</p>
template GetIssuingCards with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    cardholder : Text
        -- ^ Only return cards belonging to the Cardholder with
        --   the provided ID.
    created : Int
        -- ^ Only return cards that were issued during the given
        --   date interval.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    expMonth : Int
        -- ^ Only return cards that have the given expiration
        --   month.
    expYear : Int
        -- ^ Only return cards that have the given expiration
        --   year.
    last4 : Text
        -- ^ Only return cards that have the given last four
        --   digits.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    name : Text
        -- ^ Only return cards that have the given name.
    source : Text
        -- ^ Only return cards whose full card number matches that
        --   of this card source ID.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
    status : Text
        -- ^ Only return cards that have the given status. One of
        --   `active`, `inactive`, `canceled`, `lost`, or
        --   `stolen`.
    type_ : Text
        -- ^ Only return cards that have the given type. One of
        --   `virtual` or `physical`.
  where
    signatory requestor

template GetIssuingCardsResponse with
    requestor : Party
    requestId : ContractId GetIssuingCards
    body : GetIssuingCardsResponseBody
  where
    signatory requestor

data GetIssuingCardsResponseBody
    = GetIssuingCardsResponseBody_200 GetIssuingCardsResponseBody200
    | GetIssuingCardsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates an Issuing <code>Card</code> object.</p>
template PostIssuingCards with
    requestor : Party
    payload : PostIssuingCardsPayload
  where
    signatory requestor

template PostIssuingCardsResponse with
    requestor : Party
    requestId : ContractId PostIssuingCards
    body : PostIssuingCardsResponseBody
  where
    signatory requestor

data PostIssuingCardsResponseBody
    = PostIssuingCardsResponseBody_200 IssuingCard
    | PostIssuingCardsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves a <code>ValueListItem</code> object.</p>
template GetRadarValueListItemsItem with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    item : Text
  where
    signatory requestor

template GetRadarValueListItemsItemResponse with
    requestor : Party
    requestId : ContractId GetRadarValueListItemsItem
    body : GetRadarValueListItemsItemResponseBody
  where
    signatory requestor

data GetRadarValueListItemsItemResponseBody
    = GetRadarValueListItemsItemResponseBody_200 RadarValueListItem
    | GetRadarValueListItemsItemResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Deletes a <code>ValueListItem</code> object,
--   removing it from its parent value list.</p>
template DeleteRadarValueListItemsItem with
    requestor : Party
    item : Text
  where
    signatory requestor

template DeleteRadarValueListItemsItemResponse with
    requestor : Party
    requestId : ContractId DeleteRadarValueListItemsItem
    body : DeleteRadarValueListItemsItemResponseBody
  where
    signatory requestor

data DeleteRadarValueListItemsItemResponseBody
    = DeleteRadarValueListItemsItemResponseBody_200 DeletedRadarValueListItem
    | DeleteRadarValueListItemsItemResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of your plans.</p>
template GetPlans with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    active : Bool
        -- ^ Only return plans that are active or inactive (e.g.,
        --   pass `false` to list all inactive plans).
    created : Int
        -- ^ A filter on the list, based on the object `created`
        --   field. The value can be a string with an integer Unix
        --   timestamp, or it can be a dictionary with a number of
        --   different query options.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    product : Text
        -- ^ Only return plans for the given product.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetPlansResponse with
    requestor : Party
    requestId : ContractId GetPlans
    body : GetPlansResponseBody
  where
    signatory requestor

data GetPlansResponseBody
    = GetPlansResponseBody_200 GetPlansResponseBody200
    | GetPlansResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>You can create plans using the API, or in the
--   Stripe <a
--   href="https://dashboard.stripe.com/subscriptions/products">Dashboard</a>.</p>
template PostPlans with
    requestor : Party
    payload : PostPlansPayload
  where
    signatory requestor

template PostPlansResponse with
    requestor : Party
    requestId : ContractId PostPlans
    body : PostPlansResponseBody
  where
    signatory requestor

data PostPlansResponseBody
    = PostPlansResponseBody_200 Plan
    | PostPlansResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves a Mandate object.</p>
template GetMandatesMandate with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    mandate : Text
  where
    signatory requestor

template GetMandatesMandateResponse with
    requestor : Party
    requestId : ContractId GetMandatesMandate
    body : GetMandatesMandateResponseBody
  where
    signatory requestor

data GetMandatesMandateResponseBody
    = GetMandatesMandateResponseBody_200 Mandate
    | GetMandatesMandateResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of file links.</p>
template GetFileLinks with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    created : Int
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    expired : Bool
        -- ^ Filter links by their expiration status. By default,
        --   all links are returned.
    file : Text
        -- ^ Only return links for the given file.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetFileLinksResponse with
    requestor : Party
    requestId : ContractId GetFileLinks
    body : GetFileLinksResponseBody
  where
    signatory requestor

data GetFileLinksResponseBody
    = GetFileLinksResponseBody_200 GetFileLinksResponseBody200
    | GetFileLinksResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a new file link object.</p>
template PostFileLinks with
    requestor : Party
    payload : PostFileLinksPayload
  where
    signatory requestor

template PostFileLinksResponse with
    requestor : Party
    requestId : ContractId PostFileLinks
    body : PostFileLinksResponseBody
  where
    signatory requestor

data PostFileLinksResponseBody
    = PostFileLinksResponseBody_200 FileLink
    | PostFileLinksResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the details of a SetupIntent that has
--   previously been created. </p> <p>Client-side
--   retrieval using a publishable key is allowed when the
--   <code>client_secret</code> is provided in the query
--   string. </p> <p>When retrieved with a publishable
--   key, only a subset of properties will be returned.
--   Please refer to the <a
--   href="#setup_intent_object">SetupIntent</a> object
--   reference for more details.</p>
template GetSetupIntentsIntent with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    clientSecret : Text
        -- ^ The client secret of the SetupIntent. Required if a
        --   publishable key is used to retrieve the SetupIntent.
    intent : Text
  where
    signatory requestor

template GetSetupIntentsIntentResponse with
    requestor : Party
    requestId : ContractId GetSetupIntentsIntent
    body : GetSetupIntentsIntentResponseBody
  where
    signatory requestor

data GetSetupIntentsIntentResponseBody
    = GetSetupIntentsIntentResponseBody_200 SetupIntent
    | GetSetupIntentsIntentResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates a SetupIntent object.</p>
template PostSetupIntentsIntent with
    requestor : Party
    intent : Text
    payload : PostSetupIntentsIntentPayload
  where
    signatory requestor

template PostSetupIntentsIntentResponse with
    requestor : Party
    requestId : ContractId PostSetupIntentsIntent
    body : PostSetupIntentsIntentResponseBody
  where
    signatory requestor

data PostSetupIntentsIntentResponseBody
    = PostSetupIntentsIntentResponseBody_200 SetupIntent
    | PostSetupIntentsIntentResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of capabilities associated with the
--   account. The capabilities are returned sorted by
--   creation date, with the most recent capability
--   appearing first.</p>
template GetAccountsAccountCapabilities with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    account : Text
  where
    signatory requestor

template GetAccountsAccountCapabilitiesResponse with
    requestor : Party
    requestId : ContractId GetAccountsAccountCapabilities
    body : GetAccountsAccountCapabilitiesResponseBody
  where
    signatory requestor

data GetAccountsAccountCapabilitiesResponseBody
    = GetAccountsAccountCapabilitiesResponseBody_200 GetAccountsAccountCapabilitiesResponseBody200
    | GetAccountsAccountCapabilitiesResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the details of an account.</p>
template GetAccountsAccount with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    account : Text
  where
    signatory requestor

template GetAccountsAccountResponse with
    requestor : Party
    requestId : ContractId GetAccountsAccount
    body : GetAccountsAccountResponseBody
  where
    signatory requestor

data GetAccountsAccountResponseBody
    = GetAccountsAccountResponseBody_200 Account
    | GetAccountsAccountResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates a connected <a
--   href="/docs/connect/accounts">Express or Custom
--   account</a> by setting the values of the parameters
--   passed. Any parameters not provided are left
--   unchanged. Most parameters can be changed only for
--   Custom accounts. (These are marked <strong>Custom
--   Only</strong> below.) Parameters marked
--   <strong>Custom and Express</strong> are supported by
--   both account types.</p> <p>To update your own
--   account, use the <a
--   href="https://dashboard.stripe.com/account">Dashboard</a>.
--   Refer to our <a
--   href="/docs/connect/updating-accounts">Connect</a>
--   documentation to learn more about updating
--   accounts.</p>
template PostAccountsAccount with
    requestor : Party
    account : Text
    payload : PostAccountsAccountPayload
  where
    signatory requestor

template PostAccountsAccountResponse with
    requestor : Party
    requestId : ContractId PostAccountsAccount
    body : PostAccountsAccountResponseBody
  where
    signatory requestor

data PostAccountsAccountResponseBody
    = PostAccountsAccountResponseBody_200 Account
    | PostAccountsAccountResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>With <a href="/docs/connect">Connect</a>, you can
--   delete Custom or Express accounts you manage.</p>
--   <p>Accounts created using test-mode keys can be
--   deleted at any time. Accounts created using live-mode
--   keys can only be deleted once all balances are
--   zero.</p> <p>If you want to delete your own account,
--   use the <a
--   href="https://dashboard.stripe.com/account">account
--   information tab in your account settings</a>
--   instead.</p>
template DeleteAccountsAccount with
    requestor : Party
    account : Text
  where
    signatory requestor

template DeleteAccountsAccountResponse with
    requestor : Party
    requestId : ContractId DeleteAccountsAccount
    body : DeleteAccountsAccountResponseBody
  where
    signatory requestor

data DeleteAccountsAccountResponseBody
    = DeleteAccountsAccountResponseBody_200 DeletedAccount
    | DeleteAccountsAccountResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a short-lived API key for a given
--   resource.</p>
template PostEphemeralKeys with
    requestor : Party
    payload : PostEphemeralKeysPayload
  where
    signatory requestor

template PostEphemeralKeysResponse with
    requestor : Party
    requestId : ContractId PostEphemeralKeys
    body : PostEphemeralKeysResponseBody
  where
    signatory requestor

data PostEphemeralKeysResponseBody
    = PostEphemeralKeysResponseBody_200 EphemeralKey
    | PostEphemeralKeysResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of application fees you’ve
--   previously collected. The application fees are
--   returned in sorted order, with the most recent fees
--   appearing first.</p>
template GetApplicationFees with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    charge : Text
        -- ^ Only return application fees for the charge specified
        --   by this charge ID.
    created : Int
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetApplicationFeesResponse with
    requestor : Party
    requestId : ContractId GetApplicationFees
    body : GetApplicationFeesResponseBody
  where
    signatory requestor

data GetApplicationFeesResponseBody
    = GetApplicationFeesResponseBody_200 GetApplicationFeesResponseBody200
    | GetApplicationFeesResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the list of your subscription
--   schedules.</p>
template GetSubscriptionSchedules with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    canceledAt : Int
        -- ^ Only return subscription schedules that were created
        --   canceled the given date interval.
    completedAt : Int
        -- ^ Only return subscription schedules that completed
        --   during the given date interval.
    created : Int
        -- ^ Only return subscription schedules that were created
        --   during the given date interval.
    customer : Text
        -- ^ Only return subscription schedules for the given
        --   customer.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    releasedAt : Int
        -- ^ Only return subscription schedules that were released
        --   during the given date interval.
    scheduled : Bool
        -- ^ Only return subscription schedules that have not
        --   started yet.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetSubscriptionSchedulesResponse with
    requestor : Party
    requestId : ContractId GetSubscriptionSchedules
    body : GetSubscriptionSchedulesResponseBody
  where
    signatory requestor

data GetSubscriptionSchedulesResponseBody
    = GetSubscriptionSchedulesResponseBody_200 GetSubscriptionSchedulesResponseBody200
    | GetSubscriptionSchedulesResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a new subscription schedule object. Each
--   customer can have up to 25 active or scheduled
--   subscriptions.</p>
template PostSubscriptionSchedules with
    requestor : Party
    payload : PostSubscriptionSchedulesPayload
  where
    signatory requestor

template PostSubscriptionSchedulesResponse with
    requestor : Party
    requestId : ContractId PostSubscriptionSchedules
    body : PostSubscriptionSchedulesResponseBody
  where
    signatory requestor

data PostSubscriptionSchedulesResponseBody
    = PostSubscriptionSchedulesResponseBody_200 SubscriptionSchedule
    | PostSubscriptionSchedulesResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>You can see a list of the refunds belonging to a
--   specific charge. Note that the 10 most recent refunds
--   are always available by default on the charge object.
--   If you need more than those 10, you can use this API
--   method and the <code>limit</code> and
--   <code>starting_after</code> parameters to page
--   through additional refunds.</p>
template GetChargesChargeRefunds with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    charge : Text
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetChargesChargeRefundsResponse with
    requestor : Party
    requestId : ContractId GetChargesChargeRefunds
    body : GetChargesChargeRefundsResponseBody
  where
    signatory requestor

data GetChargesChargeRefundsResponseBody
    = GetChargesChargeRefundsResponseBody_200 GetChargesChargeRefundsResponseBody200
    | GetChargesChargeRefundsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Create a refund.</p>
template PostChargesChargeRefunds with
    requestor : Party
    charge : Text
    payload : PostChargesChargeRefundsPayload
  where
    signatory requestor

template PostChargesChargeRefundsResponse with
    requestor : Party
    requestId : ContractId PostChargesChargeRefunds
    body : PostChargesChargeRefundsResponseBody
  where
    signatory requestor

data PostChargesChargeRefundsResponseBody
    = PostChargesChargeRefundsResponseBody_200 Refund
    | PostChargesChargeRefundsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Declines a pending Issuing
--   <code>Authorization</code> object.</p>
template PostIssuingAuthorizationsAuthorizationDecline with
    requestor : Party
    authorization : Text
    payload : PostIssuingAuthorizationsAuthorizationDeclinePayload
  where
    signatory requestor

template PostIssuingAuthorizationsAuthorizationDeclineResponse with
    requestor : Party
    requestId : ContractId PostIssuingAuthorizationsAuthorizationDecline
    body : PostIssuingAuthorizationsAuthorizationDeclineResponseBody
  where
    signatory requestor

data PostIssuingAuthorizationsAuthorizationDeclineResponseBody
    = PostIssuingAuthorizationsAuthorizationDeclineResponseBody_200 IssuingAuthorization
    | PostIssuingAuthorizationsAuthorizationDeclineResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of <code>ValueListItem</code>
--   objects. The objects are sorted in descending order
--   by creation date, with the most recently created
--   object appearing first.</p>
template GetRadarValueListItems with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    created : Int
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
    value : Text
        -- ^ Return items belonging to the parent list whose value
        --   matches the specified value (using an "is like"
        --   match).
    valueList : Text
        -- ^ Identifier for the parent value list this item
        --   belongs to.
  where
    signatory requestor

template GetRadarValueListItemsResponse with
    requestor : Party
    requestId : ContractId GetRadarValueListItems
    body : GetRadarValueListItemsResponseBody
  where
    signatory requestor

data GetRadarValueListItemsResponseBody
    = GetRadarValueListItemsResponseBody_200 GetRadarValueListItemsResponseBody200
    | GetRadarValueListItemsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Creates a new <code>ValueListItem</code> object,
--   which is added to the specified parent value
--   list.</p>
template PostRadarValueListItems with
    requestor : Party
    payload : PostRadarValueListItemsPayload
  where
    signatory requestor

template PostRadarValueListItemsResponse with
    requestor : Party
    requestId : ContractId PostRadarValueListItems
    body : PostRadarValueListItemsResponseBody
  where
    signatory requestor

data PostRadarValueListItemsResponseBody
    = PostRadarValueListItemsResponseBody_200 RadarValueListItem
    | PostRadarValueListItemsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Lists all Country Spec objects available in the
--   API.</p>
template GetCountrySpecs with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetCountrySpecsResponse with
    requestor : Party
    requestId : ContractId GetCountrySpecs
    body : GetCountrySpecsResponseBody
  where
    signatory requestor

data GetCountrySpecsResponseBody
    = GetCountrySpecsResponseBody_200 GetCountrySpecsResponseBody200
    | GetCountrySpecsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the details of an existing payout.
--   Supply the unique payout ID from either a payout
--   creation request or the payout list, and Stripe will
--   return the corresponding payout information.</p>
template GetPayoutsPayout with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    payout : Text
  where
    signatory requestor

template GetPayoutsPayoutResponse with
    requestor : Party
    requestId : ContractId GetPayoutsPayout
    body : GetPayoutsPayoutResponseBody
  where
    signatory requestor

data GetPayoutsPayoutResponseBody
    = GetPayoutsPayoutResponseBody_200 Payout
    | GetPayoutsPayoutResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates the specified payout by setting the values
--   of the parameters passed. Any parameters not provided
--   will be left unchanged. This request accepts only the
--   metadata as arguments.</p>
template PostPayoutsPayout with
    requestor : Party
    payout : Text
    payload : PostPayoutsPayoutPayload
  where
    signatory requestor

template PostPayoutsPayoutResponse with
    requestor : Party
    requestId : ContractId PostPayoutsPayout
    body : PostPayoutsPayoutResponseBody
  where
    signatory requestor

data PostPayoutsPayoutResponseBody
    = PostPayoutsPayoutResponseBody_200 Payout
    | PostPayoutsPayoutResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>List bitcoin transacitons for a given
--   receiver.</p>
template GetBitcoinTransactions with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    customer : Text
        -- ^ Only return transactions for the customer specified
        --   by this customer ID.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    receiver : Text
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetBitcoinTransactionsResponse with
    requestor : Party
    requestId : ContractId GetBitcoinTransactions
    body : GetBitcoinTransactionsResponseBody
  where
    signatory requestor

data GetBitcoinTransactionsResponseBody
    = GetBitcoinTransactionsResponseBody_200 GetBitcoinTransactionsResponseBody200
    | GetBitcoinTransactionsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the invoice with the given ID.</p>
template GetInvoicesInvoice with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    invoice : Text
  where
    signatory requestor

template GetInvoicesInvoiceResponse with
    requestor : Party
    requestId : ContractId GetInvoicesInvoice
    body : GetInvoicesInvoiceResponseBody
  where
    signatory requestor

data GetInvoicesInvoiceResponseBody
    = GetInvoicesInvoiceResponseBody_200 Invoice
    | GetInvoicesInvoiceResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Draft invoices are fully editable. Once an invoice
--   is <a
--   href="/docs/billing/invoices/workflow#finalized">finalized</a>,
--   monetary values, as well as
--   <code>collection_method</code>, become
--   uneditable.</p> <p>If you would like to stop the
--   Stripe Billing engine from automatically finalizing,
--   reattempting payments on, sending reminders for, or
--   <a
--   href="/docs/billing/invoices/reconciliation">automatically
--   reconciling</a> invoices, pass
--   <code>auto_advance=false</code>.</p>
template PostInvoicesInvoice with
    requestor : Party
    invoice : Text
    payload : PostInvoicesInvoicePayload
  where
    signatory requestor

template PostInvoicesInvoiceResponse with
    requestor : Party
    requestId : ContractId PostInvoicesInvoice
    body : PostInvoicesInvoiceResponseBody
  where
    signatory requestor

data PostInvoicesInvoiceResponseBody
    = PostInvoicesInvoiceResponseBody_200 Invoice
    | PostInvoicesInvoiceResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Permanently deletes a draft invoice. This cannot
--   be undone. Attempts to delete invoices that are no
--   longer in a draft state will fail; once an invoice
--   has been finalized, it must be <a
--   href="#void_invoice">voided</a>.</p>
template DeleteInvoicesInvoice with
    requestor : Party
    invoice : Text
  where
    signatory requestor

template DeleteInvoicesInvoiceResponse with
    requestor : Party
    requestId : ContractId DeleteInvoicesInvoice
    body : DeleteInvoicesInvoiceResponseBody
  where
    signatory requestor

data DeleteInvoicesInvoiceResponseBody
    = DeleteInvoicesInvoiceResponseBody_200 DeletedInvoice
    | DeleteInvoicesInvoiceResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of capabilities associated with the
--   account. The capabilities are returned sorted by
--   creation date, with the most recent capability
--   appearing first.</p>
template GetAccountCapabilities with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
  where
    signatory requestor

template GetAccountCapabilitiesResponse with
    requestor : Party
    requestId : ContractId GetAccountCapabilities
    body : GetAccountCapabilitiesResponseBody
  where
    signatory requestor

data GetAccountCapabilitiesResponseBody
    = GetAccountCapabilitiesResponseBody_200 GetAccountCapabilitiesResponseBody200
    | GetAccountCapabilitiesResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves an existing person.</p>
template GetAccountsAccountPeoplePerson with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    account : Text
    person : Text
  where
    signatory requestor

template GetAccountsAccountPeoplePersonResponse with
    requestor : Party
    requestId : ContractId GetAccountsAccountPeoplePerson
    body : GetAccountsAccountPeoplePersonResponseBody
  where
    signatory requestor

data GetAccountsAccountPeoplePersonResponseBody
    = GetAccountsAccountPeoplePersonResponseBody_200 Person
    | GetAccountsAccountPeoplePersonResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Updates an existing person.</p>
template PostAccountsAccountPeoplePerson with
    requestor : Party
    account : Text
    person : Text
    payload : PostAccountsAccountPeoplePersonPayload
  where
    signatory requestor

template PostAccountsAccountPeoplePersonResponse with
    requestor : Party
    requestId : ContractId PostAccountsAccountPeoplePerson
    body : PostAccountsAccountPeoplePersonResponseBody
  where
    signatory requestor

data PostAccountsAccountPeoplePersonResponseBody
    = PostAccountsAccountPeoplePersonResponseBody_200 Person
    | PostAccountsAccountPeoplePersonResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Deletes an existing person’s relationship to the
--   account’s legal entity. Any person with a
--   relationship for an account can be deleted through
--   the API, except if the person is the
--   <code>account_opener</code>. If your integration is
--   using the <code>executive</code> parameter, you
--   cannot delete the only verified
--   <code>executive</code> on file.</p>
template DeleteAccountsAccountPeoplePerson with
    requestor : Party
    account : Text
    person : Text
  where
    signatory requestor

template DeleteAccountsAccountPeoplePersonResponse with
    requestor : Party
    requestId : ContractId DeleteAccountsAccountPeoplePerson
    body : DeleteAccountsAccountPeoplePersonResponseBody
  where
    signatory requestor

data DeleteAccountsAccountPeoplePersonResponseBody
    = DeleteAccountsAccountPeoplePersonResponseBody_200 DeletedPerson
    | DeleteAccountsAccountPeoplePersonResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Returns a list of Issuing <code>Settlement</code>
--   objects. The objects are sorted in descending order
--   by creation date, with the most recently created
--   object appearing first.</p>
template GetIssuingSettlements with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    created : Int
        -- ^ Only return issuing settlements that were created
        --   during the given date interval.
    endingBefore : Text
        -- ^ A cursor for use in pagination. `ending_before` is an
        --   object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, starting with `obj_bar`, your subsequent
        --   call can include `ending_before=obj_bar` in order to
        --   fetch the previous page of the list.
    limit : Int
        -- ^ A limit on the number of objects to be returned.
        --   Limit can range between 1 and 100, and the default is
        --   10.
    startingAfter : Text
        -- ^ A cursor for use in pagination. `starting_after` is
        --   an object ID that defines your place in the list. For
        --   instance, if you make a list request and receive 100
        --   objects, ending with `obj_foo`, your subsequent call
        --   can include `starting_after=obj_foo` in order to
        --   fetch the next page of the list.
  where
    signatory requestor

template GetIssuingSettlementsResponse with
    requestor : Party
    requestId : ContractId GetIssuingSettlements
    body : GetIssuingSettlementsResponseBody
  where
    signatory requestor

data GetIssuingSettlementsResponseBody
    = GetIssuingSettlementsResponseBody_200 GetIssuingSettlementsResponseBody200
    | GetIssuingSettlementsResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieves the details of an issuer fraud record
--   that has previously been created. </p> <p>Please
--   refer to the <a
--   href="#issuer_fraud_record_object">issuer fraud
--   record</a> object reference for more details.</p>
template GetIssuerFraudRecordsIssuerFraudRecord with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    issuerFraudRecord : Text
  where
    signatory requestor

template GetIssuerFraudRecordsIssuerFraudRecordResponse with
    requestor : Party
    requestId : ContractId GetIssuerFraudRecordsIssuerFraudRecord
    body : GetIssuerFraudRecordsIssuerFraudRecordResponseBody
  where
    signatory requestor

data GetIssuerFraudRecordsIssuerFraudRecordResponseBody
    = GetIssuerFraudRecordsIssuerFraudRecordResponseBody_200 IssuerFraudRecord
    | GetIssuerFraudRecordsIssuerFraudRecordResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Retrieve an apple pay domain.</p>
template GetApplePayDomainsDomain with
    requestor : Party
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    domain : Text
  where
    signatory requestor

template GetApplePayDomainsDomainResponse with
    requestor : Party
    requestId : ContractId GetApplePayDomainsDomain
    body : GetApplePayDomainsDomainResponseBody
  where
    signatory requestor

data GetApplePayDomainsDomainResponseBody
    = GetApplePayDomainsDomainResponseBody_200 ApplePayDomain
    | GetApplePayDomainsDomainResponseBody_default_ Error
    deriving (Eq, Ord, Show)

-- | <p>Delete an apple pay domain.</p>
template DeleteApplePayDomainsDomain with
    requestor : Party
    domain : Text
  where
    signatory requestor

template DeleteApplePayDomainsDomainResponse with
    requestor : Party
    requestId : ContractId DeleteApplePayDomainsDomain
    body : DeleteApplePayDomainsDomainResponseBody
  where
    signatory requestor

data DeleteApplePayDomainsDomainResponseBody
    = DeleteApplePayDomainsDomainResponseBody_200 DeletedApplePayDomain
    | DeleteApplePayDomainsDomainResponseBody_default_ Error
    deriving (Eq, Ord, Show)

data GetReportingReportTypesResponseBody200Data = GetReportingReportTypesResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetReportingReportTypesResponseBody200 = GetReportingReportTypesResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [ReportingReportType]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostPaymentIntentsIntentCancelPayload = PostPaymentIntentsIntentCancelPayload with
    cancellationReason : Optional Text
        -- ^ Reason for canceling this PaymentIntent. Possible
        --   values are `duplicate`, `fraudulent`,
        --   `requested_by_customer`, or `abandoned`
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
      deriving (Eq, Ord, Show)

data PostProductsIdPayloadMetadata = PostProductsIdPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostProductsIdPayload = PostProductsIdPayload with
    images : Optional Text
        -- ^ A list of up to 8 URLs of images for this product,
        --   meant to be displayable to the customer.
    unitLabel : Optional Text
        -- ^ A label that represents units of this product in
        --   Stripe and on customers’ receipts and invoices. When
        --   set, this will be included in associated invoice line
        --   item descriptions. May only be set if `type=service`.
    shippable : Optional Bool
        -- ^ Whether this product is shipped (i.e., physical
        --   goods). Defaults to `true`. May only be set if
        --   `type=good`.
    url : Optional Text
        -- ^ A URL of a publicly-accessible webpage for this
        --   product. May only be set if `type=good`.
    caption : Optional Text
        -- ^ A short one-line description of the product, meant to
        --   be displayable to the customer. May only be set if
        --   `type=good`.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    packageDimensions : Optional Text
        -- ^ The dimensions of this product for shipping purposes.
        --   A SKU associated with this product can override this
        --   value by having its own `package_dimensions`. May
        --   only be set if `type=good`.
    active : Optional Bool
        -- ^ Whether the product is available for purchase.
    name : Optional Text
        -- ^ The product's name, meant to be displayable to the
        --   customer. Whenever this product is sold via a
        --   subscription, name will show up on associated invoice
        --   line item descriptions.
    attributes : Optional Text
        -- ^ A list of up to 5 alphanumeric attributes that each
        --   SKU can provide values for (e.g., `["color",
        --   "size"]`). If a value for `attributes` is specified,
        --   the list specified will replace the existing
        --   attributes list on this product. Any attributes not
        --   present after the update will be deleted from the
        --   SKUs for this product.
    metadata : Optional PostProductsIdPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    deactivateOn : [Text]
        -- ^ An array of Connect application names or identifiers
        --   that should not be able to order the SKUs for this
        --   product. May only be set if `type=good`.
    description : Optional Text
        -- ^ The product's description, meant to be displayable to
        --   the customer. Use this field to optionally store a
        --   long form explanation of the product being sold for
        --   your own rendering purposes.
    statementDescriptor : Optional Text
        -- ^ An arbitrary string to be displayed on your
        --   customer's credit card or bank statement. While most
        --   banks display this information consistently, some may
        --   display it incorrectly or not at all. This may be up
        --   to 22 characters. The statement description may not
        --   include `<`, `>`, `\`, `"`, `'` characters, and will
        --   appear on your customer's statement in capital
        --   letters. Non-ASCII characters are automatically
        --   stripped. It must contain at least one letter. May
        --   only be set if `type=service`.
      deriving (Eq, Ord, Show)

data PostSubscriptionItemsItemPayloadMetadata = PostSubscriptionItemsItemPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostSubscriptionItemsItemPayload = PostSubscriptionItemsItemPayload with
    billingThresholds : Optional Text
        -- ^ Define thresholds at which an invoice will be sent,
        --   and the subscription advanced to a new billing
        --   period. When updating, pass an empty string to remove
        --   previously-defined thresholds.
    quantity : Optional Int
        -- ^ The quantity you'd like to apply to the subscription
        --   item you're creating.
    plan : Optional Text
        -- ^ The identifier of the new plan for this subscription
        --   item.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    offSession : Optional Bool
        -- ^ Indicates if a customer is on or off-session while an
        --   invoice payment is attempted.
    paymentBehavior : Optional Text
        -- ^ Use `allow_incomplete` to create subscriptions with
        --   `status=incomplete` if the first invoice cannot be
        --   paid. Creating subscriptions with this status allows
        --   you to manage scenarios where additional user actions
        --   are needed to pay a subscription's invoice. For
        --   example, SCA regulation may require 3DS
        --   authentication to complete payment. See the [SCA
        --   Migration
        --   Guide](https://stripe.com/docs/billing/migration/strong-customer-authentication)
        --   for Billing to learn more. This is the default
        --   behavior. Use `error_if_incomplete` if you want
        --   Stripe to return an HTTP 402 status code if a
        --   subscription's first invoice cannot be paid. For
        --   example, if a payment method requires 3DS
        --   authentication due to SCA regulation and further user
        --   action is needed, this parameter does not create a
        --   subscription and returns an error instead. This was
        --   the default behavior for API versions prior to
        --   2019-03-14. See the
        --   [changelog](https://stripe.com/docs/upgrades#2019-03-14)
        --   to learn more.
    prorationDate : Optional Int
        -- ^ If set, the proration will be calculated as though
        --   the subscription was updated at the given time. This
        --   can be used to apply the same proration that was
        --   previewed with the [upcoming
        --   invoice](https://stripe.com/docs/api#retrieve_customer_invoice)
        --   endpoint.
    prorationBehavior : Optional Text
        -- ^ Determines how to handle
        --   [prorations](https://stripe.com/docs/subscriptions/billing-cycle#prorations)
        --   when the billing cycle changes (e.g., when switching
        --   plans, resetting `billing_cycle_anchor=now`, or
        --   starting a trial), or if an item's `quantity`
        --   changes. Valid values are `create_prorations`,
        --   `none`, or `always_invoice`. Passing
        --   `create_prorations` will cause proration invoice
        --   items to be created when applicable. These proration
        --   items will only be invoiced immediately under
        --   [certain
        --   conditions](https://stripe.com/docs/subscriptions/upgrading-downgrading#immediate-payment).
        --   In order to always invoice immediately for
        --   prorations, pass `always_invoice`. Prorations can be
        --   disabled by passing `none`.
    prorate : Optional Bool
        -- ^ Flag indicating whether to
        --   [prorate](https://stripe.com/docs/billing/subscriptions/prorations)
        --   switching plans during a billing cycle. This field
        --   has been deprecated and will be removed in a future
        --   API version. Use
        --   `proration_behavior=create_prorations` as a
        --   replacement for `prorate=true` and
        --   `proration_behavior=none` for `prorate=false`.
    metadata : Optional PostSubscriptionItemsItemPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    taxRates : Optional Text
        -- ^ A list of [Tax
        --   Rate](https://stripe.com/docs/api/tax_rates) ids.
        --   These Tax Rates will override the
        --   [`default_tax_rates`](https://stripe.com/docs/api/subscriptions/create#create_subscription-default_tax_rates)
        --   on the Subscription. When updating, pass an empty
        --   string to remove previously-defined tax rates.
      deriving (Eq, Ord, Show)

data DeleteSubscriptionItemsItemPayload = DeleteSubscriptionItemsItemPayload with
    prorationDate : Optional Int
        -- ^ If set, the proration will be calculated as though
        --   the subscription was updated at the given time. This
        --   can be used to apply the same proration that was
        --   previewed with the [upcoming
        --   invoice](https://stripe.com/docs/api#retrieve_customer_invoice)
        --   endpoint.
    prorate : Optional Bool
        -- ^ Flag indicating whether to
        --   [prorate](https://stripe.com/docs/billing/subscriptions/prorations)
        --   switching plans during a billing cycle.
    clearUsage : Optional Bool
        -- ^ Delete all usage for the given subscription item.
        --   Allowed only when the current plan's `usage_type` is
        --   `metered`.
      deriving (Eq, Ord, Show)

data GetTopupsResponseBody200Data = GetTopupsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetTopupsResponseBody200 = GetTopupsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Topup]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostTopupsPayloadMetadata = PostTopupsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostTopupsPayload = PostTopupsPayload with
    amount : Int
        -- ^ A positive integer representing how much to transfer.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    metadata : Optional PostTopupsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    source : Optional Text
        -- ^ The ID of a source to transfer funds from. For most
        --   users, this should be left unspecified which will use
        --   the bank account that was set up in the dashboard for
        --   the specified currency. In test mode, this can be a
        --   test bank token (see [Testing
        --   Top-ups](https://stripe.com/docs/connect/testing#testing-top-ups)).
    transferGroup : Optional Text
        -- ^ A string that identifies this top-up as part of a
        --   group.
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users.
    statementDescriptor : Optional Text
        -- ^ Extra information about a top-up for the source's
        --   bank statement. Limited to 15 ASCII characters.
      deriving (Eq, Ord, Show)

data PostSubscriptionItemsSubscriptionItemUsageRecordsPayload = PostSubscriptionItemsSubscriptionItemUsageRecordsPayload with
    quantity : Int
        -- ^ The usage quantity for the specified timestamp.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    action : Optional Text
        -- ^ Valid values are `increment` (default) or `set`. When
        --   using `increment` the specified `quantity` will be
        --   added to the usage at the specified timestamp. The
        --   `set` action will overwrite the usage quantity at
        --   that timestamp. If the subscription has [billing
        --   thresholds](https://stripe.com/docs/api/subscriptions/object#subscription_object-billing_thresholds),
        --   `increment` is the only allowed value.
    timestamp : Int
        -- ^ The timestamp for the usage event. This timestamp
        --   must be within the current billing period of the
        --   subscription of the provided `subscription_item`.
      deriving (Eq, Ord, Show)

data PostCustomersCustomerSourcesIdVerifyPayload = PostCustomersCustomerSourcesIdVerifyPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    amounts : [Int]
        -- ^ Two positive integers, in *cents*, equal to the
        --   values of the microdeposits sent to the bank account.
      deriving (Eq, Ord, Show)

data GetRadarEarlyFraudWarningsResponseBody200Data = GetRadarEarlyFraudWarningsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetRadarEarlyFraudWarningsResponseBody200 = GetRadarEarlyFraudWarningsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [RadarEarlyFraudWarning]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data GetBalanceTransactionsResponseBody200Data = GetBalanceTransactionsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetBalanceTransactionsResponseBody200 = GetBalanceTransactionsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [BalanceTransaction]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostCreditNotesIdPayloadMetadata = PostCreditNotesIdPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostCreditNotesIdPayload = PostCreditNotesIdPayload with
    memo : Optional Text
        -- ^ Credit note memo.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    metadata : Optional PostCreditNotesIdPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data GetSubscriptionItemsResponseBody200Data = GetSubscriptionItemsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetSubscriptionItemsResponseBody200 = GetSubscriptionItemsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [SubscriptionItem]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostSubscriptionItemsPayloadMetadata = PostSubscriptionItemsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostSubscriptionItemsPayload = PostSubscriptionItemsPayload with
    billingThresholds : Optional Text
        -- ^ Define thresholds at which an invoice will be sent,
        --   and the subscription advanced to a new billing
        --   period. When updating, pass an empty string to remove
        --   previously-defined thresholds.
    quantity : Optional Int
        -- ^ The quantity you'd like to apply to the subscription
        --   item you're creating.
    plan : Optional Text
        -- ^ The identifier of the plan to add to the
        --   subscription.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    paymentBehavior : Optional Text
        -- ^ Use `allow_incomplete` to create subscriptions with
        --   `status=incomplete` if the first invoice cannot be
        --   paid. Creating subscriptions with this status allows
        --   you to manage scenarios where additional user actions
        --   are needed to pay a subscription's invoice. For
        --   example, SCA regulation may require 3DS
        --   authentication to complete payment. See the [SCA
        --   Migration
        --   Guide](https://stripe.com/docs/billing/migration/strong-customer-authentication)
        --   for Billing to learn more. This is the default
        --   behavior. Use `error_if_incomplete` if you want
        --   Stripe to return an HTTP 402 status code if a
        --   subscription's first invoice cannot be paid. For
        --   example, if a payment method requires 3DS
        --   authentication due to SCA regulation and further user
        --   action is needed, this parameter does not create a
        --   subscription and returns an error instead. This was
        --   the default behavior for API versions prior to
        --   2019-03-14. See the
        --   [changelog](https://stripe.com/docs/upgrades#2019-03-14)
        --   to learn more.
    prorationDate : Optional Int
        -- ^ If set, the proration will be calculated as though
        --   the subscription was updated at the given time. This
        --   can be used to apply the same proration that was
        --   previewed with the [upcoming
        --   invoice](https://stripe.com/docs/api#retrieve_customer_invoice)
        --   endpoint.
    prorationBehavior : Optional Text
        -- ^ Determines how to handle
        --   [prorations](https://stripe.com/docs/subscriptions/billing-cycle#prorations)
        --   when the billing cycle changes (e.g., when switching
        --   plans, resetting `billing_cycle_anchor=now`, or
        --   starting a trial), or if an item's `quantity`
        --   changes. Valid values are `create_prorations`,
        --   `none`, or `always_invoice`. Passing
        --   `create_prorations` will cause proration invoice
        --   items to be created when applicable. These proration
        --   items will only be invoiced immediately under
        --   [certain
        --   conditions](https://stripe.com/docs/subscriptions/upgrading-downgrading#immediate-payment).
        --   In order to always invoice immediately for
        --   prorations, pass `always_invoice`. Prorations can be
        --   disabled by passing `none`.
    prorate : Optional Bool
        -- ^ Flag indicating whether to
        --   [prorate](https://stripe.com/docs/billing/subscriptions/prorations)
        --   switching plans during a billing cycle. This field
        --   has been deprecated and will be removed in a future
        --   API version. Use
        --   `proration_behavior=create_prorations` as a
        --   replacement for `prorate=true` and
        --   `proration_behavior=none` for `prorate=false`.
    metadata : Optional PostSubscriptionItemsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    taxRates : Optional Text
        -- ^ A list of [Tax
        --   Rate](https://stripe.com/docs/api/tax_rates) ids.
        --   These Tax Rates will override the
        --   [`default_tax_rates`](https://stripe.com/docs/api/subscriptions/create#create_subscription-default_tax_rates)
        --   on the Subscription. When updating, pass an empty
        --   string to remove previously-defined tax rates.
    subscription : Text
        -- ^ The identifier of the subscription to modify.
      deriving (Eq, Ord, Show)

data GetChargesResponseBody200Data = GetChargesResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetChargesResponseBody200 = GetChargesResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Charge]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostChargesPayloadDestination = PostChargesPayloadDestination with
    amount : Optional Int
    account : Text
      deriving (Eq, Ord, Show)

data PostChargesPayloadTransferData = PostChargesPayloadTransferData with
    destination : Text
    amount : Optional Int
      deriving (Eq, Ord, Show)

data PostChargesPayloadShippingAddress = PostChargesPayloadShippingAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Text
      deriving (Eq, Ord, Show)

data PostChargesPayloadShipping = PostChargesPayloadShipping with
    carrier : Optional Text
    phone : Optional Text
    trackingNumber : Optional Text
    address : PostChargesPayloadShippingAddress
    name : Text
      deriving (Eq, Ord, Show)

data PostChargesPayloadMetadata = PostChargesPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostChargesPayload = PostChargesPayload with
    card : Optional Text
        -- ^ A token, like the ones returned by
        --   [Stripe.js](https://stripe.com/docs/stripe.js).
    destination : Optional PostChargesPayloadDestination
    capture : Optional Bool
        -- ^ Whether to immediately capture the charge. Defaults
        --   to `true`. When `false`, the charge issues an
        --   authorization (or pre-authorization), and will need
        --   to be
        --   [captured](https://stripe.com/docs/api#capture_charge)
        --   later. Uncaptured charges expire in _seven days_. For
        --   more information, see the [authorizing charges and
        --   settling
        --   later](https://stripe.com/docs/charges/placing-a-hold)
        --   documentation.
    amount : Optional Int
        -- ^ Amount intended to be collected by this
        --   PaymentIntent. A positive integer representing how
        --   much to charge in the [smallest currency
        --   unit](https://stripe.com/docs/currencies#zero-decimal)
        --   (e.g., 100 cents to charge $1.00 or 100 to charge
        --   ¥100, a zero-decimal currency). The minimum amount is
        --   $0.50 US or [equivalent in charge
        --   currency](https://stripe.com/docs/currencies#minimum-and-maximum-charge-amounts).
        --   The amount value supports up to eight digits (e.g., a
        --   value of 99999999 for a USD charge of $999,999.99).
    receiptEmail : Optional Text
        -- ^ The email address to which this charge's
        --   [receipt](https://stripe.com/docs/dashboard/receipts)
        --   will be sent. The receipt will not be sent until the
        --   charge is paid, and no receipts will be sent for test
        --   mode charges. If this charge is for a
        --   [Customer](https://stripe.com/docs/api/customers/object),
        --   the email address specified here will override the
        --   customer's email address. If `receipt_email` is
        --   specified for a charge in live mode, a receipt will
        --   be sent regardless of your [email
        --   settings](https://dashboard.stripe.com/account/emails).
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    transferData : Optional PostChargesPayloadTransferData
        -- ^ An optional dictionary including the account to
        --   automatically transfer to as part of a destination
        --   charge. [See the Connect
        --   documentation](https://stripe.com/docs/connect/destination-charges)
        --   for details.
    customer : Optional Text
        -- ^ The ID of an existing customer that will be charged
        --   in this request.
    onBehalfOf : Optional Text
        -- ^ The Stripe account ID for which these funds are
        --   intended. Automatically set if you use the
        --   `destination` parameter. For details, see [Creating
        --   Separate Charges and
        --   Transfers](https://stripe.com/docs/connect/charges-transfers#on-behalf-of).
    currency : Optional Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    shipping : Optional PostChargesPayloadShipping
        -- ^ Shipping information for the charge. Helps prevent
        --   fraud on charges for physical goods.
    metadata : Optional PostChargesPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    source : Optional Text
        -- ^ A payment source to be charged. This can be the ID of
        --   a [card](https://stripe.com/docs/api#cards) (i.e.,
        --   credit or debit card), a [bank
        --   account](https://stripe.com/docs/api#bank_accounts),
        --   a [source](https://stripe.com/docs/api#sources), a
        --   [token](https://stripe.com/docs/api#tokens), or a
        --   [connected
        --   account](https://stripe.com/docs/connect/account-debits#charging-a-connected-account).
        --   For certain sources---namely,
        --   [cards](https://stripe.com/docs/api#cards), [bank
        --   accounts](https://stripe.com/docs/api#bank_accounts),
        --   and attached
        --   [sources](https://stripe.com/docs/api#sources)---you
        --   must also pass the ID of the associated customer.
    applicationFeeAmount : Optional Int
        -- ^ A fee in %s that will be applied to the charge and
        --   transferred to the application owner's Stripe
        --   account. The request must be made with an OAuth key
        --   or the `Stripe-Account` header in order to take an
        --   application fee. For more information, see the
        --   application fees
        --   [documentation](https://stripe.com/docs/connect/direct-charges#collecting-fees).
    applicationFee : Optional Int
    transferGroup : Optional Text
        -- ^ A string that identifies this transaction as part of
        --   a group. For details, see [Grouping
        --   transactions](https://stripe.com/docs/connect/charges-transfers#grouping-transactions).
    description : Optional Text
        -- ^ An arbitrary string which you can attach to a
        --   `Charge` object. It is displayed when in the web
        --   interface alongside the charge. Note that if you use
        --   Stripe to send automatic email receipts to your
        --   customers, your receipt emails will include the
        --   `description` of the charge(s) that they are
        --   describing.
    statementDescriptor : Optional Text
        -- ^ For card charges, use `statement_descriptor_suffix`
        --   instead. Otherwise, you can use this value as the
        --   complete description of a charge on your customers’
        --   statements. Must contain at least one letter, maximum
        --   22 characters.
    statementDescriptorSuffix : Optional Text
        -- ^ Provides information about the charge that customers
        --   see on their statements. Concatenated with the prefix
        --   (shortened descriptor) or statement descriptor that’s
        --   set on the account to form the complete statement
        --   descriptor. Maximum 22 characters for the
        --   concatenated descriptor.
      deriving (Eq, Ord, Show)

data DeleteEphemeralKeysKeyPayload = DeleteEphemeralKeysKeyPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
      deriving (Eq, Ord, Show)

data PostChargesChargeDisputeClosePayload = PostChargesChargeDisputeClosePayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
      deriving (Eq, Ord, Show)

data PutAccountsAccountLogoutPayload = PutAccountsAccountLogoutPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
      deriving (Eq, Ord, Show)

data GetSubscriptionItemsSubscriptionItemUsageRecordSummariesResponseBody200Data = GetSubscriptionItemsSubscriptionItemUsageRecordSummariesResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetSubscriptionItemsSubscriptionItemUsageRecordSummariesResponseBody200 = GetSubscriptionItemsSubscriptionItemUsageRecordSummariesResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [UsageRecordSummary]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostPlansPlanPayloadMetadata = PostPlansPlanPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostPlansPlanPayload = PostPlansPlanPayload with
    trialPeriodDays : Optional Int
        -- ^ Default number of trial days when subscribing a
        --   customer to this plan using
        --   [`trial_from_plan=true`](https://stripe.com/docs/api#create_subscription-trial_from_plan).
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    active : Optional Bool
        -- ^ Whether the plan is currently available for new
        --   subscriptions.
    metadata : Optional PostPlansPlanPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    nickname : Optional Text
        -- ^ A brief description of the plan, hidden from
        --   customers.
    product : Optional Text
        -- ^ The product the plan belongs to. Note that after
        --   updating, statement descriptors and line items of the
        --   plan in active subscriptions will be affected.
      deriving (Eq, Ord, Show)

data PutAccountLogoutPayload = PutAccountLogoutPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    account : Text
      deriving (Eq, Ord, Show)

data PostSubscriptionsSubscriptionExposedIdPayloadItems = PostSubscriptionsSubscriptionExposedIdPayloadItems with
      deriving (Eq, Ord, Show)

data PostSubscriptionsSubscriptionExposedIdPayloadMetadata = PostSubscriptionsSubscriptionExposedIdPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostSubscriptionsSubscriptionExposedIdPayload = PostSubscriptionsSubscriptionExposedIdPayload with
    trialFromPlan : Optional Bool
        -- ^ Indicates if a plan's `trial_period_days` should be
        --   applied to the subscription. Setting `trial_end` per
        --   subscription is preferred, and this defaults to
        --   `false`. Setting this flag to `true` together with
        --   `trial_end` is not allowed.
    coupon : Optional Text
        -- ^ The code of the coupon to apply to this subscription.
        --   A coupon applied to a subscription will only affect
        --   invoices created for that particular subscription.
    billingThresholds : Optional Text
        -- ^ Define thresholds at which an invoice will be sent,
        --   and the subscription advanced to a new billing
        --   period. Pass an empty string to remove
        --   previously-defined thresholds.
    pendingInvoiceItemInterval : Optional Text
        -- ^ Specifies an interval for how often to bill for any
        --   pending invoice items. It is analogous to calling
        --   [Create an
        --   invoice](https://stripe.com/docs/api#create_invoice)
        --   for the given subscription at the specified interval.
    cancelAtPeriodEnd : Optional Bool
        -- ^ Boolean indicating whether this subscription should
        --   cancel at the end of the current period.
    items : [PostSubscriptionsSubscriptionExposedIdPayloadItems]
        -- ^ List of subscription items, each with an attached
        --   plan.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    offSession : Optional Bool
        -- ^ Indicates if a customer is on or off-session while an
        --   invoice payment is attempted.
    collectionMethod : Optional Text
        -- ^ Either `charge_automatically`, or `send_invoice`.
        --   When charging automatically, Stripe will attempt to
        --   pay this subscription at the end of the cycle using
        --   the default source attached to the customer. When
        --   sending an invoice, Stripe will email your customer
        --   an invoice with payment instructions. Defaults to
        --   `charge_automatically`.
    paymentBehavior : Optional Text
        -- ^ Use `allow_incomplete` to create subscriptions with
        --   `status=incomplete` if the first invoice cannot be
        --   paid. Creating subscriptions with this status allows
        --   you to manage scenarios where additional user actions
        --   are needed to pay a subscription's invoice. For
        --   example, SCA regulation may require 3DS
        --   authentication to complete payment. See the [SCA
        --   Migration
        --   Guide](https://stripe.com/docs/billing/migration/strong-customer-authentication)
        --   for Billing to learn more. This is the default
        --   behavior. Use `error_if_incomplete` if you want
        --   Stripe to return an HTTP 402 status code if a
        --   subscription's first invoice cannot be paid. For
        --   example, if a payment method requires 3DS
        --   authentication due to SCA regulation and further user
        --   action is needed, this parameter does not create a
        --   subscription and returns an error instead. This was
        --   the default behavior for API versions prior to
        --   2019-03-14. See the
        --   [changelog](https://stripe.com/docs/upgrades#2019-03-14)
        --   to learn more.
    prorationDate : Optional Int
        -- ^ If set, the proration will be calculated as though
        --   the subscription was updated at the given time. This
        --   can be used to apply exactly the same proration that
        --   was previewed with [upcoming
        --   invoice](https://stripe.com/docs/api#retrieve_customer_invoice)
        --   endpoint. It can also be used to implement custom
        --   proration logic, such as prorating by day instead of
        --   by second, by providing the time that you wish to use
        --   for proration calculations.
    prorationBehavior : Optional Text
        -- ^ Determines how to handle
        --   [prorations](https://stripe.com/docs/subscriptions/billing-cycle#prorations)
        --   when the billing cycle changes (e.g., when switching
        --   plans, resetting `billing_cycle_anchor=now`, or
        --   starting a trial), or if an item's `quantity`
        --   changes. Valid values are `create_prorations`,
        --   `none`, or `always_invoice`. Passing
        --   `create_prorations` will cause proration invoice
        --   items to be created when applicable. These proration
        --   items will only be invoiced immediately under
        --   [certain
        --   conditions](https://stripe.com/docs/subscriptions/upgrading-downgrading#immediate-payment).
        --   In order to always invoice immediately for
        --   prorations, pass `always_invoice`. Prorations can be
        --   disabled by passing `none`.
    cancelAt : Optional Text
        -- ^ A timestamp at which the subscription should cancel.
        --   If set to a date before the current period ends, this
        --   will cause a proration if prorations have been
        --   enabled using `proration_behavior`. If set during a
        --   future period, this will always cause a proration for
        --   that period.
    prorate : Optional Bool
        -- ^ This field has been renamed to `proration_behavior`.
        --   `prorate=true` can be replaced with
        --   `proration_behavior=create_prorations` and
        --   `prorate=false` can be replaced with
        --   `proration_behavior=none`.
    defaultTaxRates : Optional Text
        -- ^ The tax rates that will apply to any subscription
        --   item that does not have `tax_rates` set. Invoices
        --   created will have their `default_tax_rates` populated
        --   from the subscription. Pass an empty string to remove
        --   previously-defined tax rates.
    applicationFeePercent : Optional Decimal
        -- ^ A non-negative decimal between 0 and 100, with at
        --   most two decimal places. This represents the
        --   percentage of the subscription invoice subtotal that
        --   will be transferred to the application owner's Stripe
        --   account. The request must be made by a platform
        --   account on a connected account in order to set an
        --   application fee percentage. For more information, see
        --   the application fees
        --   [documentation](https://stripe.com/docs/connect/subscriptions#collecting-fees-on-subscriptions).
    trialEnd : Optional Text
        -- ^ Unix timestamp representing the end of the trial
        --   period the customer will get before being charged for
        --   the first time. This will always overwrite any trials
        --   that might apply via a subscribed plan. If set,
        --   trial_end will override the default trial period of
        --   the plan the customer is being subscribed to. The
        --   special value `now` can be provided to end the
        --   customer's trial immediately. Can be at most two
        --   years from `billing_cycle_anchor`.
    billingCycleAnchor : Optional Text
        -- ^ Either `now` or `unchanged`. Setting the value to
        --   `now` resets the subscription's billing cycle anchor
        --   to the current time. For more information, see the
        --   billing cycle
        --   [documentation](https://stripe.com/docs/billing/subscriptions/billing-cycle).
    metadata : Optional PostSubscriptionsSubscriptionExposedIdPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    defaultPaymentMethod : Optional Text
        -- ^ ID of the default payment method for the
        --   subscription. It must belong to the customer
        --   associated with the subscription. If not set,
        --   invoices will use the default payment method in the
        --   customer's invoice settings.
    defaultSource : Optional Text
        -- ^ ID of the default payment source for the
        --   subscription. It must belong to the customer
        --   associated with the subscription and be in a
        --   chargeable state. If not set, defaults to the
        --   customer's default source.
    daysUntilDue : Optional Int
        -- ^ Number of days a customer has to pay invoices
        --   generated by this subscription. Valid only for
        --   subscriptions where `collection_method` is set to
        --   `send_invoice`.
    taxPercent : Optional Text
        -- ^ A non-negative decimal (with at most four decimal
        --   places) between 0 and 100. This represents the
        --   percentage of the subscription invoice subtotal that
        --   will be calculated and added as tax to the final
        --   amount in each billing period. For example, a plan
        --   which charges $10/month with a `tax_percent` of
        --   `20.0` will charge $12 per invoice. To unset a
        --   previously-set value, pass an empty string. This
        --   field has been deprecated and will be removed in a
        --   future API version, for further information view the
        --   [migration
        --   docs](https://stripe.com/docs/billing/migration/taxes)
        --   for `tax_rates`.
      deriving (Eq, Ord, Show)

data DeleteSubscriptionsSubscriptionExposedIdPayload = DeleteSubscriptionsSubscriptionExposedIdPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    prorate : Optional Bool
        -- ^ Will generate a proration invoice item that credits
        --   remaining unused time until the subscription period
        --   end.
    invoiceNow : Optional Bool
        -- ^ Will generate a final invoice that invoices for any
        --   un-invoiced metered usage and new/pending proration
        --   invoice items.
      deriving (Eq, Ord, Show)

data GetIssuingTransactionsResponseBody200Data = GetIssuingTransactionsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetIssuingTransactionsResponseBody200 = GetIssuingTransactionsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [IssuingTransaction]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data GetBitcoinReceiversResponseBody200Data = GetBitcoinReceiversResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetBitcoinReceiversResponseBody200 = GetBitcoinReceiversResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [BitcoinReceiver]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostTransfersTransferPayloadMetadata = PostTransfersTransferPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostTransfersTransferPayload = PostTransfersTransferPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    metadata : Optional PostTransfersTransferPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users.
      deriving (Eq, Ord, Show)

data GetReviewsResponseBody200Data = GetReviewsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetReviewsResponseBody200 = GetReviewsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Review]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data GetEventsResponseBody200Data = GetEventsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetEventsResponseBody200 = GetEventsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Event]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostIssuingDisputesDisputePayloadMetadata = PostIssuingDisputesDisputePayloadMetadata with
      deriving (Eq, Ord, Show)

data PostIssuingDisputesDisputePayload = PostIssuingDisputesDisputePayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    metadata : Optional PostIssuingDisputesDisputePayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data GetTerminalReadersResponseBody200Data = GetTerminalReadersResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetTerminalReadersResponseBody200 = GetTerminalReadersResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [TerminalReader]
        -- ^ A list of readers
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostTerminalReadersPayloadMetadata = PostTerminalReadersPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostTerminalReadersPayload = PostTerminalReadersPayload with
    location : Optional Text
        -- ^ The location to assign the reader to. If no location
        --   is specified, the reader will be assigned to the
        --   account's default location.
    registrationCode : Optional Text
        -- ^ A code generated by the reader used for registering
        --   to an account.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    metadata : Optional PostTerminalReadersPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    label_ : Optional Text
        -- ^ Custom label given to the reader for easier
        --   identification. If no label is specified, the
        --   registration code will be used.
      deriving (Eq, Ord, Show)

data GetRadarValueListsResponseBody200Data = GetRadarValueListsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetRadarValueListsResponseBody200 = GetRadarValueListsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [RadarValueList]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostRadarValueListsPayloadMetadata = PostRadarValueListsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostRadarValueListsPayload = PostRadarValueListsPayload with
    itemType : Optional Text
        -- ^ Type of the items in the value list. One of
        --   `card_fingerprint`, `card_bin`, `email`,
        --   `ip_address`, `country`, `string`, or
        --   `case_sensitive_string`. Use `string` if the item
        --   type is unknown or mixed.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    alias : Text
        -- ^ The name of the value list for use in rules.
    name : Text
        -- ^ The human-readable name of the value list.
    metadata : Optional PostRadarValueListsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data PostCustomersCustomerBalanceTransactionsTransactionPayloadMetadata = PostCustomersCustomerBalanceTransactionsTransactionPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostCustomersCustomerBalanceTransactionsTransactionPayload = PostCustomersCustomerBalanceTransactionsTransactionPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    metadata : Optional PostCustomersCustomerBalanceTransactionsTransactionPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users.
      deriving (Eq, Ord, Show)

data GetReportingReportRunsResponseBody200Data = GetReportingReportRunsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetReportingReportRunsResponseBody200 = GetReportingReportRunsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [ReportingReportRun]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostReportingReportRunsPayloadParameters = PostReportingReportRunsPayloadParameters with
    intervalEnd : Optional Int
    connectedAccount : Optional Text
    reportingCategory : Optional Text
    currency : Optional Text
    columns : [Text]
    intervalStart : Optional Int
    timezone : Optional Text
    payout : Optional Text
      deriving (Eq, Ord, Show)

data PostReportingReportRunsPayload = PostReportingReportRunsPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    parameters : Optional PostReportingReportRunsPayloadParameters
        -- ^ Parameters specifying how the report should be run.
        --   Different Report Types have different required and
        --   optional parameters, listed in the [API Access to
        --   Reports](https://stripe.com/docs/reporting/statements/api)
        --   documentation.
    reportType : Optional Text
        -- ^ The ID of the [report
        --   type](https://stripe.com/docs/reporting/statements/api#report-types)
        --   to run, such as `"balance.summary.1"`.
      deriving (Eq, Ord, Show)

data GetAccountPersonsResponseBody200Data = GetAccountPersonsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetAccountPersonsResponseBody200 = GetAccountPersonsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Person]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostAccountPersonsPayloadRelationship = PostAccountPersonsPayloadRelationship with
    director : Optional Bool
    owner : Optional Bool
    percentOwnership : Optional Text
    title : Optional Text
    executive : Optional Bool
    representative : Optional Bool
      deriving (Eq, Ord, Show)

data PostAccountPersonsPayloadAddressKana = PostAccountPersonsPayloadAddressKana with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPersonsPayloadAddress = PostAccountPersonsPayloadAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPersonsPayloadMetadata = PostAccountPersonsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostAccountPersonsPayloadAddressKanji = PostAccountPersonsPayloadAddressKanji with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPersonsPayloadVerificationDocument = PostAccountPersonsPayloadVerificationDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPersonsPayloadVerificationAdditionalDocument = PostAccountPersonsPayloadVerificationAdditionalDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPersonsPayloadVerification = PostAccountPersonsPayloadVerification with
    document : Optional PostAccountPersonsPayloadVerificationDocument
    additionalDocument : Optional PostAccountPersonsPayloadVerificationAdditionalDocument
      deriving (Eq, Ord, Show)

data PostAccountPersonsPayload = PostAccountPersonsPayload with
    relationship : Optional PostAccountPersonsPayloadRelationship
        -- ^ The relationship that this person has with the
        --   account's legal entity.
    email : Optional Text
        -- ^ The person's email address.
    addressKana : Optional PostAccountPersonsPayloadAddressKana
        -- ^ The Kana variation of the person's address (Japan
        --   only).
    phone : Optional Text
        -- ^ The person's phone number.
    lastNameKanji : Optional Text
        -- ^ The Kanji variation of the person's last name (Japan
        --   only).
    firstName : Optional Text
        -- ^ The person's first name.
    lastNameKana : Optional Text
        -- ^ The Kana variation of the person's last name (Japan
        --   only).
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    address : Optional PostAccountPersonsPayloadAddress
        -- ^ The person's address.
    account : Optional Text
    personToken : Optional Text
        -- ^ A [person
        --   token](https://stripe.com/docs/connect/account-tokens),
        --   used to securely provide details to the person.
    idNumber : Optional Text
        -- ^ The person's ID number, as appropriate for their
        --   country. For example, a social security number in the
        --   U.S., social insurance number in Canada, etc. Instead
        --   of the number itself, you can also provide a [PII
        --   token provided by
        --   Stripe.js](https://stripe.com/docs/stripe.js#collecting-pii-data).
    dob : Optional Text
        -- ^ The person's date of birth.
    gender : Optional Text
        -- ^ The person's gender (International regulations
        --   require either "male" or "female").
    firstNameKanji : Optional Text
        -- ^ The Kanji variation of the person's first name (Japan
        --   only).
    lastName : Optional Text
        -- ^ The person's last name.
    maidenName : Optional Text
        -- ^ The person's maiden name.
    metadata : Optional PostAccountPersonsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    firstNameKana : Optional Text
        -- ^ The Kana variation of the person's first name (Japan
        --   only).
    ssnLast4 : Optional Text
        -- ^ The last 4 digits of the person's social security
        --   number.
    addressKanji : Optional PostAccountPersonsPayloadAddressKanji
        -- ^ The Kanji variation of the person's address (Japan
        --   only).
    verification : Optional PostAccountPersonsPayloadVerification
        -- ^ The person's verification status.
      deriving (Eq, Ord, Show)

data PostIssuingCardsCardPayloadAuthorizationControlsSpendingLimits = PostIssuingCardsCardPayloadAuthorizationControlsSpendingLimits with
      deriving (Eq, Ord, Show)

data PostIssuingCardsCardPayloadAuthorizationControls = PostIssuingCardsCardPayloadAuthorizationControls with
    spendingLimits : [PostIssuingCardsCardPayloadAuthorizationControlsSpendingLimits]
    maxApprovals : Optional Int
    allowedCategories : [Text]
    blockedCategories : [Text]
      deriving (Eq, Ord, Show)

data PostIssuingCardsCardPayload = PostIssuingCardsCardPayload with
    status : Optional Text
        -- ^ Whether authorizations can be approved on this card.
    authorizationControls : Optional PostIssuingCardsCardPayloadAuthorizationControls
        -- ^ Spending rules that give you some control over how
        --   your cards can be used. Refer to our
        --   [authorizations](https://stripe.com/docs/issuing/authorizations)
        --   documentation for more details.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    metadata : Optional Text
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data GetAccountsAccountExternalAccountsResponseBody200Data = GetAccountsAccountExternalAccountsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetAccountsAccountExternalAccountsResponseBody200 = GetAccountsAccountExternalAccountsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [BankAccount]
        -- ^ The list contains all external accounts that have
        --   been attached to the Stripe account. These may be
        --   bank accounts or cards.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostAccountsAccountExternalAccountsPayloadMetadata = PostAccountsAccountExternalAccountsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostAccountsAccountExternalAccountsPayload = PostAccountsAccountExternalAccountsPayload with
    defaultForCurrency : Optional Bool
        -- ^ When set to true, or if this is the first external
        --   account added in this currency, this account becomes
        --   the default external account for its currency.
    bankAccount : Optional Text
        -- ^ Either a token, like the ones returned by
        --   [Stripe.js](https://stripe.com/docs/stripe.js), or a
        --   dictionary containing a user's bank account details.
    externalAccount : Optional Text
        -- ^ Please refer to full
        --   [documentation](https://stripe.com/docs/api) instead.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    metadata : Optional PostAccountsAccountExternalAccountsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data GetIssuerFraudRecordsResponseBody200Data = GetIssuerFraudRecordsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetIssuerFraudRecordsResponseBody200 = GetIssuerFraudRecordsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [IssuerFraudRecord]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostAccountsAccountCapabilitiesCapabilityPayload = PostAccountsAccountCapabilitiesCapabilityPayload with
    requested : Optional Bool
        -- ^ Passing true requests the capability for the account,
        --   if it is not already requested. A requested
        --   capability may not immediately become active. Any
        --   requirements to activate the capability are returned
        --   in the `requirements` arrays.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
      deriving (Eq, Ord, Show)

data PostInvoiceitemsInvoiceitemPayloadPeriod = PostInvoiceitemsInvoiceitemPayloadPeriod with
    start : Int
    end : Int
      deriving (Eq, Ord, Show)

data PostInvoiceitemsInvoiceitemPayloadMetadata = PostInvoiceitemsInvoiceitemPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostInvoiceitemsInvoiceitemPayload = PostInvoiceitemsInvoiceitemPayload with
    amount : Optional Int
        -- ^ The integer amount in **%s** of the charge to be
        --   applied to the upcoming invoice. If you want to apply
        --   a credit to the customer's account, pass a negative
        --   amount.
    period : Optional PostInvoiceitemsInvoiceitemPayloadPeriod
        -- ^ The period associated with this invoice item.
    quantity : Optional Int
        -- ^ Non-negative integer. The quantity of units for the
        --   invoice item.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    unitAmountDecimal : Optional Text
        -- ^ Same as `unit_amount`, but accepts a decimal value
        --   with at most 12 decimal places. Only one of
        --   `unit_amount` and `unit_amount_decimal` can be set.
    discountable : Optional Bool
        -- ^ Controls whether discounts apply to this invoice
        --   item. Defaults to false for prorations or negative
        --   invoice items, and true for all other invoice items.
        --   Cannot be set to true for prorations.
    metadata : Optional PostInvoiceitemsInvoiceitemPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    unitAmount : Optional Int
        -- ^ The integer unit amount in **%s** of the charge to be
        --   applied to the upcoming invoice. This unit_amount
        --   will be multiplied by the quantity to get the full
        --   amount. If you want to apply a credit to the
        --   customer's account, pass a negative unit_amount.
    taxRates : Optional Text
        -- ^ The tax rates which apply to the invoice item. When
        --   set, the `default_tax_rates` on the invoice do not
        --   apply to this invoice item. Pass an empty string to
        --   remove previously-defined tax rates.
    description : Optional Text
        -- ^ An arbitrary string which you can attach to the
        --   invoice item. The description is displayed in the
        --   invoice for easy tracking.
      deriving (Eq, Ord, Show)

data PostTaxRatesTaxRatePayloadMetadata = PostTaxRatesTaxRatePayloadMetadata with
      deriving (Eq, Ord, Show)

data PostTaxRatesTaxRatePayload = PostTaxRatesTaxRatePayload with
    displayName : Optional Text
        -- ^ The display name of the tax rate, which will be shown
        --   to users.
    jurisdiction : Optional Text
        -- ^ The jurisdiction for the tax rate.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    active : Optional Bool
        -- ^ Flag determining whether the tax rate is active or
        --   inactive. Inactive tax rates continue to work where
        --   they are currently applied however they cannot be
        --   used for new applications.
    metadata : Optional PostTaxRatesTaxRatePayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    description : Optional Text
        -- ^ An arbitrary string attached to the tax rate for your
        --   internal use only. It will not be visible to your
        --   customers.
      deriving (Eq, Ord, Show)

data GetRecipientsResponseBody200Data = GetRecipientsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetRecipientsResponseBody200 = GetRecipientsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Recipient]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostRecipientsPayloadMetadata = PostRecipientsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostRecipientsPayload = PostRecipientsPayload with
    card : Optional Text
        -- ^ A U.S. Visa or MasterCard debit card (_not_ prepaid)
        --   to attach to the recipient. If the debit card is not
        --   valid, recipient creation will fail. You can provide
        --   either a token, like the ones returned by
        --   [Stripe.js](https://stripe.com/docs/stripe-js), or a
        --   dictionary containing a user's debit card details,
        --   with the options described below. Although not all
        --   information is required, the extra info helps prevent
        --   fraud.
    email : Optional Text
        -- ^ The recipient's email address. It is displayed
        --   alongside the recipient in the web interface, and can
        --   be useful for searching and tracking.
    taxId : Optional Text
        -- ^ The recipient's tax ID, as a string. For type
        --   `individual`, the full SSN; for type `corporation`,
        --   the full EIN.
    bankAccount : Optional Text
        -- ^ A bank account to attach to the recipient. You can
        --   provide either a token, like the ones returned by
        --   [Stripe.js](https://stripe.com/docs/stripe-js), or a
        --   dictionary containing a user's bank account details,
        --   with the options described below.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    name : Text
        -- ^ The recipient's full, legal name. For type
        --   `individual`, should be in the format `First Last`,
        --   `First Middle Last`, or `First M Last` (no prefixes
        --   or suffixes). For `corporation`, the full,
        --   incorporated name.
    metadata : Optional PostRecipientsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    type_ : Optional Text
        -- ^ Type of the recipient: either `individual` or
        --   `corporation`.
    description : Optional Text
        -- ^ An arbitrary string which you can attach to a
        --   `Recipient` object. It is displayed alongside the
        --   recipient in the web interface.
      deriving (Eq, Ord, Show)

data PostAccountsAccountLoginLinksPayload = PostAccountsAccountLoginLinksPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    redirectUrl : Optional Text
        -- ^ Where to redirect the user after they log out of
        --   their dashboard.
      deriving (Eq, Ord, Show)

data PostPaymentIntentsIntentConfirmPayloadMandateDataCustomerAcceptanceOnline = PostPaymentIntentsIntentConfirmPayloadMandateDataCustomerAcceptanceOnline with
    userAgent : Optional Text
    ipAddress : Optional Text
      deriving (Eq, Ord, Show)

data PostPaymentIntentsIntentConfirmPayloadMandateDataCustomerAcceptanceOffline = PostPaymentIntentsIntentConfirmPayloadMandateDataCustomerAcceptanceOffline with
      deriving (Eq, Ord, Show)

data PostPaymentIntentsIntentConfirmPayloadMandateDataCustomerAcceptance = PostPaymentIntentsIntentConfirmPayloadMandateDataCustomerAcceptance with
    online : Optional PostPaymentIntentsIntentConfirmPayloadMandateDataCustomerAcceptanceOnline
    offline : Optional PostPaymentIntentsIntentConfirmPayloadMandateDataCustomerAcceptanceOffline
    type_ : Optional Text
    acceptedAt : Optional Int
      deriving (Eq, Ord, Show)

data PostPaymentIntentsIntentConfirmPayloadMandateData = PostPaymentIntentsIntentConfirmPayloadMandateData with
    customerAcceptance : Optional PostPaymentIntentsIntentConfirmPayloadMandateDataCustomerAcceptance
      deriving (Eq, Ord, Show)

data PostPaymentIntentsIntentConfirmPayloadPaymentMethodOptionsCardInstallments = PostPaymentIntentsIntentConfirmPayloadPaymentMethodOptionsCardInstallments with
    enabled : Optional Bool
    plan : Optional Text
      deriving (Eq, Ord, Show)

data PostPaymentIntentsIntentConfirmPayloadPaymentMethodOptionsCard = PostPaymentIntentsIntentConfirmPayloadPaymentMethodOptionsCard with
    installments : Optional PostPaymentIntentsIntentConfirmPayloadPaymentMethodOptionsCardInstallments
    requestThreeDSecure : Optional Text
      deriving (Eq, Ord, Show)

data PostPaymentIntentsIntentConfirmPayloadPaymentMethodOptions = PostPaymentIntentsIntentConfirmPayloadPaymentMethodOptions with
    card : Optional PostPaymentIntentsIntentConfirmPayloadPaymentMethodOptionsCard
      deriving (Eq, Ord, Show)

data PostPaymentIntentsIntentConfirmPayload = PostPaymentIntentsIntentConfirmPayload with
    setupFutureUsage : Optional Text
        -- ^ Indicates that you intend to make future payments
        --   with this PaymentIntent's payment method. If present,
        --   the payment method used with this PaymentIntent can
        --   be
        --   [attached](https://stripe.com/docs/api/payment_methods/attach)
        --   to a Customer, even after the transaction completes.
        --   Use `on_session` if you intend to only reuse the
        --   payment method when your customer is present in your
        --   checkout flow. Use `off_session` if your customer may
        --   or may not be in your checkout flow. Stripe uses
        --   `setup_future_usage` to dynamically optimize your
        --   payment flow and comply with regional legislation and
        --   network rules. For example, if your customer is
        --   impacted by
        --   [SCA](https://stripe.com/docs/strong-customer-authentication),
        --   using `off_session` will ensure that they are
        --   authenticated while processing this PaymentIntent.
        --   You will then be able to collect [off-session
        --   payments](https://stripe.com/docs/payments/cards/charging-saved-cards#off-session-payments-with-saved-cards)
        --   for this customer. If `setup_future_usage` is already
        --   set and you are performing a request using a
        --   publishable key, you may only update the value from
        --   `on_session` to `off_session`.
    receiptEmail : Optional Text
        -- ^ Email address that the receipt for the resulting
        --   payment will be sent to.
    paymentMethod : Optional Text
        -- ^ ID of the payment method (a PaymentMethod, Card, or
        --   [compatible
        --   Source](https://stripe.com/docs/payments/payment-methods#compatibility)
        --   object) to attach to this PaymentIntent.
    clientSecret : Optional Text
        -- ^ The client secret of the PaymentIntent.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    mandate : Optional Text
        -- ^ ID of the mandate to be used for this payment.
    offSession : Optional Text
        -- ^ Set to `true` to indicate that the customer is not in
        --   your checkout flow during this payment attempt, and
        --   therefore is unable to authenticate. This parameter
        --   is intended for scenarios where you collect card
        --   details and [charge them
        --   later](https://stripe.com/docs/payments/cards/charging-saved-cards).
    mandateData : Optional PostPaymentIntentsIntentConfirmPayloadMandateData
        -- ^ This hash contains details about the Mandate to
        --   create
    savePaymentMethod : Optional Bool
        -- ^ If the PaymentIntent has a `payment_method` and a
        --   `customer` or if you're attaching a payment method to
        --   the PaymentIntent in this request, you can pass
        --   `save_payment_method=true` to save the payment method
        --   to the customer. Defaults to `false`. If the payment
        --   method is already saved to a customer, this does
        --   nothing. If this type of payment method cannot be
        --   saved to a customer, the request will error. _Note
        --   that saving a payment method using this parameter
        --   does not guarantee that the payment method can be
        --   charged._ To ensure that only payment methods which
        --   can be charged are saved to a customer, you can
        --   [manually
        --   save](https://stripe.com/docs/api/customers/create#create_customer-source)
        --   the payment method in response to the
        --   [`payment_intent.succeeded`
        --   webhook](https://stripe.com/docs/api/events/types#event_types-payment_intent.succeeded).
    shipping : Optional Text
        -- ^ Shipping information for this PaymentIntent.
    paymentMethodOptions : Optional PostPaymentIntentsIntentConfirmPayloadPaymentMethodOptions
        -- ^ Payment-method-specific configuration for this
        --   PaymentIntent.
    useStripeSdk : Optional Bool
        -- ^ Set to `true` only when using manual confirmation and
        --   the iOS or Android SDKs to handle additional
        --   authentication steps.
    errorOnRequiresAction : Optional Bool
        -- ^ Set to `true` to fail the payment attempt if the
        --   PaymentIntent transitions into `requires_action`.
        --   This parameter is intended for simpler integrations
        --   that do not handle customer actions, like [saving
        --   cards without
        --   authentication](https://stripe.com/docs/payments/save-card-without-authentication).
    paymentMethodTypes : [Text]
        -- ^ The list of payment method types (e.g. card) that
        --   this PaymentIntent is allowed to use.
    returnUrl : Optional Text
        -- ^ The URL to redirect your customer back to after they
        --   authenticate or cancel their payment on the payment
        --   method's app or site. If you'd prefer to redirect to
        --   a mobile application, you can alternatively supply an
        --   application URI scheme. This parameter is only used
        --   for cards and other redirect-based payment methods.
      deriving (Eq, Ord, Show)

data GetBalanceHistoryResponseBody200Data = GetBalanceHistoryResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetBalanceHistoryResponseBody200 = GetBalanceHistoryResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [BalanceTransaction]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data GetBitcoinReceiversReceiverTransactionsResponseBody200Data = GetBitcoinReceiversReceiverTransactionsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetBitcoinReceiversReceiverTransactionsResponseBody200 = GetBitcoinReceiversReceiverTransactionsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [BitcoinTransaction]
        -- ^ Details about each object.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data GetPaymentMethodsResponseBody200Data = GetPaymentMethodsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetPaymentMethodsResponseBody200 = GetPaymentMethodsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [PaymentMethod]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostPaymentMethodsPayloadCard = PostPaymentMethodsPayloadCard with
      deriving (Eq, Ord, Show)

data PostPaymentMethodsPayloadIdeal = PostPaymentMethodsPayloadIdeal with
    bank : Optional Text
      deriving (Eq, Ord, Show)

data PostPaymentMethodsPayloadFpx = PostPaymentMethodsPayloadFpx with
    bank : Text
      deriving (Eq, Ord, Show)

data PostPaymentMethodsPayloadMetadata = PostPaymentMethodsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostPaymentMethodsPayloadBillingDetailsAddress = PostPaymentMethodsPayloadBillingDetailsAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostPaymentMethodsPayloadBillingDetails = PostPaymentMethodsPayloadBillingDetails with
    email : Optional Text
    phone : Optional Text
    address : Optional PostPaymentMethodsPayloadBillingDetailsAddress
    name : Optional Text
      deriving (Eq, Ord, Show)

data PostPaymentMethodsPayloadSepaDebit = PostPaymentMethodsPayloadSepaDebit with
    iban : Text
      deriving (Eq, Ord, Show)

data PostPaymentMethodsPayload = PostPaymentMethodsPayload with
    card : Optional PostPaymentMethodsPayloadCard
        -- ^ If this is a `card` PaymentMethod, this hash contains
        --   the user's card details. For backwards compatibility,
        --   you can alternatively provide a Stripe token (e.g.,
        --   for Apple Pay, Amex Express Checkout, or legacy
        --   Checkout) into the card hash with format `card:
        --   {token: "tok_visa"}`. When creating with a card
        --   number, you must meet the requirements for [PCI
        --   compliance](https://stripe.com/docs/security#validating-pci-compliance).
        --   We strongly recommend using Stripe.js instead of
        --   interacting with this API directly.
    paymentMethod : Optional Text
        -- ^ The PaymentMethod to share.
    ideal : Optional PostPaymentMethodsPayloadIdeal
        -- ^ If this is an `ideal` PaymentMethod, this hash
        --   contains details about the iDEAL payment method.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    fpx : Optional PostPaymentMethodsPayloadFpx
        -- ^ If this is an `fpx` PaymentMethod, this hash contains
        --   details about the FPX payment method.
    customer : Optional Text
        -- ^ The `Customer` to whom the original PaymentMethod is
        --   attached.
    metadata : Optional PostPaymentMethodsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    type_ : Optional Text
        -- ^ The type of the PaymentMethod. An additional hash is
        --   included on the PaymentMethod with a name matching
        --   this value. It contains additional information
        --   specific to the PaymentMethod type. Required unless
        --   `payment_method` is specified (see the [Cloning
        --   PaymentMethods](https://stripe.com/docs/payments/payment-methods/connect#cloning-payment-methods)
        --   guide)
    billingDetails : Optional PostPaymentMethodsPayloadBillingDetails
        -- ^ Billing information associated with the PaymentMethod
        --   that may be used or required by particular types of
        --   payment methods.
    sepaDebit : Optional PostPaymentMethodsPayloadSepaDebit
        -- ^ If this is a `sepa_debit` PaymentMethod, this hash
        --   contains details about the SEPA debit bank account.
      deriving (Eq, Ord, Show)

data PostPaymentMethodsPaymentMethodPayloadCard = PostPaymentMethodsPaymentMethodPayloadCard with
    expMonth : Optional Int
    expYear : Optional Int
      deriving (Eq, Ord, Show)

data PostPaymentMethodsPaymentMethodPayloadMetadata = PostPaymentMethodsPaymentMethodPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostPaymentMethodsPaymentMethodPayloadBillingDetailsAddress = PostPaymentMethodsPaymentMethodPayloadBillingDetailsAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostPaymentMethodsPaymentMethodPayloadBillingDetails = PostPaymentMethodsPaymentMethodPayloadBillingDetails with
    email : Optional Text
    phone : Optional Text
    address : Optional PostPaymentMethodsPaymentMethodPayloadBillingDetailsAddress
    name : Optional Text
      deriving (Eq, Ord, Show)

data PostPaymentMethodsPaymentMethodPayloadSepaDebit = PostPaymentMethodsPaymentMethodPayloadSepaDebit with
      deriving (Eq, Ord, Show)

data PostPaymentMethodsPaymentMethodPayload = PostPaymentMethodsPaymentMethodPayload with
    card : Optional PostPaymentMethodsPaymentMethodPayloadCard
        -- ^ If this is a `card` PaymentMethod, this hash contains
        --   the user's card details.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    metadata : Optional PostPaymentMethodsPaymentMethodPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    billingDetails : Optional PostPaymentMethodsPaymentMethodPayloadBillingDetails
        -- ^ Billing information associated with the PaymentMethod
        --   that may be used or required by particular types of
        --   payment methods.
    sepaDebit : Optional PostPaymentMethodsPaymentMethodPayloadSepaDebit
        -- ^ If this is a `sepa_debit` PaymentMethod, this hash
        --   contains details about the SEPA debit bank account.
      deriving (Eq, Ord, Show)

data PostAccountPeoplePersonPayloadRelationship = PostAccountPeoplePersonPayloadRelationship with
    director : Optional Bool
    owner : Optional Bool
    percentOwnership : Optional Text
    title : Optional Text
    executive : Optional Bool
    representative : Optional Bool
      deriving (Eq, Ord, Show)

data PostAccountPeoplePersonPayloadAddressKana = PostAccountPeoplePersonPayloadAddressKana with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPeoplePersonPayloadAddress = PostAccountPeoplePersonPayloadAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPeoplePersonPayloadMetadata = PostAccountPeoplePersonPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostAccountPeoplePersonPayloadAddressKanji = PostAccountPeoplePersonPayloadAddressKanji with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPeoplePersonPayloadVerificationDocument = PostAccountPeoplePersonPayloadVerificationDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPeoplePersonPayloadVerificationAdditionalDocument = PostAccountPeoplePersonPayloadVerificationAdditionalDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPeoplePersonPayloadVerification = PostAccountPeoplePersonPayloadVerification with
    document : Optional PostAccountPeoplePersonPayloadVerificationDocument
    additionalDocument : Optional PostAccountPeoplePersonPayloadVerificationAdditionalDocument
      deriving (Eq, Ord, Show)

data PostAccountPeoplePersonPayload = PostAccountPeoplePersonPayload with
    relationship : Optional PostAccountPeoplePersonPayloadRelationship
        -- ^ The relationship that this person has with the
        --   account's legal entity.
    email : Optional Text
        -- ^ The person's email address.
    addressKana : Optional PostAccountPeoplePersonPayloadAddressKana
        -- ^ The Kana variation of the person's address (Japan
        --   only).
    phone : Optional Text
        -- ^ The person's phone number.
    lastNameKanji : Optional Text
        -- ^ The Kanji variation of the person's last name (Japan
        --   only).
    firstName : Optional Text
        -- ^ The person's first name.
    lastNameKana : Optional Text
        -- ^ The Kana variation of the person's last name (Japan
        --   only).
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    address : Optional PostAccountPeoplePersonPayloadAddress
        -- ^ The person's address.
    account : Optional Text
    personToken : Optional Text
        -- ^ A [person
        --   token](https://stripe.com/docs/connect/account-tokens),
        --   used to securely provide details to the person.
    idNumber : Optional Text
        -- ^ The person's ID number, as appropriate for their
        --   country. For example, a social security number in the
        --   U.S., social insurance number in Canada, etc. Instead
        --   of the number itself, you can also provide a [PII
        --   token provided by
        --   Stripe.js](https://stripe.com/docs/stripe.js#collecting-pii-data).
    dob : Optional Text
        -- ^ The person's date of birth.
    gender : Optional Text
        -- ^ The person's gender (International regulations
        --   require either "male" or "female").
    firstNameKanji : Optional Text
        -- ^ The Kanji variation of the person's first name (Japan
        --   only).
    lastName : Optional Text
        -- ^ The person's last name.
    maidenName : Optional Text
        -- ^ The person's maiden name.
    metadata : Optional PostAccountPeoplePersonPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    firstNameKana : Optional Text
        -- ^ The Kana variation of the person's first name (Japan
        --   only).
    ssnLast4 : Optional Text
        -- ^ The last 4 digits of the person's social security
        --   number.
    addressKanji : Optional PostAccountPeoplePersonPayloadAddressKanji
        -- ^ The Kanji variation of the person's address (Japan
        --   only).
    verification : Optional PostAccountPeoplePersonPayloadVerification
        -- ^ The person's verification status.
      deriving (Eq, Ord, Show)

data PostAccountBankAccountsIdPayloadMetadata = PostAccountBankAccountsIdPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostAccountBankAccountsIdPayload = PostAccountBankAccountsIdPayload with
    defaultForCurrency : Optional Bool
        -- ^ When set to true, this becomes the default external
        --   account for its currency.
    addressCity : Optional Text
        -- ^ City/District/Suburb/Town/Village.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    addressState : Optional Text
        -- ^ State/County/Province/Region.
    accountHolderType : Optional Text
        -- ^ The type of entity that holds the account. This can
        --   be either `individual` or `company`.
    name : Optional Text
        -- ^ Cardholder name.
    expMonth : Optional Text
        -- ^ Two digit number representing the card’s expiration
        --   month.
    addressZip : Optional Text
        -- ^ ZIP or postal code.
    metadata : Optional PostAccountBankAccountsIdPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    expYear : Optional Text
        -- ^ Four digit number representing the card’s expiration
        --   year.
    addressLine2 : Optional Text
        -- ^ Address line 2 (Apartment/Suite/Unit/Building).
    addressLine1 : Optional Text
        -- ^ Address line 1 (Street address/PO Box/Company name).
    accountHolderName : Optional Text
        -- ^ The name of the person or business that owns the bank
        --   account.
    addressCountry : Optional Text
        -- ^ Billing address country, if provided when creating
        --   card.
      deriving (Eq, Ord, Show)

data PostChargesChargeDisputePayloadMetadata = PostChargesChargeDisputePayloadMetadata with
      deriving (Eq, Ord, Show)

data PostChargesChargeDisputePayloadEvidence = PostChargesChargeDisputePayloadEvidence with
    receipt : Optional Text
    shippingDocumentation : Optional Text
    duplicateChargeDocumentation : Optional Text
    refundRefusalExplanation : Optional Text
    billingAddress : Optional Text
    uncategorizedText : Optional Text
    accessActivityLog : Optional Text
    cancellationRebuttal : Optional Text
    cancellationPolicy : Optional Text
    cancellationPolicyDisclosure : Optional Text
    shippingAddress : Optional Text
    shippingTrackingNumber : Optional Text
    serviceDate : Optional Text
    duplicateChargeExplanation : Optional Text
    uncategorizedFile : Optional Text
    refundPolicyDisclosure : Optional Text
    customerName : Optional Text
    customerSignature : Optional Text
    productDescription : Optional Text
    serviceDocumentation : Optional Text
    duplicateChargeId : Optional Text
    customerCommunication : Optional Text
    customerPurchaseIp : Optional Text
    customerEmailAddress : Optional Text
    refundPolicy : Optional Text
    shippingDate : Optional Text
    shippingCarrier : Optional Text
      deriving (Eq, Ord, Show)

data PostChargesChargeDisputePayload = PostChargesChargeDisputePayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    submit : Optional Bool
        -- ^ Whether to immediately submit evidence to the bank.
        --   If `false`, evidence is staged on the dispute. Staged
        --   evidence is visible in the API and Dashboard, and can
        --   be submitted to the bank by making another request
        --   with this attribute set to `true` (the default).
    metadata : Optional PostChargesChargeDisputePayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    evidence : Optional PostChargesChargeDisputePayloadEvidence
        -- ^ Evidence to upload, to respond to a dispute. Updating
        --   any field in the hash will submit all fields in the
        --   hash for review. The combined character count of all
        --   fields is limited to 150,000.
      deriving (Eq, Ord, Show)

data PostIssuingSettlementsSettlementPayloadMetadata = PostIssuingSettlementsSettlementPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostIssuingSettlementsSettlementPayload = PostIssuingSettlementsSettlementPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    metadata : Optional PostIssuingSettlementsSettlementPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data PostCustomersCustomerCardsIdPayloadOwnerAddress = PostCustomersCustomerCardsIdPayloadOwnerAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostCustomersCustomerCardsIdPayloadOwner = PostCustomersCustomerCardsIdPayloadOwner with
    email : Optional Text
    phone : Optional Text
    address : Optional PostCustomersCustomerCardsIdPayloadOwnerAddress
    name : Optional Text
      deriving (Eq, Ord, Show)

data PostCustomersCustomerCardsIdPayloadMetadata = PostCustomersCustomerCardsIdPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostCustomersCustomerCardsIdPayload = PostCustomersCustomerCardsIdPayload with
    addressCity : Optional Text
        -- ^ City/District/Suburb/Town/Village.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    addressState : Optional Text
        -- ^ State/County/Province/Region.
    owner : Optional PostCustomersCustomerCardsIdPayloadOwner
    accountHolderType : Optional Text
        -- ^ The type of entity that holds the account. This can
        --   be either `individual` or `company`.
    name : Optional Text
        -- ^ Cardholder name.
    expMonth : Optional Text
        -- ^ Two digit number representing the card’s expiration
        --   month.
    addressZip : Optional Text
        -- ^ ZIP or postal code.
    metadata : Optional PostCustomersCustomerCardsIdPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    expYear : Optional Text
        -- ^ Four digit number representing the card’s expiration
        --   year.
    addressLine2 : Optional Text
        -- ^ Address line 2 (Apartment/Suite/Unit/Building).
    addressLine1 : Optional Text
        -- ^ Address line 1 (Street address/PO Box/Company name).
    accountHolderName : Optional Text
        -- ^ The name of the person or business that owns the bank
        --   account.
    addressCountry : Optional Text
        -- ^ Billing address country, if provided when creating
        --   card.
      deriving (Eq, Ord, Show)

data DeleteCustomersCustomerCardsIdPayload = DeleteCustomersCustomerCardsIdPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
      deriving (Eq, Ord, Show)

data PostAccountsAccountRejectPayload = PostAccountsAccountRejectPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    reason : Text
        -- ^ The reason for rejecting the account. Can be `fraud`,
        --   `terms_of_service`, or `other`.
      deriving (Eq, Ord, Show)

data GetTerminalLocationsResponseBody200Data = GetTerminalLocationsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetTerminalLocationsResponseBody200 = GetTerminalLocationsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [TerminalLocation]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostTerminalLocationsPayloadAddress = PostTerminalLocationsPayloadAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostTerminalLocationsPayloadMetadata = PostTerminalLocationsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostTerminalLocationsPayload = PostTerminalLocationsPayload with
    displayName : Optional Text
        -- ^ A name for the location.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    address : PostTerminalLocationsPayloadAddress
        -- ^ The full address of the location.
    metadata : Optional PostTerminalLocationsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data PostPaymentMethodsPaymentMethodDetachPayload = PostPaymentMethodsPaymentMethodDetachPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
      deriving (Eq, Ord, Show)

data GetAccountPeopleResponseBody200Data = GetAccountPeopleResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetAccountPeopleResponseBody200 = GetAccountPeopleResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Person]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostAccountPeoplePayloadRelationship = PostAccountPeoplePayloadRelationship with
    director : Optional Bool
    owner : Optional Bool
    percentOwnership : Optional Text
    title : Optional Text
    executive : Optional Bool
    representative : Optional Bool
      deriving (Eq, Ord, Show)

data PostAccountPeoplePayloadAddressKana = PostAccountPeoplePayloadAddressKana with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPeoplePayloadAddress = PostAccountPeoplePayloadAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPeoplePayloadMetadata = PostAccountPeoplePayloadMetadata with
      deriving (Eq, Ord, Show)

data PostAccountPeoplePayloadAddressKanji = PostAccountPeoplePayloadAddressKanji with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPeoplePayloadVerificationDocument = PostAccountPeoplePayloadVerificationDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPeoplePayloadVerificationAdditionalDocument = PostAccountPeoplePayloadVerificationAdditionalDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPeoplePayloadVerification = PostAccountPeoplePayloadVerification with
    document : Optional PostAccountPeoplePayloadVerificationDocument
    additionalDocument : Optional PostAccountPeoplePayloadVerificationAdditionalDocument
      deriving (Eq, Ord, Show)

data PostAccountPeoplePayload = PostAccountPeoplePayload with
    relationship : Optional PostAccountPeoplePayloadRelationship
        -- ^ The relationship that this person has with the
        --   account's legal entity.
    email : Optional Text
        -- ^ The person's email address.
    addressKana : Optional PostAccountPeoplePayloadAddressKana
        -- ^ The Kana variation of the person's address (Japan
        --   only).
    phone : Optional Text
        -- ^ The person's phone number.
    lastNameKanji : Optional Text
        -- ^ The Kanji variation of the person's last name (Japan
        --   only).
    firstName : Optional Text
        -- ^ The person's first name.
    lastNameKana : Optional Text
        -- ^ The Kana variation of the person's last name (Japan
        --   only).
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    address : Optional PostAccountPeoplePayloadAddress
        -- ^ The person's address.
    account : Optional Text
    personToken : Optional Text
        -- ^ A [person
        --   token](https://stripe.com/docs/connect/account-tokens),
        --   used to securely provide details to the person.
    idNumber : Optional Text
        -- ^ The person's ID number, as appropriate for their
        --   country. For example, a social security number in the
        --   U.S., social insurance number in Canada, etc. Instead
        --   of the number itself, you can also provide a [PII
        --   token provided by
        --   Stripe.js](https://stripe.com/docs/stripe.js#collecting-pii-data).
    dob : Optional Text
        -- ^ The person's date of birth.
    gender : Optional Text
        -- ^ The person's gender (International regulations
        --   require either "male" or "female").
    firstNameKanji : Optional Text
        -- ^ The Kanji variation of the person's first name (Japan
        --   only).
    lastName : Optional Text
        -- ^ The person's last name.
    maidenName : Optional Text
        -- ^ The person's maiden name.
    metadata : Optional PostAccountPeoplePayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    firstNameKana : Optional Text
        -- ^ The Kana variation of the person's first name (Japan
        --   only).
    ssnLast4 : Optional Text
        -- ^ The last 4 digits of the person's social security
        --   number.
    addressKanji : Optional PostAccountPeoplePayloadAddressKanji
        -- ^ The Kanji variation of the person's address (Japan
        --   only).
    verification : Optional PostAccountPeoplePayloadVerification
        -- ^ The person's verification status.
      deriving (Eq, Ord, Show)

data GetInvoicesInvoiceLinesResponseBody200Data = GetInvoicesInvoiceLinesResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetInvoicesInvoiceLinesResponseBody200 = GetInvoicesInvoiceLinesResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [LineItem]
        -- ^ Details about each object.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostReviewsReviewApprovePayload = PostReviewsReviewApprovePayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
      deriving (Eq, Ord, Show)

data GetFilesResponseBody200Data = GetFilesResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetFilesResponseBody200 = GetFilesResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [File]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostFilesPayloadFileLinkDataMetadata = PostFilesPayloadFileLinkDataMetadata with
      deriving (Eq, Ord, Show)

data PostFilesPayloadFileLinkData = PostFilesPayloadFileLinkData with
    create : Bool
    expiresAt : Optional Int
    metadata : Optional PostFilesPayloadFileLinkDataMetadata
      deriving (Eq, Ord, Show)

data PostFilesPayload = PostFilesPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    fileLinkData : Optional PostFilesPayloadFileLinkData
        -- ^ Optional parameters to automatically create a [file
        --   link](https://stripe.com/docs/api#file_links) for the
        --   newly created file.
    purpose : Text
        -- ^ The purpose of the uploaded file. Possible values are
        --   `additional_verification`, `business_icon`,
        --   `business_logo`, `customer_signature`,
        --   `dispute_evidence`, `identity_document`,
        --   `pci_document`, or `tax_document_user_upload`.
    file : Text
        -- ^ A file to upload. The file should follow the
        --   specifications of RFC 2388 (which defines file
        --   transfers for the `multipart/form-data` protocol).
      deriving (Eq, Ord, Show)

data GetApplePayDomainsResponseBody200Data = GetApplePayDomainsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetApplePayDomainsResponseBody200 = GetApplePayDomainsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [ApplePayDomain]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostApplePayDomainsPayload = PostApplePayDomainsPayload with
    domainName : Optional Text
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
      deriving (Eq, Ord, Show)

data PostAccountPersonsPersonPayloadRelationship = PostAccountPersonsPersonPayloadRelationship with
    director : Optional Bool
    owner : Optional Bool
    percentOwnership : Optional Text
    title : Optional Text
    executive : Optional Bool
    representative : Optional Bool
      deriving (Eq, Ord, Show)

data PostAccountPersonsPersonPayloadAddressKana = PostAccountPersonsPersonPayloadAddressKana with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPersonsPersonPayloadAddress = PostAccountPersonsPersonPayloadAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPersonsPersonPayloadMetadata = PostAccountPersonsPersonPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostAccountPersonsPersonPayloadAddressKanji = PostAccountPersonsPersonPayloadAddressKanji with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPersonsPersonPayloadVerificationDocument = PostAccountPersonsPersonPayloadVerificationDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPersonsPersonPayloadVerificationAdditionalDocument = PostAccountPersonsPersonPayloadVerificationAdditionalDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPersonsPersonPayloadVerification = PostAccountPersonsPersonPayloadVerification with
    document : Optional PostAccountPersonsPersonPayloadVerificationDocument
    additionalDocument : Optional PostAccountPersonsPersonPayloadVerificationAdditionalDocument
      deriving (Eq, Ord, Show)

data PostAccountPersonsPersonPayload = PostAccountPersonsPersonPayload with
    relationship : Optional PostAccountPersonsPersonPayloadRelationship
        -- ^ The relationship that this person has with the
        --   account's legal entity.
    email : Optional Text
        -- ^ The person's email address.
    addressKana : Optional PostAccountPersonsPersonPayloadAddressKana
        -- ^ The Kana variation of the person's address (Japan
        --   only).
    phone : Optional Text
        -- ^ The person's phone number.
    lastNameKanji : Optional Text
        -- ^ The Kanji variation of the person's last name (Japan
        --   only).
    firstName : Optional Text
        -- ^ The person's first name.
    lastNameKana : Optional Text
        -- ^ The Kana variation of the person's last name (Japan
        --   only).
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    address : Optional PostAccountPersonsPersonPayloadAddress
        -- ^ The person's address.
    account : Optional Text
    personToken : Optional Text
        -- ^ A [person
        --   token](https://stripe.com/docs/connect/account-tokens),
        --   used to securely provide details to the person.
    idNumber : Optional Text
        -- ^ The person's ID number, as appropriate for their
        --   country. For example, a social security number in the
        --   U.S., social insurance number in Canada, etc. Instead
        --   of the number itself, you can also provide a [PII
        --   token provided by
        --   Stripe.js](https://stripe.com/docs/stripe.js#collecting-pii-data).
    dob : Optional Text
        -- ^ The person's date of birth.
    gender : Optional Text
        -- ^ The person's gender (International regulations
        --   require either "male" or "female").
    firstNameKanji : Optional Text
        -- ^ The Kanji variation of the person's first name (Japan
        --   only).
    lastName : Optional Text
        -- ^ The person's last name.
    maidenName : Optional Text
        -- ^ The person's maiden name.
    metadata : Optional PostAccountPersonsPersonPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    firstNameKana : Optional Text
        -- ^ The Kana variation of the person's first name (Japan
        --   only).
    ssnLast4 : Optional Text
        -- ^ The last 4 digits of the person's social security
        --   number.
    addressKanji : Optional PostAccountPersonsPersonPayloadAddressKanji
        -- ^ The Kanji variation of the person's address (Japan
        --   only).
    verification : Optional PostAccountPersonsPersonPayloadVerification
        -- ^ The person's verification status.
      deriving (Eq, Ord, Show)

data PostTokensPayloadPersonRelationship = PostTokensPayloadPersonRelationship with
    director : Optional Bool
    owner : Optional Bool
    percentOwnership : Optional Text
    title : Optional Text
    executive : Optional Bool
    representative : Optional Bool
      deriving (Eq, Ord, Show)

data PostTokensPayloadPersonAddressKana = PostTokensPayloadPersonAddressKana with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostTokensPayloadPersonAddress = PostTokensPayloadPersonAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostTokensPayloadPersonMetadata = PostTokensPayloadPersonMetadata with
      deriving (Eq, Ord, Show)

data PostTokensPayloadPersonAddressKanji = PostTokensPayloadPersonAddressKanji with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostTokensPayloadPersonVerificationDocument = PostTokensPayloadPersonVerificationDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostTokensPayloadPersonVerificationAdditionalDocument = PostTokensPayloadPersonVerificationAdditionalDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostTokensPayloadPersonVerification = PostTokensPayloadPersonVerification with
    document : Optional PostTokensPayloadPersonVerificationDocument
    additionalDocument : Optional PostTokensPayloadPersonVerificationAdditionalDocument
      deriving (Eq, Ord, Show)

data PostTokensPayloadPerson = PostTokensPayloadPerson with
    relationship : Optional PostTokensPayloadPersonRelationship
    email : Optional Text
    addressKana : Optional PostTokensPayloadPersonAddressKana
    phone : Optional Text
    lastNameKanji : Optional Text
    firstName : Optional Text
    lastNameKana : Optional Text
    address : Optional PostTokensPayloadPersonAddress
    idNumber : Optional Text
    dob : Optional Text
    gender : Optional Text
    firstNameKanji : Optional Text
    lastName : Optional Text
    maidenName : Optional Text
    metadata : Optional PostTokensPayloadPersonMetadata
    firstNameKana : Optional Text
    ssnLast4 : Optional Text
    addressKanji : Optional PostTokensPayloadPersonAddressKanji
    verification : Optional PostTokensPayloadPersonVerification
      deriving (Eq, Ord, Show)

data PostTokensPayloadBankAccount = PostTokensPayloadBankAccount with
    country : Text
    accountHolderType : Optional Text
    currency : Optional Text
    accountNumber : Optional Text
    routingNumber : Optional Text
    accountHolderName : Optional Text
      deriving (Eq, Ord, Show)

data PostTokensPayloadAccountCompanyAddressKana = PostTokensPayloadAccountCompanyAddressKana with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostTokensPayloadAccountCompanyAddress = PostTokensPayloadAccountCompanyAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostTokensPayloadAccountCompanyAddressKanji = PostTokensPayloadAccountCompanyAddressKanji with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostTokensPayloadAccountCompanyVerificationDocument = PostTokensPayloadAccountCompanyVerificationDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostTokensPayloadAccountCompanyVerification = PostTokensPayloadAccountCompanyVerification with
    document : Optional PostTokensPayloadAccountCompanyVerificationDocument
      deriving (Eq, Ord, Show)

data PostTokensPayloadAccountCompany = PostTokensPayloadAccountCompany with
    addressKana : Optional PostTokensPayloadAccountCompanyAddressKana
    phone : Optional Text
    nameKanji : Optional Text
    taxIdRegistrar : Optional Text
    taxId : Optional Text
    ownersProvided : Optional Bool
    structure : Optional Text
    directorsProvided : Optional Bool
    address : Optional PostTokensPayloadAccountCompanyAddress
    name : Optional Text
    nameKana : Optional Text
    executivesProvided : Optional Bool
    addressKanji : Optional PostTokensPayloadAccountCompanyAddressKanji
    vatId : Optional Text
    verification : Optional PostTokensPayloadAccountCompanyVerification
      deriving (Eq, Ord, Show)

data PostTokensPayloadAccountIndividualAddressKana = PostTokensPayloadAccountIndividualAddressKana with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostTokensPayloadAccountIndividualAddress = PostTokensPayloadAccountIndividualAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostTokensPayloadAccountIndividualMetadata = PostTokensPayloadAccountIndividualMetadata with
      deriving (Eq, Ord, Show)

data PostTokensPayloadAccountIndividualAddressKanji = PostTokensPayloadAccountIndividualAddressKanji with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostTokensPayloadAccountIndividualVerificationDocument = PostTokensPayloadAccountIndividualVerificationDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostTokensPayloadAccountIndividualVerificationAdditionalDocument = PostTokensPayloadAccountIndividualVerificationAdditionalDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostTokensPayloadAccountIndividualVerification = PostTokensPayloadAccountIndividualVerification with
    document : Optional PostTokensPayloadAccountIndividualVerificationDocument
    additionalDocument : Optional PostTokensPayloadAccountIndividualVerificationAdditionalDocument
      deriving (Eq, Ord, Show)

data PostTokensPayloadAccountIndividual = PostTokensPayloadAccountIndividual with
    email : Optional Text
    addressKana : Optional PostTokensPayloadAccountIndividualAddressKana
    phone : Optional Text
    lastNameKanji : Optional Text
    firstName : Optional Text
    lastNameKana : Optional Text
    address : Optional PostTokensPayloadAccountIndividualAddress
    idNumber : Optional Text
    dob : Optional Text
    gender : Optional Text
    firstNameKanji : Optional Text
    lastName : Optional Text
    maidenName : Optional Text
    metadata : Optional PostTokensPayloadAccountIndividualMetadata
    firstNameKana : Optional Text
    ssnLast4 : Optional Text
    addressKanji : Optional PostTokensPayloadAccountIndividualAddressKanji
    verification : Optional PostTokensPayloadAccountIndividualVerification
      deriving (Eq, Ord, Show)

data PostTokensPayloadAccount = PostTokensPayloadAccount with
    businessType : Optional Text
    company : Optional PostTokensPayloadAccountCompany
    tosShownAndAccepted : Optional Bool
    individual : Optional PostTokensPayloadAccountIndividual
      deriving (Eq, Ord, Show)

data PostTokensPayloadPii = PostTokensPayloadPii with
    idNumber : Optional Text
      deriving (Eq, Ord, Show)

data PostTokensPayload = PostTokensPayload with
    card : Optional Text
    person : Optional PostTokensPayloadPerson
        -- ^ Information for the person this token will represent.
    bankAccount : Optional PostTokensPayloadBankAccount
        -- ^ The bank account this token will represent.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    account : Optional PostTokensPayloadAccount
        -- ^ Information for the account this token will
        --   represent.
    customer : Optional Text
        -- ^ The customer (owned by the application's account) for
        --   which to create a token. This can be used only with
        --   an [OAuth access
        --   token](https://stripe.com/docs/connect/standard-accounts)
        --   or [Stripe-Account
        --   header](https://stripe.com/docs/connect/authentication).
        --   For more details, see [Cloning Saved Payment
        --   Methods](https://stripe.com/docs/connect/cloning-saved-payment-methods).
    pii : Optional PostTokensPayloadPii
        -- ^ The PII this token will represent.
      deriving (Eq, Ord, Show)

data GetCustomersCustomerCardsResponseBody200Data = GetCustomersCustomerCardsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetCustomersCustomerCardsResponseBody200 = GetCustomersCustomerCardsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Card]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostCustomersCustomerCardsPayloadMetadata = PostCustomersCustomerCardsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostCustomersCustomerCardsPayload = PostCustomersCustomerCardsPayload with
    card : Optional Text
        -- ^ A token, like the ones returned by
        --   [Stripe.js](https://stripe.com/docs/stripe.js).
    bankAccount : Optional Text
        -- ^ Either a token, like the ones returned by
        --   [Stripe.js](https://stripe.com/docs/stripe.js), or a
        --   dictionary containing a user's bank account details.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    alipayAccount : Optional Text
        -- ^ A token returned by
        --   [Stripe.js](https://stripe.com/docs/stripe.js)
        --   representing the user’s Alipay account details.
    metadata : Optional PostCustomersCustomerCardsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    source : Optional Text
        -- ^ Please refer to full
        --   [documentation](https://stripe.com/docs/api) instead.
      deriving (Eq, Ord, Show)

data PostSetupIntentsIntentCancelPayload = PostSetupIntentsIntentCancelPayload with
    cancellationReason : Optional Text
        -- ^ Reason for canceling this SetupIntent. Possible
        --   values are `abandoned`, `requested_by_customer`, or
        --   `duplicate`
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
      deriving (Eq, Ord, Show)

data GetAccountsResponseBody200Data = GetAccountsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetAccountsResponseBody200 = GetAccountsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Account]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostAccountsPayloadBusinessProfile = PostAccountsPayloadBusinessProfile with
    supportPhone : Optional Text
    supportEmail : Optional Text
    url : Optional Text
    supportUrl : Optional Text
    name : Optional Text
    productDescription : Optional Text
    mcc : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsPayloadSettingsCardPaymentsDeclineOn = PostAccountsPayloadSettingsCardPaymentsDeclineOn with
    avsFailure : Optional Bool
    cvcFailure : Optional Bool
      deriving (Eq, Ord, Show)

data PostAccountsPayloadSettingsCardPayments = PostAccountsPayloadSettingsCardPayments with
    statementDescriptorPrefix : Optional Text
    declineOn : Optional PostAccountsPayloadSettingsCardPaymentsDeclineOn
      deriving (Eq, Ord, Show)

data PostAccountsPayloadSettingsPayoutsSchedule = PostAccountsPayloadSettingsPayoutsSchedule with
    monthlyAnchor : Optional Int
    interval : Optional Text
    weeklyAnchor : Optional Text
    delayDays : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsPayloadSettingsPayouts = PostAccountsPayloadSettingsPayouts with
    schedule : Optional PostAccountsPayloadSettingsPayoutsSchedule
    debitNegativeBalances : Optional Bool
    statementDescriptor : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsPayloadSettingsBranding = PostAccountsPayloadSettingsBranding with
    icon : Optional Text
    primaryColor : Optional Text
    logo : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsPayloadSettingsPayments = PostAccountsPayloadSettingsPayments with
    statementDescriptorKanji : Optional Text
    statementDescriptorKana : Optional Text
    statementDescriptor : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsPayloadSettings = PostAccountsPayloadSettings with
    cardPayments : Optional PostAccountsPayloadSettingsCardPayments
    payouts : Optional PostAccountsPayloadSettingsPayouts
    branding : Optional PostAccountsPayloadSettingsBranding
    payments : Optional PostAccountsPayloadSettingsPayments
      deriving (Eq, Ord, Show)

data PostAccountsPayloadCompanyAddressKana = PostAccountsPayloadCompanyAddressKana with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsPayloadCompanyAddress = PostAccountsPayloadCompanyAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsPayloadCompanyAddressKanji = PostAccountsPayloadCompanyAddressKanji with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsPayloadCompanyVerificationDocument = PostAccountsPayloadCompanyVerificationDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsPayloadCompanyVerification = PostAccountsPayloadCompanyVerification with
    document : Optional PostAccountsPayloadCompanyVerificationDocument
      deriving (Eq, Ord, Show)

data PostAccountsPayloadCompany = PostAccountsPayloadCompany with
    addressKana : Optional PostAccountsPayloadCompanyAddressKana
    phone : Optional Text
    nameKanji : Optional Text
    taxIdRegistrar : Optional Text
    taxId : Optional Text
    ownersProvided : Optional Bool
    structure : Optional Text
    directorsProvided : Optional Bool
    address : Optional PostAccountsPayloadCompanyAddress
    name : Optional Text
    nameKana : Optional Text
    executivesProvided : Optional Bool
    addressKanji : Optional PostAccountsPayloadCompanyAddressKanji
    vatId : Optional Text
    verification : Optional PostAccountsPayloadCompanyVerification
      deriving (Eq, Ord, Show)

data PostAccountsPayloadMetadata = PostAccountsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostAccountsPayloadTosAcceptance = PostAccountsPayloadTosAcceptance with
    userAgent : Optional Text
    ip : Optional Text
    date : Optional Int
      deriving (Eq, Ord, Show)

data PostAccountsPayloadIndividualAddressKana = PostAccountsPayloadIndividualAddressKana with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsPayloadIndividualAddress = PostAccountsPayloadIndividualAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsPayloadIndividualMetadata = PostAccountsPayloadIndividualMetadata with
      deriving (Eq, Ord, Show)

data PostAccountsPayloadIndividualAddressKanji = PostAccountsPayloadIndividualAddressKanji with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsPayloadIndividualVerificationDocument = PostAccountsPayloadIndividualVerificationDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsPayloadIndividualVerificationAdditionalDocument = PostAccountsPayloadIndividualVerificationAdditionalDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsPayloadIndividualVerification = PostAccountsPayloadIndividualVerification with
    document : Optional PostAccountsPayloadIndividualVerificationDocument
    additionalDocument : Optional PostAccountsPayloadIndividualVerificationAdditionalDocument
      deriving (Eq, Ord, Show)

data PostAccountsPayloadIndividual = PostAccountsPayloadIndividual with
    email : Optional Text
    addressKana : Optional PostAccountsPayloadIndividualAddressKana
    phone : Optional Text
    lastNameKanji : Optional Text
    firstName : Optional Text
    lastNameKana : Optional Text
    address : Optional PostAccountsPayloadIndividualAddress
    idNumber : Optional Text
    dob : Optional Text
    gender : Optional Text
    firstNameKanji : Optional Text
    lastName : Optional Text
    maidenName : Optional Text
    metadata : Optional PostAccountsPayloadIndividualMetadata
    firstNameKana : Optional Text
    ssnLast4 : Optional Text
    addressKanji : Optional PostAccountsPayloadIndividualAddressKanji
    verification : Optional PostAccountsPayloadIndividualVerification
      deriving (Eq, Ord, Show)

data PostAccountsPayload = PostAccountsPayload with
    email : Optional Text
        -- ^ The email address of the account holder. For Custom
        --   accounts, this is only to make the account easier to
        --   identify to you: Stripe will never directly email
        --   your users.
    businessProfile : Optional PostAccountsPayloadBusinessProfile
        -- ^ Business information about the account.
    accountToken : Optional Text
        -- ^ An [account
        --   token](https://stripe.com/docs/api#create_account_token),
        --   used to securely provide details to the account.
    settings : Optional PostAccountsPayloadSettings
        -- ^ Options for customizing how the account functions
        --   within Stripe.
    country : Optional Text
        -- ^ The country in which the account holder resides, or
        --   in which the business is legally established. This
        --   should be an ISO 3166-1 alpha-2 country code. For
        --   example, if you are in the United States and the
        --   business for which you're creating an account is
        --   legally represented in Canada, you would use `CA` as
        --   the country for the account being created.
    bankAccount : Optional Text
        -- ^ Either a token, like the ones returned by
        --   [Stripe.js](https://stripe.com/docs/stripe.js), or a
        --   dictionary containing a user's bank account details.
    externalAccount : Optional Text
        -- ^ A card or bank account to attach to the account. You
        --   can provide either a token, like the ones returned by
        --   [Stripe.js](https://stripe.com/docs/stripe.js), or a
        --   dictionary, as documented in the `external_account`
        --   parameter for [bank
        --   account](https://stripe.com/docs/api#account_create_bank_account)
        --   creation. <br><br>By default, providing an external
        --   account sets it as the new default external account
        --   for its currency, and deletes the old default if one
        --   exists. To add additional external accounts without
        --   replacing the existing default for the currency, use
        --   the bank account or card creation API.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    businessType : Optional Text
        -- ^ The business type.
    company : Optional PostAccountsPayloadCompany
        -- ^ Information about the company or business. This field
        --   is null unless `business_type` is set to `company`,
        --   `government_entity`, or `non_profit`.
    metadata : Optional PostAccountsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    type_ : Optional Text
        -- ^ The type of Stripe account to create. Currently must
        --   be `custom`, as only [Custom
        --   accounts](https://stripe.com/docs/connect/custom-accounts)
        --   may be created via the API.
    defaultCurrency : Optional Text
        -- ^ Three-letter ISO currency code representing the
        --   default currency for the account. This must be a
        --   currency that [Stripe supports in the account's
        --   country](https://stripe.com/docs/payouts).
    tosAcceptance : Optional PostAccountsPayloadTosAcceptance
        -- ^ Details on the account's acceptance of the [Stripe
        --   Services
        --   Agreement](https://stripe.com/docs/connect/updating-accounts#tos-acceptance).
    individual : Optional PostAccountsPayloadIndividual
        -- ^ Information about the person represented by the
        --   account. This field is null unless `business_type` is
        --   set to `individual`.
    requestedCapabilities : [Text]
        -- ^ The set of capabilities you want to unlock for this
        --   account. Each capability will be inactive until you
        --   have provided its specific requirements and Stripe
        --   has verified them. An account may have some of its
        --   requested capabilities be active and some be
        --   inactive.
      deriving (Eq, Ord, Show)

data GetProductsResponseBody200Data = GetProductsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetProductsResponseBody200 = GetProductsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Product]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostProductsPayloadPackageDimensions = PostProductsPayloadPackageDimensions with
    length : Decimal
    height : Decimal
    weight : Decimal
    width : Decimal
      deriving (Eq, Ord, Show)

data PostProductsPayloadMetadata = PostProductsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostProductsPayload = PostProductsPayload with
    images : [Text]
        -- ^ A list of up to 8 URLs of images for this product,
        --   meant to be displayable to the customer.
    unitLabel : Optional Text
        -- ^ A label that represents units of this product in
        --   Stripe and on customers’ receipts and invoices. When
        --   set, this will be included in associated invoice line
        --   item descriptions.
    shippable : Optional Bool
        -- ^ Whether this product is shipped (i.e., physical
        --   goods). Defaults to `true`. May only be set if
        --   type=`good`.
    url : Optional Text
        -- ^ A URL of a publicly-accessible webpage for this
        --   product. May only be set if type=`good`.
    caption : Optional Text
        -- ^ A short one-line description of the product, meant to
        --   be displayable to the customer. May only be set if
        --   type=`good`.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    packageDimensions : Optional PostProductsPayloadPackageDimensions
        -- ^ The dimensions of this product for shipping purposes.
        --   A SKU associated with this product can override this
        --   value by having its own `package_dimensions`. May
        --   only be set if type=`good`.
    active : Optional Bool
        -- ^ Whether the product is currently available for
        --   purchase. Defaults to `true`.
    name : Text
        -- ^ The product's name, meant to be displayable to the
        --   customer. Whenever this product is sold via a
        --   subscription, name will show up on associated invoice
        --   line item descriptions.
    attributes : [Text]
        -- ^ A list of up to 5 alphanumeric attributes.
    metadata : Optional PostProductsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    id : Optional Text
        -- ^ An identifier will be randomly generated by Stripe.
        --   You can optionally override this ID, but the ID must
        --   be unique across all products in your Stripe account.
    type_ : Optional Text
        -- ^ The type of the product. Defaults to `service` if not
        --   explicitly specified, enabling use of this product
        --   with Subscriptions and Plans. Set this parameter to
        --   `good` to use this product with Orders and SKUs. On
        --   API versions before `2018-02-05`, this field defaults
        --   to `good` for compatibility reasons.
    deactivateOn : [Text]
        -- ^ An array of Connect application names or identifiers
        --   that should not be able to order the SKUs for this
        --   product. May only be set if type=`good`.
    description : Optional Text
        -- ^ The product's description, meant to be displayable to
        --   the customer. Use this field to optionally store a
        --   long form explanation of the product being sold for
        --   your own rendering purposes.
    statementDescriptor : Optional Text
        -- ^ An arbitrary string to be displayed on your
        --   customer's credit card or bank statement. While most
        --   banks display this information consistently, some may
        --   display it incorrectly or not at all. This may be up
        --   to 22 characters. The statement description may not
        --   include `<`, `>`, `\`, `"`, `'` characters, and will
        --   appear on your customer's statement in capital
        --   letters. Non-ASCII characters are automatically
        --   stripped. It must contain at least one letter.
      deriving (Eq, Ord, Show)

data PostPayoutsPayoutCancelPayload = PostPayoutsPayoutCancelPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
      deriving (Eq, Ord, Show)

data PostCustomersCustomerSourcesIdPayloadOwnerAddress = PostCustomersCustomerSourcesIdPayloadOwnerAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostCustomersCustomerSourcesIdPayloadOwner = PostCustomersCustomerSourcesIdPayloadOwner with
    email : Optional Text
    phone : Optional Text
    address : Optional PostCustomersCustomerSourcesIdPayloadOwnerAddress
    name : Optional Text
      deriving (Eq, Ord, Show)

data PostCustomersCustomerSourcesIdPayloadMetadata = PostCustomersCustomerSourcesIdPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostCustomersCustomerSourcesIdPayload = PostCustomersCustomerSourcesIdPayload with
    addressCity : Optional Text
        -- ^ City/District/Suburb/Town/Village.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    addressState : Optional Text
        -- ^ State/County/Province/Region.
    owner : Optional PostCustomersCustomerSourcesIdPayloadOwner
    accountHolderType : Optional Text
        -- ^ The type of entity that holds the account. This can
        --   be either `individual` or `company`.
    name : Optional Text
        -- ^ Cardholder name.
    expMonth : Optional Text
        -- ^ Two digit number representing the card’s expiration
        --   month.
    addressZip : Optional Text
        -- ^ ZIP or postal code.
    metadata : Optional PostCustomersCustomerSourcesIdPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    expYear : Optional Text
        -- ^ Four digit number representing the card’s expiration
        --   year.
    addressLine2 : Optional Text
        -- ^ Address line 2 (Apartment/Suite/Unit/Building).
    addressLine1 : Optional Text
        -- ^ Address line 1 (Street address/PO Box/Company name).
    accountHolderName : Optional Text
        -- ^ The name of the person or business that owns the bank
        --   account.
    addressCountry : Optional Text
        -- ^ Billing address country, if provided when creating
        --   card.
      deriving (Eq, Ord, Show)

data DeleteCustomersCustomerSourcesIdPayload = DeleteCustomersCustomerSourcesIdPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
      deriving (Eq, Ord, Show)

data PostInvoicesInvoiceMarkUncollectiblePayload = PostInvoicesInvoiceMarkUncollectiblePayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
      deriving (Eq, Ord, Show)

data GetCustomersCustomerSourcesResponseBody200Data = GetCustomersCustomerSourcesResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetCustomersCustomerSourcesResponseBody200 = GetCustomersCustomerSourcesResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [AlipayAccount]
        -- ^ Details about each object.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostCustomersCustomerSourcesPayloadMetadata = PostCustomersCustomerSourcesPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostCustomersCustomerSourcesPayload = PostCustomersCustomerSourcesPayload with
    card : Optional Text
        -- ^ A token, like the ones returned by
        --   [Stripe.js](https://stripe.com/docs/stripe.js).
    bankAccount : Optional Text
        -- ^ Either a token, like the ones returned by
        --   [Stripe.js](https://stripe.com/docs/stripe.js), or a
        --   dictionary containing a user's bank account details.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    alipayAccount : Optional Text
        -- ^ A token returned by
        --   [Stripe.js](https://stripe.com/docs/stripe.js)
        --   representing the user’s Alipay account details.
    metadata : Optional PostCustomersCustomerSourcesPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    source : Optional Text
        -- ^ Please refer to full
        --   [documentation](https://stripe.com/docs/api) instead.
      deriving (Eq, Ord, Show)

data PostRecipientsIdPayloadMetadata = PostRecipientsIdPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostRecipientsIdPayload = PostRecipientsIdPayload with
    card : Optional Text
        -- ^ A U.S. Visa or MasterCard debit card (not prepaid) to
        --   attach to the recipient. You can provide either a
        --   token, like the ones returned by
        --   [Stripe.js](https://stripe.com/docs/stripe-js), or a
        --   dictionary containing a user's debit card details,
        --   with the options described below. Passing `card` will
        --   create a new card, make it the new recipient default
        --   card, and delete the old recipient default (if one
        --   exists). If you want to add additional debit cards
        --   instead of replacing the existing default, use the
        --   [card creation
        --   API](https://stripe.com/docs/api#create_card).
        --   Whenever you attach a card to a recipient, Stripe
        --   will automatically validate the debit card.
    email : Optional Text
        -- ^ The recipient's email address. It is displayed
        --   alongside the recipient in the web interface, and can
        --   be useful for searching and tracking.
    taxId : Optional Text
        -- ^ The recipient's tax ID, as a string. For type
        --   `individual`, the full SSN; for type `corporation`,
        --   the full EIN.
    defaultCard : Optional Text
        -- ^ ID of the card to set as the recipient's new default
        --   for payouts.
    bankAccount : Optional Text
        -- ^ A bank account to attach to the recipient. You can
        --   provide either a token, like the ones returned by
        --   [Stripe.js](https://stripe.com/docs/stripe-js), or a
        --   dictionary containing a user's bank account details,
        --   with the options described below.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    name : Optional Text
        -- ^ The recipient's full, legal name. For type
        --   `individual`, should be in the format `First Last`,
        --   `First Middle Last`, or `First M Last` (no prefixes
        --   or suffixes). For `corporation`, the full,
        --   incorporated name.
    metadata : Optional PostRecipientsIdPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    description : Optional Text
        -- ^ An arbitrary string which you can attach to a
        --   `Recipient` object. It is displayed alongside the
        --   recipient in the web interface.
      deriving (Eq, Ord, Show)

data PostIssuingTransactionsTransactionPayload = PostIssuingTransactionsTransactionPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    metadata : Optional Text
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data PostIssuingAuthorizationsAuthorizationPayload = PostIssuingAuthorizationsAuthorizationPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    metadata : Optional Text
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data GetCheckoutSessionsResponseBody200Data = GetCheckoutSessionsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetCheckoutSessionsResponseBody200 = GetCheckoutSessionsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [CheckoutSession]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostCheckoutSessionsPayloadLineItems = PostCheckoutSessionsPayloadLineItems with
      deriving (Eq, Ord, Show)

data PostCheckoutSessionsPayloadSetupIntentDataMetadata = PostCheckoutSessionsPayloadSetupIntentDataMetadata with
      deriving (Eq, Ord, Show)

data PostCheckoutSessionsPayloadSetupIntentData = PostCheckoutSessionsPayloadSetupIntentData with
    onBehalfOf : Optional Text
    metadata : Optional PostCheckoutSessionsPayloadSetupIntentDataMetadata
    description : Optional Text
      deriving (Eq, Ord, Show)

data PostCheckoutSessionsPayloadSubscriptionDataItems = PostCheckoutSessionsPayloadSubscriptionDataItems with
      deriving (Eq, Ord, Show)

data PostCheckoutSessionsPayloadSubscriptionDataMetadata = PostCheckoutSessionsPayloadSubscriptionDataMetadata with
      deriving (Eq, Ord, Show)

data PostCheckoutSessionsPayloadSubscriptionData = PostCheckoutSessionsPayloadSubscriptionData with
    trialFromPlan : Optional Bool
    trialPeriodDays : Optional Int
    items : [PostCheckoutSessionsPayloadSubscriptionDataItems]
    defaultTaxRates : [Text]
    applicationFeePercent : Optional Decimal
    trialEnd : Optional Int
    metadata : Optional PostCheckoutSessionsPayloadSubscriptionDataMetadata
      deriving (Eq, Ord, Show)

data PostCheckoutSessionsPayloadMetadata = PostCheckoutSessionsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostCheckoutSessionsPayloadPaymentIntentDataTransferData = PostCheckoutSessionsPayloadPaymentIntentDataTransferData with
    destination : Text
    amount : Optional Int
      deriving (Eq, Ord, Show)

data PostCheckoutSessionsPayloadPaymentIntentDataShippingAddress = PostCheckoutSessionsPayloadPaymentIntentDataShippingAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Text
      deriving (Eq, Ord, Show)

data PostCheckoutSessionsPayloadPaymentIntentDataShipping = PostCheckoutSessionsPayloadPaymentIntentDataShipping with
    carrier : Optional Text
    phone : Optional Text
    trackingNumber : Optional Text
    address : PostCheckoutSessionsPayloadPaymentIntentDataShippingAddress
    name : Text
      deriving (Eq, Ord, Show)

data PostCheckoutSessionsPayloadPaymentIntentDataMetadata = PostCheckoutSessionsPayloadPaymentIntentDataMetadata with
      deriving (Eq, Ord, Show)

data PostCheckoutSessionsPayloadPaymentIntentData = PostCheckoutSessionsPayloadPaymentIntentData with
    captureMethod : Optional Text
    setupFutureUsage : Optional Text
    receiptEmail : Optional Text
    transferData : Optional PostCheckoutSessionsPayloadPaymentIntentDataTransferData
    onBehalfOf : Optional Text
    shipping : Optional PostCheckoutSessionsPayloadPaymentIntentDataShipping
    metadata : Optional PostCheckoutSessionsPayloadPaymentIntentDataMetadata
    applicationFeeAmount : Optional Int
    description : Optional Text
    statementDescriptor : Optional Text
    statementDescriptorSuffix : Optional Text
      deriving (Eq, Ord, Show)

data PostCheckoutSessionsPayload = PostCheckoutSessionsPayload with
    lineItems : [PostCheckoutSessionsPayloadLineItems]
        -- ^ A list of items the customer is purchasing. Use this
        --   parameter for one-time payments or adding invoice
        --   line items to a subscription (used in conjunction
        --   with `subscription_data`).
    clientReferenceId : Optional Text
        -- ^ A unique string to reference the Checkout Session.
        --   This can be a customer ID, a cart ID, or similar, and
        --   can be used to reconcile the session with your
        --   internal systems.
    successUrl : Optional Text
        -- ^ The URL to which Stripe should send customers when
        --   payment or setup is complete. If you’d like access to
        --   the Checkout Session for the successful payment, read
        --   more about it in our guide on [fulfilling your
        --   payments with
        --   webhooks](/docs/payments/checkout/fulfillment#webhooks).
    cancelUrl : Optional Text
        -- ^ The URL the customer will be directed to if they
        --   decide to cancel payment and return to your website.
    locale : Optional Text
        -- ^ The IETF language tag of the locale Checkout is
        --   displayed in. If blank or `auto`, the browser's
        --   locale is used.
    mode : Optional Text
        -- ^ The mode of the Checkout Session, one of `payment`,
        --   `setup`, or `subscription`.
    billingAddressCollection : Optional Text
        -- ^ Specify whether Checkout should collect the
        --   customer's billing address.
    setupIntentData : Optional PostCheckoutSessionsPayloadSetupIntentData
        -- ^ A subset of parameters to be passed to SetupIntent
        --   creation for Checkout Sessions in `setup` mode.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    customer : Optional Text
        -- ^ ID of an existing customer, if one exists. Only
        --   supported for Checkout Sessions in `payment` or
        --   `subscription` mode, but not Checkout Sessions in
        --   `setup` mode. The email stored on the customer will
        --   be used to prefill the email field on the Checkout
        --   page. If the customer changes their email on the
        --   Checkout page, the Customer object will be updated
        --   with the new email. If blank for Checkout Sessions in
        --   `payment` or `subscription` mode, Checkout will
        --   create a new customer object based on information
        --   provided during the session.
    subscriptionData : Optional PostCheckoutSessionsPayloadSubscriptionData
        -- ^ A subset of parameters to be passed to subscription
        --   creation for Checkout Sessions in `subscription`
        --   mode.
    customerEmail : Optional Text
        -- ^ If provided, this value will be used when the
        --   Customer object is created. If not provided,
        --   customers will be asked to enter their email address.
        --   Use this parameter to prefill customer data if you
        --   already have an email on file. To access information
        --   about the customer once a session is complete, use
        --   the `customer` field.
    metadata : Optional PostCheckoutSessionsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    submitType : Optional Text
        -- ^ Describes the type of transaction being performed by
        --   Checkout in order to customize relevant text on the
        --   page, such as the submit button. `submit_type` can
        --   only be specified on Checkout Sessions in `payment`
        --   mode, but not Checkout Sessions in `subscription` or
        --   `setup` mode.
    paymentMethodTypes : [Text]
        -- ^ A list of the types of payment methods (e.g. card)
        --   this Checkout Session is allowed to accept. The only
        --   supported values today are `card` and `ideal`.
    paymentIntentData : Optional PostCheckoutSessionsPayloadPaymentIntentData
        -- ^ A subset of parameters to be passed to PaymentIntent
        --   creation for Checkout Sessions in `payment` mode.
      deriving (Eq, Ord, Show)

data PostPaymentMethodsPaymentMethodAttachPayload = PostPaymentMethodsPaymentMethodAttachPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    customer : Text
        -- ^ The ID of the customer to which to attach the
        --   PaymentMethod.
      deriving (Eq, Ord, Show)

data PostIssuingCardholdersCardholderPayloadAuthorizationControlsSpendingLimits = PostIssuingCardholdersCardholderPayloadAuthorizationControlsSpendingLimits with
      deriving (Eq, Ord, Show)

data PostIssuingCardholdersCardholderPayloadAuthorizationControls = PostIssuingCardholdersCardholderPayloadAuthorizationControls with
    spendingLimits : [PostIssuingCardholdersCardholderPayloadAuthorizationControlsSpendingLimits]
    spendingLimitsCurrency : Optional Text
    allowedCategories : [Text]
    blockedCategories : [Text]
      deriving (Eq, Ord, Show)

data PostIssuingCardholdersCardholderPayloadCompany = PostIssuingCardholdersCardholderPayloadCompany with
    taxId : Optional Text
      deriving (Eq, Ord, Show)

data PostIssuingCardholdersCardholderPayloadMetadata = PostIssuingCardholdersCardholderPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostIssuingCardholdersCardholderPayloadBillingAddress = PostIssuingCardholdersCardholderPayloadBillingAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Text
    city : Text
    line1 : Text
      deriving (Eq, Ord, Show)

data PostIssuingCardholdersCardholderPayloadBilling = PostIssuingCardholdersCardholderPayloadBilling with
    address : PostIssuingCardholdersCardholderPayloadBillingAddress
      deriving (Eq, Ord, Show)

data PostIssuingCardholdersCardholderPayloadIndividualDob = PostIssuingCardholdersCardholderPayloadIndividualDob with
    day : Int
    year : Int
    month : Int
      deriving (Eq, Ord, Show)

data PostIssuingCardholdersCardholderPayloadIndividualVerificationDocument = PostIssuingCardholdersCardholderPayloadIndividualVerificationDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostIssuingCardholdersCardholderPayloadIndividualVerification = PostIssuingCardholdersCardholderPayloadIndividualVerification with
    document : Optional PostIssuingCardholdersCardholderPayloadIndividualVerificationDocument
      deriving (Eq, Ord, Show)

data PostIssuingCardholdersCardholderPayloadIndividual = PostIssuingCardholdersCardholderPayloadIndividual with
    firstName : Optional Text
    dob : Optional PostIssuingCardholdersCardholderPayloadIndividualDob
    lastName : Optional Text
    verification : Optional PostIssuingCardholdersCardholderPayloadIndividualVerification
      deriving (Eq, Ord, Show)

data PostIssuingCardholdersCardholderPayload = PostIssuingCardholdersCardholderPayload with
    email : Optional Text
        -- ^ The cardholder's email address.
    status : Optional Text
        -- ^ Specifies whether to permit authorizations on this
        --   cardholder's cards.
    phoneNumber : Optional Text
        -- ^ The cardholder's phone number.
    authorizationControls : Optional PostIssuingCardholdersCardholderPayloadAuthorizationControls
        -- ^ Spending rules that give you some control over how
        --   your cards can be used. Refer to our
        --   [authorizations](https://stripe.com/docs/issuing/authorizations)
        --   documentation for more details.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    isDefault : Optional Bool
        -- ^ Specifies whether to set this as the default
        --   cardholder.
    company : Optional PostIssuingCardholdersCardholderPayloadCompany
        -- ^ Additional information about a `business_entity`
        --   cardholder.
    metadata : Optional PostIssuingCardholdersCardholderPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    billing : Optional PostIssuingCardholdersCardholderPayloadBilling
        -- ^ The cardholder's billing address.
    individual : Optional PostIssuingCardholdersCardholderPayloadIndividual
        -- ^ Additional information about an `individual`
        --   cardholder.
      deriving (Eq, Ord, Show)

data PostSkusIdPayloadAttributes = PostSkusIdPayloadAttributes with
      deriving (Eq, Ord, Show)

data PostSkusIdPayloadInventory = PostSkusIdPayloadInventory with
    quantity : Optional Int
    value : Optional Text
    type_ : Optional Text
      deriving (Eq, Ord, Show)

data PostSkusIdPayloadMetadata = PostSkusIdPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostSkusIdPayload = PostSkusIdPayload with
    image : Optional Text
        -- ^ The URL of an image for this SKU, meant to be
        --   displayable to the customer.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    packageDimensions : Optional Text
        -- ^ The dimensions of this SKU for shipping purposes.
    active : Optional Bool
        -- ^ Whether this SKU is available for purchase.
    currency : Optional Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    attributes : Optional PostSkusIdPayloadAttributes
        -- ^ A dictionary of attributes and values for the
        --   attributes defined by the product. When specified,
        --   `attributes` will partially update the existing
        --   attributes dictionary on the product, with the
        --   postcondition that a value must be present for each
        --   attribute key on the product.
    inventory : Optional PostSkusIdPayloadInventory
        -- ^ Description of the SKU's inventory.
    metadata : Optional PostSkusIdPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    price : Optional Int
        -- ^ The cost of the item as a positive integer in the
        --   smallest currency unit (that is, 100 cents to charge
        --   $1.00, or 100 to charge ¥100, Japanese Yen being a
        --   zero-decimal currency).
    product : Optional Text
        -- ^ The ID of the product that this SKU should belong to.
        --   The product must exist, have the same set of
        --   attribute names as the SKU's current product, and be
        --   of type `good`.
      deriving (Eq, Ord, Show)

data GetWebhookEndpointsResponseBody200Data = GetWebhookEndpointsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetWebhookEndpointsResponseBody200 = GetWebhookEndpointsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [WebhookEndpoint]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostWebhookEndpointsPayload = PostWebhookEndpointsPayload with
    apiVersion : Optional Text
        -- ^ Events sent to this endpoint will be generated with
        --   this Stripe Version instead of your account's default
        --   Stripe Version.
    url : Text
        -- ^ The URL of the webhook endpoint.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    connect : Optional Bool
        -- ^ Whether this endpoint should receive events from
        --   connected accounts (`true`), or from your account
        --   (`false`). Defaults to `false`.
    enabledEvents : [Text]
        -- ^ The list of events to enable for this endpoint. You
        --   may specify `['*']` to enable all events, except
        --   those that require explicit selection.
      deriving (Eq, Ord, Show)

data PostSourcesPayloadMandateAcceptanceOnline = PostSourcesPayloadMandateAcceptanceOnline with
    userAgent : Optional Text
    ip : Optional Text
    date : Optional Int
      deriving (Eq, Ord, Show)

data PostSourcesPayloadMandateAcceptanceOffline = PostSourcesPayloadMandateAcceptanceOffline with
    contactEmail : Optional Text
      deriving (Eq, Ord, Show)

data PostSourcesPayloadMandateAcceptance = PostSourcesPayloadMandateAcceptance with
    status : Text
    userAgent : Optional Text
    online : Optional PostSourcesPayloadMandateAcceptanceOnline
    ip : Optional Text
    offline : Optional PostSourcesPayloadMandateAcceptanceOffline
    date : Optional Int
    type_ : Optional Text
      deriving (Eq, Ord, Show)

data PostSourcesPayloadMandate = PostSourcesPayloadMandate with
    amount : Optional Text
    interval : Optional Text
    currency : Optional Text
    notificationMethod : Optional Text
    acceptance : Optional PostSourcesPayloadMandateAcceptance
      deriving (Eq, Ord, Show)

data PostSourcesPayloadOwnerAddress = PostSourcesPayloadOwnerAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostSourcesPayloadOwner = PostSourcesPayloadOwner with
    email : Optional Text
    phone : Optional Text
    address : Optional PostSourcesPayloadOwnerAddress
    name : Optional Text
      deriving (Eq, Ord, Show)

data PostSourcesPayloadReceiver = PostSourcesPayloadReceiver with
    refundAttributesMethod : Optional Text
      deriving (Eq, Ord, Show)

data PostSourcesPayloadSourceOrderItems = PostSourcesPayloadSourceOrderItems with
      deriving (Eq, Ord, Show)

data PostSourcesPayloadSourceOrderShippingAddress = PostSourcesPayloadSourceOrderShippingAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Text
      deriving (Eq, Ord, Show)

data PostSourcesPayloadSourceOrderShipping = PostSourcesPayloadSourceOrderShipping with
    carrier : Optional Text
    phone : Optional Text
    trackingNumber : Optional Text
    address : PostSourcesPayloadSourceOrderShippingAddress
    name : Optional Text
      deriving (Eq, Ord, Show)

data PostSourcesPayloadSourceOrder = PostSourcesPayloadSourceOrder with
    items : [PostSourcesPayloadSourceOrderItems]
    shipping : Optional PostSourcesPayloadSourceOrderShipping
      deriving (Eq, Ord, Show)

data PostSourcesPayloadRedirect = PostSourcesPayloadRedirect with
    returnUrl : Optional Text
      deriving (Eq, Ord, Show)

data PostSourcesPayloadMetadata = PostSourcesPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostSourcesPayload = PostSourcesPayload with
    amount : Optional Int
        -- ^ Amount associated with the source. This is the amount
        --   for which the source will be chargeable once ready.
        --   Required for `single_use` sources. Not supported for
        --   `receiver` type sources, where charge amount may not
        --   be specified until funds land.
    flow : Optional Text
        -- ^ The authentication `flow` of the source to create.
        --   `flow` is one of `redirect`, `receiver`,
        --   `code_verification`, `none`. It is generally inferred
        --   unless a type supports multiple flows.
    token : Optional Text
        -- ^ An optional token used to create the source. When
        --   passed, token properties will override source
        --   parameters.
    originalSource : Optional Text
        -- ^ The source to share.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    mandate : Optional PostSourcesPayloadMandate
        -- ^ Information about a mandate possibility attached to a
        --   source object (generally for bank debits) as well as
        --   its acceptance status.
    owner : Optional PostSourcesPayloadOwner
        -- ^ Information about the owner of the payment instrument
        --   that may be used or required by particular source
        --   types.
    receiver : Optional PostSourcesPayloadReceiver
        -- ^ Optional parameters for the receiver flow. Can be set
        --   only if the source is a receiver (`flow` is
        --   `receiver`).
    customer : Optional Text
        -- ^ The `Customer` to whom the original source is
        --   attached to. Must be set when the original source is
        --   not a `Source` (e.g., `Card`).
    sourceOrder : Optional PostSourcesPayloadSourceOrder
        -- ^ Information about the items and shipping associated
        --   with the source. Required for transactional credit
        --   (for example Klarna) sources before you can charge
        --   it.
    currency : Optional Text
        -- ^ Three-letter [ISO code for the
        --   currency](https://stripe.com/docs/currencies)
        --   associated with the source. This is the currency for
        --   which the source will be chargeable once ready.
    redirect : Optional PostSourcesPayloadRedirect
        -- ^ Parameters required for the redirect flow. Required
        --   if the source is authenticated by a redirect (`flow`
        --   is `redirect`).
    metadata : Optional PostSourcesPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    usage : Optional Text
        -- ^ Either `reusable` or `single_use`. Whether this
        --   source should be reusable or not. Some source types
        --   may or may not be reusable by construction, while
        --   others may leave the option at creation. If an
        --   incompatible value is passed, an error will be
        --   returned.
    type_ : Optional Text
        -- ^ The `type` of the source to create. Required unless
        --   `customer` and `original_source` are specified (see
        --   the [Cloning card
        --   Sources](https://stripe.com/docs/sources/connect#cloning-card-sources)
        --   guide)
    statementDescriptor : Optional Text
        -- ^ An arbitrary string to be displayed on your
        --   customer's statement. As an example, if your website
        --   is `RunClub` and the item you're charging for is a
        --   race ticket, you may want to specify a
        --   `statement_descriptor` of `RunClub 5K race ticket.`
        --   While many payment types will display this
        --   information, some may not display it at all.
      deriving (Eq, Ord, Show)

data PostAccountLoginLinksPayload = PostAccountLoginLinksPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    redirectUrl : Optional Text
        -- ^ Where to redirect the user after they log out of
        --   their dashboard.
    account : Text
      deriving (Eq, Ord, Show)

data PostAccountCapabilitiesCapabilityPayload = PostAccountCapabilitiesCapabilityPayload with
    requested : Optional Bool
        -- ^ Passing true requests the capability for the account,
        --   if it is not already requested. A requested
        --   capability may not immediately become active. Any
        --   requirements to activate the capability are returned
        --   in the `requirements` arrays.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
      deriving (Eq, Ord, Show)

data PostCustomersCustomerBankAccountsIdVerifyPayload = PostCustomersCustomerBankAccountsIdVerifyPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    amounts : [Int]
        -- ^ Two positive integers, in *cents*, equal to the
        --   values of the microdeposits sent to the bank account.
      deriving (Eq, Ord, Show)

data GetCreditNotesPreviewLinesResponseBody200Data = GetCreditNotesPreviewLinesResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetCreditNotesPreviewLinesResponseBody200 = GetCreditNotesPreviewLinesResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [CreditNoteLineItem]
        -- ^ Details about each object.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostInvoicesInvoiceFinalizePayload = PostInvoicesInvoiceFinalizePayload with
    autoAdvance : Optional Bool
        -- ^ Controls whether Stripe will perform [automatic
        --   collection](https://stripe.com/docs/billing/invoices/workflow/#auto_advance)
        --   of the invoice. When `false`, the invoice's state
        --   will not automatically advance without an explicit
        --   action.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
      deriving (Eq, Ord, Show)

data GetSkusResponseBody200Data = GetSkusResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetSkusResponseBody200 = GetSkusResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Sku]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostSkusPayloadPackageDimensions = PostSkusPayloadPackageDimensions with
    length : Decimal
    height : Decimal
    weight : Decimal
    width : Decimal
      deriving (Eq, Ord, Show)

data PostSkusPayloadAttributes = PostSkusPayloadAttributes with
      deriving (Eq, Ord, Show)

data PostSkusPayloadInventory = PostSkusPayloadInventory with
    quantity : Optional Int
    value : Optional Text
    type_ : Optional Text
      deriving (Eq, Ord, Show)

data PostSkusPayloadMetadata = PostSkusPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostSkusPayload = PostSkusPayload with
    image : Optional Text
        -- ^ The URL of an image for this SKU, meant to be
        --   displayable to the customer.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    packageDimensions : Optional PostSkusPayloadPackageDimensions
        -- ^ The dimensions of this SKU for shipping purposes.
    active : Optional Bool
        -- ^ Whether the SKU is available for purchase. Default to
        --   `true`.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    attributes : Optional PostSkusPayloadAttributes
        -- ^ A dictionary of attributes and values for the
        --   attributes defined by the product. If, for example, a
        --   product's attributes are `["size", "gender"]`, a
        --   valid SKU has the following dictionary of attributes:
        --   `{"size": "Medium", "gender": "Unisex"}`.
    inventory : PostSkusPayloadInventory
        -- ^ Description of the SKU's inventory.
    metadata : Optional PostSkusPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    id : Optional Text
        -- ^ The identifier for the SKU. Must be unique. If not
        --   provided, an identifier will be randomly generated.
    price : Int
        -- ^ The cost of the item as a nonnegative integer in the
        --   smallest currency unit (that is, 100 cents to charge
        --   $1.00, or 100 to charge ¥100, Japanese Yen being a
        --   zero-decimal currency).
    product : Text
        -- ^ The ID of the product this SKU is associated with.
        --   Must be a product with type `good`.
      deriving (Eq, Ord, Show)

data GetPayoutsResponseBody200Data = GetPayoutsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetPayoutsResponseBody200 = GetPayoutsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Payout]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostPayoutsPayloadMetadata = PostPayoutsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostPayoutsPayload = PostPayoutsPayload with
    destination : Optional Text
        -- ^ The ID of a bank account or a card to send the payout
        --   to. If no destination is supplied, the default
        --   external account for the specified currency will be
        --   used.
    amount : Int
        -- ^ A positive integer in cents representing how much to
        --   payout.
    sourceType : Optional Text
        -- ^ The balance type of your Stripe balance to draw this
        --   payout from. Balances for different payment sources
        --   are kept separately. You can find the amounts with
        --   the balances API. One of `bank_account`, `card`, or
        --   `fpx`.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    method : Optional Text
        -- ^ The method used to send this payout, which can be
        --   `standard` or `instant`. `instant` is only supported
        --   for payouts to debit cards. (See [Instant payouts for
        --   marketplaces for more
        --   information](https://stripe.com/blog/instant-payouts-for-marketplaces).)
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    metadata : Optional PostPayoutsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users.
    statementDescriptor : Optional Text
        -- ^ A string to be displayed on the recipient's bank or
        --   card statement. This may be at most 22 characters.
        --   Attempting to use a `statement_descriptor` longer
        --   than 22 characters will return an error. Note: Most
        --   banks will truncate this information and/or display
        --   it inconsistently. Some may not display it at all.
      deriving (Eq, Ord, Show)

data PostInvoicesInvoiceVoidPayload = PostInvoicesInvoiceVoidPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
      deriving (Eq, Ord, Show)

data GetTransfersIdReversalsResponseBody200Data = GetTransfersIdReversalsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetTransfersIdReversalsResponseBody200 = GetTransfersIdReversalsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [TransferReversal]
        -- ^ Details about each object.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostTransfersIdReversalsPayloadMetadata = PostTransfersIdReversalsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostTransfersIdReversalsPayload = PostTransfersIdReversalsPayload with
    amount : Optional Int
        -- ^ A positive integer in %s representing how much of
        --   this transfer to reverse. Can only reverse up to the
        --   unreversed amount remaining of the transfer. Partial
        --   transfer reversals are only allowed for transfers to
        --   Stripe Accounts. Defaults to the entire transfer
        --   amount.
    refundApplicationFee : Optional Bool
        -- ^ Boolean indicating whether the application fee should
        --   be refunded when reversing this transfer. If a full
        --   transfer reversal is given, the full application fee
        --   will be refunded. Otherwise, the application fee will
        --   be refunded with an amount proportional to the amount
        --   of the transfer reversed.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    metadata : Optional PostTransfersIdReversalsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    description : Optional Text
        -- ^ An arbitrary string which you can attach to a
        --   reversal object. It is displayed alongside the
        --   reversal in the Dashboard. This will be unset if you
        --   POST an empty value.
      deriving (Eq, Ord, Show)

data GetInvoicesUpcomingLinesResponseBody200Data = GetInvoicesUpcomingLinesResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetInvoicesUpcomingLinesResponseBody200 = GetInvoicesUpcomingLinesResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [LineItem]
        -- ^ Details about each object.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data GetCustomersResponseBody200Data = GetCustomersResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetCustomersResponseBody200 = GetCustomersResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Customer]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostCustomersPayloadTaxIdData = PostCustomersPayloadTaxIdData with
      deriving (Eq, Ord, Show)

data PostCustomersPayloadInvoiceSettings = PostCustomersPayloadInvoiceSettings with
    footer : Optional Text
    customFields : Optional Text
    defaultPaymentMethod : Optional Text
      deriving (Eq, Ord, Show)

data PostCustomersPayloadMetadata = PostCustomersPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostCustomersPayload = PostCustomersPayload with
    email : Optional Text
        -- ^ Customer's email address. It's displayed alongside
        --   the customer in your dashboard and can be useful for
        --   searching and tracking. This may be up to *512
        --   characters*.
    phone : Optional Text
        -- ^ The customer's phone number.
    paymentMethod : Optional Text
        -- ^ ID of the PaymentMethod to attach to the customer
    taxIdData : [PostCustomersPayloadTaxIdData]
        -- ^ The customer's tax IDs.
    coupon : Optional Text
        -- ^ The code of the coupon to apply to this subscription.
        --   A coupon applied to a subscription will only affect
        --   invoices created for that particular subscription.
    invoiceSettings : Optional PostCustomersPayloadInvoiceSettings
        -- ^ Default invoice settings for this customer.
    balance : Optional Int
        -- ^ An integer amount in %s that represents the
        --   customer's current balance, which affect the
        --   customer's future invoices. A negative amount
        --   represents a credit that decreases the amount due on
        --   an invoice; a positive amount increases the amount
        --   due on an invoice.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    address : Optional Text
        -- ^ The customer's address.
    name : Optional Text
        -- ^ The customer's full name or business name.
    shipping : Optional Text
        -- ^ The customer's shipping information. Appears on
        --   invoices emailed to this customer.
    metadata : Optional PostCustomersPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    source : Optional Text
        -- ^ The source can be a
        --   [Token](https://stripe.com/docs/api#tokens) or a
        --   [Source](https://stripe.com/docs/api#sources), as
        --   returned by
        --   [Elements](https://stripe.com/docs/elements). You
        --   must provide a source if the customer does not
        --   already have a valid source attached, and you are
        --   subscribing the customer to be charged automatically
        --   for a plan that is not free. Passing `source` will
        --   create a new source object, make it the customer
        --   default source, and delete the old customer default
        --   if one exists. If you want to add an additional
        --   source, instead use the [card creation
        --   API](https://stripe.com/docs/api#create_card) to add
        --   the card and then the [customer update
        --   API](https://stripe.com/docs/api#update_customer) to
        --   set it as the default. Whenever you attach a card to
        --   a customer, Stripe will automatically validate the
        --   card.
    taxExempt : Optional Text
        -- ^ The customer's tax exemption. One of `none`,
        --   `exempt`, or `reverse`.
    invoicePrefix : Optional Text
        -- ^ The prefix for the customer used to generate unique
        --   invoice numbers. Must be 3–12 uppercase letters or
        --   numbers.
    description : Optional Text
        -- ^ An arbitrary string that you can attach to a customer
        --   object. It is displayed alongside the customer in the
        --   dashboard.
    preferredLocales : [Text]
        -- ^ Customer's preferred languages, ordered by
        --   preference.
      deriving (Eq, Ord, Show)

data Post3dSecurePayload = Post3dSecurePayload with
    card : Optional Text
        -- ^ The ID of a card token, or the ID of a card belonging
        --   to the given customer.
    amount : Int
        -- ^ Amount of the charge that you will create when
        --   authentication completes.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    customer : Optional Text
        -- ^ The customer associated with this 3D secure
        --   authentication.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    returnUrl : Optional Text
        -- ^ The URL that the cardholder's browser will be
        --   returned to when authentication completes.
      deriving (Eq, Ord, Show)

data PostAccountLinksPayload = PostAccountLinksPayload with
    successUrl : Optional Text
        -- ^ The URL that the user will be redirected to upon
        --   leaving or completing the linked flow successfully.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    collect : Optional Text
        -- ^ Which information the platform needs to collect from
        --   the user. One of `currently_due` or `eventually_due`.
        --   Default is `currently_due`.
    account : Text
        -- ^ The identifier of the account to create an account
        --   link for.
    failureUrl : Optional Text
        -- ^ The URL that the user will be redirected to if the
        --   account link is no longer valid.
    type_ : Optional Text
        -- ^ The type of account link the user is requesting.
        --   Possible values are `custom_account_verification` or
        --   `custom_account_update`.
      deriving (Eq, Ord, Show)

data PostTerminalReadersReaderPayloadMetadata = PostTerminalReadersReaderPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostTerminalReadersReaderPayload = PostTerminalReadersReaderPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    metadata : Optional PostTerminalReadersReaderPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    label_ : Optional Text
        -- ^ The new label of the reader.
      deriving (Eq, Ord, Show)

data PostTopupsTopupPayloadMetadata = PostTopupsTopupPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostTopupsTopupPayload = PostTopupsTopupPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    metadata : Optional PostTopupsTopupPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users.
      deriving (Eq, Ord, Show)

data GetCouponsResponseBody200Data = GetCouponsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetCouponsResponseBody200 = GetCouponsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Coupon]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostCouponsPayloadMetadata = PostCouponsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostCouponsPayload = PostCouponsPayload with
    maxRedemptions : Optional Int
        -- ^ A positive integer specifying the number of times the
        --   coupon can be redeemed before it's no longer valid.
        --   For example, you might have a 50% off coupon that the
        --   first 20 readers of your blog can use.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    amountOff : Optional Int
        -- ^ A positive integer representing the amount to
        --   subtract from an invoice total (required if
        --   `percent_off` is not passed).
    name : Optional Text
        -- ^ Name of the coupon displayed to customers on, for
        --   instance invoices, or receipts. By default the `id`
        --   is shown if `name` is not set.
    currency : Optional Text
        -- ^ Three-letter [ISO code for the
        --   currency](https://stripe.com/docs/currencies) of the
        --   `amount_off` parameter (required if `amount_off` is
        --   passed).
    durationInMonths : Optional Int
        -- ^ Required only if `duration` is `repeating`, in which
        --   case it must be a positive integer that specifies the
        --   number of months the discount will be in effect.
    redeemBy : Optional Int
        -- ^ Unix timestamp specifying the last time at which the
        --   coupon can be redeemed. After the redeem_by date, the
        --   coupon can no longer be applied to new customers.
    metadata : Optional PostCouponsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    id : Optional Text
        -- ^ Unique string of your choice that will be used to
        --   identify this coupon when applying it to a customer.
        --   This is often a specific code you'll give to your
        --   customer to use when signing up (e.g., `FALL25OFF`).
        --   If you don't want to specify a particular code, you
        --   can leave the ID blank and we'll generate a random
        --   code for you.
    percentOff : Optional Decimal
        -- ^ A positive float larger than 0, and smaller or equal
        --   to 100, that represents the discount the coupon will
        --   apply (required if `amount_off` is not passed).
    duration : Text
        -- ^ Specifies how long the discount will be in effect.
        --   Can be `forever`, `once`, or `repeating`.
      deriving (Eq, Ord, Show)

data PostDisputesDisputePayloadMetadata = PostDisputesDisputePayloadMetadata with
      deriving (Eq, Ord, Show)

data PostDisputesDisputePayloadEvidence = PostDisputesDisputePayloadEvidence with
    receipt : Optional Text
    shippingDocumentation : Optional Text
    duplicateChargeDocumentation : Optional Text
    refundRefusalExplanation : Optional Text
    billingAddress : Optional Text
    uncategorizedText : Optional Text
    accessActivityLog : Optional Text
    cancellationRebuttal : Optional Text
    cancellationPolicy : Optional Text
    cancellationPolicyDisclosure : Optional Text
    shippingAddress : Optional Text
    shippingTrackingNumber : Optional Text
    serviceDate : Optional Text
    duplicateChargeExplanation : Optional Text
    uncategorizedFile : Optional Text
    refundPolicyDisclosure : Optional Text
    customerName : Optional Text
    customerSignature : Optional Text
    productDescription : Optional Text
    serviceDocumentation : Optional Text
    duplicateChargeId : Optional Text
    customerCommunication : Optional Text
    customerPurchaseIp : Optional Text
    customerEmailAddress : Optional Text
    refundPolicy : Optional Text
    shippingDate : Optional Text
    shippingCarrier : Optional Text
      deriving (Eq, Ord, Show)

data PostDisputesDisputePayload = PostDisputesDisputePayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    submit : Optional Bool
        -- ^ Whether to immediately submit evidence to the bank.
        --   If `false`, evidence is staged on the dispute. Staged
        --   evidence is visible in the API and Dashboard, and can
        --   be submitted to the bank by making another request
        --   with this attribute set to `true` (the default).
    metadata : Optional PostDisputesDisputePayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    evidence : Optional PostDisputesDisputePayloadEvidence
        -- ^ Evidence to upload, to respond to a dispute. Updating
        --   any field in the hash will submit all fields in the
        --   hash for review. The combined character count of all
        --   fields is limited to 150,000.
      deriving (Eq, Ord, Show)

data GetDisputesResponseBody200Data = GetDisputesResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetDisputesResponseBody200 = GetDisputesResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Dispute]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostAccountsAccountBankAccountsIdPayloadMetadata = PostAccountsAccountBankAccountsIdPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostAccountsAccountBankAccountsIdPayload = PostAccountsAccountBankAccountsIdPayload with
    defaultForCurrency : Optional Bool
        -- ^ When set to true, this becomes the default external
        --   account for its currency.
    addressCity : Optional Text
        -- ^ City/District/Suburb/Town/Village.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    addressState : Optional Text
        -- ^ State/County/Province/Region.
    accountHolderType : Optional Text
        -- ^ The type of entity that holds the account. This can
        --   be either `individual` or `company`.
    name : Optional Text
        -- ^ Cardholder name.
    expMonth : Optional Text
        -- ^ Two digit number representing the card’s expiration
        --   month.
    addressZip : Optional Text
        -- ^ ZIP or postal code.
    metadata : Optional PostAccountsAccountBankAccountsIdPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    expYear : Optional Text
        -- ^ Four digit number representing the card’s expiration
        --   year.
    addressLine2 : Optional Text
        -- ^ Address line 2 (Apartment/Suite/Unit/Building).
    addressLine1 : Optional Text
        -- ^ Address line 1 (Street address/PO Box/Company name).
    accountHolderName : Optional Text
        -- ^ The name of the person or business that owns the bank
        --   account.
    addressCountry : Optional Text
        -- ^ Billing address country, if provided when creating
        --   card.
      deriving (Eq, Ord, Show)

data GetSigmaScheduledQueryRunsResponseBody200Data = GetSigmaScheduledQueryRunsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetSigmaScheduledQueryRunsResponseBody200 = GetSigmaScheduledQueryRunsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [ScheduledQueryRun]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostOrdersIdPayPayloadMetadata = PostOrdersIdPayPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostOrdersIdPayPayload = PostOrdersIdPayPayload with
    email : Optional Text
        -- ^ The email address of the customer placing the order.
        --   Required if not previously specified for the order.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    customer : Optional Text
        -- ^ The ID of an existing customer that will be charged
        --   for this order. If no customer was attached to the
        --   order at creation, either `source` or `customer` is
        --   required. Otherwise, the specified customer will be
        --   charged instead of the one attached to the order.
    metadata : Optional PostOrdersIdPayPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    source : Optional Text
        -- ^ A [Token](https://stripe.com/docs/api#tokens)'s or a
        --   [Source](https://stripe.com/docs/api#sources)'s ID,
        --   as returned by
        --   [Elements](https://stripe.com/docs/elements). If no
        --   customer was attached to the order at creation,
        --   either `source` or `customer` is required. Otherwise,
        --   the specified source will be charged intead of the
        --   customer attached to the order.
    applicationFee : Optional Int
        -- ^ A fee in %s that will be applied to the order and
        --   transferred to the application owner's Stripe
        --   account. The request must be made with an OAuth key
        --   or the `Stripe-Account` header in order to take an
        --   application fee. For more information, see the
        --   application fees
        --   [documentation](https://stripe.com/docs/connect/direct-charges#collecting-fees).
      deriving (Eq, Ord, Show)

data GetSubscriptionsResponseBody200Data = GetSubscriptionsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetSubscriptionsResponseBody200 = GetSubscriptionsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Subscription]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostSubscriptionsPayloadItems = PostSubscriptionsPayloadItems with
      deriving (Eq, Ord, Show)

data PostSubscriptionsPayloadMetadata = PostSubscriptionsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostSubscriptionsPayload = PostSubscriptionsPayload with
    trialFromPlan : Optional Bool
        -- ^ Indicates if a plan's `trial_period_days` should be
        --   applied to the subscription. Setting `trial_end` per
        --   subscription is preferred, and this defaults to
        --   `false`. Setting this flag to `true` together with
        --   `trial_end` is not allowed.
    trialPeriodDays : Optional Int
        -- ^ Integer representing the number of trial period days
        --   before the customer is charged for the first time.
        --   This will always overwrite any trials that might
        --   apply via a subscribed plan.
    coupon : Optional Text
        -- ^ The code of the coupon to apply to this subscription.
        --   A coupon applied to a subscription will only affect
        --   invoices created for that particular subscription.
    billingThresholds : Optional Text
        -- ^ Define thresholds at which an invoice will be sent,
        --   and the subscription advanced to a new billing
        --   period. Pass an empty string to remove
        --   previously-defined thresholds.
    pendingInvoiceItemInterval : Optional Text
        -- ^ Specifies an interval for how often to bill for any
        --   pending invoice items. It is analogous to calling
        --   [Create an
        --   invoice](https://stripe.com/docs/api#create_invoice)
        --   for the given subscription at the specified interval.
    backdateStartDate : Optional Int
        -- ^ For new subscriptions, a past timestamp to backdate
        --   the subscription's start date to. If set, the first
        --   invoice will contain a proration for the timespan
        --   between the start date and the current time. Can be
        --   combined with trials and the billing cycle anchor.
    cancelAtPeriodEnd : Optional Bool
        -- ^ Boolean indicating whether this subscription should
        --   cancel at the end of the current period.
    items : [PostSubscriptionsPayloadItems]
        -- ^ A list of up to 20 subscription items, each with an
        --   attached plan.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    offSession : Optional Bool
        -- ^ Indicates if a customer is on or off-session while an
        --   invoice payment is attempted.
    collectionMethod : Optional Text
        -- ^ Either `charge_automatically`, or `send_invoice`.
        --   When charging automatically, Stripe will attempt to
        --   pay this subscription at the end of the cycle using
        --   the default source attached to the customer. When
        --   sending an invoice, Stripe will email your customer
        --   an invoice with payment instructions. Defaults to
        --   `charge_automatically`.
    paymentBehavior : Optional Text
        -- ^ Use `allow_incomplete` to create subscriptions with
        --   `status=incomplete` if the first invoice cannot be
        --   paid. Creating subscriptions with this status allows
        --   you to manage scenarios where additional user actions
        --   are needed to pay a subscription's invoice. For
        --   example, SCA regulation may require 3DS
        --   authentication to complete payment. See the [SCA
        --   Migration
        --   Guide](https://stripe.com/docs/billing/migration/strong-customer-authentication)
        --   for Billing to learn more. This is the default
        --   behavior. Use `error_if_incomplete` if you want
        --   Stripe to return an HTTP 402 status code if a
        --   subscription's first invoice cannot be paid. For
        --   example, if a payment method requires 3DS
        --   authentication due to SCA regulation and further user
        --   action is needed, this parameter does not create a
        --   subscription and returns an error instead. This was
        --   the default behavior for API versions prior to
        --   2019-03-14. See the
        --   [changelog](https://stripe.com/docs/upgrades#2019-03-14)
        --   to learn more.
    prorationBehavior : Optional Text
        -- ^ Determines how to handle
        --   [prorations](https://stripe.com/docs/subscriptions/billing-cycle#prorations)
        --   resulting from the `billing_cycle_anchor`. Valid
        --   values are `create_prorations` or `none`. Passing
        --   `create_prorations` will cause proration invoice
        --   items to be created when applicable. Prorations can
        --   be disabled by passing `none`. If no value is passed,
        --   the default is `create_prorations`.
    customer : Text
        -- ^ The identifier of the customer to subscribe.
    cancelAt : Optional Int
        -- ^ A timestamp at which the subscription should cancel.
        --   If set to a date before the current period ends, this
        --   will cause a proration if prorations have been
        --   enabled using `proration_behavior`. If set during a
        --   future period, this will always cause a proration for
        --   that period.
    prorate : Optional Bool
        -- ^ This field has been renamed to `proration_behavior`.
        --   `prorate=true` can be replaced with
        --   `proration_behavior=create_prorations` and
        --   `prorate=false` can be replaced with
        --   `proration_behavior=none`.
    defaultTaxRates : Optional Text
        -- ^ The tax rates that will apply to any subscription
        --   item that does not have `tax_rates` set. Invoices
        --   created will have their `default_tax_rates` populated
        --   from the subscription.
    applicationFeePercent : Optional Decimal
        -- ^ A non-negative decimal between 0 and 100, with at
        --   most two decimal places. This represents the
        --   percentage of the subscription invoice subtotal that
        --   will be transferred to the application owner's Stripe
        --   account. The request must be made by a platform
        --   account on a connected account in order to set an
        --   application fee percentage. For more information, see
        --   the application fees
        --   [documentation](https://stripe.com/docs/connect/subscriptions#collecting-fees-on-subscriptions).
    trialEnd : Optional Text
        -- ^ Unix timestamp representing the end of the trial
        --   period the customer will get before being charged for
        --   the first time. This will always overwrite any trials
        --   that might apply via a subscribed plan. If set,
        --   trial_end will override the default trial period of
        --   the plan the customer is being subscribed to. The
        --   special value `now` can be provided to end the
        --   customer's trial immediately. Can be at most two
        --   years from `billing_cycle_anchor`.
    billingCycleAnchor : Optional Int
        -- ^ A future timestamp to anchor the subscription's
        --   [billing
        --   cycle](https://stripe.com/docs/subscriptions/billing-cycle).
        --   This is used to determine the date of the first full
        --   invoice, and, for plans with `month` or `year`
        --   intervals, the day of the month for subsequent
        --   invoices.
    metadata : Optional PostSubscriptionsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    defaultPaymentMethod : Optional Text
        -- ^ ID of the default payment method for the
        --   subscription. It must belong to the customer
        --   associated with the subscription. If not set,
        --   invoices will use the default payment method in the
        --   customer's invoice settings.
    defaultSource : Optional Text
        -- ^ ID of the default payment source for the
        --   subscription. It must belong to the customer
        --   associated with the subscription and be in a
        --   chargeable state. If not set, defaults to the
        --   customer's default source.
    daysUntilDue : Optional Int
        -- ^ Number of days a customer has to pay invoices
        --   generated by this subscription. Valid only for
        --   subscriptions where `collection_method` is set to
        --   `send_invoice`.
    taxPercent : Optional Text
        -- ^ A non-negative decimal (with at most four decimal
        --   places) between 0 and 100. This represents the
        --   percentage of the subscription invoice subtotal that
        --   will be calculated and added as tax to the final
        --   amount in each billing period. For example, a plan
        --   which charges $10/month with a `tax_percent` of
        --   `20.0` will charge $12 per invoice. To unset a
        --   previously-set value, pass an empty string. This
        --   field has been deprecated and will be removed in a
        --   future API version, for further information view the
        --   [migration
        --   docs](https://stripe.com/docs/billing/migration/taxes)
        --   for `tax_rates`.
      deriving (Eq, Ord, Show)

data GetInvoiceitemsResponseBody200Data = GetInvoiceitemsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetInvoiceitemsResponseBody200 = GetInvoiceitemsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Invoiceitem]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostInvoiceitemsPayloadPeriod = PostInvoiceitemsPayloadPeriod with
    start : Int
    end : Int
      deriving (Eq, Ord, Show)

data PostInvoiceitemsPayloadMetadata = PostInvoiceitemsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostInvoiceitemsPayload = PostInvoiceitemsPayload with
    amount : Optional Int
        -- ^ The integer amount in **%s** of the charge to be
        --   applied to the upcoming invoice. Passing in a
        --   negative `amount` will reduce the `amount_due` on the
        --   invoice.
    period : Optional PostInvoiceitemsPayloadPeriod
        -- ^ The period associated with this invoice item.
    quantity : Optional Int
        -- ^ Non-negative integer. The quantity of units for the
        --   invoice item.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    invoice : Optional Text
        -- ^ The ID of an existing invoice to add this invoice
        --   item to. When left blank, the invoice item will be
        --   added to the next upcoming scheduled invoice. This is
        --   useful when adding invoice items in response to an
        --   invoice.created webhook. You can only add invoice
        --   items to draft invoices.
    unitAmountDecimal : Optional Text
        -- ^ Same as `unit_amount`, but accepts a decimal value
        --   with at most 12 decimal places. Only one of
        --   `unit_amount` and `unit_amount_decimal` can be set.
    customer : Text
        -- ^ The ID of the customer who will be billed when this
        --   invoice item is billed.
    discountable : Optional Bool
        -- ^ Controls whether discounts apply to this invoice
        --   item. Defaults to false for prorations or negative
        --   invoice items, and true for all other invoice items.
    currency : Optional Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    metadata : Optional PostInvoiceitemsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    unitAmount : Optional Int
        -- ^ The integer unit amount in **%s** of the charge to be
        --   applied to the upcoming invoice. This `unit_amount`
        --   will be multiplied by the quantity to get the full
        --   amount. Passing in a negative `unit_amount` will
        --   reduce the `amount_due` on the invoice.
    taxRates : [Text]
        -- ^ The tax rates which apply to the invoice item. When
        --   set, the `default_tax_rates` on the invoice do not
        --   apply to this invoice item.
    subscription : Optional Text
        -- ^ The ID of a subscription to add this invoice item to.
        --   When left blank, the invoice item will be be added to
        --   the next upcoming scheduled invoice. When set,
        --   scheduled invoices for subscriptions other than the
        --   specified subscription will ignore the invoice item.
        --   Use this when you want to express that an invoice
        --   item has been accrued within the context of a
        --   particular subscription.
    description : Optional Text
        -- ^ An arbitrary string which you can attach to the
        --   invoice item. The description is displayed in the
        --   invoice for easy tracking.
      deriving (Eq, Ord, Show)

data PostRadarValueListsValueListPayloadMetadata = PostRadarValueListsValueListPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostRadarValueListsValueListPayload = PostRadarValueListsValueListPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    alias : Optional Text
        -- ^ The name of the value list for use in rules.
    name : Optional Text
        -- ^ The human-readable name of the value list.
    metadata : Optional PostRadarValueListsValueListPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data GetInvoicesResponseBody200Data = GetInvoicesResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetInvoicesResponseBody200 = GetInvoicesResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Invoice]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostInvoicesPayloadMetadata = PostInvoicesPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostInvoicesPayload = PostInvoicesPayload with
    dueDate : Optional Int
        -- ^ The date on which payment for this invoice is due.
        --   Valid only for invoices where
        --   `collection_method=send_invoice`.
    autoAdvance : Optional Bool
        -- ^ Controls whether Stripe will perform [automatic
        --   collection](https://stripe.com/docs/billing/invoices/workflow/#auto_advance)
        --   of the invoice. When `false`, the invoice's state
        --   will not automatically advance without an explicit
        --   action.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    collectionMethod : Optional Text
        -- ^ Either `charge_automatically`, or `send_invoice`.
        --   When charging automatically, Stripe will attempt to
        --   pay this invoice using the default source attached to
        --   the customer. When sending an invoice, Stripe will
        --   email this invoice to the customer with payment
        --   instructions. Defaults to `charge_automatically`.
    customer : Text
        -- ^ The ID of the customer who will be billed.
    defaultTaxRates : [Text]
        -- ^ The tax rates that will apply to any line item that
        --   does not have `tax_rates` set.
    metadata : Optional PostInvoicesPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    footer : Optional Text
        -- ^ Footer to be displayed on the invoice.
    customFields : Optional Text
        -- ^ A list of up to 4 custom fields to be displayed on
        --   the invoice.
    applicationFeeAmount : Optional Int
        -- ^ A fee in %s that will be applied to the invoice and
        --   transferred to the application owner's Stripe
        --   account. The request must be made with an OAuth key
        --   or the Stripe-Account header in order to take an
        --   application fee. For more information, see the
        --   application fees
        --   [documentation](https://stripe.com/docs/connect/subscriptions#invoices).
    subscription : Optional Text
        -- ^ The ID of the subscription to invoice, if any. If not
        --   set, the created invoice will include all pending
        --   invoice items for the customer. If set, the created
        --   invoice will only include pending invoice items for
        --   that subscription and pending invoice items not
        --   associated with any subscription. The subscription's
        --   billing cycle and regular subscription events won't
        --   be affected.
    defaultPaymentMethod : Optional Text
        -- ^ ID of the default payment method for the invoice. It
        --   must belong to the customer associated with the
        --   invoice. If not set, defaults to the subscription's
        --   default payment method, if any, or to the default
        --   payment method in the customer's invoice settings.
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users. Referenced as 'memo'
        --   in the Dashboard.
    statementDescriptor : Optional Text
        -- ^ Extra information about a charge for the customer's
        --   credit card statement. It must contain at least one
        --   letter. If not specified and this invoice is part of
        --   a subscription, the default `statement_descriptor`
        --   will be set to the first subscription item's
        --   product's `statement_descriptor`.
    defaultSource : Optional Text
        -- ^ ID of the default payment source for the invoice. It
        --   must belong to the customer associated with the
        --   invoice and be in a chargeable state. If not set,
        --   defaults to the subscription's default source, if
        --   any, or to the customer's default source.
    daysUntilDue : Optional Int
        -- ^ The number of days from when the invoice is created
        --   until it is due. Valid only for invoices where
        --   `collection_method=send_invoice`.
    taxPercent : Optional Decimal
        -- ^ The percent tax rate applied to the invoice,
        --   represented as a decimal number. This field has been
        --   deprecated and will be removed in a future API
        --   version, for further information view the [migration
        --   docs](https://stripe.com/docs/billing/migration/taxes)
        --   for `tax_rates`.
      deriving (Eq, Ord, Show)

data PostSourcesSourcePayloadMandateAcceptanceOnline = PostSourcesSourcePayloadMandateAcceptanceOnline with
    userAgent : Optional Text
    ip : Optional Text
    date : Optional Int
      deriving (Eq, Ord, Show)

data PostSourcesSourcePayloadMandateAcceptanceOffline = PostSourcesSourcePayloadMandateAcceptanceOffline with
    contactEmail : Optional Text
      deriving (Eq, Ord, Show)

data PostSourcesSourcePayloadMandateAcceptance = PostSourcesSourcePayloadMandateAcceptance with
    status : Text
    userAgent : Optional Text
    online : Optional PostSourcesSourcePayloadMandateAcceptanceOnline
    ip : Optional Text
    offline : Optional PostSourcesSourcePayloadMandateAcceptanceOffline
    date : Optional Int
    type_ : Optional Text
      deriving (Eq, Ord, Show)

data PostSourcesSourcePayloadMandate = PostSourcesSourcePayloadMandate with
    amount : Optional Text
    interval : Optional Text
    currency : Optional Text
    notificationMethod : Optional Text
    acceptance : Optional PostSourcesSourcePayloadMandateAcceptance
      deriving (Eq, Ord, Show)

data PostSourcesSourcePayloadOwnerAddress = PostSourcesSourcePayloadOwnerAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostSourcesSourcePayloadOwner = PostSourcesSourcePayloadOwner with
    email : Optional Text
    phone : Optional Text
    address : Optional PostSourcesSourcePayloadOwnerAddress
    name : Optional Text
      deriving (Eq, Ord, Show)

data PostSourcesSourcePayloadSourceOrderItems = PostSourcesSourcePayloadSourceOrderItems with
      deriving (Eq, Ord, Show)

data PostSourcesSourcePayloadSourceOrderShippingAddress = PostSourcesSourcePayloadSourceOrderShippingAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Text
      deriving (Eq, Ord, Show)

data PostSourcesSourcePayloadSourceOrderShipping = PostSourcesSourcePayloadSourceOrderShipping with
    carrier : Optional Text
    phone : Optional Text
    trackingNumber : Optional Text
    address : PostSourcesSourcePayloadSourceOrderShippingAddress
    name : Optional Text
      deriving (Eq, Ord, Show)

data PostSourcesSourcePayloadSourceOrder = PostSourcesSourcePayloadSourceOrder with
    items : [PostSourcesSourcePayloadSourceOrderItems]
    shipping : Optional PostSourcesSourcePayloadSourceOrderShipping
      deriving (Eq, Ord, Show)

data PostSourcesSourcePayloadMetadata = PostSourcesSourcePayloadMetadata with
      deriving (Eq, Ord, Show)

data PostSourcesSourcePayload = PostSourcesSourcePayload with
    amount : Optional Int
        -- ^ Amount associated with the source.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    mandate : Optional PostSourcesSourcePayloadMandate
        -- ^ Information about a mandate possibility attached to a
        --   source object (generally for bank debits) as well as
        --   its acceptance status.
    owner : Optional PostSourcesSourcePayloadOwner
        -- ^ Information about the owner of the payment instrument
        --   that may be used or required by particular source
        --   types.
    sourceOrder : Optional PostSourcesSourcePayloadSourceOrder
        -- ^ Information about the items and shipping associated
        --   with the source. Required for transactional credit
        --   (for example Klarna) sources before you can charge
        --   it.
    metadata : Optional PostSourcesSourcePayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data PostAccountsAccountExternalAccountsIdPayloadMetadata = PostAccountsAccountExternalAccountsIdPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostAccountsAccountExternalAccountsIdPayload = PostAccountsAccountExternalAccountsIdPayload with
    defaultForCurrency : Optional Bool
        -- ^ When set to true, this becomes the default external
        --   account for its currency.
    addressCity : Optional Text
        -- ^ City/District/Suburb/Town/Village.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    addressState : Optional Text
        -- ^ State/County/Province/Region.
    accountHolderType : Optional Text
        -- ^ The type of entity that holds the account. This can
        --   be either `individual` or `company`.
    name : Optional Text
        -- ^ Cardholder name.
    expMonth : Optional Text
        -- ^ Two digit number representing the card’s expiration
        --   month.
    addressZip : Optional Text
        -- ^ ZIP or postal code.
    metadata : Optional PostAccountsAccountExternalAccountsIdPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    expYear : Optional Text
        -- ^ Four digit number representing the card’s expiration
        --   year.
    addressLine2 : Optional Text
        -- ^ Address line 2 (Apartment/Suite/Unit/Building).
    addressLine1 : Optional Text
        -- ^ Address line 1 (Street address/PO Box/Company name).
    accountHolderName : Optional Text
        -- ^ The name of the person or business that owns the bank
        --   account.
    addressCountry : Optional Text
        -- ^ Billing address country, if provided when creating
        --   card.
      deriving (Eq, Ord, Show)

data PostChargesChargeRefundPayloadMetadata = PostChargesChargeRefundPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostChargesChargeRefundPayload = PostChargesChargeRefundPayload with
    amount : Optional Int
    refundApplicationFee : Optional Bool
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    paymentIntent : Optional Text
    reason : Optional Text
    reverseTransfer : Optional Bool
    metadata : Optional PostChargesChargeRefundPayloadMetadata
      deriving (Eq, Ord, Show)

data PostCreditNotesIdVoidPayload = PostCreditNotesIdVoidPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
      deriving (Eq, Ord, Show)

data PostOrdersIdReturnsPayload = PostOrdersIdReturnsPayload with
    items : Optional Text
        -- ^ List of items to return.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
      deriving (Eq, Ord, Show)

data PostWebhookEndpointsWebhookEndpointPayload = PostWebhookEndpointsWebhookEndpointPayload with
    disabled : Optional Bool
        -- ^ Disable the webhook endpoint if set to true.
    url : Optional Text
        -- ^ The URL of the webhook endpoint.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    enabledEvents : [Text]
        -- ^ The list of events to enable for this endpoint. You
        --   may specify `['*']` to enable all events, except
        --   those that require explicit selection.
      deriving (Eq, Ord, Show)

data PostIssuingVerificationsPayload = PostIssuingVerificationsPayload with
    card : Text
        -- ^ The `Card` for which a verification is requested
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    verificationMethod : Optional Text
        -- ^ The method used to send the cardholder the
        --   verification (one of `email` or `sms`)
    scope : Text
        -- ^ The scope of the verification (one of
        --   `card_pin_retrieve` or `card_pin_update`)
      deriving (Eq, Ord, Show)

data PostOrdersIdPayloadShipping = PostOrdersIdPayloadShipping with
    carrier : Text
    trackingNumber : Optional Text
      deriving (Eq, Ord, Show)

data PostOrdersIdPayloadMetadata = PostOrdersIdPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostOrdersIdPayload = PostOrdersIdPayload with
    status : Optional Text
        -- ^ Current order status. One of `created`, `paid`,
        --   `canceled`, `fulfilled`, or `returned`. More detail
        --   in the [Orders
        --   Guide](https://stripe.com/docs/orders/guide#understanding-order-statuses).
    coupon : Optional Text
        -- ^ A coupon code that represents a discount to be
        --   applied to this order. Must be one-time duration and
        --   in same currency as the order. An order can have
        --   multiple coupons.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    selectedShippingMethod : Optional Text
        -- ^ The shipping method to select for fulfilling this
        --   order. If specified, must be one of the `id`s of a
        --   shipping method in the `shipping_methods` array. If
        --   specified, will overwrite the existing selected
        --   shipping method, updating `items` as necessary.
    shipping : Optional PostOrdersIdPayloadShipping
        -- ^ Tracking information once the order has been
        --   fulfilled.
    metadata : Optional PostOrdersIdPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data GetOrdersResponseBody200Data = GetOrdersResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetOrdersResponseBody200 = GetOrdersResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Order]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostOrdersPayloadItems = PostOrdersPayloadItems with
      deriving (Eq, Ord, Show)

data PostOrdersPayloadShippingAddress = PostOrdersPayloadShippingAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Text
      deriving (Eq, Ord, Show)

data PostOrdersPayloadShipping = PostOrdersPayloadShipping with
    phone : Optional Text
    address : PostOrdersPayloadShippingAddress
    name : Text
      deriving (Eq, Ord, Show)

data PostOrdersPayloadMetadata = PostOrdersPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostOrdersPayload = PostOrdersPayload with
    email : Optional Text
        -- ^ The email address of the customer placing the order.
    coupon : Optional Text
        -- ^ A coupon code that represents a discount to be
        --   applied to this order. Must be one-time duration and
        --   in same currency as the order. An order can have
        --   multiple coupons.
    items : [PostOrdersPayloadItems]
        -- ^ List of items constituting the order. An order can
        --   have up to 25 items.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    customer : Optional Text
        -- ^ The ID of an existing customer to use for this order.
        --   If provided, the customer email and shipping address
        --   will be used to create the order. Subsequently, the
        --   customer will also be charged to pay the order. If
        --   `email` or `shipping` are also provided, they will
        --   override the values retrieved from the customer
        --   object.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    shipping : Optional PostOrdersPayloadShipping
        -- ^ Shipping address for the order. Required if any of
        --   the SKUs are for products that have `shippable` set
        --   to true.
    metadata : Optional PostOrdersPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data PostTransfersTransferReversalsIdPayloadMetadata = PostTransfersTransferReversalsIdPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostTransfersTransferReversalsIdPayload = PostTransfersTransferReversalsIdPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    metadata : Optional PostTransfersTransferReversalsIdPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data PostApplicationFeesIdRefundPayload = PostApplicationFeesIdRefundPayload with
    amount : Optional Int
    directive : Optional Text
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
      deriving (Eq, Ord, Show)

data GetExchangeRatesResponseBody200Data = GetExchangeRatesResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetExchangeRatesResponseBody200 = GetExchangeRatesResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [ExchangeRate]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data GetApplicationFeesIdRefundsResponseBody200Data = GetApplicationFeesIdRefundsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetApplicationFeesIdRefundsResponseBody200 = GetApplicationFeesIdRefundsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [FeeRefund]
        -- ^ Details about each object.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostApplicationFeesIdRefundsPayloadMetadata = PostApplicationFeesIdRefundsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostApplicationFeesIdRefundsPayload = PostApplicationFeesIdRefundsPayload with
    amount : Optional Int
        -- ^ A positive integer, in _%s_, representing how much of
        --   this fee to refund. Can refund only up to the
        --   remaining unrefunded amount of the fee.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    metadata : Optional PostApplicationFeesIdRefundsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data GetIssuingCardholdersResponseBody200Data = GetIssuingCardholdersResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetIssuingCardholdersResponseBody200 = GetIssuingCardholdersResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [IssuingCardholder]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostIssuingCardholdersPayloadAuthorizationControlsSpendingLimits = PostIssuingCardholdersPayloadAuthorizationControlsSpendingLimits with
      deriving (Eq, Ord, Show)

data PostIssuingCardholdersPayloadAuthorizationControls = PostIssuingCardholdersPayloadAuthorizationControls with
    spendingLimits : [PostIssuingCardholdersPayloadAuthorizationControlsSpendingLimits]
    spendingLimitsCurrency : Optional Text
    allowedCategories : [Text]
    blockedCategories : [Text]
      deriving (Eq, Ord, Show)

data PostIssuingCardholdersPayloadCompany = PostIssuingCardholdersPayloadCompany with
    taxId : Optional Text
      deriving (Eq, Ord, Show)

data PostIssuingCardholdersPayloadMetadata = PostIssuingCardholdersPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostIssuingCardholdersPayloadBillingAddress = PostIssuingCardholdersPayloadBillingAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Text
    city : Text
    line1 : Text
      deriving (Eq, Ord, Show)

data PostIssuingCardholdersPayloadBilling = PostIssuingCardholdersPayloadBilling with
    address : PostIssuingCardholdersPayloadBillingAddress
      deriving (Eq, Ord, Show)

data PostIssuingCardholdersPayloadIndividualDob = PostIssuingCardholdersPayloadIndividualDob with
    day : Int
    year : Int
    month : Int
      deriving (Eq, Ord, Show)

data PostIssuingCardholdersPayloadIndividualVerificationDocument = PostIssuingCardholdersPayloadIndividualVerificationDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostIssuingCardholdersPayloadIndividualVerification = PostIssuingCardholdersPayloadIndividualVerification with
    document : Optional PostIssuingCardholdersPayloadIndividualVerificationDocument
      deriving (Eq, Ord, Show)

data PostIssuingCardholdersPayloadIndividual = PostIssuingCardholdersPayloadIndividual with
    firstName : Optional Text
    dob : Optional PostIssuingCardholdersPayloadIndividualDob
    lastName : Optional Text
    verification : Optional PostIssuingCardholdersPayloadIndividualVerification
      deriving (Eq, Ord, Show)

data PostIssuingCardholdersPayload = PostIssuingCardholdersPayload with
    email : Optional Text
        -- ^ The cardholder's email address.
    status : Optional Text
        -- ^ Specifies whether to permit authorizations on this
        --   cardholder's cards. Defaults to `active`.
    phoneNumber : Optional Text
        -- ^ The cardholder's phone number. This will be
        --   transformed to
        --   [E.164](https://en.wikipedia.org/wiki/E.164) if it is
        --   not provided in that format already.
    authorizationControls : Optional PostIssuingCardholdersPayloadAuthorizationControls
        -- ^ Spending rules that give you control over how your
        --   cardholders can make charges. Refer to our
        --   [authorizations](https://stripe.com/docs/issuing/authorizations)
        --   documentation for more details.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    isDefault : Optional Bool
        -- ^ Specifies whether to set this as the default
        --   cardholder.
    name : Text
        -- ^ The cardholder's name. This will be printed on cards
        --   issued to them.
    company : Optional PostIssuingCardholdersPayloadCompany
        -- ^ Additional information about a `business_entity`
        --   cardholder.
    metadata : Optional PostIssuingCardholdersPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    type_ : Optional Text
        -- ^ One of `individual` or `business_entity`.
    billing : PostIssuingCardholdersPayloadBilling
        -- ^ The cardholder's billing address.
    individual : Optional PostIssuingCardholdersPayloadIndividual
        -- ^ Additional information about an `individual`
        --   cardholder.
      deriving (Eq, Ord, Show)

data PostIssuingCardsCardPinPayloadVerification = PostIssuingCardsCardPinPayloadVerification with
    oneTimeCode : Optional Text
    id : Text
      deriving (Eq, Ord, Show)

data PostIssuingCardsCardPinPayload = PostIssuingCardsCardPinPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    pin : Text
        -- ^ The new desired PIN
    verification : PostIssuingCardsCardPinPayloadVerification
        -- ^ The id of the `Verification` that was sent and the
        --   code entered by the cardholder
      deriving (Eq, Ord, Show)

data PostDisputesDisputeClosePayload = PostDisputesDisputeClosePayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
      deriving (Eq, Ord, Show)

data GetAccountsAccountPersonsResponseBody200Data = GetAccountsAccountPersonsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetAccountsAccountPersonsResponseBody200 = GetAccountsAccountPersonsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Person]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostAccountsAccountPersonsPayloadRelationship = PostAccountsAccountPersonsPayloadRelationship with
    director : Optional Bool
    owner : Optional Bool
    percentOwnership : Optional Text
    title : Optional Text
    executive : Optional Bool
    representative : Optional Bool
      deriving (Eq, Ord, Show)

data PostAccountsAccountPersonsPayloadAddressKana = PostAccountsAccountPersonsPayloadAddressKana with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPersonsPayloadAddress = PostAccountsAccountPersonsPayloadAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPersonsPayloadMetadata = PostAccountsAccountPersonsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostAccountsAccountPersonsPayloadAddressKanji = PostAccountsAccountPersonsPayloadAddressKanji with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPersonsPayloadVerificationDocument = PostAccountsAccountPersonsPayloadVerificationDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPersonsPayloadVerificationAdditionalDocument = PostAccountsAccountPersonsPayloadVerificationAdditionalDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPersonsPayloadVerification = PostAccountsAccountPersonsPayloadVerification with
    document : Optional PostAccountsAccountPersonsPayloadVerificationDocument
    additionalDocument : Optional PostAccountsAccountPersonsPayloadVerificationAdditionalDocument
      deriving (Eq, Ord, Show)

data PostAccountsAccountPersonsPayload = PostAccountsAccountPersonsPayload with
    relationship : Optional PostAccountsAccountPersonsPayloadRelationship
        -- ^ The relationship that this person has with the
        --   account's legal entity.
    email : Optional Text
        -- ^ The person's email address.
    addressKana : Optional PostAccountsAccountPersonsPayloadAddressKana
        -- ^ The Kana variation of the person's address (Japan
        --   only).
    phone : Optional Text
        -- ^ The person's phone number.
    lastNameKanji : Optional Text
        -- ^ The Kanji variation of the person's last name (Japan
        --   only).
    firstName : Optional Text
        -- ^ The person's first name.
    lastNameKana : Optional Text
        -- ^ The Kana variation of the person's last name (Japan
        --   only).
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    address : Optional PostAccountsAccountPersonsPayloadAddress
        -- ^ The person's address.
    personToken : Optional Text
        -- ^ A [person
        --   token](https://stripe.com/docs/connect/account-tokens),
        --   used to securely provide details to the person.
    idNumber : Optional Text
        -- ^ The person's ID number, as appropriate for their
        --   country. For example, a social security number in the
        --   U.S., social insurance number in Canada, etc. Instead
        --   of the number itself, you can also provide a [PII
        --   token provided by
        --   Stripe.js](https://stripe.com/docs/stripe.js#collecting-pii-data).
    dob : Optional Text
        -- ^ The person's date of birth.
    gender : Optional Text
        -- ^ The person's gender (International regulations
        --   require either "male" or "female").
    firstNameKanji : Optional Text
        -- ^ The Kanji variation of the person's first name (Japan
        --   only).
    lastName : Optional Text
        -- ^ The person's last name.
    maidenName : Optional Text
        -- ^ The person's maiden name.
    metadata : Optional PostAccountsAccountPersonsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    firstNameKana : Optional Text
        -- ^ The Kana variation of the person's first name (Japan
        --   only).
    ssnLast4 : Optional Text
        -- ^ The last 4 digits of the person's social security
        --   number.
    addressKanji : Optional PostAccountsAccountPersonsPayloadAddressKanji
        -- ^ The Kanji variation of the person's address (Japan
        --   only).
    verification : Optional PostAccountsAccountPersonsPayloadVerification
        -- ^ The person's verification status.
      deriving (Eq, Ord, Show)

data GetOrderReturnsResponseBody200Data = GetOrderReturnsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetOrderReturnsResponseBody200 = GetOrderReturnsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [OrderReturn]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data GetCreditNotesResponseBody200Data = GetCreditNotesResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetCreditNotesResponseBody200 = GetCreditNotesResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [CreditNote]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostCreditNotesPayloadLines = PostCreditNotesPayloadLines with
      deriving (Eq, Ord, Show)

data PostCreditNotesPayloadMetadata = PostCreditNotesPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostCreditNotesPayload = PostCreditNotesPayload with
    amount : Optional Int
        -- ^ The integer amount in **%s** representing the total
        --   amount of the credit note.
    memo : Optional Text
        -- ^ The credit note's memo appears on the credit note
        --   PDF.
    lines : [PostCreditNotesPayloadLines]
        -- ^ Line items that make up the credit note.
    refundAmount : Optional Int
        -- ^ The integer amount in **%s** representing the amount
        --   to refund. If set, a refund will be created for the
        --   charge associated with the invoice.
    outOfBandAmount : Optional Int
        -- ^ The integer amount in **%s** representing the amount
        --   that is credited outside of Stripe.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    creditAmount : Optional Int
        -- ^ The integer amount in **%s** representing the amount
        --   to credit the customer's balance, which will be
        --   automatically applied to their next invoice.
    refund : Optional Text
        -- ^ ID of an existing refund to link this credit note to.
    invoice : Text
        -- ^ ID of the invoice.
    reason : Optional Text
        -- ^ Reason for issuing this credit note, one of
        --   `duplicate`, `fraudulent`, `order_change`, or
        --   `product_unsatisfactory`
    metadata : Optional PostCreditNotesPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data PostCustomersCustomerPayloadInvoiceSettings = PostCustomersCustomerPayloadInvoiceSettings with
    footer : Optional Text
    customFields : Optional Text
    defaultPaymentMethod : Optional Text
      deriving (Eq, Ord, Show)

data PostCustomersCustomerPayloadMetadata = PostCustomersCustomerPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostCustomersCustomerPayload = PostCustomersCustomerPayload with
    card : Optional Text
        -- ^ A token, like the ones returned by
        --   [Stripe.js](https://stripe.com/docs/stripe.js).
    email : Optional Text
        -- ^ Customer's email address. It's displayed alongside
        --   the customer in your dashboard and can be useful for
        --   searching and tracking. This may be up to *512
        --   characters*.
    defaultBankAccount : Optional Text
        -- ^ ID of bank account to make the customer's new default
        --   for invoice payments.
    phone : Optional Text
        -- ^ The customer's phone number.
    defaultCard : Optional Text
        -- ^ ID of card to make the customer's new default for
        --   invoice payments.
    bankAccount : Optional Text
        -- ^ Either a token, like the ones returned by
        --   [Stripe.js](https://stripe.com/docs/stripe.js), or a
        --   dictionary containing a user's bank account details.
    coupon : Optional Text
        -- ^ The code of the coupon to apply to this subscription.
        --   A coupon applied to a subscription will only affect
        --   invoices created for that particular subscription.
    invoiceSettings : Optional PostCustomersCustomerPayloadInvoiceSettings
        -- ^ Default invoice settings for this customer.
    balance : Optional Int
        -- ^ An integer amount in %s that represents the
        --   customer's current balance, which affect the
        --   customer's future invoices. A negative amount
        --   represents a credit that decreases the amount due on
        --   an invoice; a positive amount increases the amount
        --   due on an invoice.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    address : Optional Text
        -- ^ The customer's address.
    defaultAlipayAccount : Optional Text
        -- ^ ID of Alipay account to make the customer's new
        --   default for invoice payments.
    name : Optional Text
        -- ^ The customer's full name or business name.
    shipping : Optional Text
        -- ^ The customer's shipping information. Appears on
        --   invoices emailed to this customer.
    trialEnd : Optional Text
        -- ^ Unix timestamp representing the end of the trial
        --   period the customer will get before being charged for
        --   the first time. This will always overwrite any trials
        --   that might apply via a subscribed plan. If set,
        --   trial_end will override the default trial period of
        --   the plan the customer is being subscribed to. The
        --   special value `now` can be provided to end the
        --   customer's trial immediately. Can be at most two
        --   years from `billing_cycle_anchor`.
    metadata : Optional PostCustomersCustomerPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    source : Optional Text
        -- ^ The source can be a
        --   [Token](https://stripe.com/docs/api#tokens) or a
        --   [Source](https://stripe.com/docs/api#sources), as
        --   returned by
        --   [Elements](https://stripe.com/docs/elements). You
        --   must provide a source if the customer does not
        --   already have a valid source attached, and you are
        --   subscribing the customer to be charged automatically
        --   for a plan that is not free. Passing `source` will
        --   create a new source object, make it the customer
        --   default source, and delete the old customer default
        --   if one exists. If you want to add an additional
        --   source, instead use the [card creation
        --   API](https://stripe.com/docs/api#create_card) to add
        --   the card and then the [customer update
        --   API](https://stripe.com/docs/api#update_customer) to
        --   set it as the default. Whenever you attach a card to
        --   a customer, Stripe will automatically validate the
        --   card.
    taxExempt : Optional Text
        -- ^ The customer's tax exemption. One of `none`,
        --   `exempt`, or `reverse`.
    invoicePrefix : Optional Text
        -- ^ The prefix for the customer used to generate unique
        --   invoice numbers. Must be 3–12 uppercase letters or
        --   numbers.
    description : Optional Text
        -- ^ An arbitrary string that you can attach to a customer
        --   object. It is displayed alongside the customer in the
        --   dashboard.
    defaultSource : Optional Text
        -- ^ If you are using payment methods created via the
        --   PaymentMethods API, see the
        --   [invoice_settings.default_payment_method](https://stripe.com/docs/api/customers/update#update_customer-invoice_settings-default_payment_method)
        --   parameter. Provide the ID of a payment source already
        --   attached to this customer to make it this customer's
        --   default payment source. If you want to add a new
        --   payment source and make it the default, see the
        --   [source](https://stripe.com/docs/api/customers/update#update_customer-source)
        --   property.
    preferredLocales : [Text]
        -- ^ Customer's preferred languages, ordered by
        --   preference.
      deriving (Eq, Ord, Show)

data PostApplicationFeesFeeRefundsIdPayloadMetadata = PostApplicationFeesFeeRefundsIdPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostApplicationFeesFeeRefundsIdPayload = PostApplicationFeesFeeRefundsIdPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    metadata : Optional PostApplicationFeesFeeRefundsIdPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data PostInvoicesInvoiceSendPayload = PostInvoicesInvoiceSendPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
      deriving (Eq, Ord, Show)

data GetRefundsResponseBody200Data = GetRefundsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetRefundsResponseBody200 = GetRefundsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Refund]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostRefundsPayloadMetadata = PostRefundsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostRefundsPayload = PostRefundsPayload with
    amount : Optional Int
    refundApplicationFee : Optional Bool
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    paymentIntent : Optional Text
    reason : Optional Text
    reverseTransfer : Optional Bool
    charge : Optional Text
    metadata : Optional PostRefundsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data PostAccountPayloadBusinessProfile = PostAccountPayloadBusinessProfile with
    supportPhone : Optional Text
    supportEmail : Optional Text
    url : Optional Text
    supportUrl : Optional Text
    name : Optional Text
    productDescription : Optional Text
    mcc : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPayloadSettingsCardPaymentsDeclineOn = PostAccountPayloadSettingsCardPaymentsDeclineOn with
    avsFailure : Optional Bool
    cvcFailure : Optional Bool
      deriving (Eq, Ord, Show)

data PostAccountPayloadSettingsCardPayments = PostAccountPayloadSettingsCardPayments with
    statementDescriptorPrefix : Optional Text
    declineOn : Optional PostAccountPayloadSettingsCardPaymentsDeclineOn
      deriving (Eq, Ord, Show)

data PostAccountPayloadSettingsPayoutsSchedule = PostAccountPayloadSettingsPayoutsSchedule with
    monthlyAnchor : Optional Int
    interval : Optional Text
    weeklyAnchor : Optional Text
    delayDays : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPayloadSettingsPayouts = PostAccountPayloadSettingsPayouts with
    schedule : Optional PostAccountPayloadSettingsPayoutsSchedule
    debitNegativeBalances : Optional Bool
    statementDescriptor : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPayloadSettingsBranding = PostAccountPayloadSettingsBranding with
    icon : Optional Text
    primaryColor : Optional Text
    logo : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPayloadSettingsPayments = PostAccountPayloadSettingsPayments with
    statementDescriptorKanji : Optional Text
    statementDescriptorKana : Optional Text
    statementDescriptor : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPayloadSettings = PostAccountPayloadSettings with
    cardPayments : Optional PostAccountPayloadSettingsCardPayments
    payouts : Optional PostAccountPayloadSettingsPayouts
    branding : Optional PostAccountPayloadSettingsBranding
    payments : Optional PostAccountPayloadSettingsPayments
      deriving (Eq, Ord, Show)

data PostAccountPayloadCompanyAddressKana = PostAccountPayloadCompanyAddressKana with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPayloadCompanyAddress = PostAccountPayloadCompanyAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPayloadCompanyAddressKanji = PostAccountPayloadCompanyAddressKanji with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPayloadCompanyVerificationDocument = PostAccountPayloadCompanyVerificationDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPayloadCompanyVerification = PostAccountPayloadCompanyVerification with
    document : Optional PostAccountPayloadCompanyVerificationDocument
      deriving (Eq, Ord, Show)

data PostAccountPayloadCompany = PostAccountPayloadCompany with
    addressKana : Optional PostAccountPayloadCompanyAddressKana
    phone : Optional Text
    nameKanji : Optional Text
    taxIdRegistrar : Optional Text
    taxId : Optional Text
    ownersProvided : Optional Bool
    structure : Optional Text
    directorsProvided : Optional Bool
    address : Optional PostAccountPayloadCompanyAddress
    name : Optional Text
    nameKana : Optional Text
    executivesProvided : Optional Bool
    addressKanji : Optional PostAccountPayloadCompanyAddressKanji
    vatId : Optional Text
    verification : Optional PostAccountPayloadCompanyVerification
      deriving (Eq, Ord, Show)

data PostAccountPayloadMetadata = PostAccountPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostAccountPayloadTosAcceptance = PostAccountPayloadTosAcceptance with
    userAgent : Optional Text
    ip : Optional Text
    date : Optional Int
      deriving (Eq, Ord, Show)

data PostAccountPayloadIndividualAddressKana = PostAccountPayloadIndividualAddressKana with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPayloadIndividualAddress = PostAccountPayloadIndividualAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPayloadIndividualMetadata = PostAccountPayloadIndividualMetadata with
      deriving (Eq, Ord, Show)

data PostAccountPayloadIndividualAddressKanji = PostAccountPayloadIndividualAddressKanji with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPayloadIndividualVerificationDocument = PostAccountPayloadIndividualVerificationDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPayloadIndividualVerificationAdditionalDocument = PostAccountPayloadIndividualVerificationAdditionalDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountPayloadIndividualVerification = PostAccountPayloadIndividualVerification with
    document : Optional PostAccountPayloadIndividualVerificationDocument
    additionalDocument : Optional PostAccountPayloadIndividualVerificationAdditionalDocument
      deriving (Eq, Ord, Show)

data PostAccountPayloadIndividual = PostAccountPayloadIndividual with
    email : Optional Text
    addressKana : Optional PostAccountPayloadIndividualAddressKana
    phone : Optional Text
    lastNameKanji : Optional Text
    firstName : Optional Text
    lastNameKana : Optional Text
    address : Optional PostAccountPayloadIndividualAddress
    idNumber : Optional Text
    dob : Optional Text
    gender : Optional Text
    firstNameKanji : Optional Text
    lastName : Optional Text
    maidenName : Optional Text
    metadata : Optional PostAccountPayloadIndividualMetadata
    firstNameKana : Optional Text
    ssnLast4 : Optional Text
    addressKanji : Optional PostAccountPayloadIndividualAddressKanji
    verification : Optional PostAccountPayloadIndividualVerification
      deriving (Eq, Ord, Show)

data PostAccountPayload = PostAccountPayload with
    email : Optional Text
        -- ^ Email address of the account representative. For
        --   Standard accounts, this is used to ask them to claim
        --   their Stripe account. For Custom accounts, this only
        --   makes the account easier to identify to platforms;
        --   Stripe does not email the account representative.
    businessProfile : Optional PostAccountPayloadBusinessProfile
        -- ^ Business information about the account.
    accountToken : Optional Text
        -- ^ An [account
        --   token](https://stripe.com/docs/api#create_account_token),
        --   used to securely provide details to the account.
    settings : Optional PostAccountPayloadSettings
        -- ^ Options for customizing how the account functions
        --   within Stripe.
    bankAccount : Optional Text
        -- ^ Either a token, like the ones returned by
        --   [Stripe.js](https://stripe.com/docs/stripe.js), or a
        --   dictionary containing a user's bank account details.
    externalAccount : Optional Text
        -- ^ A card or bank account to attach to the account. You
        --   can provide either a token, like the ones returned by
        --   [Stripe.js](https://stripe.com/docs/stripe.js), or a
        --   dictionary, as documented in the `external_account`
        --   parameter for [bank
        --   account](https://stripe.com/docs/api#account_create_bank_account)
        --   creation. <br><br>By default, providing an external
        --   account sets it as the new default external account
        --   for its currency, and deletes the old default if one
        --   exists. To add additional external accounts without
        --   replacing the existing default for the currency, use
        --   the bank account or card creation API.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    businessType : Optional Text
        -- ^ The business type.
    company : Optional PostAccountPayloadCompany
        -- ^ Information about the company or business. This field
        --   is null unless `business_type` is set to `company`,
        --   `government_entity`, or `non_profit`.
    metadata : Optional PostAccountPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    defaultCurrency : Optional Text
        -- ^ Three-letter ISO currency code representing the
        --   default currency for the account. This must be a
        --   currency that [Stripe supports in the account's
        --   country](https://stripe.com/docs/payouts).
    tosAcceptance : Optional PostAccountPayloadTosAcceptance
        -- ^ Details on the account's acceptance of the [Stripe
        --   Services
        --   Agreement](https://stripe.com/docs/connect/updating-accounts#tos-acceptance).
    individual : Optional PostAccountPayloadIndividual
        -- ^ Information about the person represented by the
        --   account. This field is null unless `business_type` is
        --   set to `individual`.
    requestedCapabilities : [Text]
        -- ^ The set of capabilities you want to unlock for this
        --   account. Each capability will be inactive until you
        --   have provided its specific requirements and Stripe
        --   has verified them. An account may have some of its
        --   requested capabilities be active and some be
        --   inactive.
      deriving (Eq, Ord, Show)

data DeleteAccountPayload = DeleteAccountPayload with
    account : Optional Text
      deriving (Eq, Ord, Show)

data PostChargesChargeRefundsRefundPayloadMetadata = PostChargesChargeRefundsRefundPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostChargesChargeRefundsRefundPayload = PostChargesChargeRefundsRefundPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    metadata : Optional PostChargesChargeRefundsRefundPayloadMetadata
      deriving (Eq, Ord, Show)

data PostRefundsRefundPayloadMetadata = PostRefundsRefundPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostRefundsRefundPayload = PostRefundsRefundPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    metadata : Optional PostRefundsRefundPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data GetTaxRatesResponseBody200Data = GetTaxRatesResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetTaxRatesResponseBody200 = GetTaxRatesResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [TaxRate]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostTaxRatesPayloadMetadata = PostTaxRatesPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostTaxRatesPayload = PostTaxRatesPayload with
    inclusive : Bool
        -- ^ This specifies if the tax rate is inclusive or
        --   exclusive.
    displayName : Optional Text
        -- ^ The display name of the tax rate, which will be shown
        --   to users.
    jurisdiction : Optional Text
        -- ^ The jurisdiction for the tax rate.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    active : Optional Bool
        -- ^ Flag determining whether the tax rate is active or
        --   inactive. Inactive tax rates continue to work where
        --   they are currently applied however they cannot be
        --   used for new applications.
    metadata : Optional PostTaxRatesPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    percentage : Decimal
        -- ^ This represents the tax rate percent out of 100.
    description : Optional Text
        -- ^ An arbitrary string attached to the tax rate for your
        --   internal use only. It will not be visible to your
        --   customers.
      deriving (Eq, Ord, Show)

data PostTerminalLocationsLocationPayloadAddress = PostTerminalLocationsLocationPayloadAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostTerminalLocationsLocationPayloadMetadata = PostTerminalLocationsLocationPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostTerminalLocationsLocationPayload = PostTerminalLocationsLocationPayload with
    displayName : Optional Text
        -- ^ A name for the location.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    address : Optional PostTerminalLocationsLocationPayloadAddress
        -- ^ The full address of the location.
    metadata : Optional PostTerminalLocationsLocationPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data PostTerminalConnectionTokensPayload = PostTerminalConnectionTokensPayload with
    location : Optional Text
        -- ^ The id of the location that this connection token is
        --   scoped to. If specified the connection token will
        --   only be usable with readers assigned to that
        --   location, otherwise the connection token will be
        --   usable with all readers.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
      deriving (Eq, Ord, Show)

data PostCouponsCouponPayloadMetadata = PostCouponsCouponPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostCouponsCouponPayload = PostCouponsCouponPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    name : Optional Text
        -- ^ Name of the coupon displayed to customers on, for
        --   instance invoices, or receipts. By default the `id`
        --   is shown if `name` is not set.
    metadata : Optional PostCouponsCouponPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data PostFileLinksLinkPayloadMetadata = PostFileLinksLinkPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostFileLinksLinkPayload = PostFileLinksLinkPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    expiresAt : Optional Text
        -- ^ A future timestamp after which the link will no
        --   longer be usable, or `now` to expire the link
        --   immediately.
    metadata : Optional PostFileLinksLinkPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data PostChargesChargePayloadShippingAddress = PostChargesChargePayloadShippingAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Text
      deriving (Eq, Ord, Show)

data PostChargesChargePayloadShipping = PostChargesChargePayloadShipping with
    carrier : Optional Text
    phone : Optional Text
    trackingNumber : Optional Text
    address : PostChargesChargePayloadShippingAddress
    name : Text
      deriving (Eq, Ord, Show)

data PostChargesChargePayloadMetadata = PostChargesChargePayloadMetadata with
      deriving (Eq, Ord, Show)

data PostChargesChargePayloadFraudDetails = PostChargesChargePayloadFraudDetails with
    userReport : Optional Text
      deriving (Eq, Ord, Show)

data PostChargesChargePayload = PostChargesChargePayload with
    receiptEmail : Optional Text
        -- ^ This is the email address that the receipt for this
        --   charge will be sent to. If this field is updated,
        --   then a new email receipt will be sent to the updated
        --   address.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    customer : Optional Text
        -- ^ The ID of an existing customer that will be
        --   associated with this request. This field may only be
        --   updated if there is no existing associated customer
        --   with this charge.
    shipping : Optional PostChargesChargePayloadShipping
        -- ^ Shipping information for the charge. Helps prevent
        --   fraud on charges for physical goods.
    metadata : Optional PostChargesChargePayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    transferGroup : Optional Text
        -- ^ A string that identifies this transaction as part of
        --   a group. `transfer_group` may only be provided if it
        --   has not been set. See the [Connect
        --   documentation](https://stripe.com/docs/connect/charges-transfers#grouping-transactions)
        --   for details.
    description : Optional Text
        -- ^ An arbitrary string which you can attach to a charge
        --   object. It is displayed when in the web interface
        --   alongside the charge. Note that if you use Stripe to
        --   send automatic email receipts to your customers, your
        --   receipt emails will include the `description` of the
        --   charge(s) that they are describing.
    fraudDetails : Optional PostChargesChargePayloadFraudDetails
        -- ^ A set of key-value pairs you can attach to a charge
        --   giving information about its riskiness. If you
        --   believe a charge is fraudulent, include a
        --   `user_report` key with a value of `fraudulent`. If
        --   you believe a charge is safe, include a `user_report`
        --   key with a value of `safe`. Stripe will use the
        --   information you send to improve our fraud detection
        --   algorithms.
      deriving (Eq, Ord, Show)

data GetSetupIntentsResponseBody200Data = GetSetupIntentsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetSetupIntentsResponseBody200 = GetSetupIntentsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [SetupIntent]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostSetupIntentsPayloadSingleUse = PostSetupIntentsPayloadSingleUse with
    amount : Int
    currency : Text
      deriving (Eq, Ord, Show)

data PostSetupIntentsPayloadMandateDataCustomerAcceptanceOnline = PostSetupIntentsPayloadMandateDataCustomerAcceptanceOnline with
    userAgent : Optional Text
    ipAddress : Optional Text
      deriving (Eq, Ord, Show)

data PostSetupIntentsPayloadMandateDataCustomerAcceptanceOffline = PostSetupIntentsPayloadMandateDataCustomerAcceptanceOffline with
      deriving (Eq, Ord, Show)

data PostSetupIntentsPayloadMandateDataCustomerAcceptance = PostSetupIntentsPayloadMandateDataCustomerAcceptance with
    online : Optional PostSetupIntentsPayloadMandateDataCustomerAcceptanceOnline
    offline : Optional PostSetupIntentsPayloadMandateDataCustomerAcceptanceOffline
    type_ : Optional Text
    acceptedAt : Optional Int
      deriving (Eq, Ord, Show)

data PostSetupIntentsPayloadMandateData = PostSetupIntentsPayloadMandateData with
    customerAcceptance : Optional PostSetupIntentsPayloadMandateDataCustomerAcceptance
      deriving (Eq, Ord, Show)

data PostSetupIntentsPayloadMetadata = PostSetupIntentsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostSetupIntentsPayloadPaymentMethodOptionsCard = PostSetupIntentsPayloadPaymentMethodOptionsCard with
    requestThreeDSecure : Optional Text
      deriving (Eq, Ord, Show)

data PostSetupIntentsPayloadPaymentMethodOptions = PostSetupIntentsPayloadPaymentMethodOptions with
    card : Optional PostSetupIntentsPayloadPaymentMethodOptionsCard
      deriving (Eq, Ord, Show)

data PostSetupIntentsPayload = PostSetupIntentsPayload with
    paymentMethod : Optional Text
        -- ^ ID of the payment method (a PaymentMethod, Card, or
        --   saved Source object) to attach to this SetupIntent.
    confirm : Optional Bool
        -- ^ Set to `true` to attempt to confirm this SetupIntent
        --   immediately. This parameter defaults to `false`. If
        --   the payment method attached is a card, a return_url
        --   may be provided in case additional authentication is
        --   required.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    singleUse : Optional PostSetupIntentsPayloadSingleUse
        -- ^ If this hash is populated, this SetupIntent will
        --   generate a single_use Mandate on success.
    mandateData : Optional PostSetupIntentsPayloadMandateData
        -- ^ This hash contains details about the Mandate to
        --   create. This parameter can only be used with
        --   [`confirm=true`](https://stripe.com/docs/api/setup_intents/create#create_setup_intent-confirm).
    customer : Optional Text
        -- ^ ID of the Customer this SetupIntent belongs to, if
        --   one exists. If present, payment methods used with
        --   this SetupIntent can only be attached to this
        --   Customer, and payment methods attached to other
        --   Customers cannot be used with this SetupIntent.
    onBehalfOf : Optional Text
        -- ^ The Stripe account ID for which this SetupIntent is
        --   created.
    metadata : Optional PostSetupIntentsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    paymentMethodOptions : Optional PostSetupIntentsPayloadPaymentMethodOptions
        -- ^ Payment-method-specific configuration for this
        --   SetupIntent.
    usage : Optional Text
        -- ^ Indicates how the payment method is intended to be
        --   used in the future. If not provided, this value
        --   defaults to `off_session`.
    paymentMethodTypes : [Text]
        -- ^ The list of payment method types (e.g. card) that
        --   this SetupIntent is allowed to use. If this is not
        --   provided, defaults to ["card"].
    returnUrl : Optional Text
        -- ^ The URL to redirect your customer back to after they
        --   authenticate or cancel their payment on the payment
        --   method's app or site. If you'd prefer to redirect to
        --   a mobile application, you can alternatively supply an
        --   application URI scheme. This parameter can only be
        --   used with
        --   [`confirm=true`](https://stripe.com/docs/api/setup_intents/create#create_setup_intent-confirm).
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users.
      deriving (Eq, Ord, Show)

data GetAccountsAccountPeopleResponseBody200Data = GetAccountsAccountPeopleResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetAccountsAccountPeopleResponseBody200 = GetAccountsAccountPeopleResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Person]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostAccountsAccountPeoplePayloadRelationship = PostAccountsAccountPeoplePayloadRelationship with
    director : Optional Bool
    owner : Optional Bool
    percentOwnership : Optional Text
    title : Optional Text
    executive : Optional Bool
    representative : Optional Bool
      deriving (Eq, Ord, Show)

data PostAccountsAccountPeoplePayloadAddressKana = PostAccountsAccountPeoplePayloadAddressKana with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPeoplePayloadAddress = PostAccountsAccountPeoplePayloadAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPeoplePayloadMetadata = PostAccountsAccountPeoplePayloadMetadata with
      deriving (Eq, Ord, Show)

data PostAccountsAccountPeoplePayloadAddressKanji = PostAccountsAccountPeoplePayloadAddressKanji with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPeoplePayloadVerificationDocument = PostAccountsAccountPeoplePayloadVerificationDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPeoplePayloadVerificationAdditionalDocument = PostAccountsAccountPeoplePayloadVerificationAdditionalDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPeoplePayloadVerification = PostAccountsAccountPeoplePayloadVerification with
    document : Optional PostAccountsAccountPeoplePayloadVerificationDocument
    additionalDocument : Optional PostAccountsAccountPeoplePayloadVerificationAdditionalDocument
      deriving (Eq, Ord, Show)

data PostAccountsAccountPeoplePayload = PostAccountsAccountPeoplePayload with
    relationship : Optional PostAccountsAccountPeoplePayloadRelationship
        -- ^ The relationship that this person has with the
        --   account's legal entity.
    email : Optional Text
        -- ^ The person's email address.
    addressKana : Optional PostAccountsAccountPeoplePayloadAddressKana
        -- ^ The Kana variation of the person's address (Japan
        --   only).
    phone : Optional Text
        -- ^ The person's phone number.
    lastNameKanji : Optional Text
        -- ^ The Kanji variation of the person's last name (Japan
        --   only).
    firstName : Optional Text
        -- ^ The person's first name.
    lastNameKana : Optional Text
        -- ^ The Kana variation of the person's last name (Japan
        --   only).
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    address : Optional PostAccountsAccountPeoplePayloadAddress
        -- ^ The person's address.
    personToken : Optional Text
        -- ^ A [person
        --   token](https://stripe.com/docs/connect/account-tokens),
        --   used to securely provide details to the person.
    idNumber : Optional Text
        -- ^ The person's ID number, as appropriate for their
        --   country. For example, a social security number in the
        --   U.S., social insurance number in Canada, etc. Instead
        --   of the number itself, you can also provide a [PII
        --   token provided by
        --   Stripe.js](https://stripe.com/docs/stripe.js#collecting-pii-data).
    dob : Optional Text
        -- ^ The person's date of birth.
    gender : Optional Text
        -- ^ The person's gender (International regulations
        --   require either "male" or "female").
    firstNameKanji : Optional Text
        -- ^ The Kanji variation of the person's first name (Japan
        --   only).
    lastName : Optional Text
        -- ^ The person's last name.
    maidenName : Optional Text
        -- ^ The person's maiden name.
    metadata : Optional PostAccountsAccountPeoplePayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    firstNameKana : Optional Text
        -- ^ The Kana variation of the person's first name (Japan
        --   only).
    ssnLast4 : Optional Text
        -- ^ The last 4 digits of the person's social security
        --   number.
    addressKanji : Optional PostAccountsAccountPeoplePayloadAddressKanji
        -- ^ The Kanji variation of the person's address (Japan
        --   only).
    verification : Optional PostAccountsAccountPeoplePayloadVerification
        -- ^ The person's verification status.
      deriving (Eq, Ord, Show)

data PostCustomersCustomerBankAccountsIdPayloadOwnerAddress = PostCustomersCustomerBankAccountsIdPayloadOwnerAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostCustomersCustomerBankAccountsIdPayloadOwner = PostCustomersCustomerBankAccountsIdPayloadOwner with
    email : Optional Text
    phone : Optional Text
    address : Optional PostCustomersCustomerBankAccountsIdPayloadOwnerAddress
    name : Optional Text
      deriving (Eq, Ord, Show)

data PostCustomersCustomerBankAccountsIdPayloadMetadata = PostCustomersCustomerBankAccountsIdPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostCustomersCustomerBankAccountsIdPayload = PostCustomersCustomerBankAccountsIdPayload with
    addressCity : Optional Text
        -- ^ City/District/Suburb/Town/Village.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    addressState : Optional Text
        -- ^ State/County/Province/Region.
    owner : Optional PostCustomersCustomerBankAccountsIdPayloadOwner
    accountHolderType : Optional Text
        -- ^ The type of entity that holds the account. This can
        --   be either `individual` or `company`.
    name : Optional Text
        -- ^ Cardholder name.
    expMonth : Optional Text
        -- ^ Two digit number representing the card’s expiration
        --   month.
    addressZip : Optional Text
        -- ^ ZIP or postal code.
    metadata : Optional PostCustomersCustomerBankAccountsIdPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    expYear : Optional Text
        -- ^ Four digit number representing the card’s expiration
        --   year.
    addressLine2 : Optional Text
        -- ^ Address line 2 (Apartment/Suite/Unit/Building).
    addressLine1 : Optional Text
        -- ^ Address line 1 (Street address/PO Box/Company name).
    accountHolderName : Optional Text
        -- ^ The name of the person or business that owns the bank
        --   account.
    addressCountry : Optional Text
        -- ^ Billing address country, if provided when creating
        --   card.
      deriving (Eq, Ord, Show)

data DeleteCustomersCustomerBankAccountsIdPayload = DeleteCustomersCustomerBankAccountsIdPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
      deriving (Eq, Ord, Show)

data GetIssuingAuthorizationsResponseBody200Data = GetIssuingAuthorizationsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetIssuingAuthorizationsResponseBody200 = GetIssuingAuthorizationsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [IssuingAuthorization]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostIssuingAuthorizationsAuthorizationApprovePayload = PostIssuingAuthorizationsAuthorizationApprovePayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    heldAmount : Optional Int
        -- ^ If the authorization's `is_held_amount_controllable`
        --   property is `true`, you may provide this value to
        --   control how much to hold for the authorization. Must
        --   be positive (use
        --   [`decline`](https://stripe.com/docs/api/issuing/authorizations/decline)
        --   to decline an authorization request).
    metadata : Optional Text
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data GetCustomersCustomerSubscriptionsResponseBody200Data = GetCustomersCustomerSubscriptionsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetCustomersCustomerSubscriptionsResponseBody200 = GetCustomersCustomerSubscriptionsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Subscription]
        -- ^ Details about each object.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostCustomersCustomerSubscriptionsPayloadItems = PostCustomersCustomerSubscriptionsPayloadItems with
      deriving (Eq, Ord, Show)

data PostCustomersCustomerSubscriptionsPayloadMetadata = PostCustomersCustomerSubscriptionsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostCustomersCustomerSubscriptionsPayload = PostCustomersCustomerSubscriptionsPayload with
    trialFromPlan : Optional Bool
        -- ^ Indicates if a plan's `trial_period_days` should be
        --   applied to the subscription. Setting `trial_end` per
        --   subscription is preferred, and this defaults to
        --   `false`. Setting this flag to `true` together with
        --   `trial_end` is not allowed.
    trialPeriodDays : Optional Int
        -- ^ Integer representing the number of trial period days
        --   before the customer is charged for the first time.
        --   This will always overwrite any trials that might
        --   apply via a subscribed plan.
    coupon : Optional Text
        -- ^ The code of the coupon to apply to this subscription.
        --   A coupon applied to a subscription will only affect
        --   invoices created for that particular subscription.
    billingThresholds : Optional Text
        -- ^ Define thresholds at which an invoice will be sent,
        --   and the subscription advanced to a new billing
        --   period. Pass an empty string to remove
        --   previously-defined thresholds.
    pendingInvoiceItemInterval : Optional Text
        -- ^ Specifies an interval for how often to bill for any
        --   pending invoice items. It is analogous to calling
        --   [Create an
        --   invoice](https://stripe.com/docs/api#create_invoice)
        --   for the given subscription at the specified interval.
    backdateStartDate : Optional Int
        -- ^ For new subscriptions, a past timestamp to backdate
        --   the subscription's start date to. If set, the first
        --   invoice will contain a proration for the timespan
        --   between the start date and the current time. Can be
        --   combined with trials and the billing cycle anchor.
    cancelAtPeriodEnd : Optional Bool
        -- ^ Boolean indicating whether this subscription should
        --   cancel at the end of the current period.
    items : [PostCustomersCustomerSubscriptionsPayloadItems]
        -- ^ A list of up to 20 subscription items, each with an
        --   attached plan.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    offSession : Optional Bool
        -- ^ Indicates if a customer is on or off-session while an
        --   invoice payment is attempted.
    collectionMethod : Optional Text
        -- ^ Either `charge_automatically`, or `send_invoice`.
        --   When charging automatically, Stripe will attempt to
        --   pay this subscription at the end of the cycle using
        --   the default source attached to the customer. When
        --   sending an invoice, Stripe will email your customer
        --   an invoice with payment instructions. Defaults to
        --   `charge_automatically`.
    paymentBehavior : Optional Text
        -- ^ Use `allow_incomplete` to create subscriptions with
        --   `status=incomplete` if the first invoice cannot be
        --   paid. Creating subscriptions with this status allows
        --   you to manage scenarios where additional user actions
        --   are needed to pay a subscription's invoice. For
        --   example, SCA regulation may require 3DS
        --   authentication to complete payment. See the [SCA
        --   Migration
        --   Guide](https://stripe.com/docs/billing/migration/strong-customer-authentication)
        --   for Billing to learn more. This is the default
        --   behavior. Use `error_if_incomplete` if you want
        --   Stripe to return an HTTP 402 status code if a
        --   subscription's first invoice cannot be paid. For
        --   example, if a payment method requires 3DS
        --   authentication due to SCA regulation and further user
        --   action is needed, this parameter does not create a
        --   subscription and returns an error instead. This was
        --   the default behavior for API versions prior to
        --   2019-03-14. See the
        --   [changelog](https://stripe.com/docs/upgrades#2019-03-14)
        --   to learn more.
    prorationBehavior : Optional Text
        -- ^ Determines how to handle
        --   [prorations](https://stripe.com/docs/subscriptions/billing-cycle#prorations)
        --   resulting from the `billing_cycle_anchor`. Valid
        --   values are `create_prorations` or `none`. Passing
        --   `create_prorations` will cause proration invoice
        --   items to be created when applicable. Prorations can
        --   be disabled by passing `none`. If no value is passed,
        --   the default is `create_prorations`.
    cancelAt : Optional Int
        -- ^ A timestamp at which the subscription should cancel.
        --   If set to a date before the current period ends, this
        --   will cause a proration if prorations have been
        --   enabled using `proration_behavior`. If set during a
        --   future period, this will always cause a proration for
        --   that period.
    prorate : Optional Bool
        -- ^ This field has been renamed to `proration_behavior`.
        --   `prorate=true` can be replaced with
        --   `proration_behavior=create_prorations` and
        --   `prorate=false` can be replaced with
        --   `proration_behavior=none`.
    defaultTaxRates : Optional Text
        -- ^ The tax rates that will apply to any subscription
        --   item that does not have `tax_rates` set. Invoices
        --   created will have their `default_tax_rates` populated
        --   from the subscription.
    applicationFeePercent : Optional Decimal
        -- ^ A non-negative decimal between 0 and 100, with at
        --   most two decimal places. This represents the
        --   percentage of the subscription invoice subtotal that
        --   will be transferred to the application owner's Stripe
        --   account. The request must be made by a platform
        --   account on a connected account in order to set an
        --   application fee percentage. For more information, see
        --   the application fees
        --   [documentation](https://stripe.com/docs/connect/subscriptions#collecting-fees-on-subscriptions).
    trialEnd : Optional Text
        -- ^ Unix timestamp representing the end of the trial
        --   period the customer will get before being charged for
        --   the first time. This will always overwrite any trials
        --   that might apply via a subscribed plan. If set,
        --   trial_end will override the default trial period of
        --   the plan the customer is being subscribed to. The
        --   special value `now` can be provided to end the
        --   customer's trial immediately. Can be at most two
        --   years from `billing_cycle_anchor`.
    billingCycleAnchor : Optional Int
        -- ^ A future timestamp to anchor the subscription's
        --   [billing
        --   cycle](https://stripe.com/docs/subscriptions/billing-cycle).
        --   This is used to determine the date of the first full
        --   invoice, and, for plans with `month` or `year`
        --   intervals, the day of the month for subsequent
        --   invoices.
    metadata : Optional PostCustomersCustomerSubscriptionsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    defaultPaymentMethod : Optional Text
        -- ^ ID of the default payment method for the
        --   subscription. It must belong to the customer
        --   associated with the subscription. If not set,
        --   invoices will use the default payment method in the
        --   customer's invoice settings.
    defaultSource : Optional Text
        -- ^ ID of the default payment source for the
        --   subscription. It must belong to the customer
        --   associated with the subscription and be in a
        --   chargeable state. If not set, defaults to the
        --   customer's default source.
    daysUntilDue : Optional Int
        -- ^ Number of days a customer has to pay invoices
        --   generated by this subscription. Valid only for
        --   subscriptions where `collection_method` is set to
        --   `send_invoice`.
    taxPercent : Optional Text
        -- ^ A non-negative decimal (with at most four decimal
        --   places) between 0 and 100. This represents the
        --   percentage of the subscription invoice subtotal that
        --   will be calculated and added as tax to the final
        --   amount in each billing period. For example, a plan
        --   which charges $10/month with a `tax_percent` of
        --   `20.0` will charge $12 per invoice. To unset a
        --   previously-set value, pass an empty string. This
        --   field has been deprecated and will be removed in a
        --   future API version, for further information view the
        --   [migration
        --   docs](https://stripe.com/docs/billing/migration/taxes)
        --   for `tax_rates`.
      deriving (Eq, Ord, Show)

data GetCustomersCustomerTaxIdsResponseBody200Data = GetCustomersCustomerTaxIdsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetCustomersCustomerTaxIdsResponseBody200 = GetCustomersCustomerTaxIdsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [TaxId]
        -- ^ Details about each object.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostCustomersCustomerTaxIdsPayload = PostCustomersCustomerTaxIdsPayload with
    value : Text
        -- ^ Value of the tax ID.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    type_ : Optional Text
        -- ^ Type of the tax ID, one of `eu_vat`, `nz_gst`,
        --   `au_abn`, `in_gst`, `no_vat`, `za_vat`, `ch_vat`,
        --   `mx_rfc`, `sg_uen`, `ru_inn`, `ca_bn`, `hk_br`,
        --   `es_cif`, `tw_vat`, `th_vat`, `jp_cn`, `li_uid`,
        --   `my_itn`, `us_ein`, `kr_brn`, or `ca_qst`
      deriving (Eq, Ord, Show)

data PostPaymentIntentsIntentPayloadTransferData = PostPaymentIntentsIntentPayloadTransferData with
    amount : Optional Int
      deriving (Eq, Ord, Show)

data PostPaymentIntentsIntentPayloadMetadata = PostPaymentIntentsIntentPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostPaymentIntentsIntentPayloadPaymentMethodOptionsCardInstallments = PostPaymentIntentsIntentPayloadPaymentMethodOptionsCardInstallments with
    enabled : Optional Bool
    plan : Optional Text
      deriving (Eq, Ord, Show)

data PostPaymentIntentsIntentPayloadPaymentMethodOptionsCard = PostPaymentIntentsIntentPayloadPaymentMethodOptionsCard with
    installments : Optional PostPaymentIntentsIntentPayloadPaymentMethodOptionsCardInstallments
    requestThreeDSecure : Optional Text
      deriving (Eq, Ord, Show)

data PostPaymentIntentsIntentPayloadPaymentMethodOptions = PostPaymentIntentsIntentPayloadPaymentMethodOptions with
    card : Optional PostPaymentIntentsIntentPayloadPaymentMethodOptionsCard
      deriving (Eq, Ord, Show)

data PostPaymentIntentsIntentPayload = PostPaymentIntentsIntentPayload with
    amount : Optional Int
        -- ^ Amount intended to be collected by this
        --   PaymentIntent. A positive integer representing how
        --   much to charge in the [smallest currency
        --   unit](https://stripe.com/docs/currencies#zero-decimal)
        --   (e.g., 100 cents to charge $1.00 or 100 to charge
        --   ¥100, a zero-decimal currency). The minimum amount is
        --   $0.50 US or [equivalent in charge
        --   currency](https://stripe.com/docs/currencies#minimum-and-maximum-charge-amounts).
        --   The amount value supports up to eight digits (e.g., a
        --   value of 99999999 for a USD charge of $999,999.99).
    setupFutureUsage : Optional Text
        -- ^ Indicates that you intend to make future payments
        --   with this PaymentIntent's payment method. If present,
        --   the payment method used with this PaymentIntent can
        --   be
        --   [attached](https://stripe.com/docs/api/payment_methods/attach)
        --   to a Customer, even after the transaction completes.
        --   Use `on_session` if you intend to only reuse the
        --   payment method when your customer is present in your
        --   checkout flow. Use `off_session` if your customer may
        --   or may not be in your checkout flow. Stripe uses
        --   `setup_future_usage` to dynamically optimize your
        --   payment flow and comply with regional legislation and
        --   network rules. For example, if your customer is
        --   impacted by
        --   [SCA](https://stripe.com/docs/strong-customer-authentication),
        --   using `off_session` will ensure that they are
        --   authenticated while processing this PaymentIntent.
        --   You will then be able to collect [off-session
        --   payments](https://stripe.com/docs/payments/cards/charging-saved-cards#off-session-payments-with-saved-cards)
        --   for this customer. If `setup_future_usage` is already
        --   set and you are performing a request using a
        --   publishable key, you may only update the value from
        --   `on_session` to `off_session`.
    receiptEmail : Optional Text
        -- ^ Email address that the receipt for the resulting
        --   payment will be sent to.
    paymentMethod : Optional Text
        -- ^ ID of the payment method (a PaymentMethod, Card, or
        --   [compatible
        --   Source](https://stripe.com/docs/payments/payment-methods#compatibility)
        --   object) to attach to this PaymentIntent.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    savePaymentMethod : Optional Bool
        -- ^ If the PaymentIntent has a `payment_method` and a
        --   `customer` or if you're attaching a payment method to
        --   the PaymentIntent in this request, you can pass
        --   `save_payment_method=true` to save the payment method
        --   to the customer. Defaults to `false`. If the payment
        --   method is already saved to a customer, this does
        --   nothing. If this type of payment method cannot be
        --   saved to a customer, the request will error. _Note
        --   that saving a payment method using this parameter
        --   does not guarantee that the payment method can be
        --   charged._ To ensure that only payment methods which
        --   can be charged are saved to a customer, you can
        --   [manually
        --   save](https://stripe.com/docs/api/customers/create#create_customer-source)
        --   the payment method in response to the
        --   [`payment_intent.succeeded`
        --   webhook](https://stripe.com/docs/api/events/types#event_types-payment_intent.succeeded).
    transferData : Optional PostPaymentIntentsIntentPayloadTransferData
        -- ^ The parameters used to automatically create a
        --   Transfer when the payment succeeds. For more
        --   information, see the PaymentIntents [use case for
        --   connected
        --   accounts](https://stripe.com/docs/payments/connected-accounts).
    customer : Optional Text
        -- ^ ID of the Customer this PaymentIntent belongs to, if
        --   one exists. If present, payment methods used with
        --   this PaymentIntent can only be attached to this
        --   Customer, and payment methods attached to other
        --   Customers cannot be used with this PaymentIntent.
    currency : Optional Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    shipping : Optional Text
        -- ^ Shipping information for this PaymentIntent.
    metadata : Optional PostPaymentIntentsIntentPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    paymentMethodOptions : Optional PostPaymentIntentsIntentPayloadPaymentMethodOptions
        -- ^ Payment-method-specific configuration for this
        --   PaymentIntent.
    applicationFeeAmount : Optional Text
        -- ^ The amount of the application fee (if any) for the
        --   resulting payment. See the PaymentIntents [use case
        --   for connected
        --   accounts](https://stripe.com/docs/payments/connected-accounts)
        --   for details.
    transferGroup : Optional Text
        -- ^ A string that identifies the resulting payment as
        --   part of a group. `transfer_group` may only be
        --   provided if it has not been set. See the
        --   PaymentIntents [use case for connected
        --   accounts](https://stripe.com/docs/payments/connected-accounts)
        --   for details.
    paymentMethodTypes : [Text]
        -- ^ The list of payment method types (e.g. card) that
        --   this PaymentIntent is allowed to use.
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users.
    statementDescriptor : Optional Text
        -- ^ For non-card charges, you can use this value as the
        --   complete description that appears on your customers’
        --   statements. Must contain at least one letter, maximum
        --   22 characters.
    statementDescriptorSuffix : Optional Text
        -- ^ Provides information about a card payment that
        --   customers see on their statements. Concatenated with
        --   the prefix (shortened descriptor) or statement
        --   descriptor that’s set on the account to form the
        --   complete statement descriptor. Maximum 22 characters
        --   for the concatenated descriptor.
      deriving (Eq, Ord, Show)

data PostSubscriptionSchedulesScheduleCancelPayload = PostSubscriptionSchedulesScheduleCancelPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    prorate : Optional Bool
        -- ^ If the subscription schedule is `active`, indicates
        --   if the cancellation should be prorated. Defaults to
        --   `true`.
    invoiceNow : Optional Bool
        -- ^ If the subscription schedule is `active`, indicates
        --   whether or not to generate a final invoice that
        --   contains any un-invoiced metered usage and
        --   new/pending proration invoice items. Defaults to
        --   `true`.
      deriving (Eq, Ord, Show)

data PostSubscriptionSchedulesSchedulePayloadPhases = PostSubscriptionSchedulesSchedulePayloadPhases with
      deriving (Eq, Ord, Show)

data PostSubscriptionSchedulesSchedulePayloadDefaultSettingsInvoiceSettings = PostSubscriptionSchedulesSchedulePayloadDefaultSettingsInvoiceSettings with
    daysUntilDue : Optional Int
      deriving (Eq, Ord, Show)

data PostSubscriptionSchedulesSchedulePayloadDefaultSettings = PostSubscriptionSchedulesSchedulePayloadDefaultSettings with
    billingThresholds : Optional Text
    invoiceSettings : Optional PostSubscriptionSchedulesSchedulePayloadDefaultSettingsInvoiceSettings
    collectionMethod : Optional Text
    defaultPaymentMethod : Optional Text
      deriving (Eq, Ord, Show)

data PostSubscriptionSchedulesSchedulePayloadMetadata = PostSubscriptionSchedulesSchedulePayloadMetadata with
      deriving (Eq, Ord, Show)

data PostSubscriptionSchedulesSchedulePayload = PostSubscriptionSchedulesSchedulePayload with
    phases : [PostSubscriptionSchedulesSchedulePayloadPhases]
        -- ^ List representing phases of the subscription
        --   schedule. Each phase can be customized to have
        --   different durations, plans, and coupons. If there are
        --   multiple phases, the `end_date` of one phase will
        --   always equal the `start_date` of the next phase. Note
        --   that past phases can be omitted.
    endBehavior : Optional Text
        -- ^ Configures how the subscription schedule behaves when
        --   it ends. Possible values are `release` or `cancel`
        --   with the default being `release`. `release` will end
        --   the subscription schedule and keep the underlying
        --   subscription running.`cancel` will end the
        --   subscription schedule and cancel the underlying
        --   subscription.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    defaultSettings : Optional PostSubscriptionSchedulesSchedulePayloadDefaultSettings
        -- ^ Object representing the subscription schedule's
        --   default settings.
    prorationBehavior : Optional Text
        -- ^ If the update changes the current phase, indicates if
        --   the changes should be prorated. Valid values are
        --   `create_prorations` or `none`, and the default value
        --   is `create_prorations`.
    prorate : Optional Bool
        -- ^ This field has been renamed to `proration_behavior`.
        --   `prorate=true` can be replaced with
        --   `proration_behavior=create_prorations` and
        --   `prorate=false` can be replaced with
        --   `proration_behavior=none`.
    metadata : Optional PostSubscriptionSchedulesSchedulePayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data PostSourcesSourceVerifyPayload = PostSourcesSourceVerifyPayload with
    values : [Text]
        -- ^ The values needed to verify the source.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
      deriving (Eq, Ord, Show)

data PostSetupIntentsIntentConfirmPayloadMandateDataCustomerAcceptanceOnline = PostSetupIntentsIntentConfirmPayloadMandateDataCustomerAcceptanceOnline with
    userAgent : Optional Text
    ipAddress : Optional Text
      deriving (Eq, Ord, Show)

data PostSetupIntentsIntentConfirmPayloadMandateDataCustomerAcceptanceOffline = PostSetupIntentsIntentConfirmPayloadMandateDataCustomerAcceptanceOffline with
      deriving (Eq, Ord, Show)

data PostSetupIntentsIntentConfirmPayloadMandateDataCustomerAcceptance = PostSetupIntentsIntentConfirmPayloadMandateDataCustomerAcceptance with
    online : Optional PostSetupIntentsIntentConfirmPayloadMandateDataCustomerAcceptanceOnline
    offline : Optional PostSetupIntentsIntentConfirmPayloadMandateDataCustomerAcceptanceOffline
    type_ : Optional Text
    acceptedAt : Optional Int
      deriving (Eq, Ord, Show)

data PostSetupIntentsIntentConfirmPayloadMandateData = PostSetupIntentsIntentConfirmPayloadMandateData with
    customerAcceptance : Optional PostSetupIntentsIntentConfirmPayloadMandateDataCustomerAcceptance
      deriving (Eq, Ord, Show)

data PostSetupIntentsIntentConfirmPayloadPaymentMethodOptionsCard = PostSetupIntentsIntentConfirmPayloadPaymentMethodOptionsCard with
    requestThreeDSecure : Optional Text
      deriving (Eq, Ord, Show)

data PostSetupIntentsIntentConfirmPayloadPaymentMethodOptions = PostSetupIntentsIntentConfirmPayloadPaymentMethodOptions with
    card : Optional PostSetupIntentsIntentConfirmPayloadPaymentMethodOptionsCard
      deriving (Eq, Ord, Show)

data PostSetupIntentsIntentConfirmPayload = PostSetupIntentsIntentConfirmPayload with
    paymentMethod : Optional Text
        -- ^ ID of the payment method (a PaymentMethod, Card, or
        --   saved Source object) to attach to this SetupIntent.
    clientSecret : Optional Text
        -- ^ The client secret of the SetupIntent.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    mandateData : Optional PostSetupIntentsIntentConfirmPayloadMandateData
        -- ^ This hash contains details about the Mandate to
        --   create
    paymentMethodOptions : Optional PostSetupIntentsIntentConfirmPayloadPaymentMethodOptions
        -- ^ Payment-method-specific configuration for this
        --   SetupIntent.
    returnUrl : Optional Text
        -- ^ The URL to redirect your customer back to after they
        --   authenticate on the payment method's app or site. If
        --   you'd prefer to redirect to a mobile application, you
        --   can alternatively supply an application URI scheme.
        --   This parameter is only used for cards and other
        --   redirect-based payment methods.
      deriving (Eq, Ord, Show)

data GetCustomersCustomerBalanceTransactionsResponseBody200Data = GetCustomersCustomerBalanceTransactionsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetCustomersCustomerBalanceTransactionsResponseBody200 = GetCustomersCustomerBalanceTransactionsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [CustomerBalanceTransaction]
        -- ^ Details about each object.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostCustomersCustomerBalanceTransactionsPayloadMetadata = PostCustomersCustomerBalanceTransactionsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostCustomersCustomerBalanceTransactionsPayload = PostCustomersCustomerBalanceTransactionsPayload with
    amount : Int
        -- ^ The integer amount in **%s** to apply to the
        --   customer's balance. Pass a negative amount to credit
        --   the customer's balance, and pass in a positive amount
        --   to debit the customer's balance.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies). If the
        --   customer's
        --   [`currency`](https://stripe.com/docs/api/customers/object#customer_object-currency)
        --   is set, this value must match it. If the customer's
        --   `currency` is not set, it will be updated to this
        --   value.
    metadata : Optional PostCustomersCustomerBalanceTransactionsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users.
      deriving (Eq, Ord, Show)

data PostTopupsTopupCancelPayload = PostTopupsTopupCancelPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
      deriving (Eq, Ord, Show)

data GetSourcesSourceSourceTransactionsResponseBody200Data = GetSourcesSourceSourceTransactionsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetSourcesSourceSourceTransactionsResponseBody200 = GetSourcesSourceSourceTransactionsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [SourceTransaction]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostAccountBankAccountsPayloadMetadata = PostAccountBankAccountsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostAccountBankAccountsPayload = PostAccountBankAccountsPayload with
    defaultForCurrency : Optional Bool
        -- ^ When set to true, or if this is the first external
        --   account added in this currency, this account becomes
        --   the default external account for its currency.
    bankAccount : Optional Text
        -- ^ Either a token, like the ones returned by
        --   [Stripe.js](https://stripe.com/docs/stripe.js), or a
        --   dictionary containing a user's bank account details.
    externalAccount : Optional Text
        -- ^ Please refer to full
        --   [documentation](https://stripe.com/docs/api) instead.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    metadata : Optional PostAccountBankAccountsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data GetIssuingDisputesResponseBody200Data = GetIssuingDisputesResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetIssuingDisputesResponseBody200 = GetIssuingDisputesResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [IssuingDispute]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostIssuingDisputesPayloadMetadata = PostIssuingDisputesPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostIssuingDisputesPayloadEvidenceOther = PostIssuingDisputesPayloadEvidenceOther with
    disputeExplanation : Optional Text
    uncategorizedFile : Optional Text
      deriving (Eq, Ord, Show)

data PostIssuingDisputesPayloadEvidenceDuplicate = PostIssuingDisputesPayloadEvidenceDuplicate with
    originalTransaction : Optional Text
    disputeExplanation : Optional Text
    uncategorizedFile : Optional Text
      deriving (Eq, Ord, Show)

data PostIssuingDisputesPayloadEvidenceFraudulent = PostIssuingDisputesPayloadEvidenceFraudulent with
    disputeExplanation : Optional Text
    uncategorizedFile : Optional Text
      deriving (Eq, Ord, Show)

data PostIssuingDisputesPayloadEvidenceProductNotReceived = PostIssuingDisputesPayloadEvidenceProductNotReceived with
    disputeExplanation : Optional Text
    uncategorizedFile : Optional Text
      deriving (Eq, Ord, Show)

data PostIssuingDisputesPayloadEvidence = PostIssuingDisputesPayloadEvidence with
    other : Optional PostIssuingDisputesPayloadEvidenceOther
    duplicate : Optional PostIssuingDisputesPayloadEvidenceDuplicate
    fraudulent : Optional PostIssuingDisputesPayloadEvidenceFraudulent
    productNotReceived : Optional PostIssuingDisputesPayloadEvidenceProductNotReceived
      deriving (Eq, Ord, Show)

data PostIssuingDisputesPayload = PostIssuingDisputesPayload with
    amount : Optional Int
        -- ^ Amount to dispute, defaults to full value, given in
        --   the currency the transaction was made in.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    reason : Text
        -- ^ The reason for the dispute.
    metadata : Optional PostIssuingDisputesPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    disputedTransaction : Optional Text
        -- ^ The ID of the issuing transaction to create a dispute
        --   for.
    evidence : Optional PostIssuingDisputesPayloadEvidence
        -- ^ A hash containing all the evidence related to the
        --   dispute. This should have a single key, equal to the
        --   provided `reason`, mapping to an appropriate evidence
        --   object.
      deriving (Eq, Ord, Show)

data PostPaymentIntentsIntentCapturePayloadTransferData = PostPaymentIntentsIntentCapturePayloadTransferData with
    amount : Optional Int
      deriving (Eq, Ord, Show)

data PostPaymentIntentsIntentCapturePayload = PostPaymentIntentsIntentCapturePayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    transferData : Optional PostPaymentIntentsIntentCapturePayloadTransferData
        -- ^ The parameters used to automatically create a
        --   Transfer when the payment is captured. For more
        --   information, see the PaymentIntents [use case for
        --   connected
        --   accounts](https://stripe.com/docs/payments/connected-accounts).
    applicationFeeAmount : Optional Int
        -- ^ The amount of the application fee (if any) that will
        --   be applied to the payment and transferred to the
        --   application owner's Stripe account. For more
        --   information, see the PaymentIntents [use case for
        --   connected
        --   accounts](https://stripe.com/docs/payments/connected-accounts).
    amountToCapture : Optional Int
        -- ^ The amount to capture from the PaymentIntent, which
        --   must be less than or equal to the original amount.
        --   Any additional amount will be automatically refunded.
        --   Defaults to the full `amount_capturable` if not
        --   provided.
    statementDescriptor : Optional Text
        -- ^ For non-card charges, you can use this value as the
        --   complete description that appears on your customers’
        --   statements. Must contain at least one letter, maximum
        --   22 characters.
    statementDescriptorSuffix : Optional Text
        -- ^ Provides information about a card payment that
        --   customers see on their statements. Concatenated with
        --   the prefix (shortened descriptor) or statement
        --   descriptor that’s set on the account to form the
        --   complete statement descriptor. Maximum 22 characters
        --   for the concatenated descriptor.
      deriving (Eq, Ord, Show)

data PostAccountsAccountPersonsPersonPayloadRelationship = PostAccountsAccountPersonsPersonPayloadRelationship with
    director : Optional Bool
    owner : Optional Bool
    percentOwnership : Optional Text
    title : Optional Text
    executive : Optional Bool
    representative : Optional Bool
      deriving (Eq, Ord, Show)

data PostAccountsAccountPersonsPersonPayloadAddressKana = PostAccountsAccountPersonsPersonPayloadAddressKana with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPersonsPersonPayloadAddress = PostAccountsAccountPersonsPersonPayloadAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPersonsPersonPayloadMetadata = PostAccountsAccountPersonsPersonPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostAccountsAccountPersonsPersonPayloadAddressKanji = PostAccountsAccountPersonsPersonPayloadAddressKanji with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPersonsPersonPayloadVerificationDocument = PostAccountsAccountPersonsPersonPayloadVerificationDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPersonsPersonPayloadVerificationAdditionalDocument = PostAccountsAccountPersonsPersonPayloadVerificationAdditionalDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPersonsPersonPayloadVerification = PostAccountsAccountPersonsPersonPayloadVerification with
    document : Optional PostAccountsAccountPersonsPersonPayloadVerificationDocument
    additionalDocument : Optional PostAccountsAccountPersonsPersonPayloadVerificationAdditionalDocument
      deriving (Eq, Ord, Show)

data PostAccountsAccountPersonsPersonPayload = PostAccountsAccountPersonsPersonPayload with
    relationship : Optional PostAccountsAccountPersonsPersonPayloadRelationship
        -- ^ The relationship that this person has with the
        --   account's legal entity.
    email : Optional Text
        -- ^ The person's email address.
    addressKana : Optional PostAccountsAccountPersonsPersonPayloadAddressKana
        -- ^ The Kana variation of the person's address (Japan
        --   only).
    phone : Optional Text
        -- ^ The person's phone number.
    lastNameKanji : Optional Text
        -- ^ The Kanji variation of the person's last name (Japan
        --   only).
    firstName : Optional Text
        -- ^ The person's first name.
    lastNameKana : Optional Text
        -- ^ The Kana variation of the person's last name (Japan
        --   only).
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    address : Optional PostAccountsAccountPersonsPersonPayloadAddress
        -- ^ The person's address.
    personToken : Optional Text
        -- ^ A [person
        --   token](https://stripe.com/docs/connect/account-tokens),
        --   used to securely provide details to the person.
    idNumber : Optional Text
        -- ^ The person's ID number, as appropriate for their
        --   country. For example, a social security number in the
        --   U.S., social insurance number in Canada, etc. Instead
        --   of the number itself, you can also provide a [PII
        --   token provided by
        --   Stripe.js](https://stripe.com/docs/stripe.js#collecting-pii-data).
    dob : Optional Text
        -- ^ The person's date of birth.
    gender : Optional Text
        -- ^ The person's gender (International regulations
        --   require either "male" or "female").
    firstNameKanji : Optional Text
        -- ^ The Kanji variation of the person's first name (Japan
        --   only).
    lastName : Optional Text
        -- ^ The person's last name.
    maidenName : Optional Text
        -- ^ The person's maiden name.
    metadata : Optional PostAccountsAccountPersonsPersonPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    firstNameKana : Optional Text
        -- ^ The Kana variation of the person's first name (Japan
        --   only).
    ssnLast4 : Optional Text
        -- ^ The last 4 digits of the person's social security
        --   number.
    addressKanji : Optional PostAccountsAccountPersonsPersonPayloadAddressKanji
        -- ^ The Kanji variation of the person's address (Japan
        --   only).
    verification : Optional PostAccountsAccountPersonsPersonPayloadVerification
        -- ^ The person's verification status.
      deriving (Eq, Ord, Show)

data PostAccountExternalAccountsIdPayloadMetadata = PostAccountExternalAccountsIdPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostAccountExternalAccountsIdPayload = PostAccountExternalAccountsIdPayload with
    defaultForCurrency : Optional Bool
        -- ^ When set to true, this becomes the default external
        --   account for its currency.
    addressCity : Optional Text
        -- ^ City/District/Suburb/Town/Village.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    addressState : Optional Text
        -- ^ State/County/Province/Region.
    accountHolderType : Optional Text
        -- ^ The type of entity that holds the account. This can
        --   be either `individual` or `company`.
    name : Optional Text
        -- ^ Cardholder name.
    expMonth : Optional Text
        -- ^ Two digit number representing the card’s expiration
        --   month.
    addressZip : Optional Text
        -- ^ ZIP or postal code.
    metadata : Optional PostAccountExternalAccountsIdPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    expYear : Optional Text
        -- ^ Four digit number representing the card’s expiration
        --   year.
    addressLine2 : Optional Text
        -- ^ Address line 2 (Apartment/Suite/Unit/Building).
    addressLine1 : Optional Text
        -- ^ Address line 1 (Street address/PO Box/Company name).
    accountHolderName : Optional Text
        -- ^ The name of the person or business that owns the bank
        --   account.
    addressCountry : Optional Text
        -- ^ Billing address country, if provided when creating
        --   card.
      deriving (Eq, Ord, Show)

data PostAccountsAccountBankAccountsPayloadMetadata = PostAccountsAccountBankAccountsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostAccountsAccountBankAccountsPayload = PostAccountsAccountBankAccountsPayload with
    defaultForCurrency : Optional Bool
        -- ^ When set to true, or if this is the first external
        --   account added in this currency, this account becomes
        --   the default external account for its currency.
    bankAccount : Optional Text
        -- ^ Either a token, like the ones returned by
        --   [Stripe.js](https://stripe.com/docs/stripe.js), or a
        --   dictionary containing a user's bank account details.
    externalAccount : Optional Text
        -- ^ Please refer to full
        --   [documentation](https://stripe.com/docs/api) instead.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    metadata : Optional PostAccountsAccountBankAccountsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data GetCreditNotesCreditNoteLinesResponseBody200Data = GetCreditNotesCreditNoteLinesResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetCreditNotesCreditNoteLinesResponseBody200 = GetCreditNotesCreditNoteLinesResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [CreditNoteLineItem]
        -- ^ Details about each object.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostCustomersCustomerSubscriptionsSubscriptionExposedIdPayloadItems = PostCustomersCustomerSubscriptionsSubscriptionExposedIdPayloadItems with
      deriving (Eq, Ord, Show)

data PostCustomersCustomerSubscriptionsSubscriptionExposedIdPayloadMetadata = PostCustomersCustomerSubscriptionsSubscriptionExposedIdPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostCustomersCustomerSubscriptionsSubscriptionExposedIdPayload = PostCustomersCustomerSubscriptionsSubscriptionExposedIdPayload with
    trialFromPlan : Optional Bool
        -- ^ Indicates if a plan's `trial_period_days` should be
        --   applied to the subscription. Setting `trial_end` per
        --   subscription is preferred, and this defaults to
        --   `false`. Setting this flag to `true` together with
        --   `trial_end` is not allowed.
    coupon : Optional Text
        -- ^ The code of the coupon to apply to this subscription.
        --   A coupon applied to a subscription will only affect
        --   invoices created for that particular subscription.
    billingThresholds : Optional Text
        -- ^ Define thresholds at which an invoice will be sent,
        --   and the subscription advanced to a new billing
        --   period. Pass an empty string to remove
        --   previously-defined thresholds.
    pendingInvoiceItemInterval : Optional Text
        -- ^ Specifies an interval for how often to bill for any
        --   pending invoice items. It is analogous to calling
        --   [Create an
        --   invoice](https://stripe.com/docs/api#create_invoice)
        --   for the given subscription at the specified interval.
    cancelAtPeriodEnd : Optional Bool
        -- ^ Boolean indicating whether this subscription should
        --   cancel at the end of the current period.
    items : [PostCustomersCustomerSubscriptionsSubscriptionExposedIdPayloadItems]
        -- ^ List of subscription items, each with an attached
        --   plan.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    offSession : Optional Bool
        -- ^ Indicates if a customer is on or off-session while an
        --   invoice payment is attempted.
    collectionMethod : Optional Text
        -- ^ Either `charge_automatically`, or `send_invoice`.
        --   When charging automatically, Stripe will attempt to
        --   pay this subscription at the end of the cycle using
        --   the default source attached to the customer. When
        --   sending an invoice, Stripe will email your customer
        --   an invoice with payment instructions. Defaults to
        --   `charge_automatically`.
    paymentBehavior : Optional Text
        -- ^ Use `allow_incomplete` to create subscriptions with
        --   `status=incomplete` if the first invoice cannot be
        --   paid. Creating subscriptions with this status allows
        --   you to manage scenarios where additional user actions
        --   are needed to pay a subscription's invoice. For
        --   example, SCA regulation may require 3DS
        --   authentication to complete payment. See the [SCA
        --   Migration
        --   Guide](https://stripe.com/docs/billing/migration/strong-customer-authentication)
        --   for Billing to learn more. This is the default
        --   behavior. Use `error_if_incomplete` if you want
        --   Stripe to return an HTTP 402 status code if a
        --   subscription's first invoice cannot be paid. For
        --   example, if a payment method requires 3DS
        --   authentication due to SCA regulation and further user
        --   action is needed, this parameter does not create a
        --   subscription and returns an error instead. This was
        --   the default behavior for API versions prior to
        --   2019-03-14. See the
        --   [changelog](https://stripe.com/docs/upgrades#2019-03-14)
        --   to learn more.
    prorationDate : Optional Int
        -- ^ If set, the proration will be calculated as though
        --   the subscription was updated at the given time. This
        --   can be used to apply exactly the same proration that
        --   was previewed with [upcoming
        --   invoice](https://stripe.com/docs/api#retrieve_customer_invoice)
        --   endpoint. It can also be used to implement custom
        --   proration logic, such as prorating by day instead of
        --   by second, by providing the time that you wish to use
        --   for proration calculations.
    prorationBehavior : Optional Text
        -- ^ Determines how to handle
        --   [prorations](https://stripe.com/docs/subscriptions/billing-cycle#prorations)
        --   when the billing cycle changes (e.g., when switching
        --   plans, resetting `billing_cycle_anchor=now`, or
        --   starting a trial), or if an item's `quantity`
        --   changes. Valid values are `create_prorations`,
        --   `none`, or `always_invoice`. Passing
        --   `create_prorations` will cause proration invoice
        --   items to be created when applicable. These proration
        --   items will only be invoiced immediately under
        --   [certain
        --   conditions](https://stripe.com/docs/subscriptions/upgrading-downgrading#immediate-payment).
        --   In order to always invoice immediately for
        --   prorations, pass `always_invoice`. Prorations can be
        --   disabled by passing `none`.
    cancelAt : Optional Text
        -- ^ A timestamp at which the subscription should cancel.
        --   If set to a date before the current period ends, this
        --   will cause a proration if prorations have been
        --   enabled using `proration_behavior`. If set during a
        --   future period, this will always cause a proration for
        --   that period.
    prorate : Optional Bool
        -- ^ This field has been renamed to `proration_behavior`.
        --   `prorate=true` can be replaced with
        --   `proration_behavior=create_prorations` and
        --   `prorate=false` can be replaced with
        --   `proration_behavior=none`.
    defaultTaxRates : Optional Text
        -- ^ The tax rates that will apply to any subscription
        --   item that does not have `tax_rates` set. Invoices
        --   created will have their `default_tax_rates` populated
        --   from the subscription. Pass an empty string to remove
        --   previously-defined tax rates.
    applicationFeePercent : Optional Decimal
        -- ^ A non-negative decimal between 0 and 100, with at
        --   most two decimal places. This represents the
        --   percentage of the subscription invoice subtotal that
        --   will be transferred to the application owner's Stripe
        --   account. The request must be made by a platform
        --   account on a connected account in order to set an
        --   application fee percentage. For more information, see
        --   the application fees
        --   [documentation](https://stripe.com/docs/connect/subscriptions#collecting-fees-on-subscriptions).
    trialEnd : Optional Text
        -- ^ Unix timestamp representing the end of the trial
        --   period the customer will get before being charged for
        --   the first time. This will always overwrite any trials
        --   that might apply via a subscribed plan. If set,
        --   trial_end will override the default trial period of
        --   the plan the customer is being subscribed to. The
        --   special value `now` can be provided to end the
        --   customer's trial immediately. Can be at most two
        --   years from `billing_cycle_anchor`.
    billingCycleAnchor : Optional Text
        -- ^ Either `now` or `unchanged`. Setting the value to
        --   `now` resets the subscription's billing cycle anchor
        --   to the current time. For more information, see the
        --   billing cycle
        --   [documentation](https://stripe.com/docs/billing/subscriptions/billing-cycle).
    metadata : Optional PostCustomersCustomerSubscriptionsSubscriptionExposedIdPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    defaultPaymentMethod : Optional Text
        -- ^ ID of the default payment method for the
        --   subscription. It must belong to the customer
        --   associated with the subscription. If not set,
        --   invoices will use the default payment method in the
        --   customer's invoice settings.
    defaultSource : Optional Text
        -- ^ ID of the default payment source for the
        --   subscription. It must belong to the customer
        --   associated with the subscription and be in a
        --   chargeable state. If not set, defaults to the
        --   customer's default source.
    daysUntilDue : Optional Int
        -- ^ Number of days a customer has to pay invoices
        --   generated by this subscription. Valid only for
        --   subscriptions where `collection_method` is set to
        --   `send_invoice`.
    taxPercent : Optional Text
        -- ^ A non-negative decimal (with at most four decimal
        --   places) between 0 and 100. This represents the
        --   percentage of the subscription invoice subtotal that
        --   will be calculated and added as tax to the final
        --   amount in each billing period. For example, a plan
        --   which charges $10/month with a `tax_percent` of
        --   `20.0` will charge $12 per invoice. To unset a
        --   previously-set value, pass an empty string. This
        --   field has been deprecated and will be removed in a
        --   future API version, for further information view the
        --   [migration
        --   docs](https://stripe.com/docs/billing/migration/taxes)
        --   for `tax_rates`.
      deriving (Eq, Ord, Show)

data DeleteCustomersCustomerSubscriptionsSubscriptionExposedIdPayload = DeleteCustomersCustomerSubscriptionsSubscriptionExposedIdPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    prorate : Optional Bool
        -- ^ Can be set to `true` if `at_period_end` is not set to
        --   `true`. Will generate a proration invoice item that
        --   credits remaining unused time until the subscription
        --   period end.
    invoiceNow : Optional Bool
        -- ^ Can be set to `true` if `at_period_end` is not set to
        --   `true`. Will generate a final invoice that invoices
        --   for any un-invoiced metered usage and new/pending
        --   proration invoice items.
      deriving (Eq, Ord, Show)

data GetCustomersCustomerBankAccountsResponseBody200Data = GetCustomersCustomerBankAccountsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetCustomersCustomerBankAccountsResponseBody200 = GetCustomersCustomerBankAccountsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [BankAccount]
        -- ^ Details about each object.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostCustomersCustomerBankAccountsPayloadMetadata = PostCustomersCustomerBankAccountsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostCustomersCustomerBankAccountsPayload = PostCustomersCustomerBankAccountsPayload with
    card : Optional Text
        -- ^ A token, like the ones returned by
        --   [Stripe.js](https://stripe.com/docs/stripe.js).
    bankAccount : Optional Text
        -- ^ Either a token, like the ones returned by
        --   [Stripe.js](https://stripe.com/docs/stripe.js), or a
        --   dictionary containing a user's bank account details.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    alipayAccount : Optional Text
        -- ^ A token returned by
        --   [Stripe.js](https://stripe.com/docs/stripe.js)
        --   representing the user’s Alipay account details.
    metadata : Optional PostCustomersCustomerBankAccountsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    source : Optional Text
        -- ^ Please refer to full
        --   [documentation](https://stripe.com/docs/api) instead.
      deriving (Eq, Ord, Show)

data PostInvoicesInvoicePayPayload = PostInvoicesInvoicePayPayload with
    paymentMethod : Optional Text
        -- ^ A PaymentMethod to be charged. The PaymentMethod must
        --   be the ID of a PaymentMethod belonging to the
        --   customer associated with the invoice being paid.
    paidOutOfBand : Optional Bool
        -- ^ Boolean representing whether an invoice is paid
        --   outside of Stripe. This will result in no charge
        --   being made.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    offSession : Optional Bool
        -- ^ Indicates if a customer is on or off-session while an
        --   invoice payment is attempted.
    source : Optional Text
        -- ^ A payment source to be charged. The source must be
        --   the ID of a source belonging to the customer
        --   associated with the invoice being paid.
    forgive : Optional Bool
        -- ^ In cases where the source used to pay the invoice has
        --   insufficient funds, passing `forgive=true` controls
        --   whether a charge should be attempted for the full
        --   amount available on the source, up to the amount to
        --   fully pay the invoice. This effectively forgives the
        --   difference between the amount available on the source
        --   and the amount due. Passing `forgive=false` will fail
        --   the charge if the source hasn't been pre-funded with
        --   the right amount. An example for this case is with
        --   ACH Credit Transfers and wires: if the amount wired
        --   is less than the amount due by a small amount, you
        --   might want to forgive the difference.
      deriving (Eq, Ord, Show)

data GetAccountExternalAccountsResponseBody200Data = GetAccountExternalAccountsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetAccountExternalAccountsResponseBody200 = GetAccountExternalAccountsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [BankAccount]
        -- ^ The list contains all external accounts that have
        --   been attached to the Stripe account. These may be
        --   bank accounts or cards.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostAccountExternalAccountsPayloadMetadata = PostAccountExternalAccountsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostAccountExternalAccountsPayload = PostAccountExternalAccountsPayload with
    defaultForCurrency : Optional Bool
        -- ^ When set to true, or if this is the first external
        --   account added in this currency, this account becomes
        --   the default external account for its currency.
    bankAccount : Optional Text
        -- ^ Either a token, like the ones returned by
        --   [Stripe.js](https://stripe.com/docs/stripe.js), or a
        --   dictionary containing a user's bank account details.
    externalAccount : Optional Text
        -- ^ Please refer to full
        --   [documentation](https://stripe.com/docs/api) instead.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    metadata : Optional PostAccountExternalAccountsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data PostSubscriptionSchedulesScheduleReleasePayload = PostSubscriptionSchedulesScheduleReleasePayload with
    preserveCancelDate : Optional Bool
        -- ^ Keep any cancellation on the subscription that the
        --   schedule has set
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
      deriving (Eq, Ord, Show)

data GetPaymentIntentsResponseBody200Data = GetPaymentIntentsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetPaymentIntentsResponseBody200 = GetPaymentIntentsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [PaymentIntent]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostPaymentIntentsPayloadMandateDataCustomerAcceptanceOnline = PostPaymentIntentsPayloadMandateDataCustomerAcceptanceOnline with
    userAgent : Optional Text
    ipAddress : Optional Text
      deriving (Eq, Ord, Show)

data PostPaymentIntentsPayloadMandateDataCustomerAcceptanceOffline = PostPaymentIntentsPayloadMandateDataCustomerAcceptanceOffline with
      deriving (Eq, Ord, Show)

data PostPaymentIntentsPayloadMandateDataCustomerAcceptance = PostPaymentIntentsPayloadMandateDataCustomerAcceptance with
    online : Optional PostPaymentIntentsPayloadMandateDataCustomerAcceptanceOnline
    offline : Optional PostPaymentIntentsPayloadMandateDataCustomerAcceptanceOffline
    type_ : Optional Text
    acceptedAt : Optional Int
      deriving (Eq, Ord, Show)

data PostPaymentIntentsPayloadMandateData = PostPaymentIntentsPayloadMandateData with
    customerAcceptance : Optional PostPaymentIntentsPayloadMandateDataCustomerAcceptance
      deriving (Eq, Ord, Show)

data PostPaymentIntentsPayloadTransferData = PostPaymentIntentsPayloadTransferData with
    destination : Text
    amount : Optional Int
      deriving (Eq, Ord, Show)

data PostPaymentIntentsPayloadShippingAddress = PostPaymentIntentsPayloadShippingAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Text
      deriving (Eq, Ord, Show)

data PostPaymentIntentsPayloadShipping = PostPaymentIntentsPayloadShipping with
    carrier : Optional Text
    phone : Optional Text
    trackingNumber : Optional Text
    address : PostPaymentIntentsPayloadShippingAddress
    name : Text
      deriving (Eq, Ord, Show)

data PostPaymentIntentsPayloadMetadata = PostPaymentIntentsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostPaymentIntentsPayloadPaymentMethodOptionsCardInstallments = PostPaymentIntentsPayloadPaymentMethodOptionsCardInstallments with
    enabled : Optional Bool
    plan : Optional Text
      deriving (Eq, Ord, Show)

data PostPaymentIntentsPayloadPaymentMethodOptionsCard = PostPaymentIntentsPayloadPaymentMethodOptionsCard with
    installments : Optional PostPaymentIntentsPayloadPaymentMethodOptionsCardInstallments
    requestThreeDSecure : Optional Text
      deriving (Eq, Ord, Show)

data PostPaymentIntentsPayloadPaymentMethodOptions = PostPaymentIntentsPayloadPaymentMethodOptions with
    card : Optional PostPaymentIntentsPayloadPaymentMethodOptionsCard
      deriving (Eq, Ord, Show)

data PostPaymentIntentsPayload = PostPaymentIntentsPayload with
    captureMethod : Optional Text
        -- ^ Controls when the funds will be captured from the
        --   customer's account.
    amount : Int
        -- ^ Amount intended to be collected by this
        --   PaymentIntent. A positive integer representing how
        --   much to charge in the [smallest currency
        --   unit](https://stripe.com/docs/currencies#zero-decimal)
        --   (e.g., 100 cents to charge $1.00 or 100 to charge
        --   ¥100, a zero-decimal currency). The minimum amount is
        --   $0.50 US or [equivalent in charge
        --   currency](https://stripe.com/docs/currencies#minimum-and-maximum-charge-amounts).
        --   The amount value supports up to eight digits (e.g., a
        --   value of 99999999 for a USD charge of $999,999.99).
    confirmationMethod : Optional Text
    setupFutureUsage : Optional Text
        -- ^ Indicates that you intend to make future payments
        --   with this PaymentIntent's payment method. If present,
        --   the payment method used with this PaymentIntent can
        --   be
        --   [attached](https://stripe.com/docs/api/payment_methods/attach)
        --   to a Customer, even after the transaction completes.
        --   For more, learn to [save card details during
        --   payment](https://stripe.com/docs/payments/save-during-payment).
        --   Stripe uses `setup_future_usage` to dynamically
        --   optimize your payment flow and comply with regional
        --   legislation and network rules. For example, if your
        --   customer is impacted by
        --   [SCA](https://stripe.com/docs/strong-customer-authentication),
        --   using `off_session` will ensure that they are
        --   authenticated while processing this PaymentIntent.
        --   You will then be able to collect [off-session
        --   payments](https://stripe.com/docs/payments/cards/charging-saved-cards#off-session-payments-with-saved-cards)
        --   for this customer.
    receiptEmail : Optional Text
        -- ^ Email address that the receipt for the resulting
        --   payment will be sent to.
    paymentMethod : Optional Text
        -- ^ ID of the payment method (a PaymentMethod, Card, or
        --   [compatible
        --   Source](https://stripe.com/docs/payments/payment-methods#compatibility)
        --   object) to attach to this PaymentIntent. If this
        --   parameter is omitted with `confirm=true`,
        --   `customer.default_source` will be attached as this
        --   PaymentIntent's payment instrument to improve the
        --   migration experience for users of the Charges API. We
        --   recommend that you explicitly provide the
        --   `payment_method` going forward.
    confirm : Optional Bool
        -- ^ Set to `true` to attempt to
        --   [confirm](https://stripe.com/docs/api/payment_intents/confirm)
        --   this PaymentIntent immediately. This parameter
        --   defaults to `false`. When creating and confirming a
        --   PaymentIntent at the same time, parameters available
        --   in the
        --   [confirm](https://stripe.com/docs/api/payment_intents/confirm)
        --   API may also be provided.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    mandate : Optional Text
        -- ^ ID of the mandate to be used for this payment. This
        --   parameter can only be used with
        --   [`confirm=true`](https://stripe.com/docs/api/payment_intents/create#create_payment_intent-confirm).
    offSession : Optional Text
        -- ^ Set to `true` to indicate that the customer is not in
        --   your checkout flow during this payment attempt, and
        --   therefore is unable to authenticate. This parameter
        --   is intended for scenarios where you collect card
        --   details and [charge them
        --   later](https://stripe.com/docs/payments/cards/charging-saved-cards).
        --   This parameter can only be used with
        --   [`confirm=true`](https://stripe.com/docs/api/payment_intents/create#create_payment_intent-confirm).
    mandateData : Optional PostPaymentIntentsPayloadMandateData
        -- ^ This hash contains details about the Mandate to
        --   create. This parameter can only be used with
        --   [`confirm=true`](https://stripe.com/docs/api/payment_intents/create#create_payment_intent-confirm).
    savePaymentMethod : Optional Bool
        -- ^ If the PaymentIntent has a `payment_method` and a
        --   `customer` or if you're attaching a payment method to
        --   the PaymentIntent in this request, you can pass
        --   `save_payment_method=true` to save the payment method
        --   to the customer. Defaults to `false`. If the payment
        --   method is already saved to a customer, this does
        --   nothing. If this type of payment method cannot be
        --   saved to a customer, the request will error. _Note
        --   that saving a payment method using this parameter
        --   does not guarantee that the payment method can be
        --   charged._ To ensure that only payment methods which
        --   can be charged are saved to a customer, you can
        --   [manually
        --   save](https://stripe.com/docs/api/customers/create#create_customer-source)
        --   the payment method in response to the
        --   [`payment_intent.succeeded`
        --   webhook](https://stripe.com/docs/api/events/types#event_types-payment_intent.succeeded).
    transferData : Optional PostPaymentIntentsPayloadTransferData
        -- ^ The parameters used to automatically create a
        --   Transfer when the payment succeeds. For more
        --   information, see the PaymentIntents [use case for
        --   connected
        --   accounts](https://stripe.com/docs/payments/connected-accounts).
    customer : Optional Text
        -- ^ ID of the Customer this PaymentIntent belongs to, if
        --   one exists. If present, payment methods used with
        --   this PaymentIntent can only be attached to this
        --   Customer, and payment methods attached to other
        --   Customers cannot be used with this PaymentIntent.
    onBehalfOf : Optional Text
        -- ^ The Stripe account ID for which these funds are
        --   intended. For details, see the PaymentIntents [use
        --   case for connected
        --   accounts](https://stripe.com/docs/payments/connected-accounts).
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    shipping : Optional PostPaymentIntentsPayloadShipping
        -- ^ Shipping information for this PaymentIntent.
    metadata : Optional PostPaymentIntentsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    paymentMethodOptions : Optional PostPaymentIntentsPayloadPaymentMethodOptions
        -- ^ Payment-method-specific configuration for this
        --   PaymentIntent.
    applicationFeeAmount : Optional Int
        -- ^ The amount of the application fee (if any) that will
        --   be applied to the payment and transferred to the
        --   application owner's Stripe account. For more
        --   information, see the PaymentIntents [use case for
        --   connected
        --   accounts](https://stripe.com/docs/payments/connected-accounts).
    transferGroup : Optional Text
        -- ^ A string that identifies the resulting payment as
        --   part of a group. See the PaymentIntents [use case for
        --   connected
        --   accounts](https://stripe.com/docs/payments/connected-accounts)
        --   for details.
    useStripeSdk : Optional Bool
        -- ^ Set to `true` only when using manual confirmation and
        --   the iOS or Android SDKs to handle additional
        --   authentication steps.
    errorOnRequiresAction : Optional Bool
        -- ^ Set to `true` to fail the payment attempt if the
        --   PaymentIntent transitions into `requires_action`.
        --   This parameter is intended for simpler integrations
        --   that do not handle customer actions, like [saving
        --   cards without
        --   authentication](https://stripe.com/docs/payments/save-card-without-authentication).
        --   This parameter can only be used with
        --   [`confirm=true`](https://stripe.com/docs/api/payment_intents/create#create_payment_intent-confirm).
    paymentMethodTypes : [Text]
        -- ^ The list of payment method types (e.g. card) that
        --   this PaymentIntent is allowed to use. If this is not
        --   provided, defaults to ["card"].
    returnUrl : Optional Text
        -- ^ The URL to redirect your customer back to after they
        --   authenticate or cancel their payment on the payment
        --   method's app or site. If you'd prefer to redirect to
        --   a mobile application, you can alternatively supply an
        --   application URI scheme. This parameter can only be
        --   used with
        --   [`confirm=true`](https://stripe.com/docs/api/payment_intents/create#create_payment_intent-confirm).
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users.
    statementDescriptor : Optional Text
        -- ^ For non-card charges, you can use this value as the
        --   complete description that appears on your customers’
        --   statements. Must contain at least one letter, maximum
        --   22 characters.
    statementDescriptorSuffix : Optional Text
        -- ^ Provides information about a card payment that
        --   customers see on their statements. Concatenated with
        --   the prefix (shortened descriptor) or statement
        --   descriptor that’s set on the account to form the
        --   complete statement descriptor. Maximum 22 characters
        --   for the concatenated descriptor.
      deriving (Eq, Ord, Show)

data GetTransfersResponseBody200Data = GetTransfersResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetTransfersResponseBody200 = GetTransfersResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Transfer]
        -- ^ Details about each object.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostTransfersPayloadMetadata = PostTransfersPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostTransfersPayload = PostTransfersPayload with
    destination : Text
        -- ^ The ID of a connected Stripe account. <a
        --   href="/docs/connect/charges-transfers">See the
        --   Connect documentation</a> for details.
    amount : Optional Int
        -- ^ A positive integer in %s representing how much to
        --   transfer.
    sourceType : Optional Text
        -- ^ The source balance to use for this transfer. One of
        --   `bank_account`, `card`, or `fpx`. For most users,
        --   this will default to `card`.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    currency : Text
        -- ^ 3-letter [ISO code for
        --   currency](https://stripe.com/docs/payouts).
    metadata : Optional PostTransfersPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    transferGroup : Optional Text
        -- ^ A string that identifies this transaction as part of
        --   a group. See the [Connect
        --   documentation](https://stripe.com/docs/connect/charges-transfers#grouping-transactions)
        --   for details.
    sourceTransaction : Optional Text
        -- ^ You can use this parameter to transfer funds from a
        --   charge before they are added to your available
        --   balance. A pending balance will transfer immediately
        --   but the funds will not become available until the
        --   original charge becomes available. [See the Connect
        --   documentation](https://stripe.com/docs/connect/charges-transfers#transfer-availability)
        --   for details.
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users.
      deriving (Eq, Ord, Show)

data PostChargesChargeCapturePayloadTransferData = PostChargesChargeCapturePayloadTransferData with
    amount : Optional Int
      deriving (Eq, Ord, Show)

data PostChargesChargeCapturePayload = PostChargesChargeCapturePayload with
    amount : Optional Int
        -- ^ The amount to capture, which must be less than or
        --   equal to the original amount. Any additional amount
        --   will be automatically refunded.
    receiptEmail : Optional Text
        -- ^ The email address to send this charge's receipt to.
        --   This will override the previously-specified email
        --   address for this charge, if one was set. Receipts
        --   will not be sent in test mode.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    transferData : Optional PostChargesChargeCapturePayloadTransferData
        -- ^ An optional dictionary including the account to
        --   automatically transfer to as part of a destination
        --   charge. [See the Connect
        --   documentation](https://stripe.com/docs/connect/destination-charges)
        --   for details.
    applicationFeeAmount : Optional Int
        -- ^ An application fee amount to add on to this charge,
        --   which must be less than or equal to the original
        --   amount.
    applicationFee : Optional Int
        -- ^ An application fee to add on to this charge.
    transferGroup : Optional Text
        -- ^ A string that identifies this transaction as part of
        --   a group. `transfer_group` may only be provided if it
        --   has not been set. See the [Connect
        --   documentation](https://stripe.com/docs/connect/charges-transfers#grouping-transactions)
        --   for details.
    statementDescriptor : Optional Text
        -- ^ For card charges, use `statement_descriptor_suffix`
        --   instead. Otherwise, you can use this value as the
        --   complete description of a charge on your customers’
        --   statements. Must contain at least one letter, maximum
        --   22 characters.
    statementDescriptorSuffix : Optional Text
        -- ^ Provides information about the charge that customers
        --   see on their statements. Concatenated with the prefix
        --   (shortened descriptor) or statement descriptor that’s
        --   set on the account to form the complete statement
        --   descriptor. Maximum 22 characters for the
        --   concatenated descriptor.
      deriving (Eq, Ord, Show)

data GetIssuingCardsResponseBody200Data = GetIssuingCardsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetIssuingCardsResponseBody200 = GetIssuingCardsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [IssuingCard]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostIssuingCardsPayloadAuthorizationControlsSpendingLimits = PostIssuingCardsPayloadAuthorizationControlsSpendingLimits with
      deriving (Eq, Ord, Show)

data PostIssuingCardsPayloadAuthorizationControls = PostIssuingCardsPayloadAuthorizationControls with
    spendingLimits : [PostIssuingCardsPayloadAuthorizationControlsSpendingLimits]
    maxApprovals : Optional Int
    allowedCategories : [Text]
    blockedCategories : [Text]
      deriving (Eq, Ord, Show)

data PostIssuingCardsPayloadShippingAddress = PostIssuingCardsPayloadShippingAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Text
    city : Text
    line1 : Text
      deriving (Eq, Ord, Show)

data PostIssuingCardsPayloadShipping = PostIssuingCardsPayloadShipping with
    address : PostIssuingCardsPayloadShippingAddress
    name : Text
    speed : Optional Text
    type_ : Optional Text
      deriving (Eq, Ord, Show)

data PostIssuingCardsPayloadMetadata = PostIssuingCardsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostIssuingCardsPayload = PostIssuingCardsPayload with
    cardholder : Optional Text
        -- ^ The
        --   [Cardholder](https://stripe.com/docs/api#issuing_cardholder_object)
        --   object with which the card will be associated.
    status : Optional Text
        -- ^ Whether authorizations can be approved on this card.
        --   Defaults to `inactive`.
    replacementReason : Optional Text
        -- ^ If `replacement_for` is specified, this should
        --   indicate why that card is being replaced.
    authorizationControls : Optional PostIssuingCardsPayloadAuthorizationControls
        -- ^ Spending rules that give you some control over how
        --   your cards can be used. Refer to our
        --   [authorizations](https://stripe.com/docs/issuing/authorizations)
        --   documentation for more details.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    currency : Text
        -- ^ The currency for the card. This currently must be
        --   `usd`.
    shipping : Optional PostIssuingCardsPayloadShipping
        -- ^ The address where the card will be shipped.
    replacementFor : Optional Text
        -- ^ The card this is meant to be a replacement for (if
        --   any).
    metadata : Optional PostIssuingCardsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    type_ : Optional Text
        -- ^ The type of card to issue. Possible values are
        --   `physical` or `virtual`.
      deriving (Eq, Ord, Show)

data GetPlansResponseBody200Data = GetPlansResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetPlansResponseBody200 = GetPlansResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Plan]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostPlansPayloadTransformUsage = PostPlansPayloadTransformUsage with
    round : Text
    divideBy : Optional Int
      deriving (Eq, Ord, Show)

data PostPlansPayloadTiers = PostPlansPayloadTiers with
      deriving (Eq, Ord, Show)

data PostPlansPayloadMetadata = PostPlansPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostPlansPayloadProductMetadata = PostPlansPayloadProductMetadata with
      deriving (Eq, Ord, Show)

data PostPlansPayloadProduct = PostPlansPayloadProduct with
    unitLabel : Optional Text
    active : Optional Bool
    name : Text
    metadata : Optional PostPlansPayloadProductMetadata
    id : Optional Text
    statementDescriptor : Optional Text
      deriving (Eq, Ord, Show)

data PostPlansPayload = PostPlansPayload with
    amount : Optional Int
        -- ^ A positive integer in %s (or 0 for a free plan)
        --   representing how much to charge on a recurring basis.
    usageType : Optional Text
        -- ^ Configures how the quantity per period should be
        --   determined. Can be either `metered` or `licensed`.
        --   `licensed` automatically bills the `quantity` set
        --   when adding it to a subscription. `metered`
        --   aggregates the total usage based on usage records.
        --   Defaults to `licensed`.
    tiersMode : Optional Text
        -- ^ Defines if the tiering price should be `graduated` or
        --   `volume` based. In `volume`-based tiering, the
        --   maximum quantity within a period determines the per
        --   unit price, in `graduated` tiering pricing can
        --   successively change as the quantity grows.
    trialPeriodDays : Optional Int
        -- ^ Default number of trial days when subscribing a
        --   customer to this plan using
        --   [`trial_from_plan=true`](https://stripe.com/docs/api#create_subscription-trial_from_plan).
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    transformUsage : Optional PostPlansPayloadTransformUsage
        -- ^ Apply a transformation to the reported usage or set
        --   quantity before computing the billed price. Cannot be
        --   combined with `tiers`.
    tiers : [PostPlansPayloadTiers]
        -- ^ Each element represents a pricing tier. This
        --   parameter requires `billing_scheme` to be set to
        --   `tiered`. See also the documentation for
        --   `billing_scheme`.
    billingScheme : Optional Text
        -- ^ Describes how to compute the price per period. Either
        --   `per_unit` or `tiered`. `per_unit` indicates that the
        --   fixed amount (specified in `amount`) will be charged
        --   per unit in `quantity` (for plans with
        --   `usage_type=licensed`), or per unit of total usage
        --   (for plans with `usage_type=metered`). `tiered`
        --   indicates that the unit pricing will be computed
        --   using a tiering strategy as defined using the `tiers`
        --   and `tiers_mode` attributes.
    active : Optional Bool
        -- ^ Whether the plan is currently available for new
        --   subscriptions. Defaults to `true`.
    interval : Text
        -- ^ Specifies billing frequency. Either `day`, `week`,
        --   `month` or `year`.
    currency : Text
        -- ^ Three-letter [ISO currency
        --   code](https://www.iso.org/iso-4217-currency-codes.html),
        --   in lowercase. Must be a [supported
        --   currency](https://stripe.com/docs/currencies).
    metadata : Optional PostPlansPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    id : Optional Text
        -- ^ An identifier randomly generated by Stripe. Used to
        --   identify this plan when subscribing a customer. You
        --   can optionally override this ID, but the ID must be
        --   unique across all plans in your Stripe account. You
        --   can, however, use the same plan ID in both live and
        --   test modes.
    amountDecimal : Optional Text
        -- ^ Same as `amount`, but accepts a decimal value with at
        --   most 12 decimal places. Only one of `amount` and
        --   `amount_decimal` can be set.
    nickname : Optional Text
        -- ^ A brief description of the plan, hidden from
        --   customers.
    intervalCount : Optional Int
        -- ^ The number of intervals between subscription
        --   billings. For example, `interval=month` and
        --   `interval_count=3` bills every 3 months. Maximum of
        --   one year interval allowed (1 year, 12 months, or 52
        --   weeks).
    product : Optional PostPlansPayloadProduct
        -- ^ The product whose pricing the created plan will
        --   represent. This can either be the ID of an existing
        --   product, or a dictionary containing fields used to
        --   create a [service
        --   product](https://stripe.com/docs/api#product_object-type).
    aggregateUsage : Optional Text
        -- ^ Specifies a usage aggregation strategy for plans of
        --   `usage_type=metered`. Allowed values are `sum` for
        --   summing up all usage during a period,
        --   `last_during_period` for using the last usage record
        --   reported within a period, `last_ever` for using the
        --   last usage record ever (across period bounds) or
        --   `max` which uses the usage record with the maximum
        --   reported usage during a period. Defaults to `sum`.
      deriving (Eq, Ord, Show)

data GetFileLinksResponseBody200Data = GetFileLinksResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetFileLinksResponseBody200 = GetFileLinksResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [FileLink]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostFileLinksPayloadMetadata = PostFileLinksPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostFileLinksPayload = PostFileLinksPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    expiresAt : Optional Int
        -- ^ A future timestamp after which the link will no
        --   longer be usable.
    metadata : Optional PostFileLinksPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    file : Text
        -- ^ The ID of the file. The file's `purpose` must be one
        --   of the following: `business_icon`, `business_logo`,
        --   `customer_signature`, `dispute_evidence`,
        --   `finance_report_run`, `pci_document`,
        --   `sigma_scheduled_query`, or
        --   `tax_document_user_upload`.
      deriving (Eq, Ord, Show)

data PostSetupIntentsIntentPayloadMetadata = PostSetupIntentsIntentPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostSetupIntentsIntentPayloadPaymentMethodOptionsCard = PostSetupIntentsIntentPayloadPaymentMethodOptionsCard with
    requestThreeDSecure : Optional Text
      deriving (Eq, Ord, Show)

data PostSetupIntentsIntentPayloadPaymentMethodOptions = PostSetupIntentsIntentPayloadPaymentMethodOptions with
    card : Optional PostSetupIntentsIntentPayloadPaymentMethodOptionsCard
      deriving (Eq, Ord, Show)

data PostSetupIntentsIntentPayload = PostSetupIntentsIntentPayload with
    paymentMethod : Optional Text
        -- ^ ID of the payment method (a PaymentMethod, Card, or
        --   saved Source object) to attach to this SetupIntent.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    customer : Optional Text
        -- ^ ID of the Customer this SetupIntent belongs to, if
        --   one exists. If present, payment methods used with
        --   this SetupIntent can only be attached to this
        --   Customer, and payment methods attached to other
        --   Customers cannot be used with this SetupIntent.
    metadata : Optional PostSetupIntentsIntentPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    paymentMethodOptions : Optional PostSetupIntentsIntentPayloadPaymentMethodOptions
        -- ^ Payment-method-specific configuration for this
        --   SetupIntent.
    paymentMethodTypes : [Text]
        -- ^ The list of payment method types (e.g. card) that
        --   this SetupIntent is allowed to set up. If this is not
        --   provided, defaults to ["card"].
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users.
      deriving (Eq, Ord, Show)

data GetAccountsAccountCapabilitiesResponseBody200Data = GetAccountsAccountCapabilitiesResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetAccountsAccountCapabilitiesResponseBody200 = GetAccountsAccountCapabilitiesResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Capability]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostAccountsAccountPayloadBusinessProfile = PostAccountsAccountPayloadBusinessProfile with
    supportPhone : Optional Text
    supportEmail : Optional Text
    url : Optional Text
    supportUrl : Optional Text
    name : Optional Text
    productDescription : Optional Text
    mcc : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPayloadSettingsCardPaymentsDeclineOn = PostAccountsAccountPayloadSettingsCardPaymentsDeclineOn with
    avsFailure : Optional Bool
    cvcFailure : Optional Bool
      deriving (Eq, Ord, Show)

data PostAccountsAccountPayloadSettingsCardPayments = PostAccountsAccountPayloadSettingsCardPayments with
    statementDescriptorPrefix : Optional Text
    declineOn : Optional PostAccountsAccountPayloadSettingsCardPaymentsDeclineOn
      deriving (Eq, Ord, Show)

data PostAccountsAccountPayloadSettingsPayoutsSchedule = PostAccountsAccountPayloadSettingsPayoutsSchedule with
    monthlyAnchor : Optional Int
    interval : Optional Text
    weeklyAnchor : Optional Text
    delayDays : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPayloadSettingsPayouts = PostAccountsAccountPayloadSettingsPayouts with
    schedule : Optional PostAccountsAccountPayloadSettingsPayoutsSchedule
    debitNegativeBalances : Optional Bool
    statementDescriptor : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPayloadSettingsBranding = PostAccountsAccountPayloadSettingsBranding with
    icon : Optional Text
    primaryColor : Optional Text
    logo : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPayloadSettingsPayments = PostAccountsAccountPayloadSettingsPayments with
    statementDescriptorKanji : Optional Text
    statementDescriptorKana : Optional Text
    statementDescriptor : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPayloadSettings = PostAccountsAccountPayloadSettings with
    cardPayments : Optional PostAccountsAccountPayloadSettingsCardPayments
    payouts : Optional PostAccountsAccountPayloadSettingsPayouts
    branding : Optional PostAccountsAccountPayloadSettingsBranding
    payments : Optional PostAccountsAccountPayloadSettingsPayments
      deriving (Eq, Ord, Show)

data PostAccountsAccountPayloadCompanyAddressKana = PostAccountsAccountPayloadCompanyAddressKana with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPayloadCompanyAddress = PostAccountsAccountPayloadCompanyAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPayloadCompanyAddressKanji = PostAccountsAccountPayloadCompanyAddressKanji with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPayloadCompanyVerificationDocument = PostAccountsAccountPayloadCompanyVerificationDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPayloadCompanyVerification = PostAccountsAccountPayloadCompanyVerification with
    document : Optional PostAccountsAccountPayloadCompanyVerificationDocument
      deriving (Eq, Ord, Show)

data PostAccountsAccountPayloadCompany = PostAccountsAccountPayloadCompany with
    addressKana : Optional PostAccountsAccountPayloadCompanyAddressKana
    phone : Optional Text
    nameKanji : Optional Text
    taxIdRegistrar : Optional Text
    taxId : Optional Text
    ownersProvided : Optional Bool
    structure : Optional Text
    directorsProvided : Optional Bool
    address : Optional PostAccountsAccountPayloadCompanyAddress
    name : Optional Text
    nameKana : Optional Text
    executivesProvided : Optional Bool
    addressKanji : Optional PostAccountsAccountPayloadCompanyAddressKanji
    vatId : Optional Text
    verification : Optional PostAccountsAccountPayloadCompanyVerification
      deriving (Eq, Ord, Show)

data PostAccountsAccountPayloadMetadata = PostAccountsAccountPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostAccountsAccountPayloadTosAcceptance = PostAccountsAccountPayloadTosAcceptance with
    userAgent : Optional Text
    ip : Optional Text
    date : Optional Int
      deriving (Eq, Ord, Show)

data PostAccountsAccountPayloadIndividualAddressKana = PostAccountsAccountPayloadIndividualAddressKana with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPayloadIndividualAddress = PostAccountsAccountPayloadIndividualAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPayloadIndividualMetadata = PostAccountsAccountPayloadIndividualMetadata with
      deriving (Eq, Ord, Show)

data PostAccountsAccountPayloadIndividualAddressKanji = PostAccountsAccountPayloadIndividualAddressKanji with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPayloadIndividualVerificationDocument = PostAccountsAccountPayloadIndividualVerificationDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPayloadIndividualVerificationAdditionalDocument = PostAccountsAccountPayloadIndividualVerificationAdditionalDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPayloadIndividualVerification = PostAccountsAccountPayloadIndividualVerification with
    document : Optional PostAccountsAccountPayloadIndividualVerificationDocument
    additionalDocument : Optional PostAccountsAccountPayloadIndividualVerificationAdditionalDocument
      deriving (Eq, Ord, Show)

data PostAccountsAccountPayloadIndividual = PostAccountsAccountPayloadIndividual with
    email : Optional Text
    addressKana : Optional PostAccountsAccountPayloadIndividualAddressKana
    phone : Optional Text
    lastNameKanji : Optional Text
    firstName : Optional Text
    lastNameKana : Optional Text
    address : Optional PostAccountsAccountPayloadIndividualAddress
    idNumber : Optional Text
    dob : Optional Text
    gender : Optional Text
    firstNameKanji : Optional Text
    lastName : Optional Text
    maidenName : Optional Text
    metadata : Optional PostAccountsAccountPayloadIndividualMetadata
    firstNameKana : Optional Text
    ssnLast4 : Optional Text
    addressKanji : Optional PostAccountsAccountPayloadIndividualAddressKanji
    verification : Optional PostAccountsAccountPayloadIndividualVerification
      deriving (Eq, Ord, Show)

data PostAccountsAccountPayload = PostAccountsAccountPayload with
    email : Optional Text
        -- ^ Email address of the account representative. For
        --   Standard accounts, this is used to ask them to claim
        --   their Stripe account. For Custom accounts, this only
        --   makes the account easier to identify to platforms;
        --   Stripe does not email the account representative.
    businessProfile : Optional PostAccountsAccountPayloadBusinessProfile
        -- ^ Business information about the account.
    accountToken : Optional Text
        -- ^ An [account
        --   token](https://stripe.com/docs/api#create_account_token),
        --   used to securely provide details to the account.
    settings : Optional PostAccountsAccountPayloadSettings
        -- ^ Options for customizing how the account functions
        --   within Stripe.
    bankAccount : Optional Text
        -- ^ Either a token, like the ones returned by
        --   [Stripe.js](https://stripe.com/docs/stripe.js), or a
        --   dictionary containing a user's bank account details.
    externalAccount : Optional Text
        -- ^ A card or bank account to attach to the account. You
        --   can provide either a token, like the ones returned by
        --   [Stripe.js](https://stripe.com/docs/stripe.js), or a
        --   dictionary, as documented in the `external_account`
        --   parameter for [bank
        --   account](https://stripe.com/docs/api#account_create_bank_account)
        --   creation. <br><br>By default, providing an external
        --   account sets it as the new default external account
        --   for its currency, and deletes the old default if one
        --   exists. To add additional external accounts without
        --   replacing the existing default for the currency, use
        --   the bank account or card creation API.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    businessType : Optional Text
        -- ^ The business type.
    company : Optional PostAccountsAccountPayloadCompany
        -- ^ Information about the company or business. This field
        --   is null unless `business_type` is set to `company`,
        --   `government_entity`, or `non_profit`.
    metadata : Optional PostAccountsAccountPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    defaultCurrency : Optional Text
        -- ^ Three-letter ISO currency code representing the
        --   default currency for the account. This must be a
        --   currency that [Stripe supports in the account's
        --   country](https://stripe.com/docs/payouts).
    tosAcceptance : Optional PostAccountsAccountPayloadTosAcceptance
        -- ^ Details on the account's acceptance of the [Stripe
        --   Services
        --   Agreement](https://stripe.com/docs/connect/updating-accounts#tos-acceptance).
    individual : Optional PostAccountsAccountPayloadIndividual
        -- ^ Information about the person represented by the
        --   account. This field is null unless `business_type` is
        --   set to `individual`.
    requestedCapabilities : [Text]
        -- ^ The set of capabilities you want to unlock for this
        --   account. Each capability will be inactive until you
        --   have provided its specific requirements and Stripe
        --   has verified them. An account may have some of its
        --   requested capabilities be active and some be
        --   inactive.
      deriving (Eq, Ord, Show)

data PostEphemeralKeysPayload = PostEphemeralKeysPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    customer : Optional Text
        -- ^ The ID of the Customer you'd like to modify using the
        --   resulting ephemeral key.
    issuingCard : Optional Text
        -- ^ The ID of the Issuing Card you'd like to access using
        --   the resulting ephemeral key.
      deriving (Eq, Ord, Show)

data GetApplicationFeesResponseBody200Data = GetApplicationFeesResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetApplicationFeesResponseBody200 = GetApplicationFeesResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [ApplicationFee]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data GetSubscriptionSchedulesResponseBody200Data = GetSubscriptionSchedulesResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetSubscriptionSchedulesResponseBody200 = GetSubscriptionSchedulesResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [SubscriptionSchedule]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostSubscriptionSchedulesPayloadPhases = PostSubscriptionSchedulesPayloadPhases with
      deriving (Eq, Ord, Show)

data PostSubscriptionSchedulesPayloadDefaultSettingsInvoiceSettings = PostSubscriptionSchedulesPayloadDefaultSettingsInvoiceSettings with
    daysUntilDue : Optional Int
      deriving (Eq, Ord, Show)

data PostSubscriptionSchedulesPayloadDefaultSettings = PostSubscriptionSchedulesPayloadDefaultSettings with
    billingThresholds : Optional Text
    invoiceSettings : Optional PostSubscriptionSchedulesPayloadDefaultSettingsInvoiceSettings
    collectionMethod : Optional Text
    defaultPaymentMethod : Optional Text
      deriving (Eq, Ord, Show)

data PostSubscriptionSchedulesPayloadMetadata = PostSubscriptionSchedulesPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostSubscriptionSchedulesPayload = PostSubscriptionSchedulesPayload with
    phases : [PostSubscriptionSchedulesPayloadPhases]
        -- ^ List representing phases of the subscription
        --   schedule. Each phase can be customized to have
        --   different durations, plans, and coupons. If there are
        --   multiple phases, the `end_date` of one phase will
        --   always equal the `start_date` of the next phase.
    endBehavior : Optional Text
        -- ^ Configures how the subscription schedule behaves when
        --   it ends. Possible values are `release` or `cancel`
        --   with the default being `release`. `release` will end
        --   the subscription schedule and keep the underlying
        --   subscription running.`cancel` will end the
        --   subscription schedule and cancel the underlying
        --   subscription.
    startDate : Optional Text
        -- ^ When the subscription schedule starts. We recommend
        --   using `now` so that it starts the subscription
        --   immediately. You can also use a Unix timestamp to
        --   backdate the subscription so that it starts on a past
        --   date, or set a future date for the subscription to
        --   start on. When you backdate, the
        --   `billing_cycle_anchor` of the subscription is
        --   equivalent to the `start_date`.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    defaultSettings : Optional PostSubscriptionSchedulesPayloadDefaultSettings
        -- ^ Object representing the subscription schedule's
        --   default settings.
    customer : Optional Text
        -- ^ The identifier of the customer to create the
        --   subscription schedule for.
    fromSubscription : Optional Text
        -- ^ Migrate an existing subscription to be managed by a
        --   subscription schedule. If this parameter is set, a
        --   subscription schedule will be created using the
        --   subscription's plan(s), set to auto-renew using the
        --   subscription's interval. When using this parameter,
        --   other parameters (such as phase values) cannot be
        --   set. To create a subscription schedule with other
        --   modifications, we recommend making two separate API
        --   calls.
    metadata : Optional PostSubscriptionSchedulesPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data GetChargesChargeRefundsResponseBody200Data = GetChargesChargeRefundsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetChargesChargeRefundsResponseBody200 = GetChargesChargeRefundsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Refund]
        -- ^ Details about each object.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostChargesChargeRefundsPayloadMetadata = PostChargesChargeRefundsPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostChargesChargeRefundsPayload = PostChargesChargeRefundsPayload with
    amount : Optional Int
    refundApplicationFee : Optional Bool
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    paymentIntent : Optional Text
    reason : Optional Text
    reverseTransfer : Optional Bool
    metadata : Optional PostChargesChargeRefundsPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data PostIssuingAuthorizationsAuthorizationDeclinePayload = PostIssuingAuthorizationsAuthorizationDeclinePayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    metadata : Optional Text
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data GetRadarValueListItemsResponseBody200Data = GetRadarValueListItemsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetRadarValueListItemsResponseBody200 = GetRadarValueListItemsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [RadarValueListItem]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostRadarValueListItemsPayload = PostRadarValueListItemsPayload with
    value : Text
        -- ^ The value of the item (whose type must match the type
        --   of the parent value list).
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    valueList : Optional Text
        -- ^ The identifier of the value list which the created
        --   item will be added to.
      deriving (Eq, Ord, Show)

data GetCountrySpecsResponseBody200Data = GetCountrySpecsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetCountrySpecsResponseBody200 = GetCountrySpecsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [CountrySpec]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostPayoutsPayoutPayloadMetadata = PostPayoutsPayoutPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostPayoutsPayoutPayload = PostPayoutsPayoutPayload with
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    metadata : Optional PostPayoutsPayoutPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
      deriving (Eq, Ord, Show)

data GetBitcoinTransactionsResponseBody200Data = GetBitcoinTransactionsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetBitcoinTransactionsResponseBody200 = GetBitcoinTransactionsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [BitcoinTransaction]
        -- ^ Details about each object.
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostInvoicesInvoicePayloadMetadata = PostInvoicesInvoicePayloadMetadata with
      deriving (Eq, Ord, Show)

data PostInvoicesInvoicePayload = PostInvoicesInvoicePayload with
    dueDate : Optional Int
        -- ^ The date on which payment for this invoice is due.
        --   Only valid for invoices where
        --   `collection_method=send_invoice`. This field can only
        --   be updated on `draft` invoices.
    autoAdvance : Optional Bool
        -- ^ Controls whether Stripe will perform [automatic
        --   collection](https://stripe.com/docs/billing/invoices/workflow/#auto_advance)
        --   of the invoice.
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    collectionMethod : Optional Text
        -- ^ Either `charge_automatically` or `send_invoice`. This
        --   field can be updated only on `draft` invoices.
    defaultTaxRates : Optional Text
        -- ^ The tax rates that will apply to any line item that
        --   does not have `tax_rates` set. Pass an empty string
        --   to remove previously-defined tax rates.
    metadata : Optional PostInvoicesInvoicePayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    footer : Optional Text
        -- ^ Footer to be displayed on the invoice.
    customFields : Optional Text
        -- ^ A list of up to 4 custom fields to be displayed on
        --   the invoice. If a value for `custom_fields` is
        --   specified, the list specified will replace the
        --   existing custom field list on this invoice. Pass an
        --   empty string to remove previously-defined fields.
    applicationFeeAmount : Optional Int
        -- ^ A fee in %s that will be applied to the invoice and
        --   transferred to the application owner's Stripe
        --   account. The request must be made with an OAuth key
        --   or the Stripe-Account header in order to take an
        --   application fee. For more information, see the
        --   application fees
        --   [documentation](https://stripe.com/docs/connect/subscriptions#invoices).
    defaultPaymentMethod : Optional Text
        -- ^ ID of the default payment method for the invoice. It
        --   must belong to the customer associated with the
        --   invoice. If not set, defaults to the subscription's
        --   default payment method, if any, or to the default
        --   payment method in the customer's invoice settings.
    description : Optional Text
        -- ^ An arbitrary string attached to the object. Often
        --   useful for displaying to users. Referenced as 'memo'
        --   in the Dashboard.
    statementDescriptor : Optional Text
        -- ^ Extra information about a charge for the customer's
        --   credit card statement. It must contain at least one
        --   letter. If not specified and this invoice is part of
        --   a subscription, the default `statement_descriptor`
        --   will be set to the first subscription item's
        --   product's `statement_descriptor`.
    defaultSource : Optional Text
        -- ^ ID of the default payment source for the invoice. It
        --   must belong to the customer associated with the
        --   invoice and be in a chargeable state. If not set,
        --   defaults to the subscription's default source, if
        --   any, or to the customer's default source.
    daysUntilDue : Optional Int
        -- ^ The number of days from which the invoice is created
        --   until it is due. Only valid for invoices where
        --   `collection_method=send_invoice`. This field can only
        --   be updated on `draft` invoices.
    taxPercent : Optional Text
        -- ^ The percent tax rate applied to the invoice,
        --   represented as a non-negative decimal number (with at
        --   most four decimal places) between 0 and 100. To unset
        --   a previously-set value, pass an empty string. This
        --   field can be updated only on `draft` invoices. This
        --   field has been deprecated and will be removed in a
        --   future API version, for further information view the
        --   [migration
        --   docs](https://stripe.com/docs/billing/migration/taxes)
        --   for `tax_rates`.
      deriving (Eq, Ord, Show)

data GetAccountCapabilitiesResponseBody200Data = GetAccountCapabilitiesResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetAccountCapabilitiesResponseBody200 = GetAccountCapabilitiesResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [Capability]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)

data PostAccountsAccountPeoplePersonPayloadRelationship = PostAccountsAccountPeoplePersonPayloadRelationship with
    director : Optional Bool
    owner : Optional Bool
    percentOwnership : Optional Text
    title : Optional Text
    executive : Optional Bool
    representative : Optional Bool
      deriving (Eq, Ord, Show)

data PostAccountsAccountPeoplePersonPayloadAddressKana = PostAccountsAccountPeoplePersonPayloadAddressKana with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPeoplePersonPayloadAddress = PostAccountsAccountPeoplePersonPayloadAddress with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPeoplePersonPayloadMetadata = PostAccountsAccountPeoplePersonPayloadMetadata with
      deriving (Eq, Ord, Show)

data PostAccountsAccountPeoplePersonPayloadAddressKanji = PostAccountsAccountPeoplePersonPayloadAddressKanji with
    line2 : Optional Text
    postalCode : Optional Text
    state : Optional Text
    town : Optional Text
    country : Optional Text
    city : Optional Text
    line1 : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPeoplePersonPayloadVerificationDocument = PostAccountsAccountPeoplePersonPayloadVerificationDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPeoplePersonPayloadVerificationAdditionalDocument = PostAccountsAccountPeoplePersonPayloadVerificationAdditionalDocument with
    front : Optional Text
    back : Optional Text
      deriving (Eq, Ord, Show)

data PostAccountsAccountPeoplePersonPayloadVerification = PostAccountsAccountPeoplePersonPayloadVerification with
    document : Optional PostAccountsAccountPeoplePersonPayloadVerificationDocument
    additionalDocument : Optional PostAccountsAccountPeoplePersonPayloadVerificationAdditionalDocument
      deriving (Eq, Ord, Show)

data PostAccountsAccountPeoplePersonPayload = PostAccountsAccountPeoplePersonPayload with
    relationship : Optional PostAccountsAccountPeoplePersonPayloadRelationship
        -- ^ The relationship that this person has with the
        --   account's legal entity.
    email : Optional Text
        -- ^ The person's email address.
    addressKana : Optional PostAccountsAccountPeoplePersonPayloadAddressKana
        -- ^ The Kana variation of the person's address (Japan
        --   only).
    phone : Optional Text
        -- ^ The person's phone number.
    lastNameKanji : Optional Text
        -- ^ The Kanji variation of the person's last name (Japan
        --   only).
    firstName : Optional Text
        -- ^ The person's first name.
    lastNameKana : Optional Text
        -- ^ The Kana variation of the person's last name (Japan
        --   only).
    expand : [Text]
        -- ^ Specifies which fields in the response should be
        --   expanded.
    address : Optional PostAccountsAccountPeoplePersonPayloadAddress
        -- ^ The person's address.
    personToken : Optional Text
        -- ^ A [person
        --   token](https://stripe.com/docs/connect/account-tokens),
        --   used to securely provide details to the person.
    idNumber : Optional Text
        -- ^ The person's ID number, as appropriate for their
        --   country. For example, a social security number in the
        --   U.S., social insurance number in Canada, etc. Instead
        --   of the number itself, you can also provide a [PII
        --   token provided by
        --   Stripe.js](https://stripe.com/docs/stripe.js#collecting-pii-data).
    dob : Optional Text
        -- ^ The person's date of birth.
    gender : Optional Text
        -- ^ The person's gender (International regulations
        --   require either "male" or "female").
    firstNameKanji : Optional Text
        -- ^ The Kanji variation of the person's first name (Japan
        --   only).
    lastName : Optional Text
        -- ^ The person's last name.
    maidenName : Optional Text
        -- ^ The person's maiden name.
    metadata : Optional PostAccountsAccountPeoplePersonPayloadMetadata
        -- ^ Set of key-value pairs that you can attach to an
        --   object. This can be useful for storing additional
        --   information about the object in a structured format.
        --   Individual keys can be unset by posting an empty
        --   value to them. All keys can be unset by posting an
        --   empty value to `metadata`.
    firstNameKana : Optional Text
        -- ^ The Kana variation of the person's first name (Japan
        --   only).
    ssnLast4 : Optional Text
        -- ^ The last 4 digits of the person's social security
        --   number.
    addressKanji : Optional PostAccountsAccountPeoplePersonPayloadAddressKanji
        -- ^ The Kanji variation of the person's address (Japan
        --   only).
    verification : Optional PostAccountsAccountPeoplePersonPayloadVerification
        -- ^ The person's verification status.
      deriving (Eq, Ord, Show)

data GetIssuingSettlementsResponseBody200Data = GetIssuingSettlementsResponseBody200Data with
      deriving (Eq, Ord, Show)

data GetIssuingSettlementsResponseBody200 = GetIssuingSettlementsResponseBody200 with
    hasMore : Optional Bool
        -- ^ True if this list has another page of items after
        --   this one that can be fetched.
    data_ : [IssuingSettlement]
    url : Text
        -- ^ The URL where this list can be accessed.
    object : Text
        -- ^ String representing the object's type. Objects of the
        --   same type share the same value. Always has the value
        --   `list`.
      deriving (Eq, Ord, Show)